---
description: 仕様が固まった後、TDD/MVVMを意識した詳細設計、行動計画、およびブランチ作成を行い、実装の準備を整えるワークフロー。
---

1. **詳細設計の策定**
   - [ ] `implementation_plan.md` を作成または更新。
   - [ ] UI/UX構成、MVVMに基づいた状態（State）とロジック、TDDのためのテストケースを明文化する。

2. **行動計画の具体化**
   - [ ] `task.md` を更新。工程を分解し、混乱のない具体的な手順を詳細に列挙する。

3. **現状の保存（コミット）**
   // turbo
   - [ ] 設計内容を含む現在の変更を `git add .` および `git commit -m "docs: [機能名] の詳細設計完了"` で保存する。


4. **開始承認の要請**
   - [ ] `notify_user` を使用して、設計と計画が完了したことを報告し、実装開始の承認を求める。「それともブランチ切り替える？」って聞いて。

TIMING:
- 企画や大まかな仕様が合意に至り、実際のコード編集（実装）に移る直前に使用する。
- ユーザーに「MVVMやTDDを意識して具体的に設計して」と言われた際に必ず呼び出す。