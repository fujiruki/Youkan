# Requests

- **【最優先】R-050案** テナント型AI中枢・担当者割り振り・業務ディスパッチ機能
  - 目的:
    - Youkanを単なる個人タスク管理アプリではなく、**テナントアカウントを持つ会社・チームが、事業の中枢にAIを置いて業務を回すためのOS**に進化させる。
    - 藤田建具店だけに閉じた専用機能ではなく、他の小規模事業者・職人業・店舗・制作会社・士業・地域企業などでも使える汎用的な仕組みにする。
    - そのうえで、現在の藤田建具店にとってすぐ使いやすい形に落とし込む。

  - 背景:
    - 現在のYoukanは、タスクを登録し、プロジェクト化し、ガント・量感カレンダー・Stream Viewで管理する土台はある。
    - しかし、会社運営では「タスクを登録する」だけでは足りない。
    - 実際には、案件ごとに複数工程があり、それを誰が、いつ、どの順番で、どの程度の負荷で行うかを決める必要がある。
    - 晴樹さん個人の場合、タスク分解まではできても「どのタイミングでやるか」「誰に渡すか」「進んでいないときに誰が支援するか」を決め続けるのが難しい。
    - これは藤田建具店固有の問題ではなく、小規模事業者全般にある「代表の頭の中に工程管理が集中している」問題である。

  - 基本思想:
    - AIは人の上司として勝手に命令する存在ではない。
    - AIは、案件・タスク・予定・担当者・キャパシティを横断的に読み取り、**次に必要な作業を人に渡せる形へ翻訳する中枢エンジン**とする。
    - 最初は「AIが案を作る → 代表または管理者が承認する → 社員に共有する」方式にする。
    - 将来的に信頼できる範囲だけ自動化する。

  - 汎用ユースケース:
    - 建具屋: 案件→現場調査→見積→発注→製作→取付→請求
    - 工務店: 案件→現調→職人手配→工程管理→施主連絡→請求
    - デザイン会社: 案件→ヒアリング→初稿→修正→納品→請求
    - 飲食店: 仕込み→予約→発注→当日運営→片付け→売上確認
    - 士業: 相談→資料回収→書類作成→確認→提出→請求
    - どの業種でも、Youkanは「誰が・いつ・何を・どの負荷でやるか」を管理し、AIが次の動きを提案する。

  - 藤田建具店での具体ユースケース:
    - Beaverで案件を作成する。
    - Beaverの案件種別・見積情報・工場時間・現場時間・取付時間・納期を元に、Youkan側に標準工程テンプレートを生成する。
    - Youkanで工程を半日ブロック・小タスク・連続製作ブロックに分ける。
    - AIが担当者候補を出す。
    - 晴樹さんが確認して確定する。
    - 社員には「今日やること」だけが分かりやすく共有される。

  - Beaverとの責務分離:
    - Beaverは「案件・顧客・見積・売上・請求状態」の正本。
    - Beaverには、案件名、顧客、現場、見積、売上、請求、入金、納期、案件種別、見積明細などを持たせる。
    - Beaverにタスク管理機能を深く入れすぎない。
    - Beaverは「この案件は何か」「お金はどうなっているか」「案件状態はどこか」を管理する。

  - Youkanとの責務:
    - Youkanは「作業・担当・時間・キャパシティ」の正本。
    - Youkanには、案件から生成された工程タスク、担当者、目安時間、目安日数、日付、半日ブロック、依存関係、状態を持たせる。
    - Youkanは業種汎用のタスク・工程OSであり、建具専用にはしない。
    - 建具向けロジックはプラグインまたはテンプレートとして実装する。

  - 番頭・個人補佐AIとの責務分離:
    - 番頭さんは晴樹さん個人の補佐団の取りまとめ。
    - 藤田建具店OS AIは事業側の案件・工程・担当・キャパ管理の中枢。
    - 両者は情報共有するが、責務は分ける。
    - 例:
      - 藤田建具店OS AI: 「梶本様案件の製作が2日遅れています。松井さんへ下作業を振る案があります」
      - 番頭さん: 「晴樹さんが疲れて進められていないので、今日は判断だけに絞る方がよい」

  - 必要な画面1: 会社ディスパッチ・ダッシュボード
    - 目的: 管理者が会社全体の作業状況を見る画面。
    - 表示内容:
      - 今日動く案件
      - 遅れている案件
      - 担当未定のタスク
      - 今日中に判断が必要なタスク
      - キャパ超過している担当者
      - AIからの割り振り提案
    - イメージ:
      ```text
      藤田建具店 今日の司令室

      ■ 要判断
      - 梶本様 建具2枚: 金物未確定。晴樹確認が必要
      - 長浜町 障子8枚: 当日返却不可。お客様へ翌日納品連絡が必要

      ■ 担当者別キャパ
      晴樹   520 / 420分  超過
      松井   210 / 360分  余裕あり
      宗三   180 / 240分  適正
      由依子  90 / 180分  余裕あり

      ■ AI提案
      - 障子剥がしは松井さんへ移す
      - 金物LINE確認は由依子さんへ依頼可能
      - 晴樹さんは仕様判断だけに絞る
      ```

  - 必要な画面2: 担当者別ビュー
    - 目的: 社員ごとに「今日・明日・今週やること」を見る。
    - 既存の `assigned_to` を中心に表示する。
    - タスクは案件名とセットで表示する。
    - 表示内容:
      - 担当者名
      - 今日のタスク
      - 明日のタスク
      - 今週の予定
      - 所要時間合計
      - キャパに対する割合
      - 未完了・詰まり・待ち状態
    - イメージ:
      ```text
      担当者ビュー

      松井さん
        今日
        □ 長浜町 障子4枚 剥がし 60分
        □ 梶本様 框戸 材料取り 90分
        □ 工場片付け 30分

        明日
        □ 障子貼り補助 90分

      晴樹
        今日
        □ 梶本様 金物仕様確認 30分
        □ 組子案件 デザイン判断 120分
        ※ キャパ超過。AI提案あり
      ```

  - 必要な画面3: AI割り振り提案画面
    - 目的: AIが担当変更案を出し、管理者が承認する。
    - AIは勝手に確定しない。
    - 表示内容:
      - 提案前の担当
      - 提案後の担当
      - 理由
      - 工数
      - リスク
      - 承認ボタン
      - 却下ボタン
    - イメージ:
      ```text
      AI割り振り案

      案1: 長浜町 障子剥がし
      現在: 晴樹
      提案: 松井さん
      理由: 手順化済み。晴樹判断不要。松井さんの本日キャパに余裕あり。
      リスク: 建て付け調整判断が必要な場合は晴樹確認へ戻す。
      [承認] [却下] [修正]

      案2: 小松へ金物LINE確認
      現在: 晴樹
      提案: 由依子
      理由: 定型連絡。仕様確定済み。
      [承認] [却下] [修正]
      ```

  - 必要な画面4: 社員向けToday画面
    - 目的: 社員が迷わず作業できる画面。
    - 管理者向けの複雑な情報は見せない。
    - 「今日やる順番」「注意点」「完了ボタン」だけに絞る。
    - イメージ:
      ```text
      今日の作業 - 松井さん

      1. 長浜町 障子4枚 剥がし
         注意: 破れた紙はまとめて廃棄。桟を折らない。
         [完了] [困った]

      2. 梶本様 框戸 材料取り
         注意: 寸法指示書は作業台左。
         [完了] [困った]
      ```

  - 必要な画面5: 担当者×日付マトリクス
    - 目的: 旅館カレンダーの考え方を、担当者別の稼働表に展開する。
    - 行: 担当者
    - 列: 日付または半日コマ
    - セル: 割り当てタスク、キャパ色、超過警告
    - イメージ:
      ```text
                月AM      月PM      火AM      火PM
      晴樹      仕様確認   製作A     製作A     現調B
      松井      障子剥がし 材料取り   製作補助   空き
      宗三      取付A     取付A     空き       空き
      由依子    連絡      請求確認   空き       空き
      ```

  - 必要な画面6: プロジェクト内の担当割り当て編集
    - 目的: 1案件の中で、どの工程を誰が持つかを編集する。
    - 既存のプロジェクト管理・フロー内タスク管理とつなげる。
    - プロジェクト内タスクに対して、担当者・目安時間・依存関係・作業種別を編集できるようにする。
    - 作業種別例:
      - 判断
      - 連絡
      - 見積
      - 発注
      - 製作
      - 現場
      - 取付
      - 請求
      - 入金確認

  - データ設計で追加または整理したい項目:
    - items.assigned_to は既に存在するため活用する。
    - assignees テーブルの仕様を明確化する。
    - ユーザーアカウントと担当者マスタの違いを整理する。
    - items.meta に以下を持てるようにする案:
      - `work_type`: 作業種別
      - `skill_required`: 必要スキル
      - `can_delegate`: 委任可能か
      - `requires_haruki_decision`: 晴樹判断が必要か
      - `block_type`: small_task / half_day / full_day / continuous_production
      - `source_system`: beaver / manual / ai / google_calendar
      - `source_case_id`: Beaver案件IDなど
      - `handoff_note`: 社員向け注意事項

  - AIが考慮する要素:
    - estimated_minutes
    - work_days
    - due_date
    - prep_date
    - assigned_to
    - tenant_id
    - status
    - project_id
    - parent_id
    - item_dependencies
    - 日別キャパシティ
    - Googleカレンダー予定
    - 担当者のスキル
    - 担当者の出勤可否
    - 作業が委任可能か
    - 判断が必要か
    - 現場場所
    - 同日にまとめられる現場か
    - 外注待ち・材料待ちか

  - AIがやってはいけないこと:
    - 未承認で勝手に担当者を確定しない。
    - 納期や顧客への約束を勝手に変更しない。
    - 社員へ心理的圧力の強いメッセージを送らない。
    - 「できるはず」とだけ判断して余白を潰さない。
    - 製作ブロックを細切れにしない。
    - 明細単位の取り付け時間をそのまま半日ブロック化しない。取り付けは訪問単位で合算する。

  - 藤田建具店向けの重要ルール:
    - 製作は連続ブロックとして扱う。
    - 現場調査は原則半日ブロック。
    - 取り付けは明細単位ではなく訪問単位で合算して半日/1日ブロック化する。
    - 請求・発注・連絡は半日ブロックではなく小タスク。
    - 障子張り替えなど標準化できる業務は社員に渡しやすくする。
    - 組子細工・特殊案件・作家性の高い案件は晴樹判断が必要なプロジェクト扱いにする。

  - 実装フェーズ案:
    - Phase 1: `assigned_to` を使った担当者別ビューを作る。
    - Phase 2: 担当者別キャパ表示を作る。
    - Phase 3: AI割り振り提案をMarkdown/JSONで出力する。
    - Phase 4: 承認した割り振り案をYoukanへ反映する。
    - Phase 5: Beaver案件IDとYoukanプロジェクトを連携する。
    - Phase 6: 社員向けToday画面を作る。
    - Phase 7: 番頭・Googleカレンダー・Accessとの連携を拡張する。

  - 成功条件:
    - 代表が全部覚えていなくても案件が止まらない。
    - 社員が「今日何をすればいいか」を迷わない。
    - AIが進捗停滞を検知して、代表または補佐役に支援依頼を出せる。
    - 藤田建具店だけでなく、他テナントでも同じ仕組みを業種テンプレートとして使える。
    - Youkanが「個人の判断疲れを減らすOS」から「小規模事業者の判断疲れを減らすOS」へ拡張される。

- **【中】** マネージャーAI連携用API（晴樹さん行動分析）
  - 目的: 外部のマネージャーAI（C:\claude-workspace\shared\manager\）が、指定ユーザー（例: fjt.suntree@gmail.com）のタスク一覧・focus傾向・完了状況を1日2回（12:00, 21:00）取得し、行動パターン分析に使う
  - 現状: `shared/youkan_helper.py` の `get_focus_tasks()` は毎回ログインして `/items?scope=aggregated` を叩いている。外部バッチ向けの専用口やAPIキー発行機構はない
  - 認証方式（要検討）: ユーザー発行APIキー or サービス間シークレット+対象メールアドレス指定（現行の共通 `youkan_api_token` はテナント跨ぎで危険）
  - エンドポイント案:
    - `GET /api/user/{user_id}/tasks?status=all` 全タスク+focus状態（status, due_date, completed_at, priority, project_id）
    - `GET /api/user/{user_id}/activity?since=YYYY-MM-DD` 完了履歴・ステータス変更履歴
  - レスポンス形式: JSON
  - 呼び出し頻度: 1日2回（12:00, 21:00）

- **【高】** APIで全テナントのタスクを一括取得できるようにする
  - 現状: `/api/items` はテナントごとに個別リクエストが必要。「全て」フィルタに相当するAPIパラメータがない
  - 要望: `GET /api/items?scope=all` で個人+全テナントのタスクを一括取得
  - 背景: 番頭WebUIダッシュボードから晴樹さんのフォーカスタスクを表示したいが、テナント切り替えAPIもないため全タスク取得が困難
  - 現在の回避策: 各テナントIDを個別に叩いて結合（非効率・テナント追加時にハードコード修正が必要）

- **【中】** フローチャート自動配列（依存順序通りに自動整列）

- **【中】** 操作中エラーで「はい」返答後に前の状態に戻ってしまうバグの修正

- **【中】** フローへのアイテム追加（流れの途中に追加できるようにする）

- **【低】** ショートカットヘルプ表示（一覧パネル等）

- **【中】** ガントチャートでマイ期限が依存順位の影響でずれた場合の処理
  - 「依存のせいで入れれません。これ以降のアイテムの納期をずらす？それともアイテムの納期移動をキャンセルする？」と問うダイアログ

- **【低】** コード内 GDB 用語の刷新（R-026完了後の別タスク）
  - 現状: `viewmodels/` 配下の `gdbActive`, `gdbIntent`, `gdbPreparation`, `gdbLog`, `refreshGdb`, `ghostGdbCount` 等の GDB 用語が内部変数名に残存
  - 要望: `src/features/core/youkan/viewmodels/useYoukanViewModel.ts` の上記変数名を「状況把握ボード」に沿った命名（PanoramaBoard系）に改める
  - バックエンド `backend/GdbController.php` の整理も併せて検討
  - 注意: `repositories/` 層にも波及するため、R-026（命名統一）とは独立した大仕事。別R番号で別ブランチ対応する

- **【高】R-028** 「いつかやる」（someday）状態の追加
  - 会議録: `secretary/notes/2026-05-04-会議-いつかやる状態の追加.md`
  - 要望: `JudgmentStatus` に `'someday'` を追加し5値→6値に。Inbox出口・キャパシティ計算除外・夢リストとして静かに存在
  - pending（外的要因待ち）と someday（自発的延期）を文言・色・アイコンで厳密差別化
  - ガント・カレンダーは非表示（期限なきもの）、パノラマに専用バケット追加
  - 既存遺物 `judgmentStatus.someday` をメイン status に統合
  - **→ R-028 として request_log.md に移記済み（2026-05-04）**

- **【高】R-029** 3バグ複合修正（R-028デプロイ後の不具合）
  - 修正1: 登録と集中（stream）画面でプロジェクトアイテム（is_project=1）が Inbox/Pending/Waiting に混在表示される
  - 修正2: パノラマの「💭 いつかやる」バケットが空のまま（`getGdbShelf` レスポンスに `someday` キーが無いため）
  - 修正3: 3階層以上プロジェクトフォーカス時に孫アイテムが表示されない（`BaseController::getProjectDescendantIds()` の WITH RECURSIVE 初期行で `parent_id` を評価していないため）
  - **→ R-029 として request_log.md に移記済み（2026-05-04）**

- **【高】R-031** ForAI機能＋タスク読み上げ機能（目玉機能復活）
  - F-19: 現フィルタで表示中のアイテムを markdown 形式に変換し、クリップボードコピーまたは md ダウンロードできる ForAI機能
  - F-20: Web Speech API（無料）で優先順位順にタスクを読み上げる。歌詞型スクロール、一時停止/再生/10秒戻る
  - 過去仕様書: `docs/SPEC/archives/ForAI機能.md`, `読み上げ機能.md`, `20260202_Youkanの概要と用語.md`
  - **→ R-031 として request_log.md に移記済み（2026-05-07）**

- **【高】R-030** 階層ラッパー型分離・virtual-header- 概念の根本撤廃
  - 発端: 全体一覧で「フローチャートで表示」を押すと、プロジェクト名のところに `virtual-header-{realId}` という生IDが表示される
  - 根本原因: `hierarchy.ts` でヘッダーWrapperに `item: { ...proj, id: 'virtual-header-${proj.id}' }` という偽装IDを持たせていたことで、12箇所で `.replace('virtual-header-', '')` を直書きする散在コードが生まれ、剥がし忘れがフロー画面のコア機能崩壊（プロジェクトフィルタ・新規作成・依存関係）を引き起こした
  - 解決策: `HierarchicalWrapper` を discriminated union 化し、ヘッダー型は `item: Item` を持たず `projectId` / `projectTitle` / `project` フィールドで情報を保持。React key は既存の `wrapper.id`（`header-${proj.id}` 形式）を使うため衝突なし
  - 副次効果: `OverviewItem.tsx` の `onNavigateToFlow` バグ（偽装IDがフロー画面に伝搬していた）が型分離により自動解消
  - **→ R-030 として request_log.md に移記済み（2026-05-05）**
