# Requests

- **【中】R-126** 「新しいYoukan」構想（タスク・スケジュール・作業量を感じられる管理ソフトへ、発注者特性に基づく再設計）
  - 2026-08-18 会話内発言原文: 「実装デプロイと並行してあたらしいyoukanをいっしょにかんがえよう。より晴樹をサポートす。タスク・スケジュール・作業量を感じれる管理ソフトにしたい。ちなみに現状、量感自体はあまり使えてなくて、ガントチャート・フローチャート・全体一覧を主に使っているよ。」
  - 発注者特性分析（個人情報を含むためリポジトリ外）: `c:\claude-workspace\agents\共有情報\晴樹個人\2026-08-18_晴樹さんの特性とYoukan補助点.md`
  - 検討材料: ChatGPT議事録（`docs/reference/vision/2026-08-18_youkan-status-design-discussion_chatgpt.md`）§5〜§12（pending_condition/review_date、重要度・緊急度はfocus選定の補助軸、節目の問い直し、todo→focus自動推薦、1日focus4h以内）、特性分析§2「今後Youkanに足すべき機能提案」（期限超過タスクの週次棚卸し3択、「やらない」判断の構造化、タイムボックス永続化、対人タスク着手支援フラグ、見積もりバッファ自動提示＋新規タスク追加時の警告、「断った回数」KPI）
  - 状態: 相談中（仕様未確定。R-125完了後に順次R番号を切る）

- **【中】R-120** フロー「詰める」に横方向圧縮を追加＋自動整理／日付整列／詰めるをプレビュー→保存確定方式に統一
  - 2026-08-17 会話内発言原文: 「詰めるボタンを押したら縦と横のスライダーと保存ボタンがでて、それをリアルタイムで動くのを見ながら調整して、保存ボタンを押すと確定。こういう流れが良い。」
  - 確認回答: 「詰める」に横方向の圧縮も新たに追加する（推奨案採用）。「自動整理」「日付整列」も同じプレビュー方式に統一する
  - 追加発言: 「その縦と横のスライダーの横にチェックマークがあり、チェックを入れたらスライダー有効、はずしたらスライダーグレーアウトして無効。つまり、詰める機能を効かすかどうかも選べる」（縦・横それぞれ独立にON/OFFできる）
  - **→ R-120 として requests_log.md に移記済み（2026-08-17）**

- **【中】R-118** フローチャートの手動配置後の「詰める」機能（上下方向の間隔圧縮）
  - 2026-08-17 会話内発言原文＋スクリーンショット添付: 「この状態のフローチャートから、上下方向をできるだけつめる。ただしえっじがみえるはんい、エッジの曲がりが潰れて見づらくなるのは回避できる範囲で　ということをしたいが、そういう機能は作れるか？これは自動整理したあと、手動でいい感じにみやすく調整したところです。」
  - 相談で確定した方針: 「自動整理」とは別の新ボタン「詰める」として実装（並べ替え・横位置変更はせず、現在の並び順・Xを保ったまま縦の隙間だけを最小限に圧縮）。縦間隔の最小値は自動整理の縦間隔スライダー（GAP_Y）を共用（「どちらも一度しか使わないから共通でいい」）
  - **→ R-118 として requests_log.md に移記済み（2026-08-17）**

- **【中】R-117** フローチャート日付表示の帯に「残り時間」を追加表示
  - 2026-08-17 会話内発言原文: 「フローチャートの日付表示、それぞれの日で　未完了がある日は最短の下に残り何時間かかるかを表示して」
  - 確認回答: 残り時間＝未完了ノードの目安時間の単純合計（依存関係は考慮しない）
  - **→ R-117 として requests_log.md に移記済み（2026-08-17）**

- **【中】R-115 / R-116** 日付整列の未定枠配置・行間隔スライダー ＋ フロー操作パネルのホバーツールチップ
  - 2026-08-17 10:26 改善要望フォーム原文: 「日付整列のとき、日付未定の枠は左中央へ配置してください。それと、日付整列のとき、のノード同士の上下幅を指定するスライダーも自動整列のように作ってください。　また、スライダーやボタンなどフローチャート上のボタンたちの上にマウスを乗せて1秒待つとヒントでなにをするものか表示するようにして」
  - 確認回答: 未定枠は「日付帯の左に専用列」として、日付帯全体の高さに対し縦中央配置
  - **→ R-115（日付整列の未定枠配置・行間隔スライダー）・R-116（フロー操作パネルのホバーツールチップ）として requests_log.md に移記済み（2026-08-17）**

- **【中】R-113 / R-114** 日付表示を「表示のみ」に分離し「日付整列」ボタンを新設 ＋ 自動整理の縦間隔スライダー
  - 2026-08-17 会話内発言原文: 「日付表示ONにしたときに、上下方向への移動は全く禁止にする処理を追加したい。　それと、自動整理のときのノードとのーどの縦方向の距離をもっとつめて描きたい、理想は、できれば今の50%-70%、これは調整できたらいいな」
  - 確認への回答原文: 「ドラッグは人間が調整したいように移動するからいいんです。許可。横も上下も許可。」「スライダーで変えられるように」「日付表示ONのときに自動調整的な操作が入るでしょう？その操作のときにって意味でした。でも、ボタンを分けましょう。日付表示はONOFF表示切替のみで動かさない。それとはべつで、自動調整ボタンと、それとは別で日付を意識した位置に上下方向だけ移動するボタンを作ってほしい」
  - **→ R-113（日付表示の分離＋日付整列ボタン）・R-114（自動整理の縦間隔スライダー）として requests_log.md に移記済み（2026-08-17）**

- **【中】R-111 / R-112** 日付表示の帯内配置を「横位置維持・縦移動のみ」に修正 ＋ フローチャート「自動整理」ボタン新設
  - 2026-08-17 会話内発言原文: 「私がさっき見せた写真を覚えてないのか？　確かに日が縦に積まれているのは正しい。ありがとう。　しかしそのなかのノードがすべて横に並んでしまってるじゃないか。下のフローチャートと同じように上から下にむけて縦に並んでほしかった。というより、日付で整理するときは元のマップの横方向移動はできるだけせずに縦方向移動だけで日付ごとの領域に移動してほしかったんだ。なぜなら元のマインドマップの形をできるだけ変えたくなかったから。　ついでだから、　マインドマップに自動整理ボタンを追加しよう　フローチャートがそれぞれが重ならず、エッジ同士も重なりが少ない位置関係、そしてノード同士の隙間もできるだけエッジが見やすい範囲で隙間は狭く。　どんな機能が求められてるかまとめたうえで、どのような仕様を実現するか聞かせて」
  - 相談で確定: 帯内で行をずらす順序は依存関係優先（依存元が上・依存先が下、同順位なら元の上下関係）／自動整理エンジンは自前実装（外部レイアウトライブラリ不使用）／自動整理はプロジェクトごとに整理して横並び
  - **→ R-111（日付帯内配置修正）・R-112（自動整理ボタン）として requests_log.md に移記済み（2026-08-17）**

- **【中】R-109** フローチャートに日付グルーピング表示（合計時間・クリティカルパス最短時間つき）を追加
  - 2026-08-17 会話内発言原文＋手書き図添付: 「フローチャート画面について上部のメニューが多分何段かあると思うんだけど、その一番下の段、メニューの一番下の段、メニューの一番下の段に日付表示チェックマークを作ってほしい。そのチェックマークを入れると今送る写真のように日付ごとに納期いつまでにこれをやるっていうのをごとに、そのタスクが表示される。フローチャートの並びで整理して、配置して、いつこれをやる、いつこれをやるっていうのはもうちょっと背景色を変えて分かりやすく表示してほしい。これはまだそのどういう感じにやったらいいかっていう相談をちょっとしてから実装に入ってほしいです。添付する写真は私が手書きで書いたこんなイメージですっていうやつです。その日に与えられている合計のタスクの時間も書かれてますね。それと枝分かれ分岐して、その日の中で分岐があったりする中で、その中で1番時間がかかる経路の合計時間を最短でもこの時間かかりますよって言う意味で、最短何時間って言う表示も合計表示の下に書いて欲しいです」
  - 手書き図の内容（読み取り）: 縦の区切り線で「8/16まで」「8/17まで」「8/18まで」の3区間に分割。区間1は3ノード直列（合計4h）。区間2は3ノード目から2方向に分岐（上1ノード・下3ノード直列）して1ノードに合流（合計7h・最短4.5h）。区間3は1ノード（合計2h）
  - **相談で確定した仕様**:
    1. フローチャート画面の右上フローティングボタン列（ヘルプ／全体表示／印刷と同じ並び）に「日付表示」チェックボックスを追加
    2. チェックON時: 有効締切（`due_date`/`prep_date`の早い方）を基準に、日付ごとの縦区切り線＋背景色分けを表示
    3. 各日付区間内は依存関係順に、既存の上下位置をできるだけ保ちつつy軸方向で整列。x軸（横）は日付区間に合わせて実際に`flow_x`/`flow_y`を書き換える
    4. 各区間の下部に「合計Xh」（その日が締切のタスクの目安時間単純合計）と「最短Yh」（その区間内の依存関係だけで計算したクリティカルパス、区間をまたぐ依存は無視）を表示
    5. チェックON適用前の全ノード位置をバックアップし、「元に戻す」ボタンで復元可能にする（1段階Undo）
  - **→ R-109 として requests_log.md に移記済み（2026-08-17）**
  - **→ R-108 として requests_log.md に移記済み（2026-08-17）**

- **【中】R-107** フローチャートノードの状態・目安時間・期限表示を1行にまとめる
  - 2026-08-17 01:54 改善要望フォーム原文: 「フローチャート画面のノードの中のレイアウトを変えたい。現状は、状態・目安時間のあと改行して『期限 8/16』のようになっているが、これを改行をやめてたい。添付画像の例なら『Inbox 1h 8/16』と書いてほしい。8/16　と表示されている根拠がマイ期限なら青、納期なら赤　というルールであれば、『期限』というラベルも不要になる」（スクリーンショット添付、ノード「枠面取り」：現状は「枠面取り」/「INBOX 1h」/「期限 8/16」の3行）
  - R-104（フローチャートノードに納期・マイ期限をさりげなく表示）のフォローアップ
  - 指揮AI事前調査済み: `FlowItemNode.tsx` L187-223がステータス・目安時間の行（`<div className="flex items-center gap-1">`）、L224-237が納期・マイ期限の行（別の`<div>`、「納期 M/d」赤字・「期限 M/d」インジゴ字とラベル付き）で、現状は別々のdivのため改行されている。要望は両者を1つの行に統合し、「納期」「期限」のラベルテキストを削除して色（赤=納期、インジゴ=マイ期限）のみで区別する
  - **→ R-107 として requests_log.md に移記済み（2026-08-17）**

- **【中】R-105-Y2** ウィークリー/デイリーガントの時間軸ブロックに目安時間表示＋依存矢印をブロック端に合わせる
  - 2026-08-16 会話内発言原文（意訳）: 「ガントチャートのウィークリー・デイリーが良くなってる、ありがとう。2点お願いがある。1つ目、ウィークリー/デイリーのガントチャートでもノードの中心に目安時間を表記してほしい、マンスリーには書いてあるよね。2つ目、ウィークリー/デイリーのガントチャートの依存関係の矢印の始点と終点をそれぞれのノードの先頭と末尾に合わせてほしい、現状はマス目の先頭と末尾だよね」
  - 指揮AI事前調査済み: (1) `RyokanGanttView.tsx`の時間軸ブロック（`timelineMode`時、L1147-1190付近）にはテキストラベルが無く、❗️はみ出し警告のみ表示している。マンスリー表示（日次チップ、L1198-1201）は`step.allocatedMinutes >= 60 ? Math.round(...)+'h' : ''`で時間ラベルを表示済み。(2) `GanttDependencyArrows`コンポーネント（L1278-）の矢印座標計算（L1337, L1339）は`timelineMode`の有無にかかわらず常に「日付セルの右端/左端」（`(sourceDayIdx + 1) * colWidth` / `targetDayIdx * colWidth`）を使っており、時間軸ブロックの実際の開始・終了位置を考慮していない。`timelineMode`時は対応する`timeBlockLayoutsByDate`のオフセット・幅を使って始点・終点を計算するよう改修が必要
  - R-105の直接のフォローアップのためR-105-Y2として扱う
  - **→ R-105-Y2 として requests_log.md に移記済み（2026-08-16）**

- **【中】R-106** ガント列幅スライダーの最大値を表示領域幅にする＋「密度」を「行高」に改称
  - 2026-08-14 14:35 改善要望フォーム原文: 「列幅の最大が80ですが、120にして。それと、行高さ調整のスライダーの名前が『密度』ですが『行高』に変更して」
  - 2026-08-16 12:28 改善要望フォーム原文（上記を発展させた最新の要望）: 「ガントチャートの列幅のスライダーの最大が80になってますが、最大を表示領域幅にしてください」
  - 指揮AI判断: 2件は同一テーマの連続要望であり、8/16の「表示領域幅」が最新かつ8/14の固定値120を包含する上位互換の要望と判断。8/16の内容をR-106として仕様化・実装し、8/14の固定値120要望は別対応不要とする。「密度」→「行高」の名称変更も同時に対応する
  - 現状: `CalendarHeader.tsx`の`COL_WIDTH_MAX = { monthly: 80, timeline: 240 }`（R-105実装時にtimelineモード=weekly/dailyのみ240へ拡張済み。monthlyは80のまま据え置き）
  - **→ R-106 として requests_log.md に移記済み（2026-08-16）**

- **【中】R-105** ガントチャートのウィークリー/デイリー表示に時間軸タイムラインを追加
  - 2026-08-16 会話内発言原文（要約）: 「このガントチャートの週表示に追加して日表示も作って、週表示・日表示だと時間も考えられるようにできないか。何時から何時までこれをする、というのを。一日を画面幅の50%で表示したとき、その24時間のうちその日にどこまでできるかは何割か、残りはグレーの背景。そしてその一日の中で、横方向に自由にタスクを配置できる。もちろん2hかかるタスクと4hかかるタスクではバーの長さが2倍になる。どんな使い勝手になっているか相談してから実装開始してほしい」
  - 相談での追加発言: 「一日の横幅が大きく表示されたとき、その一日を100%として、左から何パーセント辺りの位置にタスクを表示するかというブロックの表示位置を記録して再現できるようにしてほしい。例えばあるタスクの目安時間が4hなら、一日が24hだから、そのブロックの幅は4/24=16.7%の幅で描画される。また、その日24hのうち左から何分の位置から取りかかるかというのを表現できるようにしたい」
  - 相談で確定した方針: (1) 稼働時間によるグレー背景は撤回・実装しない。(2) 週表示もデイリーと同じ時間軸タイムライン形式にする。(3) 自動配置は「その日の先頭、もしくは依存関係タスクのあと。それが24時間をはみ出るなら右端をその日のケツに揃えて警告マーク❗️を添える」。(4) 既存の`RyokanGanttView`を拡張する形とする
  - **→ R-105 として requests_log.md に移記済み（2026-08-16）**

- **【高】R-102** フローチャート印刷ボタンで全体が中央に印刷されない
  - **→ R-102 として requests_log.md に移記済み（2026-08-14）**

- **【中】R-103** 目安時間の全角入力を半角として認識する
  - **→ R-103 として requests_log.md に移記済み（2026-08-14）**

- **【中】R-104** フローチャートの各ノードに納期・マイ期限をさりげなく表示する（設計検討）
  - **→ R-104 として requests_log.md に移記済み（2026-08-14）**

- **【低】** 技術的負債: `DashboardScreen.tsx`内の到達不能な`viewMode==='calendar'`パスの削除・整理
  - 2026-08-14 R-097実装時にAgentが発見。`App.tsx`のルーティングでは`currentView==='calendar'`は`features/core/calendar/screens/VolumeCalendarScreen.tsx`（`// [NEW]`コメントあり）へ遷移し、`DashboardScreen.tsx`内部の`viewMode==='calendar'`という別の切替ロジックへ至るUI導線は存在しない
  - 実害はない（単に使われないコードが残っているだけ）が、将来このパスに手を加えてしまう混乱を避けるため、いずれ削除・整理する価値がある
  - 優先度低: バグではなく技術的負債の解消（R-093と同種）

- **【最優先】R-050案** テナント型AI中枢・担当者割り振り・業務ディスパッチ機能
  - **2026-07-09 kaigi2会議で意思決定済み。Phase1（画面2: 担当者別ビュー）は仕様確定・着手中のため `docs/requests_log.md` に移記した。**
  - 会議結論の全文: `docs/kaigi/2026-07-09-R050テナント型AI中枢設計.md`
  - Phase1確定仕様: `docs/SPEC/04_データ設計.md` §3.8（assigned_to値域整理・assigneeKind型）、§5.3（可視性ルール4番目・管理者スコープ）
  - Phase2以降（会社ディスパッチダッシュボード・AI割り振り提案・社員向けToday画面・担当者×日付マトリクス・プロジェクト内担当編集・meta.dispatchスキーマ等）の原文・ビジョン・少数意見は `docs/reference/vision/R050_Tenant_AI_Business_OS.md` に保存。次回Phase2以降の着手判断時に参照すること。

- **【低】R-067** アーカイブ除外のバックエンド防御強化（`is_archived`）
  - 背景: R-066 の表示条件整理で判明。`backend/GdbController.php`・`backend/TodayController.php` の SQL は `items.deleted_at IS NULL` でゴミ箱アイテムは除外しているが、`is_archived` によるアーカイブアイテムの除外条件がない
  - 現状はフロント側 `hierarchy.ts:231`（`if (item.isArchived) return false`）と `VolumeCalendarScreen.tsx:27` の `isDisplayableCalendarItem` でカバーされており実害なし。ただし将来のリグレッションリスクとして、該当 SQL に `AND COALESCE(items.is_archived,0)=0` を追加する価値がある
  - 優先度低: バグではなく技術的負債の解消

- **【低】R-093** 技術的負債: `vite.config.js`/`vite.config.d.ts`のコミット済み残骸を削除
  - 複数Agent（R-081, R-086担当）が実機検証中に発見・報告。`JWCADTategu.Web/vite.config.js`・`vite.config.d.ts`という過去に`tsc`等で生成されたコンパイル済みファイルがgit管理下に残っており、Viteの設定ファイル探索順で`vite.config.ts`より優先して読み込まれる。現状は内容がほぼ同一のため実害はないが、将来`.ts`側を編集しても`.js`側が優先され反映されない不具合の原因になりうる
  - **→ R-093 として requests_log.md に移記済み（2026-08-13）**

- **【低】R-095** バグ疑い: 全体一覧「その他」ドロップダウンで`action.onClick is not a function`例外
  - 2026-08-13 R-094-B実装Agentが実機検証中に偶発的に踏んで報告。`ContextMenu.tsx`で、全体一覧の「その他」ドロップダウンの一部項目クリック時に発生。再現手順は未特定、R-094-Bとは無関係
  - **→ R-095 として requests_log.md に移記済み（2026-08-13）**

- **【低】** Beaver（自作案件管理システム）連携: 案件からのタスク登録・引用
  - 2026-06-11 ユーザー発言: 「のちのちBeaverという自作の案件管理システムの案件からタスク登録とか引用できたらいいなとは思っているけど、いまはいい」
  - Beaver は `C:\Fujiruki\Projects\Beaver`（フロント 5178 / バック 8003）
  - 着手時は連携方向（Beaver→Youkan のタスク取り込み or 双方向）・認証方式から仕様詰めが必要

- **【中】** マネージャーAI連携用API（晴樹さん行動分析）
  - 目的: 外部のマネージャーAI（C:\claude-workspace\shared\manager\）が、指定ユーザー（例: fjt.suntree@gmail.com）のタスク一覧・focus傾向・完了状況を1日2回（12:00, 21:00）取得し、行動パターン分析に使う
  - 現状: `shared/youkan_helper.py` の `get_focus_tasks()` は毎回ログインして `/items?scope=aggregated` を叩いている。外部バッチ向けの専用口やAPIキー発行機構はない
  - 認証方式（要検討）: ユーザー発行APIキー or サービス間シークレット+対象メールアドレス指定（現行の共通 `youkan_api_token` はテナント跨ぎで危険）
  - エンドポイント案:
    - `GET /api/user/{user_id}/tasks?status=all` 全タスク+focus状態（status, due_date, completed_at, priority, project_id）
    - `GET /api/user/{user_id}/activity?since=YYYY-MM-DD` 完了履歴・ステータス変更履歴
  - レスポンス形式: JSON
  - 呼び出し頻度: 1日2回（12:00, 21:00）

- **【高】** APIで全テナントのタスクを一括取得できるようにする
  - 現状: `/api/items` はテナントごとに個別リクエストが必要。「全て」フィルタに相当するAPIパラメータがない
  - 要望: `GET /api/items?scope=all` で個人+全テナントのタスクを一括取得
  - 背景: 番頭WebUIダッシュボードから晴樹さんのフォーカスタスクを表示したいが、テナント切り替えAPIもないため全タスク取得が困難
  - 現在の回避策: 各テナントIDを個別に叩いて結合（非効率・テナント追加時にハードコード修正が必要）

- **【中】** フローチャート自動配列（依存順序通りに自動整列）

- **【中】** 操作中エラーで「はい」返答後に前の状態に戻ってしまうバグの修正

- **【中】** フローへのアイテム追加（流れの途中に追加できるようにする）

- **【低】** ショートカットヘルプ表示（一覧パネル等）

- **【中】** ガントチャートでマイ期限が依存順位の影響でずれた場合の処理
  - 「依存のせいで入れれません。これ以降のアイテムの納期をずらす？それともアイテムの納期移動をキャンセルする？」と問うダイアログ

- **【低】** コード内 GDB 用語の刷新（R-026完了後の別タスク）
  - 現状: `viewmodels/` 配下の `gdbActive`, `gdbIntent`, `gdbPreparation`, `gdbLog`, `refreshGdb`, `ghostGdbCount` 等の GDB 用語が内部変数名に残存
  - 要望: `src/features/core/youkan/viewmodels/useYoukanViewModel.ts` の上記変数名を「状況把握ボード」に沿った命名（PanoramaBoard系）に改める
  - バックエンド `backend/GdbController.php` の整理も併せて検討
  - 注意: `repositories/` 層にも波及するため、R-026（命名統一）とは独立した大仕事。別R番号で別ブランチ対応する

- **【高】R-028** 「いつかやる」（someday）状態の追加
  - 会議録: `secretary/notes/2026-05-04-会議-いつかやる状態の追加.md`
  - 要望: `JudgmentStatus` に `'someday'` を追加し5値→6値に。Inbox出口・キャパシティ計算除外・夢リストとして静かに存在
  - pending（外的要因待ち）と someday（自発的延期）を文言・色・アイコンで厳密差別化
  - ガント・カレンダーは非表示（期限なきもの）、パノラマに専用バケット追加
  - 既存遺物 `judgmentStatus.someday` をメイン status に統合
  - **→ R-028 として requests_log.md に移記済み（2026-05-04）**

- **【高】R-029** 3バグ複合修正（R-028デプロイ後の不具合）
  - 修正1: 登録と集中（stream）画面でプロジェクトアイテム（is_project=1）が Inbox/Pending/Waiting に混在表示される
  - 修正2: パノラマの「💭 いつかやる」バケットが空のまま（`getGdbShelf` レスポンスに `someday` キーが無いため）
  - 修正3: 3階層以上プロジェクトフォーカス時に孫アイテムが表示されない（`BaseController::getProjectDescendantIds()` の WITH RECURSIVE 初期行で `parent_id` を評価していないため）
  - **→ R-029 として requests_log.md に移記済み（2026-05-04）**

- **【高】R-031** ForAI機能＋タスク読み上げ機能（目玉機能復活）
  - F-19: 現フィルタで表示中のアイテムを markdown 形式に変換し、クリップボードコピーまたは md ダウンロードできる ForAI機能
  - F-20: Web Speech API（無料）で優先順位順にタスクを読み上げる。歌詞型スクロール、一時停止/再生/10秒戻る
  - 過去仕様書: `docs/SPEC/archives/ForAI機能.md`, `読み上げ機能.md`, `20260202_Youkanの概要と用語.md`
  - **→ R-031 として requests_log.md に移記済み（2026-05-07）**

- **【高】R-030** 階層ラッパー型分離・virtual-header- 概念の根本撤廃
  - 発端: 全体一覧で「フローチャートで表示」を押すと、プロジェクト名のところに `virtual-header-{realId}` という生IDが表示される
  - 根本原因: `hierarchy.ts` でヘッダーWrapperに `item: { ...proj, id: 'virtual-header-${proj.id}' }` という偽装IDを持たせていたことで、12箇所で `.replace('virtual-header-', '')` を直書きする散在コードが生まれ、剥がし忘れがフロー画面のコア機能崩壊（プロジェクトフィルタ・新規作成・依存関係）を引き起こした
  - 解決策: `HierarchicalWrapper` を discriminated union 化し、ヘッダー型は `item: Item` を持たず `projectId` / `projectTitle` / `project` フィールドで情報を保持。React key は既存の `wrapper.id`（`header-${proj.id}` 形式）を使うため衝突なし
  - 副次効果: `OverviewItem.tsx` の `onNavigateToFlow` バグ（偽装IDがフロー画面に伝搬していた）が型分離により自動解消
  - **→ R-030 として requests_log.md に移記済み（2026-05-05）**



- **【高】R-075** フローチャート/モーダル文字入力遅延の原因調査・修正
  - 2026-08-12 04:32 原文: 「フローチャートをひらいているうえのモーダル（詳細画面や改善要望を送る）のなかでの文字入力などの処理がすごく遅い。1文字に1秒くらいかかっている。原因調査して動作を早くしてほしい。codexにたのむのがよさそう。」
  - **→ R-075 として requests_log.md に移記済み（2026-08-12）。Codexへ調査・修正委譲**

- **【低】** Beaver案件↔Youkanプロジェクト連携（改善要望フォーム経由・着手時期指定あり）
  - 2026-08-12 04:40 原文: 「youkanトbeaverの連携を考えてほしい。たとえばbeaverで作った案件がプロジェクトとしてyoukanに登録されて、beaverの案件画面から『youkanで開く』ボタンを押したらｙｏｕｋａｎでプロジェクトフォーカスの状態で全体一覧画面が開いているとか。つまりyoukanでその案件のプロジェクトのタスク一覽を開ける。」
  - 発注者指定: 8/30以降に着手（それまでは仕様検討も含め着手しない）
  - 既存の低優先度項目（本ファイル内「Beaver連携: 案件からのタスク登録・引用」）と統合検討

- **【高】R-077** フローチャートでEnterキーによるノード追加が失敗する（API Error: 409）
  - 2026-08-12 発注者報告原文: 「フローチャートでノードを選んでEnterをおしたら次のように表示される。ノード追加失敗 Error: API Error: 409」
  - **→ R-077 として requests_log.md に移記済み（2026-08-12）**

- **【中】R-078** ガント/フロー右クリックメニューの「前に挿入」「後に挿入」にキーボードショートカット追加
  - 2026-08-12 05:13 原文: 「この右クリックメニューの前に挿入はa、後に挿入はbでキーボード操作できるようにして」（スクリーンショット添付、ガントチャート左列のアイテム右クリックメニュー。藤田建具店テナント「石鎚山」プロジェクトで確認。R-066で追加された「前に挿入」「後に挿入」メニュー項目が対象）
  - スクリーンショット保存先: `backend/data/requests_sub_uploads/019ff275-7c86-7151-968a-9f45cb6baa78.png`（本番サーバーのみ）
  - **→ R-078 として requests_log.md に移記済み（2026-08-12）**

- **【中】R-079** フローチャート要素タイトル編集中、テキストボックス内ドラッグでノードが動いてしまう
  - 2026-08-12 08:29 原文: 「フローチャート画面の要素のタイトルを編集できる状態のとき、テキストボックス内をドラッグすると要素がドラッグしてしまうが、そのときはテキストをドラッグで選択したいので、そのときはテキストボックスが動かないでほしい。」
  - **→ R-079 として requests_log.md に移記済み（2026-08-12）**

- **【中】R-080** フローチャートでEnter押下時、新規ノードのタイトル「新規アイテム」が選択状態になっていない（要実機確認）
  - 2026-08-12 08:32 原文: 「フローチャート画面でノードを選択した状態でEnter押したときに、新しく生まれたノードのタイトルを編集状態にして、既存のタイトル『新規アイテム』が選択された状態になっていてほしい。つまり、Enterで新ノードを作って『あ』と打ってEnterを押せば、あを打つ前は『新規アイテム』という文字が選択状態になっていて、『あ』を押してEnterを押したあとは、タイトルは『あ』で確定する状態」
  - R-074（2026-08-12デプロイ済み）で`isNewNode`による自動フォーカス＋全選択は実装済みのはず。本報告はR-074デプロイ後（07:13デプロイ、本報告08:32）のため、実際に本番で機能していない可能性がある。実装Agentは着手前に本番で再現確認し、既存実装のバグか・別シナリオ（プレーンなEnterのみでTab連鎖を使わない場合等）の話か切り分けること
  - **→ R-080 として requests_log.md に移記済み（2026-08-12）**

- **【低】R-083** Youkanのロゴマーク・アイコン作成、ファビコン設定
  - 2026-08-12 11:05 原文: 「youkanのロゴマークとアイコンをつくって。そしてファビコンを設定して。」
  - **→ R-083 として requests_log.md に移記済み（2026-08-12）**

- **【高】R-081** ガントチャートで日付未配置タスクの視覚的強調
  - 2026-08-12 会話内発言原文: 「ガントチャートでタスクはあるけど日付が決まらず配置されていないタスクは、タスク名の背景色が少し強調されるようにしてほしい。このタスクはまだカレンダー上に置いてないんだなってわかる。これは最初のデプロイに含めるくらい優先して」
  - **→ R-081 として requests_log.md に移記済み（2026-08-12）**

- **【高】** ガント「前に挿入」「後に挿入」で依存関係も自動的に繋ぎ直す
  - 2026-08-12 会話内発言原文: 「ガントチャートの後に挿入、前に挿入の機能で入力したら、依存関係も上手く挿入されてほしい。タスクA→B→Cという依存関係があり、Bの後に挿入でDを作ったとき、A→B→D→Cという依存関係が生まれるようなイメージ。」
  - 指揮AI事前調査済み: 現状`RyokanGanttView.tsx`の`submitInlineInsert()`（R-066実装）は新規アイテム作成＋`ApiClient.reorderItems()`で表示順を並べ替えるのみで、依存関係（Dependency）の作成・繋ぎ変えは一切行っていない

- **【中】R-086** フローチャートでエッジ選択時の視覚的フィードバック強化
  - 2026-08-12 23:45 原文: 「フローチャートでエッジをクリックして選択したとき、選択状態だということがわかりづらい。発光するようにしてほしい。」
  - **→ R-086 として requests_log.md に移記済み（2026-08-12）**

- **【高】R-085** フローチャート新規ノードで目安時間入力後Enter押下時「Database Error during update」エラー
  - 2026-08-12 23:47 原文: 「フローチャートで新規作成したアイテムにタイトルを決めたアト、目安時間を入力してEnterを押したら以下のエラーが出た。どういう意味かわからないため、エラーの文面を改良できるなら改良してほしい。もしシステムのバグがあるようなら直して。API通信エラー PUT /items/019ff670-0cec-7876-a67a-771ead67e71b: Database Error during update」
  - 2026-08-12 23:51 補足原文（発注者自身の仮説）: 「もしかしたらノードを選択状態からTabでも新しいノードが作れるし、Enterでも新しいノードが作れるんだ、それとTabによるタイトル入力→目安時間入力への移動と衝突しているかも。ノード選択状態とタイトル入力状態で、Tabの機能を切り替えたほうが良いね。ノード選択下状態でTabなら、枝分かれで新規ノード追加だ。タイトル入力状態でTabキーがおされたら、そのタイトルがあるノードの目安時間の編集へ移動だね。」
  - **→ R-085 として requests_log.md に移記済み（2026-08-12）**

- **【中】R-087** ガントチャート日付ヘッダーに合計時間数を表示
  - 2026-08-13 会話内発言原文: 「ガントチャートで縦（列）が日付だけど、その日付の下に合計何時間になっているのかを表示する機能をつけて。その日のキャパ割合を色で表すバーが存在するから、その下に時間数を書こうか。単位は時間だ。」
  - **→ R-087 として requests_log.md に移記済み（2026-08-13）**

- **【中】R-090** フローチャートのズームアウト制限緩和＋「全体」ズーム・パンボタン新設
  - 2026-08-13 00:22 原文: 「フローチャート画面のズームアウトの制限をもっとゆるめて、遠くまでズームアウトできるようにしてほしい。また、ヘルプのボタンの下に『全体』がちょうど入る位置にズーム・パンするボタンをつくってほしい。」
  - **→ R-090 として requests_log.md に移記済み（2026-08-13）**

- **【高】R-088** ガントチャートのダブルクリックによるノード作成時の依存関係自動設定＋エッジ描画バグ
  - 2026-08-13 00:29 原文: 「ガントチャートで何もない場所をダブルクリックしたら、新しいノードが作られる。これは便利。ただ、そのとき、なにかノードが選択された状態で、そのノードより上をダブルクリックしたらそのノードの前に行うべきだという依存関係、そのノードより下をダブルクリックしたら、そのノードの後に行うべきだという依存関係ニ設定、ノードが選択されていない状態でのダブルクリックなら、依存関係なしでのノード作成としてほしい。また、こうやって作られたノードの依存関係のエッジが描画されないバグがある。更新ボタンを押したらエッジが表示されます。このバグもなおして。」
  - **→ R-088 として requests_log.md に移記済み（2026-08-13）**

- **【中】R-089** R-085（フロー目安時間更新エラー）低頻度残存事象の追加調査
  - 2026-08-13 デプロイAgent報告: WALモード化・書き込み逐次化後もチェーン作成5回中1回、極低頻度で「Database Error during update」が再発。直後のリトライで成功しデータ損失はないが、完全解消はできていない可能性がある
  - **→ R-089 として requests_log.md に移記済み（2026-08-13）**

- **【中】R-092** 全体一覧の右クリックメニュー統一＋挿入時の依存関係自動構築
  - 2026-08-13 03:53 原文の一部: 「全体一覽画面のアイテムを右クリックした時に、ガントチャートとおなじ右クリックメニューが出るようにしてほしい、もちろん全部機能して。項目も全部同じ、後に挿入と前に挿入も同じようにつくって。（中略）また、これら挿入では依存関係が構築されるのも同じ。」
  - **→ R-092 として requests_log.md に移記済み（2026-08-13）。連続インライン入力UX（Bパート）は別要望として保留**

- **【中】R-091** 依存関係順ソートを全ビューへ展開
  - 2026-08-13 03:53 原文の一部: 「全体一覽画面、状況把握画面、グリッド、タイムラインにおいても、ガントと同じように依存関係のあるタスクは、その依存関係による前後の序列を崩さずに並ぶ。」
  - **→ R-091 として requests_log.md に移記済み（2026-08-13）**

- **【中】R-094** ガント・全体一覧の前後挿入で連続インライン入力UX（目安時間フォーカス連鎖）
  - 2026-08-13 03:53 原文の一部: 「前・後に挿入でインライン入力のテキストボックスが現れ、そこに入力してEnterで確定した場合はその後に挿入するために続けてインラインテキストボックスが出現する。と同時に、いま確定したタイトルの行の目安時間入力にフォーカスして入力状態になる。そこもEnterで確定したら、次は先程出現して空欄のままのインラインボックスにフォーカスし、入力状態になる。もし出現した状態のインラインテキストボックスが入力がされなければその行を保存せずに消える。」
  - 対象範囲確定: ガント（R-078/R-084）・全体一覧（R-092）の両方の「前に挿入」「後に挿入」に適用
  - **→ R-094 として requests_log.md に移記済み（2026-08-13）**

- **【中】R-071** 改善要望送信フォーム（画像添付付き）
  - 2026-07-25 原文: 「ホーム画面のタイトルの文字をさらに50%サイズにして、そしてその横に『改善要望を送る』ボタンをおいてほしい。押すと、改善してほしいという要望を送ることができて、それをrequests_sub.mdに追記していく形で記録しておいてほしい。それが送信された日時も併記。私達はたまにそのファイルを読んで、その機能いいね、requests.mdに書き写そう、ということをしたい」
  - 2026-07-29 追加要望: 画像（スクリーンショット）も添付できるようにしてほしい
  - **→ R-071 として requests_log.md に移記済み（2026-07-29）。仕様: `docs/SPEC/02_機能仕様.md` F-23, `docs/SPEC/03_画面設計.md` §14, `docs/SPEC/04_データ設計.md` §3.9**
