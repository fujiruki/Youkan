# 06. 変更履歴

## 2026-08-18: 今週の残量1行と登録時の一言（R-128）
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [変更] | ヘッダーの「Reality (Total Load) 123/480 min」（7px英字）を `今週 必要 Yh ／ 枠 Xh`（不足時のみ赤字 `Zh足りない`）の日本語1行に置き換え | R-126会議の採用案C。常時バー新設はしない |
| [追加] | 週負荷の式を `logic/weekLoad.ts`（TS）と `QuantityService::calcWeekLoad`（PHP）に同一定義で実装。`POST /items`・`POST /integrations/inbox`・`PATCH /items/{id}`（期限/目安変更時）のレスポンスに `week_load` を同梱、`GET /quantity/week` 新設 | 番頭が登録直後に「今週Zh足りない。外す候補: A／B」と返すため |
| [追加] | 画面側は登録・期限/目安変更の直後に不足時のみToast1回 | 判断UIなし |

### Impact
- `02_機能仕様.md` F-27（新設）、`03_画面設計.md` §16（新設）、`05_技術設計.md` R-128節（新設）
- 実装ファイル一覧は実装Agentが完了時に追記

---

## 2026-08-18: 要判断キュー「捌く」（R-127）
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [追加] | 要判断キュー（期限超過＋再確認到来）を `logic/reviewQueue.ts` で一元定義。ヘッダー件数バッジ・1日1回の誘導カード・右下オーバーレイの軽量カード ReviewSweep（今日やる／後日+7日／断った、3件で停止、数字キー操作）・全体一覧「要判断」フィルタ | R-126会議の採用案B。発注者指示で常設バナー・一括処分・アニメーションは不採用 |
| [追加] | 個人設定「判断の言葉」（`users.preferences.judgment_phrases`）。ReviewSweep に1フレーズずつ表示 | 発注者が集めている判断補助の短文（例「正しさより速さが正義」）。番頭も更新可 |
| [追加] | 「断った」に `meta.declined=true`。今週の断った件数（`cancelled`＋`status_updated_at`）を完了ビューに数字表示 | |

### Impact
- `02_機能仕様.md` F-26（新設）、`03_画面設計.md` §15（新設）、`04_データ設計.md` §3.2 preferences・§3.5 meta.declined、`05_技術設計.md` R-127節（新設）
- 実装ファイル一覧（ブランチ `feat/R-127-review-sweep`）:
  - 新規: `JWCADTategu.Web/src/features/core/youkan/logic/reviewQueue.ts`（`buildReviewQueue`/`countDeclinedThisWeek`）と同 `__tests__/reviewQueue.test.ts`
  - 新規: `JWCADTategu.Web/src/features/core/youkan/components/Review/ReviewSweep.tsx`・`ReviewPrompt.tsx` と同 `__tests__/`
  - 変更: `JWCADTategu.Web/src/features/core/youkan/screens/DashboardScreen.tsx`（キュー算出・ReviewPrompt/ReviewSweep描画・ヘッダーへの件数通知）
  - 変更: `JWCADTategu.Web/src/components/Layout/YoukanHeader.tsx`・同 `.test.tsx`（「全体一覧」タブの件数バッジ、`SubNavTab`にbadge対応追加）
  - 変更: `JWCADTategu.Web/src/features/core/youkan/screens/PersonalSettingsScreen.tsx`（「判断の言葉」テキストエリア、`users.preferences.judgment_phrases`）
  - 変更: `JWCADTategu.Web/src/features/core/youkan/components/OverviewBoard/useOverviewItems.ts`・`OverviewBoard.tsx`・`ViewControls.tsx`（全体一覧「要判断」フィルタチップ）
  - 変更: `JWCADTategu.Web/src/features/core/session/youkanKeys.ts`（`REVIEW_PROMPT_DISMISSED`/`REVIEW_SWEEP_PENDING`キー、`REVIEW_QUEUE_UPDATE`/`OPEN_REVIEW_SWEEP`イベント追加）
  - バックエンド・DBの変更なし（`preferences`は既存の汎用JSON列、`meta.declined`は既存`meta`列を利用）

---

## 2026-08-18: 状態`todo`（後日着手）追加・pending付帯情報（pending_condition/review_date）・decision_hold統合（R-125）
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [追加] | `JudgmentStatus`に`todo`（後日着手＝やると決めた順番待ち）を追加（7値→8値）。本道を`inbox → todo → focus → done`と定義 | ChatGPT議論`docs/reference/vision/2026-08-18_youkan-status-design-discussion_chatgpt.md`を採用 |
| [追加] | 判断モーダルに「後日着手」ボタン（`decisionToStatus('later')`→`todo`）。状態切替・右クリック・全画面表示に`todo`を追加。状況把握・登録と集中に「後日着手」バケット/セクション | 「今日やる」→focusは維持 |
| [追加] | `items.pending_condition`（何待ちか）・`items.review_date`（再確認日）を追加。`pending`時に詳細モーダルで任意入力。`review_date`到来分は「再確認」バッジで先頭表示 | 通知・自動復帰はR-126で検討 |
| [変更] | `pending`の定義を「条件・時期待ち／判断保留」、`waiting`を「人待ち」に明確化。`decision_hold`（判断保留）を`pending`に吸収し、`DecisionController.php`のhold書き込みを`pending`に統一、既存データを`UPDATE`で移行 | 仕様§4.4.1（hold→pending）とバックエンド実装の不一致を解消 |
| [変更] | R-028の「状態を7以上に増やさない」則を撤回し、上限を8値（本道4＋例外4）に再定義 | |
| [バグ修正] | `DecisionController.php::resolve()`の`decision='yes'`が`JudgmentStatus`型外のレガシー値`confirmed`を書き込んでいた（仕様§4.4.1違反）のを`focus`に修正。フローチャート等でこの経路を通ると全体一覧等のシェルフ分類から消える不具合があった。既存の`confirmed`データはシェルフ分類の読み取り側（`CloudYoukanRepository`/`YoukanRepository`）で`focus`相当として拾う読み取り互換のみ対応（データ移行SQLは未実施。件数を本番で確認してから別途判断） | レビューで発見（R-125実装中に同ファイルの既存バグとして検出） |

### Impact
- `04_データ設計.md` §3.1, §4.1, §4.4, §4.4.1, §4.6（新設）
- `02_機能仕様.md` F-02状態表
- `03_画面設計.md` §3.2.1, §6.1.3（新設）
- `05_技術設計.md` getGdbShelf構造、R-125技術仕様（新設）

#### 実装ファイル一覧（フロントエンド）
- `JWCADTategu.Web/src/features/core/youkan/types.ts`（`JudgmentStatus`8値化、`Item.pendingCondition`/`reviewDate`、`GdbShelf.todo`）
- `JWCADTategu.Web/src/features/core/youkan/logic/decisionResolution.ts`（`Decision`に`later`追加、`decisionToStatus('later')`→`todo`）
- `JWCADTategu.Web/src/features/core/youkan/logic/statusUtils.ts`（`STATUS_META.todo`、`isReviewDue`新設）
- `JWCADTategu.Web/src/features/core/youkan/repositories/CloudYoukanRepository.ts`（`getGdbShelf`にtodoバケット、decision_hold→pending同一視、`resolveDecision`型統一）
- `JWCADTategu.Web/src/features/core/youkan/repositories/YoukanRepository.ts`（同上・ローカルフォールバック側）
- `JWCADTategu.Web/src/features/core/youkan/viewmodels/useYoukanViewModel.ts`（`gdbTodo`状態を新設し全経路に配線、`resolveDecision`型統一、`getQuantityContext`からtodoを意図的に除外）
- `JWCADTategu.Web/src/api/client.ts`（`resolveDecision`の型統一）
- `JWCADTategu.Web/src/features/core/youkan/components/Modal/DecisionDetailModal.tsx`（「後日着手」ボタン、pending付帯情報入力欄）
- `JWCADTategu.Web/src/features/core/youkan/screens/DashboardScreen.tsx`（「後日着手」セクション、Pending再確認ソート、`unifiedAllItems`にtodo反映）
- `JWCADTategu.Web/src/features/core/youkan/components/PanoramaBoard/PanoramaBoard.tsx`（todoバケット、D&D対応、Pending再確認ソート）
- `JWCADTategu.Web/src/features/core/youkan/components/PanoramaBoard/ItemCard.tsx`（todo/再確認バッジ）
- `JWCADTategu.Web/src/features/core/youkan/components/Dashboard/SmartItemRow.tsx`（todo/再確認バッジ）
- `JWCADTategu.Web/src/features/core/youkan/components/OverviewBoard/OverviewItem.tsx`（todoステータスドット）
- `JWCADTategu.Web/src/features/core/youkan/components/OverviewBoard/useOverviewItems.ts`（`gdbTodo`をマージ対象へ追加）
- `JWCADTategu.Web/src/features/core/youkan/hooks/buildItemContextMenuActions.tsx`（`onResolveLater`メニュー項目）
- `JWCADTategu.Web/src/features/core/youkan/screens/FlowScreen.tsx`（右クリックメニューに後日着手を配線）
- `JWCADTategu.Web/src/features/core/youkan/components/Calendar/RyokanGanttView.tsx`（同上）
- `JWCADTategu.Web/src/features/core/youkan/components/Flow/FlowItemNode.tsx`（todo色）
- `JWCADTategu.Web/src/features/core/calendar/screens/VolumeCalendarScreen.tsx`（`handleDecision`型統一）

#### 実装ファイル一覧（バックエンド）
- `backend/db.php`（`pending_condition`/`review_date`カラム追加、`decision_hold`→`pending`の1回限りデータ移行）
- `backend/BaseController.php`（`mapItemRow`にキャメルケース変換追加）
- `backend/ItemController.php`（`update()`許可カラムに2フィールド追加）
- `backend/DecisionController.php`（`later`追加、`hold`の書き込み先を`decision_hold`から`pending`へ変更）
- `backend/GdbController.php`（未使用の旧エンドポイントだが仕様網羅のため最小限追随。詳細は本文の確認事項参照）

#### テスト（TDD Red→Green）
- `JWCADTategu.Web/src/features/core/youkan/logic/__tests__/decisionResolution.test.ts`
- `JWCADTategu.Web/src/features/core/youkan/logic/statusUtils.test.ts`
- `JWCADTategu.Web/src/features/core/youkan/repositories/CloudYoukanRepository.test.ts`
- `JWCADTategu.Web/src/features/core/youkan/viewmodels/__tests__/useYoukanViewModel.capacity.test.tsx`（新規）
- `JWCADTategu.Web/src/features/core/youkan/logic/QuantityEngine.test.ts`
- `backend/tests/test_r125_todo_pending.php`（新規）

---

## 2026-08-17: 「断る」判断の結果statusを`cancelled`へ統一・重複実装解消（R-124）
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [バグ修正] | フロー画面の詳細モーダルで「保留にする」を押すと三項演算子の握りつぶしで「断る」扱いになり`decision_rejected`が書き込まれるバグを修正 | 石鎚山プロジェクトで17件が意図せず却下扱いになった直接原因 |
| [バグ修正] | 状況把握・登録と集中（`useYoukanViewModel.resolveDecision`）の「断る」がstatus:`done`（完了）になっていたバグを修正 | |
| [バグ修正] | ガント右クリックメニューの「断る」がstatus:`done`を直接書き込んでいたバグを修正 | |
| [バグ修正] | `CloudYoukanRepository.getGdbShelf`のバケット分類が6値のみ判定しており、`cancelled`/`decision_rejected`のアイテムがどのバケットにも一致せずシェルフ（ひいては全体一覧）から消えていたバグを修正 | R-123で報告された症状の直接原因 |
| [追加] | `JudgmentStatus`型に`cancelled`を追加（6値→7値） | 発注者指示によりB案（却下を正式な状態として確立）を採用 |
| [追加] | `logic/decisionResolution.ts`の`decisionToStatus(decision, note)`にyes/hold/断るの状態決定ロジックを一本化 | 状況把握・全体一覧・フロー・ガント・カレンダーの重複実装を解消 |
| [追加] | `STATUS_META.cancelled`に`label`（狭い箇所用「断った」）・`labelWide`（広い箇所用「キャンセル・断った」）を追加 | 発注者指示: 「キャンセル」だけの婉曲表現を避け「断った」を必ず残す |
| [変更] | `hierarchy.ts`の`hideCompleted`が`done`と同様に`cancelled`も隠すよう変更 | 「履歴」として`done`と同じグループ扱い |
| [変更] | バックエンド`DecisionController.php`の`no`判定結果を`decision_rejected`から`cancelled`に変更、`CalendarController.php`/`GdbController.php`のステータス除外・包含リストに`cancelled`を追加 | 既存の`decision_rejected`データは表示互換のため読み取り側で`cancelled`同様に扱う（データ移行は別タスク） |

### Impact
- `JWCADTategu.Web/src/features/core/youkan/types.ts`
- `JWCADTategu.Web/src/features/core/youkan/logic/decisionResolution.ts`（新規）
- `JWCADTategu.Web/src/features/core/youkan/logic/statusUtils.ts`
- `JWCADTategu.Web/src/features/core/youkan/logic/hierarchy.ts`
- `JWCADTategu.Web/src/features/core/youkan/viewmodels/useYoukanViewModel.ts`
- `JWCADTategu.Web/src/features/core/youkan/screens/FlowScreen.tsx`
- `JWCADTategu.Web/src/features/core/youkan/components/Calendar/RyokanGanttView.tsx`
- `JWCADTategu.Web/src/features/core/youkan/components/Flow/FlowItemNode.tsx`
- `JWCADTategu.Web/src/features/core/youkan/hooks/buildItemContextMenuActions.tsx`
- `JWCADTategu.Web/src/features/core/youkan/repositories/CloudYoukanRepository.ts`
- `JWCADTategu.Web/src/features/core/youkan/repositories/YoukanRepository.ts`
- `backend/DecisionController.php`
- `backend/CalendarController.php`
- `backend/GdbController.php`
- `04_データ設計.md` §4.1, §4.4.1

---

## 2026-08-14: フローチャート画面に全体印刷ボタンを追加（R-101）
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [追加] | `FlowScreen.tsx`に「印刷」ボタンを追加。押下時、既存の「全体表示」相当の`fitView`をduration 0で実行してから`window.print()`を呼ぶ | R-098と同じ`.no-print`/`@media print`の仕組みを再利用 |

### Impact
- `FlowScreen.tsx`
- `03_画面設計.md` §7.9

---

## 2026-08-14: ガント完了アイテムのカレンダー要素・依存関係エッジをグレー化（R-100）
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [変更] | 完了表示トグルon時、完了済アイテムの日別割当チップ・目安納期ハンドルをグレー系に変更 | 従来は完了状態に関わらず`bg-indigo-500`/`bg-indigo-400`固定だった |
| [変更] | 依存関係矢印（`GanttDependencyArrows`）を、source/targetいずれかが完了済みならグレー系にする | 従来は`stroke="#6366f1"`固定だった |

### Impact
- `RyokanGanttView.tsx`
- `03_画面設計.md` §5.4

---

## 2026-08-14: GET /itemsに依存関係グラフを埋め込み（R-099）
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [追加] | `GET /items`系レスポンスの各アイテムに`dependsOn`（前提アイテムID配列）・`blocks`（後続アイテムID配列）を追加 | `GET /dependencies`を別途叩かなくても依存グラフを1回のリクエストで読めるようにする。AI等の外部クライアント向け |
| [設計判断] | N+1クエリを禁止し、`item_dependencies`をテナントスコープで一括取得してからメモリ上で隣接リストを構築する方式とした | R-091で「起動時APIコスト増」を理由に避けた埋め込みを、コストを抑えた形で今回は許容 |

### Impact
- `ItemController.php`
- `04_データ設計.md` §3.5

---

## 2026-08-14: ガントのマンスリー/ウィークリー表示モード＋コンテンツ印刷ボタン（R-097, R-098）
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [追加] | ガントビューに「マンスリー/ウィークリー」表示モード切替トグルを追加（R-097） | 日付範囲自体は変化しない。呼称は列幅・行高さの記憶枠の切替ラベル |
| [追加] | 日付列幅（列方向）スライダーを新設（R-097） | 既存の行高さ（行方向）スライダーと並べて常時表示。`RyokanGanttView.tsx`の`colWidth`固定値24pxを可変化 |
| [追加] | モードごとに列幅・行高さを独立して`localStorage`に記憶（R-097） | マンスリー/ウィークリーそれぞれの調整値をモード切替後も保持・復元 |
| [追加] | ガント・全体一覧にメニュー部分を除いたコンテンツのみを印刷するボタン（R-098） | `window.print()` + `@media print` CSS。専用PDFライブラリは不使用 |

### Impact
- `CalendarHeader.tsx`
- `RyokanGanttView.tsx`
- `youkanKeys.ts`（新規`localStorage`キー）
- `OverviewBoard.tsx`（印刷ボタン）
- `03_画面設計.md` §5.3

---

## 2026-08-12: Flow 上のモーダル入力応答改善（R-075）

- Flow の animated edge と全画面 `backdrop-filter` が同時に動作する合成負荷を避けるため、モーダル表示中だけ edge animation を一時停止する。
- モーダルを閉じると animation は自動的に再開し、Flow 単体の表示仕様は変更しない。
- ガントでも改善要望入力の遅延が報告されたため、共通要因である `SimpleModal` の全画面 `backdrop-filter` を廃止し、半透明背景のみとする。
- 判断詳細モーダルも同じ再合成負荷を避けるため `backdrop-filter` を廃止し、半透明背景のみとする。

## 2026-07-31: due_status意図しない変化の修正（R-073）
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [バグ修正] | `SmartDateInput`の`onBlur`で、パース結果が既存値と同じでも`onChange`が発火し、`DecisionDetailModal`側が無条件に`dueStatus: 'confirmed'`を送信していた。差分チェックを追加し、値が実際に変化した場合のみ通知するよう修正 | 本番検証（R-070デプロイ時）で「納期」フィールドをクリックしただけで`due_status`が`null`→`confirmed`に変化する副作用として発見 |
| [文書化] | `due_status`の値域（`null`/`confirmed`/`waiting_external`）と遷移条件（`due_date`の入力・変更・クリア時のみ更新）を現行仕様書に明記。旧アーカイブ設計書にのみ存在し現行仕様に未記載だった | `04_データ設計.md` §4.5 新設 |

### Impact
- `SmartDateInput.tsx`
- `DecisionDetailModal.tsx`（`dueStatus`送信箇所）
- `04_データ設計.md` §4.5

---

## 2026-07-31: Google OAuth失効検知・再連携UX実装（R-072）
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [バグ修正] | `GoogleCalendarService::refreshAccessToken()`が`invalid_grant`を判別せず、設定画面には「連携解除されました」表示が仕様として存在したのに実装が漏れており到達しなかった。`invalid_grant`検知時に`user_google_oauth.invalidated_at`/`last_error`へ記録し、設定画面の失効表示・再連携導線を実際に機能させる | 本番で`/google/calendars`が`invalid_grant`起因の500を継続的に返していたことを検証中に発見 |
| [設計判断] | 根本原因（OAuth同意画面が「テスト中」ステータスのままだとリフレッシュトークンが7日で強制失効）はGoogle Cloud Console側の設定変更が必要で、発注者本人のみが対応可能。今回のコード修正はあくまで検知・UX側の対症療法と位置付ける | `04_データ設計.md` §3.6 に注記 |

### Impact
- `backend/services/GoogleCalendarService.php`
- `backend/GoogleCalendarController.php`
- `user_google_oauth`テーブル（`invalidated_at`/`last_error`カラム追加、マイグレーション必要）
- `GoogleCalendarSection.tsx`
- `03_画面設計.md` §12.1, `04_データ設計.md` §3.6

---

## 2026-07-29: 改善要望送信フォーム（F-23, R-071）仕様確定
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [新規] | ホーム画面ヘッダーから画像添付付きで改善要望を送信できるフォームを追加 | ロゴ50%縮小＋ボタン配置（PC）、MenuDrawer「ツール」追加（スマホ） |
| [設計判断] | 送信内容はDBではなくサーバーローカルの `backend/data/requests_sub.md` に直接追記。回収は手動SSH/scpのみでAPI同期は持たない | 発注者確認済み。簡便さを優先し、複雑な同期APIは作らない方針 |
| [設計判断] | `backend/data/` を `upload.ps1` のデプロイ除外対象に追加する必要あり（既存の `.env`/`*.sqlite`/`*.log` 除外と同じ理由） | 怠ると次回デプロイで本番の蓄積データが消失するため実装時必須確認事項として明記 |

### Impact
- F-23 改善要望送信フォーム
- `03_画面設計.md` §14, `04_データ設計.md` §3.9
- `upload.ps1`（デプロイ除外リスト変更が必要）

---

## 2026-07-10: R-050 Phase1実装完了（担当者別ビュー・管理者スコープ403バグ修正）

### 出典
バックエンド実装引き継ぎ: `docs/handover/R-050-phase1-backend.md`

### 決定事項
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [完了] | バックエンド実装（管理者スコープAPI新設・`assigned_to` ID空間解決の横展開）をマージ | `feature/R-050-phase1-assignee-view` ブランチ、マージコミット `322caf1` |
| [完了] | フロントエンド実装（担当者別ビュー画面 `AssigneeViewScreen`）をマージ | `feature/R-050-phase1-assignee-view-ui` ブランチ、マージコミット `67e2586` |
| [修正] | 実機検証（claude-in-chromeでブラウザ操作）で発見したバグ「会社アカウント（テナント自身ログイン、`account_type=tenant`）で管理者スコープ切替が403になる」を修正 | `fix/R-050-phase1-tenant-account-admin-scope` ブランチ、マージコミット `ce65aa2` |

### 原因（会社アカウント403バグ）
`BaseController::assertAdminScopeAllowed()` が `memberships` テーブルを `user_id = currentUserId` で検索していたが、会社アカウントログイン時は `currentUserId` がテナントID自身であり `memberships` に該当行が無いため権限判定に失敗していた。修正後、会社アカウント・ユーザーアカウント両方で200を確認、テナント分離（他テナントのID指定で404）も維持されていることを確認済み。

### 影響範囲
- 担当者別ビュー（画面2）はエンドツーエンドで動作する状態になった
- **本番デプロイ（`upload.ps1`）は未実施**。次回セッションでデプロイ判断を行う（`docs/handover/R-050-phase1-backend.md` 参照）

---

## 2026-07-09: R-050 Phase1 フロントエンド仕様確定（担当者別ビュー画面設計）

### 出典
バックエンド実装引き継ぎ: `docs/handover/R-050-phase1-backend.md`／会議録8節ステップ3: `docs/kaigi/2026-07-09-R050テナント型AI中枢設計.md`

### 決定事項
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [追加] | `ViewState = 'assigneeView'` として独立画面を新設 | 画面4（社員向けToday）とは統合しない。03_画面設計.md §8, §13 |
| [追加] | デフォルト表示は `scope=team&assigned_to=<自分のuserId>` 固定 | 既存ルール1〜3（created_by=自分 OR assigned_to=自分）ではなく assigned_to のみに絞る。03_画面設計.md §13.2 |
| [追加] | 担当者切り替えUIは `tenant.role IN ('owner','admin')` のみ表示 | 候補は `GET /tenant/members`（users由来）と既存 `useAssignees()`（assignees由来）をマージ。03_画面設計.md §13.3 |
| [確定] | `ApiClient.getMembers()`（`/members`宛）は実体不一致のバグのため本機能では使用せず、`CompanySettingsScreen.tsx` と同じ `GET /tenant/members` 直叩きに統一する | 既存バグの修正自体は本タスクのスコープ外 |
| [追加] | 今日／明日／今週の日付バケット分類・所要時間合計・未完了/詰まり/待ち集計のロジックをPhase1で新規定義 | 03_画面設計.md §13.5〜§13.7。評価語（「超過」等）は非審判原則を踏まえ不採用 |
| [追加] | `Item.assigneeKind` 型・`ApiClient.getAllItems()` の `scope:'team'`/`assigned_to` オプションをフロントに追加 | 03_画面設計.md §13.8 |

### 影響範囲
- フロントエンド新規実装は別Agentへ委譲（`feature/R-050-phase1-assignee-view-ui` ブランチ）
- バックエンド（`backend/`配下）は変更しない

---

## 凡例

- **[新規]**: 新しい仕様の追加
- **[変更]**: 既存仕様の変更
- **[修正]**: バグ修正に伴う仕様の明確化
- **[統合]**: 既存ドキュメントからの統合

---

## 2026-07-09: R-069 `/stocks` 未認証アクセス脆弱性の緊急修正

### 背景
R-050案の設計会議調査で、本番稼働中の `backend/index.php` に登録された `/stocks` 系ルート（`GET/POST /stocks`、`PUT/PATCH /stocks/{id}`、`POST /stocks/{id}/assign`）が `StockController.php` にディスパッチされる際、`authenticate()` が一切呼ばれず `tenant_id` によるスコープ分離も無いまま到達可能であることが判明した。特に `assign()` はリクエストの任意の `userId` を検証せず `items` テーブルへ直接 `INSERT` できるため、未認証の第三者が他テナントの `items` にレコードを書き込める状態だった。

### 調査結果
- フロントエンドで `/stocks` API を呼ぶのは `useStocks.ts`（呼び出し元は `DispatchScreen.tsx` のみ）のみ。`DispatchScreen.tsx` と `CompanyDashboard.tsx` はアプリのルーティング・画面遷移から一切参照されていない死蔵コードであることを確認済み（相互参照のみ）。
- したがって `/stocks` API は実際には稼働中のどの画面からも呼ばれておらず、実害のある機能を壊さずにルート自体を無効化できると判断した。

### 決定事項
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [修正] | `backend/index.php` から `/stocks` 系ルート登録（3ブロック）をコメントアウトし無効化 | `StockController.php` 自体は削除せず残置。将来再導入する場合は `authenticate()` と `currentTenantId` スコープを実装してから有効化する旨をコード内コメントに明記 |
| [追加] | `backend/tests/test_stocks_unauthenticated_access.php` を新規追加 | PHP内蔵サーバーを一時ディレクトリで起動し、Authorizationヘッダーなしで `/stocks` 系3エンドポイントを叩き 404 になることを検証する統合テスト |

### 影響範囲
- `backend/index.php`（`/stocks` 系ルート3ブロックの無効化のみ。他ルートは無変更）
- フロントエンド変更なし（`useStocks.ts`/`DispatchScreen.tsx`/`CompanyDashboard.tsx` は死蔵のまま残置、再利用時は要再設計）
- ブランチ: `fix/stock-controller-auth`（R-050 Phase1作業とは分離）

---

## 2026-07-09: R-050 Phase1着手（テナント型AI中枢・担当者別ビュー）意思決定

### 出典
kaigi2会議（専門家6名 × 2ラウンド + 敵対的検証23エージェント）: `docs/kaigi/2026-07-09-R050テナント型AI中枢設計.md`

### 決定事項
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [変更] | 可視性ルールに管理者スコープ（4番目）を新設 | `memberships.role IN ('owner','admin')` のユーザーは所属テナント内で `assigned_to` を問わず閲覧可能。既存ルール1〜3は変更なし。04_データ設計.md §5.3 |
| [修正] | `assigned_to` のID空間表示バグ（会社アイテムの97%が `u_` 形式でJOIN失敗）をアプリケーション層で是正する方針を確定 | discriminator列追加マイグレーションは不採用。`ItemController.php::getAssigneeEmail()` の `u_` プレフィックス判定を一覧系クエリへ横展開する。04_データ設計.md §3.8 |
| [追加] | 担当候補の型 `assigneeKind: 'user' \| 'assignee'` を確定 | Phase3（AI提案設計）以降で再定義しない。04_データ設計.md §3.8 |
| [明確化] | 孤児データ（どちらのIDにも一致しない `assigned_to` 値）は自動修復せず「未割当」表示＋ログ出力とする | 実数の棚卸しはPhase1実装時またはPhase2着手前に別途実施 |
| [確定] | Phase1のスコープは「画面2: 担当者別ビュー」単体に限定 | 画面4（社員向けToday）との統合（会議での候補C）は不採用。Phase6検討時に再評価 |
| [先送り] | meta.dispatchスキーマ・dispatch_proposalsテーブル・承認ACL・サーキットブレーカー設計はPhase3着手判断時に改めて統合設計 | 今回は設計対象外 |
| [設計注記] | `DispatchScreen.tsx` / `CompanyDashboard.tsx` / `StockController.php` は土台にしない | 詳細は下記「調査結果」参照 |

### 調査結果（既存3ファイルの扱い）
- `JWCADTategu.Web/src/features/core/youkan/screens/DispatchScreen.tsx`、`CompanyDashboard.tsx`: どちらもモックデータのみのコンポーネントで、アプリ内のルーティング・画面遷移から一切参照されていない（相互参照のみ、`DispatchScreen.tsx` が `useStocks` フックを使用）。最終更新は2026-02-24で以降凍結。**死蔵コード。再利用禁止。**
- `backend/StockController.php`: kaigi会議の想定（「router.php未登録のため実害ゼロ」）とは異なり、実際には `backend/index.php` に `require_once 'StockController.php'` の上で `/stocks`・`/stocks/:id`・`/stocks/:id/assign` の3ルートが登録済みで、`router.php` 経由で到達可能。`authenticate()` 呼び出しも `tenant_id` チェックも一切経由せず、`assign()` は任意の `userId` を受け取って直接 `items` テーブルへ `INSERT` する。**単なる「再利用禁止」注記では不十分。認証・テナント分離が皆無のまま本番到達可能な状態にあるため、削除または認証追加を伴う修正が必要。** コード変更は本ドキュメント作業のスコープ外のため未対応。別タスクとして緊急度高めに起票することを推奨。

### 影響範囲
- コード変更なし（本エントリはdocs配下の仕様整理のみ）
- Phase1のバックエンド/フロントエンド実装は別Agentへ委譲（`feature/R-050-phase1-assignee-view` ブランチ予定）

---

## 2026-07-08: 量感カレンダー/ガントの表示条件整理（R-066）

### 決定事項
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [明確化] | 期限が過去でも未完了アイテムはガント・量感カレンダーに表示し続ける仕様は意図的である | 督促目的。バグではなく対応不要と確定 |
| [明確化] | 完了済みアイテムの表示/非表示は R-065 右上「完了表示」トグルによる意図的仕様 | バグではない |
| [確認] | 削除済みアイテム（ゴミ箱）は `GdbController.php`/`TodayController.php` の SQL で `deleted_at IS NULL` により元々除外済み | — |
| [確認] | アーカイブアイテムはバックエンド SQL に `is_archived` 除外の穴があるが、フロント `hierarchy.ts` の `isArchived` 判定と `VolumeCalendarScreen.tsx` の `isDisplayableCalendarItem` で現状実害なくカバーされている | バックエンド防御強化は技術的負債として R-067 に分離 |

### 影響範囲
- ガントチャート・量感カレンダーの表示条件そのものはコード変更なし（判断確定のみ）
- 未着手事項は `docs/requests.md` に R-067（アーカイブ除外のバックエンド防御強化・低優先度）、R-068（量感カレンダー本体のスクロール性能改善・高優先度）として分離起票

---

## 2026-07-08: 詳細カレンダーのスクロール即応性改善

### 決定事項
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [修正] | 詳細画面内の量感カレンダーでは、通常カレンダー用のスクロール処理を接続しない | 表示月同期・範囲自動拡張が不要なため |
| [修正] | 詳細カレンダーの量感セルでは装飾トランジションと不要なペイント範囲を抑制する | スクロール開始時の体感遅延を減らす |

### 影響範囲
- 詳細画面左カラムの量感カレンダー
- 通常の量感カレンダー、ガント、タイムラインの範囲拡張・表示月同期は維持

---

## 2026-07-06: 量感カレンダー分母ルールの確定

### 決定事項
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [変更] | 個人アカウントの「全て」は本人の業務キャパシティ合計を分母にする | 所属会社ごとのキャパシティは足し上げない |
| [変更] | 個人/会社/個別会社フィルタごとに分母を切り替える | 個人=総量-会社配分、会社=会社配分合計、個別会社=該当会社配分 |
| [追加] | 会社/チームスコープの分母を主力メンバーのキャパシティ合計と定義 | 現行実装は `memberships.is_core=1` の `daily_capacity_minutes` 合計 |
| [明確化] | 会社配分未設定の会社は 0 分として扱う | 総キャパシティへフォールバックしない |
| [追加] | メンバー別 `capacity_profile` を `memberships` に保存 | 会社/チーム分母に曜日別・日別例外・会社別配分を反映 |

### 影響範囲
- F-06 量感カレンダーのヒートマップ背景色と CapacityBar
- 詳細画面の量感カレンダーは同じ `QuantityEngine` を使うため同じ分母ルールに従う
- `/members` と `/tenant/members` は `capacityProfile` を返す。`/tenant/members/{id}` は `capacity_profile` を保存する
- 会社/チーム分母は、core member の `capacityProfile` を優先し、未設定時は `dailyCapacityMinutes` を使う

---

## 2026-06-15: スマホ詳細画面レイアウト再設計（R-064）

### 決定事項
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [変更] | スマホ横画面を2カラム化（カレンダー左・フィールド右） | `landscape-compact:` バリアント限定（高さ≤500px の横向きのみ） |
| [変更] | ヘッダーをコンパクト化 | タイトル縮小・視点ラベルバッジ非表示・日付ピル化 |
| [変更] | クイック選択ボタン群を横画面で非表示 | タップ入力（SmartDateInput）は維持 |
| [変更] | メモ/Boost をフィールドカラムへ移動 | カレンダー高さを最大化 |
| [変更] | 縦画面: カレンダー下に独立スクロール領域 | ネストスクロール回避 |
| [変更] | 目安期間ブレイクダウンを折りたたみ（デフォルト閉じ） | カレンダー直下に配置 |
| [追加] | Tailwind カスタムスクリーン `landscape-compact` | `(orientation: landscape) and (max-height: 500px)` |

### 出典
secretary/notes/2026-06-15-会議-スマホ詳細画面レイアウト再設計.md

### 影響範囲
- デスクトップ（md以上・高さ大）・縦画面の見た目は不変
- 横画面でも SmartDateInput によるタップ入力機能は維持

### 棄却した提案
- `landscape:` バリアント（デスクトップ横向きにも適用されたため `landscape-compact:` に変更）

---

## 2026-06-15: 詳細カレンダー Google 連携・量感フィルタ連動・時刻非表示（R-061〜R-063）

### 決定事項
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [追加] | 詳細モーダルカレンダーに Google 外部イベントを供給・表示（R-061） | `useExternalEvents` + `useGoogleCalendars` を `DecisionDetailModal` 内で呼び出し、3層 props で伝達 |
| [追加] | 密度トグル FULL / COMPACT（R-061） | `localStorage` キー `youkan_detail_calendar_density`、デフォルト FULL |
| [変更] | 詳細カレンダーの量感母集団を左上フィルタに連動（R-062） | `focusedTenantId` / `focusedProjectId` を渡さない方針。アイテム所属による絞り込みを廃止 |
| [変更] | 詳細カレンダーの外部イベントチップ時刻を非表示（R-063） | `ExternalEventChip.hideTime=true` prop 追加。グリッドビューは時刻表示を維持 |

### 影響範囲
- グリッドビュー（`VolumeCalendarScreen`）の表示は変更なし
- `ExternalEventChip` に `hideTime` prop が追加されるが、既存の全呼び出し側はデフォルト `false` のため影響なし

---

## 2026-05-11: スマホUI改善＋ボトムシート基盤（R-033）

### 決定事項
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [追加] | F-21 スマホフィルター | ヘッダー右に Filter アイコン＋ボトムシート |
| [追加] | F-22 スマホ用その他メニュー | DecisionDetailModal でスマホ時ボトムシート化 |
| [追加] | MobileBottomSheet 共通コンポーネント | safe-area対応、touch-action抑制 |
| [追加] | スマホメニュー設計原則6ルール | 恒久メンテナンス則 |
| [変更] | スマホヘッダー右側整理 | ForAi/Speech をヘッダー外へ集約、Filter を追加 |

### 出典
secretary/notes/2026-05-11-会議-スマホUI改善.md

### 影響範囲
- 新機能のため既存PCの挙動への影響なし
- スマホ表示時のヘッダー構成のみ変更（ForAi/Speech は MenuDrawer/フローティングで継続アクセス可）

### 棄却した提案
- アナリティクス導入（別タスク）
- SimpleModal 破壊的変更（リスク回避）
- WCAG 完全準拠（別タスク）

---

## 2026-05-07: 目玉機能復活 — ForAI機能＋タスク読み上げ機能（R-031）

### 決定事項
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [追加] | F-19 ForAI機能（AI連携エクスポート） | 過去仕様書から復活実装 |
| [追加] | F-20 タスク読み上げ機能 | Web Speech API、優先順位順、歌詞型スクロール |
| [追加] | 共通ユーティリティ6種（clipboard, useMediaQuery, speechSynthesis, useSpeechSynthesis, forAiExporter, perspectiveLabel） | 横断的に利用可能 |
| [変更] | YoukanHeader 右側に ForAI/読み上げボタン追加 | PC 用 |
| [変更] | MenuDrawer Tools セクションに ForAI 追加 | スマホ用 |

### 出典
過去仕様書: `docs/SPEC/archives/ForAI機能.md`, `読み上げ機能.md`, `20260202_Youkanの概要と用語.md`

### 影響範囲
- 新機能のため既存挙動への影響なし
- マイグレーション不要

### 棄却した提案
- JSON 出力対応（v2 で検討）
- ForAI 拡張の依存関係/場所/タスク種類（20260305 提案、別 task）

---

## 2026-05-05: 階層ラッパー型分離・virtual-header- 撤廃（R-030）

### 決定事項
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [変更] | HierarchicalWrapper を discriminated union 化 | item/header の型分離 |
| [削除] | virtual-header- プレフィックス生成（hierarchy.ts 2箇所） | 偽装ID廃止 |
| [削除] | .replace('virtual-header-', '') 散在コード（12箇所） | 不要化 |
| [削除] | OverviewBoard.tsx のローカル stripId 関数 | 不要化 |
| [修正] | OverviewItem の onNavigateToFlow バグが自動解消 | 型分離の副次効果 |
| [追加] | hierarchy.types.test.ts | 新型のTDDテスト |

### 影響範囲
- マイグレーション不要
- 既存ユーザー操作: 改善のみ（フロー画面のコア機能回復）
- 過去 URL ブックマーク（virtual-header-付き）は今後生成されないため、既存のものは破損リスクなし

### 設計判断
- TDD・MVVM・リアクティブ・メンテナンス性を最優先
- TypeScript の型システムで「ヘッダーから item.id を取り出す」誤りをコンパイル時に検出
- 関連する別R番号タスクで検討した「共通ヘルパー idUtils.ts」「多層防御」は型分離により不要となり全廃

### メンテナンス則
- 階層ラッパー型は discriminated union（type プロパティで分岐）
- ヘッダー型は item: Item を持たない
- React key には wrapper.id を使う
- item.id 加工パターン禁止

---

## 2026-05-04: R-029 3バグ複合修正

### 決定事項
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [変更] | Stream モードで is_project=1 アイテムを非表示 | プロジェクトはパノラマ・全体一覧で扱う |
| [追加] | getGdbShelf レスポンスに someday キー追加 | R-028 の暫定派生フィルターを是正 |
| [変更] | getProjectDescendantIds の WITH RECURSIVE 初期行に parent_id 追加 | 3階層以上のプロジェクトフォーカスで子孫取得 |

### 影響範囲
- マイグレーション不要
- 既存ユーザー操作感: 改善のみ（バグ修正）

---

## 2026-05-04: 「いつかやる」状態追加（R-028）

### 決定事項
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [追加] | JudgmentStatus に 'someday' を追加 | 5値→6値 |
| [追加] | パノラマに Someday バケット | Pending と Log の間 |
| [追加] | 全体一覧に Someday含む/除く フィルター | デフォルト除く |
| [追加] | 詳細モーダルに 💭 いつかやる 状態ボタン | purple 色 |
| [変更] | キャパシティ計算から someday を除外 | Youkan独自の差別化 |
| [変更] | ガント・カレンダーで someday 非表示 | 期限なきもの |
| [整理] | 既存遺物 judgmentStatus の someday を廃止 | メインstatusに統合 |

### 設計判断（会議録参照）
secretary/notes/2026-05-04-会議-いつかやる状態の追加.md

### 影響範囲
- データマイグレーション不要
- 既存ユーザー: someday 状態は明示的に使うまで影響なし
- 付随機能（自動アーカイブ・リマインダー）は別task

### 棄却した提案
- マネタイズ起点議論
- 自動アーカイブ・シャッフル表示・週次リマインダー（別task）
- フラグ別軸での実装

---

## 2026-04-27: 親→子孫カスケード機能（R-027）

### 決定事項
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [追加] | テナント/プロジェクト移動カスケード | 子孫の tenant_id 連動 |
| [変更] | archive/trash/restore/delete カスケード | is_project 条件撤廃、親子関係で常に伝播 |
| [追加] | 移動先テナント権限チェック | joinedTenants 外で 403 |
| [追加] | API応答に affectedDescendantIds | 部分リフレッシュ用 |
| [追加] | ViewModel: refreshItems / optimisticCascadeTenant | 楽観的UI更新＋ロールバック |
| [追加] | バックエンドヘルパー: cascadeTenantToDescendants 等 | update/archive 等から共用 |
| [追加] | バッチ取得API: GET /items/batch or POST /items/batch | refreshItems 用 |

### 影響範囲
- 既存ユーザーの操作感: 親操作で子孫が連動するように変わる（仕様書の意図通り）
- API互換: 戻り値に affectedDescendantIds を追加（既存呼出側は無視可、後方互換）
- DB マイグレーション不要

---

## 2026-04-22: 命名統一リファクタリング（R-026）

### 決定事項

| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [上書き] | R-008「内部モード値・コンポーネント名は変更なし」方針を破棄 | UIラベルと内部名称の一致を優先 |
| [変更] | `GlobalBoard`/`YoukanBoard` → `PanoramaBoard` | フォルダ・ファイル・コンポーネント・Props名統一 |
| [変更] | `NewspaperBoard` → `OverviewBoard` | 同上 |
| [変更] | viewMode値 `'board'` 廃止 → `'panorama'` | ヘッダー送信値と内部値を一致 |
| [変更] | viewMode値 `'newspaper'` → `'overview'` | UIラベル「全体一覧」と整合 |
| [追加] | `ViewModeContext` 新設 | CustomEvent駆動→Context駆動に移行。FilterContextとの責務分離（ViewMode=画面切替、Filter=テナント絞り込み） |
| [追加] | `components/Common/` 新設 | ContextMenu等の複数ビュー横断共通部品置き場 |
| [追加] | `logic/filterUtils.ts` 新設 | `isTenantFilter`, `isCompanyContext`, `getSelectedTenantId` ヘルパー関数群 |
| [追加] | `migrateLocalStorage` 実装 | 旧値→新値の自動変換（`youkan_schema_version=2` ガードで冪等保証） |

### 影響範囲

- 既存ユーザーの localStorage は自動マイグレーション（`'board'`→`'panorama'`、`'newspaper'`→`'overview'`、`youkan_newspaper_*`→`youkan_overview_*`）
- 外部APIへの影響なし
- `docs/SPEC/03_画面設計.md` §5.5 にコンポーネント命名規約を新設
- `docs/SPEC/05_技術設計.md` のディレクトリ構造更新

---

## 2026-04-02: フローView仕様追加（R-014）

| 種別 | 内容 | 反映先 |
|:--|:--|:--|
| [新規] | フローView（5つ目のView）の仕様策定 | 03_画面設計.md §7 |
| [新規] | `item_dependencies` テーブル定義 | 04_データ設計.md §3.5 |
| [新規] | 依存関係CRUD API定義 | 04_データ設計.md §5.1 |

---

## 2026-04-02: ガント一覧モード仕様追加+バグ修正（R-011〜R-013）

| 種別 | 内容 | 反映先 |
|:--|:--|:--|
| [新規] | ガントビューの「プロジェクト別/一覧」切替仕様を明文化 | 03_画面設計.md §5.3 |
| [新規] | 一覧モード: プロジェクト非表示・フラットリスト・ソート順・プロジェクト名末尾表示 | 03_画面設計.md §5.3 |
| [修正] | CalendarController.php: is_projectカラムSELECT追加+プロジェクト除外フィルタ | — |
| [修正] | CalendarController.php: SQLパラメータ数不一致修正 | — |
| [修正] | 本番DB不正データ（ID=NULLの「総会」レコード）を削除 | — |
| [修正] | hierarchy.ts: IDがnullのプロジェクトをprojectMapから除外する防御コード | — |
| [修正] | 循環参照防止付きツリー構築関数をhierarchy.tsに共通化 | — |

---

## 2026-03-31: View名変更（R-008）

| 種別 | 内容 | 反映先 |
|:--|:--|:--|
| [変更] | 「全体一覧」(Panorama)→「状況把握」に名称変更 | 03_画面設計.md §1, §3 |
| [変更] | 「全体一覧2」(Newspaper View)→「全体一覧」(Overview View) に名称変更 | 03_画面設計.md §1, §4 |

~~内部モード値(`panorama`, `newspaper`)、URL、localStorageキー、コンポーネント名は変更なし。~~

> **注意**: 上記の「コンポーネント名は変更なし」方針は **R-026（2026-04-22）で上書き**された。UIラベルと内部名称の一致を優先する方針に変更。

---

## 2026-03-31: 仕様追加（R-004〜R-007）

| 種別 | 内容 | 反映先 |
|:--|:--|:--|
| [変更] | ブラウザタブタイトルを「JBWOS」→「Youkan」に変更 | 03_画面設計.md §0 |
| [新規] | Overview View（旧: Newspaper View / 全体一覧）の初期フィルタを「全て」に固定 | 03_画面設計.md §4.0 |
| [新規] | `completed_at`カラム追加。完了日の記録+カレンダーでの振り返り表示 | 04_データ設計.md §3.1, 02_機能仕様.md §F-02, §F-06 |
| [新規] | Panorama View「今月を表示」ボタンで上下左右の中央スクロール | 03_画面設計.md §3.3 |

---

## 2026-03-24: SdDD形式への仕様書統合

### 実施内容

SdDD（仕様書駆動開発）の構造を導入。既存の散在したドキュメント群からYoukanの最新仕様を抽出し、以下のファイルに統合した。

| ファイル | 統合元 |
|:--|:--|
| `SPEC.md` | 新規作成（目次・概要） |
| `spec/01_概要.md` | 00_Vision/User_Voices/20260202_Youkanの概要と用語.md, 00_Vision/System_Grand_Design.md, 00_Vision/SYSTEM_PHILOSOPHY_AND_VISION.md |
| `spec/02_機能仕様.md` | spec/00_MASTER_SPEC.md, spec/03_MORNING_PLANNING.md, 00_Vision/Capacity_Management_Spec.md, 00_Vision/User_Voices/* |
| `spec/03_画面設計.md` | spec/02_VIEW_DEFINITIONS.md, spec/00_MASTER_SPEC.md |
| `spec/04_データ設計.md` | spec/01_STATE_MATRIX.md, backend/db.php, backend/ItemController.php, backend/TodayController.php |
| `spec/05_技術設計.md` | package.json, バックエンドコード構成, 00_Vision/System_Grand_Design.md |

### 注意事項

- 旧ファイル（spec/00_MASTER_SPEC.md, 01_STATE_MATRIX.md, 02_VIEW_DEFINITIONS.md, 03_MORNING_PLANNING.md）は参照用として残置
- `docs/SPEC/`（大文字）は`docs/spec/`（小文字）と同内容。SdDDでは`docs/spec/`を正とする
- 旧名称JBWOS時代のドキュメントが`docs/`配下に多数存在するが、新spec/*.mdに統合済みの内容が正
- 既存の`docs/requests.md`の内容は`request_log.md`にも転記した

---

## 2026-07-07: Volume Calendar Gantt item visibility fix
### Decisions
| Type | Content | Notes |
|:--|:--|:--|
| [Fix] | Gantt view calls `/calendar/items` with `mode=gantt` and fetches active visible tasks without limiting them to the visible date range. | Grid/range views keep the existing date-range behavior. |
| [Fix] | Project focus membership is resolved through descendant `items.id`, `items.project_id`, and `items.parent_id`. | Prevents tasks attached only by `parent_id` from disappearing in the Gantt chart. |
| [Fix] | Company filter treats `domain === 'business'` as company work even when `tenantId` is empty. | Aligns VolumeCalendarScreen with existing Youkan filtering semantics. |

### Impact
- F-06 Volume Calendar Gantt view
- `/calendar/items?mode=gantt`
- `VolumeCalendarScreen` personal/company filter

---

## 2026-07-07: `focus` / Today / focusedProject の概念分離を明文化
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [明確化] | `status = focus` はアイテム状態、Today は当日の実行キュー、`focusedProjectId` / `activeProject` はプロジェクト文脈であり、それぞれ別軸として扱うことを明記 | 全体一覧・ガント・量感カレンダーの表示条件判断で混同しないため |
