# 06. 変更履歴

## 2026-08-19: ガント ホバー行の強調（見出し列と時間軸を同じ行と分かるように）（R-150）
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [変更] | `RyokanGanttView.tsx` の行 div のホバーを `bg-slate-50` → `bg-slate-100`（dark `slate-800/70`）に一段濃くし、下線を `hover:shadow-[inset_0_-2px_0_0_rgb(203_213_225)]`（dark slate-600）で太く見せる（`border-b` 幅は不変）。sticky の見出し列は独自背景（white／amber）で行ホバー色を隠すため、見出し列側に `group-hover:` で同じ背景（未配置は amber を一段濃く）と同じ inset 下線（既存の右側影と併記）を付与 | `03_画面設計.md` §5.4「ホバー行の強調」、`docs/requests_log.md` R-150行 |

### Impact
- `JWCADTategu.Web/src/features/core/youkan/components/Calendar/RyokanGanttView.tsx`: 行 div と見出し列のクラス追加のみ（レイアウト・行高不変）
- テスト: `components/Calendar/__tests__/RyokanGanttView.rowHover.test.tsx` を新規追加（行・見出し列のクラス存在と `border` 幅不変のアサーション）

---

## 2026-08-19: 同形異義UIの手当て（R-147 バグ寄り2件／R-148 母集団1語ルール、実装待ち）
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [仕様] | Codex レビュー（`docs/reference/review/2026-08-19-同形異義UIレビュー_codex.md`）に基づき、R-147: 週負荷の分母定義の明文化・超過分パネルの編集対象を有効締切に一致、R-148: `03_画面設計.md` §0 に「集計値の母集団1語」ルールを新設し #1〜#4・#6・#7 に適用 | `docs/requests_log.md` R-147・R-148 |

### Impact
- 実装時に追記

---

## 2026-08-19: ガント「プロジェクト別」表示でも日付ヘッダーの合計時間・CapacityBar を表示（R-146）
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [変更] | `RyokanGanttView.tsx` の `dailyCapacityStats` にあった `showGroups` 早期 return と描画側の `!showGroups` ガードを削除し、プロジェクト別表示でも一覧と同じ CapacityBar＋合計時間（F-34）を表示。集計対象はガントに渡された items（プロジェクト絞り込みは `useVolumeCalendarViewModel` の API 取得時点で `projectId` により済んでいるため自前フィルタ不要。絞り込み中＝そのプロジェクトの割当合計＝チームの負荷）。分母は従来どおり日次キャパ | `02_機能仕様.md` F-34、`03_画面設計.md` §5.6、`docs/requests_log.md` R-146行 |

### Impact
- `JWCADTategu.Web/src/features/core/youkan/components/Calendar/RyokanGanttView.tsx`: 早期 return・`showGroups` 依存・描画ガードの削除のみ（一覧表示の挙動は不変）
- テスト: `components/Calendar/__tests__/RyokanGanttView.dailyTotalHours.test.tsx` に R-146 節を追加（showGroups=true で合計・CapacityBar 表示、絞り込み済み items の合計値）

---

## 2026-08-19: ガント時間軸で 24h をはみ出すブロックが幅1分の極小表示になるバグを修正（R-145）
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [バグ修正] | 仕様 F-40「24hはみ出し時は右端を24:00に揃えて描画（幅は保つ）」に対し、`logic/ganttTimeBlocks.ts` は開始位置を据え置いて幅を 24:00 で切り落としていた。手動オフセット飽和値 1439 と依存カスケード（前工程の終端 1440 起点）の両経路で幅1分になっていた。はみ出す場合は `start = max(0, 1440 − 割当分)`、幅 = `min(割当分, 1440)`、`overflow=true`、後続の起点 = `min(start + 割当分, 1440)` に変更。ドラッグ保存値の 0〜1439 クランプは変更なし | `docs/requests_log.md` R-145行、`03_画面設計.md` F-40 時間軸節（「幅は保ったまま」を明記） |

### Impact
- `JWCADTategu.Web/src/features/core/youkan/logic/ganttTimeBlocks.ts`: ループ内の start/width/ends 計算のみ変更（描画側 `RyokanGanttView.tsx` のブロック位置・依存矢印端は同じ start/width を参照するため変更なし）
- テスト: `logic/__tests__/ganttTimeBlocks.test.ts` の切り詰め前提3件を仕様（右端揃え）に修正、回帰ケース「手動1439＋6h → 18:00開始・6h幅」「先行が 24:00 で終わっても後続が1分幅にならない」を追加。`components/Calendar/__tests__/RyokanGanttView.timelineBlocks.test.tsx` の「右端が日末尾に揃い」のアサーションを幅保持に修正

---

## 2026-08-19: 押し出し見込トークンと締切変更時の連鎖3択（R-144、仕様確定・実装予定）
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [仕様] | 予実管理 v1。依存の前工程の有効締切を起点に直後1工程へ `予定通りなら M/d (+N)` トークン（計算値）、依存元の締切変更で後続が押し出されるとき右下カード3択（後続も同じ日数ずらす／この1件だけ／戻す） | `02_機能仕様.md` F-57、`03_画面設計.md` §21、`05_技術設計.md`、会議録 `docs/kaigi/2026-08-19-予実管理.md` |

### Impact
- 実装時に追記（未着手）

---

## 2026-08-19: 右クリックメニュー「後日着手 (h)」を「今日やる」直下に統一追加（R-143）
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [変更] | 全ての右クリックメニュー（全体一覧・ガント・フロー・状況把握・登録と集中）で「今日やる」の直下に「後日着手 (h)」を常時表示し、メニュー表示中の `h` キーで実行。状態変更は `decisionToStatus('later')`→`todo` | `03_画面設計.md` §6.1.5、フローのショートカット表、`docs/requests_log.md` R-143行 |

### Impact
- `hooks/buildItemContextMenuActions.tsx`: `onResolveLater` を必須化し、「後日着手 (h)」を常時表示・`shortcut: 'h'`（既存呼び出し元4画面は全て渡し済み）
- `components/OverviewBoard/OverviewBoard.tsx`: 「今日やる (Focus)」直下に「後日着手 (h)」を追加（`updateItem(id, { status: decisionToStatus('later') })`）
- テスト: `hooks/__tests__/buildItemContextMenuActions.test.ts`（並び順・shortcut）、`OverviewBoard/__tests__/OverviewBoard.contextMenu.test.tsx`（表示位置・クリック・`h` キー）
## 2026-08-19: フローチャート・状況把握の詳細モーダルでテナント「PRIVATE」／プロジェクト「Inbox（未分類）」と誤表示（R-142）
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [バグ修正] | `FlowScreen.tsx` の `<DecisionDetailModal>` が `joinedTenants`・`allProjects` を渡しておらず（既定 `[]`）、`DecisionDetailModal.tsx` の所属テナント／プロジェクトのルックアップが失敗してフォールバック文言を表示していた。`PanoramaBoard.tsx` は `allProjects` のみ漏れ。選択肢が空のため「Private」を押すと `tenantId=null, projectId=null` が実際に書き込まれる誤操作経路にもなっていた。両画面で props を渡す。`DecisionDetailModal` 側は不変 | `docs/requests_log.md` R-142行 |

### Impact
- `JWCADTategu.Web/src/features/core/youkan/screens/FlowScreen.tsx`: 外側 `FlowScreen` が既に取得している全アイテム（`selectorItems`）から `isProject` を抽出し `FlowCanvas` に `allProjects` として渡す（project_id 絞り込み中でも全プロジェクトが選択肢に出る）。`FlowCanvas` は `joinedTenants={auth.joinedTenants || []}`・`allProjects` をモーダルへ渡す
- `JWCADTategu.Web/src/features/core/youkan/components/PanoramaBoard/PanoramaBoard.tsx`: `allProjects={allProjects}`（`vm.allProjects`）を追加
- 全 `<DecisionDetailModal>` 呼び出しを grep: `VolumeCalendarScreen`・`DashboardScreen`・`SpeechView`・`ProjectRegistryScreen` は既に両 props を渡しており漏れなし
- テスト（新規、モックした `DecisionDetailModal` に空でない配列が渡ることを検証）: `screens/__tests__/FlowScreen.detailModalProps.test.tsx`、`components/PanoramaBoard/__tests__/PanoramaBoard.detailModalProps.test.tsx`

---

## 2026-08-19: `POST /integrations/inbox` 登録アイテムの `created_by`／`tenant_id` 未設定を修正（R-141）
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [バグ修正] | `IntegrationController::createInboxItem()` の INSERT が `created_by`／`tenant_id` を設定していなかったため、番頭がこの口で登録したアイテムが `BaseController::fetchAggregatedItems()`（全体一覧・digest）の条件 `created_by = 本人 OR assigned_to = 本人` に該当せず本人に見えなかった。`created_by` に認証ユーザーの `id`、`tenant_id` に `authenticate()` が解決した `currentTenantId` を入れる。リクエストで `tenant_id` を明示した場合は `joinedTenants` に含まれるものだけ採用し、非所属は400 | `docs/requests_log.md` R-141行、`04_データ設計.md` §5.1 |

### Impact
- `backend/IntegrationController.php`: INSERT に `created_by`／`tenant_id` を追加。テナント解決を独自の memberships 先頭取得から `currentTenantId`／`joinedTenants` に置換（`authenticate()` と同じ値）。入出力を `getInput()`／`sendJSON()`／`sendError()` に統一（応答形式は不変）
- `backend/tests/test_r141_inbox_owner.php`（新規）: api_token 登録直後に `fetchAggregatedItems()` に含まれる・`created_by`/`tenant_id` の値・JWT の `tenant_id` 採用・所属テナント明示の採用・非所属400（未作成）・title無し400（9件）
- 既存 `test_r140_digest_api_tokens.php` 33件・`QuantityServiceWeekLoadTest` Green

---

## 2026-08-18: 番頭連携API — ダイジェスト1本＋連携トークン発行（R-140）
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [追加] | `GET /integrations/digest`（要判断キュー先頭N件・週負荷・未連絡遅延・断った件数・focus）。要判断キューの判定を PHP `ReviewQueueService` に実装し、TS `reviewQueue.ts` と共有フィクスチャで一致を担保 | `02_機能仕様.md` F-56、`05_技術設計.md` |
| [追加] | 個人設定「外部連携トークン」（発行／一覧／失効、`/user/api-tokens`）。既存 `api_tokens` テーブルをそのまま使う | `03_画面設計.md` §20、`04_データ設計.md` §5.1・§5.2(c) |
| [文書] | 番頭への依頼事項 §1・§2・§6 は digest に置き換え可能 | `docs/handover/2026-08-18-番頭への依頼事項.md` |

### Impact
- ブランチ `feature/R-140-integrations-digest`
- バックエンド
  - `backend/ReviewQueueService.php`（新規）: `ReviewQueueService::build(array $items, string $today)`。F-52 の対象判定・並びを TS `logic/reviewQueue.ts` と同一定義で実装（有効締切は `QuantityService::getEffectiveDeadlineFromItem` を共用）
  - `backend/tests/fixtures/review_queue_cases.json`（新規）: TS/PHP 共有フィクスチャ4ケース（期限超過 due/prep・再確認到来・目安なし/0・プロジェクト／done/cancelled/someday／削除／アーカイブ除外・並び順の境界・古いものを除外しない）
  - `backend/tests/test_review_queue_service.php`（新規）: フィクスチャ一致テスト
  - `backend/IntegrationController.php`: `BaseController` を継承し `GET /integrations/digest`（`review_queue`／`week_load`／`uncontacted_overdue`／`declined_this_week`／`focus`）を追加。`/integrations/inbox` の独自 api_token 認証を `BaseController::authenticate()`（JWT／api_token 両対応）に統一（重複コード削除）
  - `backend/BaseController.php`: `fetchAggregatedItems()` を追加（`scope=aggregated` のアイテム集合SQLを `ItemController` から切り出し）
  - `backend/ItemController.php`: aggregated（project_id なし）分岐を `fetchAggregatedItems()` 呼び出しに置換（挙動変更なし）
  - `backend/QuantityService.php`: `getEffectiveDeadlineFromItem` を `public static` 化
  - `backend/TodayController.php`: focus の並び比較を `compareFocusOrder()`（public static）に切り出し（挙動変更なし）
  - `backend/UserController.php`: `GET/POST /user/api-tokens`・`DELETE /user/api-tokens/{id}` を追加（`bin2hex(random_bytes(32))`、DELETE は本人条件付き、他人のIDは404）
  - `backend/tests/test_r140_digest_api_tokens.php`（新規）: api_token Bearer で digest 200・他ユーザー非混入・JWT と同結果・limit 上限・不正トークン401、発行→利用→一覧（平文なし・last_used_at）→失効→401・他人404・label空400（33件）
- フロントエンド
  - `JWCADTategu.Web/src/features/core/youkan/repositories/ApiTokenRepository.ts`（新規）: `listApiTokens/createApiToken/revokeApiToken`
  - `JWCADTategu.Web/src/features/core/youkan/hooks/useApiTokens.ts`（新規）: ViewModel（一覧・発行直後の平文保持・失効）
  - `JWCADTategu.Web/src/features/core/youkan/components/Settings/ApiTokenSection.tsx`（新規）: §20 の表示（一覧1行＝ラベル／発行 M/d／最終利用 M/d（未使用は —）／失効。発行後は平文を等幅で1回表示＋［コピー］＋注意書き。失効は行内「失効しますか？［はい］［いいえ］」、`window.confirm` 不使用）
  - `JWCADTategu.Web/src/features/core/youkan/screens/PersonalSettingsScreen.tsx`: 「判断の言葉」の直下に `ApiTokenSection` を配置
  - `JWCADTategu.Web/src/features/core/youkan/logic/__tests__/reviewQueue.fixture.test.ts`（新規）: 共有フィクスチャ一致テスト
  - `JWCADTategu.Web/src/features/core/youkan/components/Settings/__tests__/ApiTokenSection.test.tsx`（新規）: 0件表示・一覧・発行・ラベル空無効・行内失効確認（5件）
  - `JWCADTategu.Web/src/features/core/youkan/screens/__tests__/PersonalSettingsScreen.test.tsx`: `ApiClient.request` のモックを追加
- SPEC 修正: `02_機能仕様.md` F-56（`overdue_days` の定義を明記）、`04_データ設計.md` §5.1（`/integrations/inbox` の認証を JWT／api_token 両対応に）、`05_技術設計.md`（共通化先 `BaseController::fetchAggregatedItems`・`TodayController::compareFocusOrder`・フィクスチャの `estimated_minutes` の null/0 の扱いを明記）
- 完了時テスト: PHP `test_review_queue_service.php` 5件・`test_r140_digest_api_tokens.php` 33件 Green、既存 `QuantityServiceWeekLoadTest`／`test_shared_session_auth`／`test_today_sort_order`／`test_reorder_focus_sort_order`／`test_r099_dependency_fields` Green（`test_real_project_title_join.php` の1件失敗は変更前から同じ）。フロント全体 195ファイル/1231テスト中 1216〜1217 Green・14 skipped（既知許容: `DecisionDetailModal`／`KeyboardAndButtons` の unhandled error 2件。`useAssigneeView.test.ts`「取得したアイテムをバケット分類」は変更前の基点コミットでも失敗する日付依存の既存失敗、`useOverviewItems.test.ts` の1件は単独実行で Green のフレーク）、`npx tsc --noEmit` エラーなし
- 未対応（報告事項）: `/integrations/inbox` の INSERT は `created_by`／`tenant_id` を入れていないため、番頭が登録したアイテムは本人の aggregated 集合（digest・全体一覧）に現れない（変更前からの挙動。R-140 の範囲外）→ R-141 で修正

---

## 2026-08-18: 完全削除 `POST /items/{id}/destroy` の500エラーを修正（R-139）
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [バグ修正] | `backend/index.php` の Item Actions ルートが `destroy` を `ItemController::destroy_permanent()` に振り分けていたが、当メソッドは archive/trash 機能導入時（2026-02-01 `75f7931`）から一度も定義されておらず、Fatal error で500になっていた（物理削除の実体は `DELETE /items/{id}` 経由の private `delete()` に存在）。public `destroy_permanent($id)` を追加して `delete($id)` に委譲。権限チェック・子孫カスケード物理削除・`deletedDescendantIds` 応答は既存 `delete()` のまま | `docs/requests_log.md` R-139行 |

### Impact
- `backend/ItemController.php`: `destroy_permanent($id)` を追加（`delete($id)` へ委譲する1行）
- `backend/index.php`: Item Actions ルートの死んだコメント・空の `method_exists` 分岐を削除（挙動変更なし）
- `backend/tests/test_r139_destroy_permanent.php`（新規）: `destroy_permanent` が public で存在すること、親子が物理削除され `deletedDescendantIds` に子が含まれることを検証
- 呼び出し元UI（`ArchiveTrashScreen` の完全削除、`ProjectRegistryScreen` の destroy、`useProjectViewModel.deleteProject`）はフロント変更なし

---

## 2026-08-18: 超過分パネル「連絡した」一括更新の並列失敗バグを修正（R-138）
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [バグ修正] | R-136 超過分パネルの「連絡した」ブロック一括更新が `group.items.forEach(onUpdateItem)` による並列送信のためSQLite単一ライターの競合で一部失敗していた（本番検証で49件中32件失敗）不具合を修正。R-121（フロー座標一括保存の並列PUT失敗）と同根。1件ずつ逐次送信に変更し、送信中はボタン無効化＋「n/N」進捗表示、失敗時は失敗件数を事実のみ表示し同じボタンで失敗分だけ再試行できるようにした。あわせて連絡先が特定できない「その他」（案件なし）ブロックには「連絡した」ボタンを表示しないようにした | `docs/requests_log.md` R-138行 |

### Impact
- `JWCADTategu.Web/src/features/core/youkan/logic/sequentialUpdates.ts`（新規）: 複数IDへの更新を1件ずつ逐次実行する汎用ヘルパー `runSequentialUpdates`。失敗したIDは失敗させたまま残りを続行し、失敗ID一覧と進捗コールバックを返す
- `JWCADTategu.Web/src/features/core/youkan/logic/__tests__/sequentialUpdates.test.ts`（新規）: 逐次実行・一部失敗時の継続・進捗コールバックのテスト3件
- `JWCADTategu.Web/src/features/core/youkan/components/Review/OverduePanel.tsx`: 「連絡した」トグルを `runSequentialUpdates` 経由の逐次送信に変更。ブロックごとの送信中状態（進捗・失敗ID）を保持し、送信中はボタン無効化＋「n/N」表示、失敗時は「K件更新できませんでした」表示＋同じボタンで失敗分のみ再送。`group.projectId === null`（「その他」）のブロックはボタン自体を非表示化
- `JWCADTategu.Web/src/features/core/youkan/components/Review/__tests__/OverduePanel.test.tsx`: 既存の連絡した/連絡取消しテストを非同期化。50件の逐次呼び出し、進捗表示、一部失敗時の再試行、「その他」ブロックの非表示のテスト5件を追加
- `docs/SPEC/06_変更履歴.md`: 本節を追加するとともに、直前のR-136/R-137節に残っていた未解決のマージ競合マーカーを解消（内容の欠落なし）
- 完了時テスト: フロントエンド全体 190ファイル/1208テストGreen・14 skipped（既知許容: `DecisionDetailModal`/`KeyboardAndButtons`のunhandled error 2件）、`npx tsc --noEmit` エラーなし
- 未解決事項: `vm.updateItem`（`useYoukanViewModel.ts`）はAPI失敗を内部で`console.error`するのみで例外を再送出しないため、本番配線（`onUpdateItem={vm.updateItem}`、`DashboardScreen.tsx`）では実際のAPI失敗が`onUpdateItem`のPromise拒否として`OverduePanel`まで伝播しない。今回の逐次化により本来の原因（SQLiteロック競合）はR-121と同様ほぼ解消される見込みだが、それでも発生し得る個別失敗（トークン切れ等）に対する再試行UIが実際に機能するかは、`vm.updateItem`側のエラー伝播を見直さない限り本番では検証できない。スコープ外のため今回は未対応（指揮AI判断事項として報告）

---

## 2026-08-18: 超過分パネル（R-136）／レガシー`confirmed`データ移行（R-125補遺）／`youkan_user`残存参照の整理（R-137）
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [追加] | R-136: ヘッダー週負荷1行クリック→右下パネルに期限超過分を案件別一覧。行ごとに納期再登録、ブロック単位「連絡した」（`meta.contacted_at`） | 02 F-55、03 §19、04 §3.5 |
| [データ] | R-125補遺: 本番 `status='confirmed'` 2件を `focus` へ移行（発注者承認 2026-08-18） | 読み取り互換は維持 |
| [修正] | R-137: `localStorage['youkan_user']` を直接読む残存箇所を `useAuth().user` に統一 | R-135と同型 |

### Impact
- R-136 実装ファイル一覧（ブランチ `feat/R-136-overdue-panel`）:
  - `JWCADTategu.Web/src/features/core/youkan/logic/overdueGroups.ts`（新規）: 期限超過分の抽出（R-128 `weekLoad.ts` の超過判定と同一条件：有効締切が今日より前）と案件別グループ化。並びは未連絡ブロックが上・最古超過日順、連絡済みは下。`meta.contacted_at` を反映
  - `JWCADTategu.Web/src/features/core/youkan/logic/__tests__/overdueGroups.test.ts`（新規）: 抽出条件・グループ化・並び・`contacted_at` 反映のテスト12件
  - `JWCADTategu.Web/src/features/core/youkan/components/Review/OverduePanel.tsx`（新規）: 右下オーバーレイパネル（R-127 ReviewSweepと同じ位置・見た目の系統）。ブロック見出し（件数・合計h・最古超過日数）、行（タイトル・目安h・元の締切・超過日数・`SmartDateInput`による納期入力）、ブロック単位「連絡した」トグル、Escで閉じる
  - `JWCADTategu.Web/src/features/core/youkan/components/Review/__tests__/OverduePanel.test.tsx`（新規）: 表示・Esc・納期Enter更新・連絡したトグル（メタ保持）のテスト10件
  - `JWCADTategu.Web/src/features/core/youkan/screens/DashboardScreen.tsx`: `overdueGroups` の算出、`isOverduePanelOpen` 状態、`OPEN_OVERDUE_PANEL` イベント/pendingフラグの受信、`OverduePanel` のレンダリング。ReviewSweepとは相互排他（片方を開いたら他方を閉じる）
  - `JWCADTategu.Web/src/components/Layout/YoukanHeader.tsx`: 週負荷1行を `cursor-pointer`・`title="超過分を見る"` にしてクリック可能化。R-127バッジと同じ疎結合パターン（sessionStorageのpendingフラグ＋カスタムイベント）で `OPEN_OVERDUE_PANEL` を発火
  - `JWCADTategu.Web/src/components/Layout/YoukanHeader.test.tsx`: 週負荷1行クリックのテスト2件を追記
  - `JWCADTategu.Web/src/features/core/session/youkanKeys.ts`: `YOUKAN_KEYS.OVERDUE_PANEL_PENDING`、`YOUKAN_EVENTS.OPEN_OVERDUE_PANEL` を追加
  - 完了時テスト: フロントエンド全体 184ファイル/1188テストGreen（既知許容: `DecisionDetailModal`/`KeyboardAndButtons`のunhandled error 2件）、`npx tsc --noEmit` エラーなし
- R-137 実装ファイル一覧:
  - `JWCADTategu.Web/src/features/core/youkan/viewmodels/useYoukanViewModel.ts`: `getQuantityContext` の `accountId` と `updateCapacityException` の自分の membership 特定を、既存の `currentUserId`（`useAuth().user.id` 由来、R-135で導入済み）のみに統一。`localStorage['youkan_user']` へのOR条件フォールバックを廃止
  - `JWCADTategu.Web/src/features/core/youkan/components/Calendar/RyokanCalendar.tsx`: `effectiveUserId` を `currentUserId` prop のみから導出（`localStorage['youkan_user']` フォールバックを廃止）
  - `JWCADTategu.Web/src/components/QuantityCalendar/DetailQuantityCalendar.tsx`: `RyokanCalendar` への `currentUserId` 受け渡しから localStorage 読み取りのIIFEを削除
  - `JWCADTategu.Web/src/features/core/youkan/components/Modal/DecisionDetailModal.tsx`: `currentUserId` prop を渡さない呼び出し元が大半のため、`useAuth()` をフォールバックにした `effectiveUserId`（`currentUserId ?? auth.user?.id`）に統一。目安期間内訳・視点ラベル・`SideCalendarPanel` への受け渡しの3箇所を修正
  - `JWCADTategu.Web/src/features/core/youkan/repositories/YoukanRepository.ts`: `getGdbShelf` のuserId解決を固定値`legacy_user`に簡略化（`getRepository()`が常に`CloudYoukanRepository`を返すため本番では到達しないコード）
  - `JWCADTategu.Web/src/components/Layout/YoukanHeader.tsx`: `getLegacyUserName()`（`localStorage['youkan_user']`読み取り）を廃止し、`user?.name || 'User'` に簡略化
  - `JWCADTategu.Web/src/features/core/projects/screens/CompanySettingsScreen.tsx`: 権限判定用の`currentUser`を`localStorage['youkan_user']`/`['youkan_tenant']`から`useAuth()`の`user`/`tenant`に変更
  - 回帰テスト（新規・拡張、計7ファイル）: `useYoukanViewModel.currentUserId.test.tsx`（拡張）、`RyokanCalendar.effectiveUserId.test.tsx`、`DetailQuantityCalendar.currentUserId.test.tsx`、`DecisionDetailModal.currentUserId.test.tsx`、`YoukanRepository.getGdbShelf.userId.test.ts`、`YoukanHeader.test.tsx`（拡張）、`CompanySettingsScreen.test.tsx`（新規）
  - 対象外・変更なし: `db/db.ts`（Dexieスキーマv16→v17移行コード内の読み取り。歴史的なワンタイム移行処理でありログイン処理に近い性質のため、指揮AI確認事項として報告）
  - 完了時テスト: フロントエンド全体 187ファイル/1177テストGreen・14 skipped（既知許容: `DecisionDetailModal`/`KeyboardAndButtons`のunhandled error 2件）、`npx tsc --noEmit` エラーなし

（注記: 本節はR-136/R-137のマージ時に競合マーカーが未解決のまま残っていたため、R-138対応時に整理・解消した）

---

## 2026-08-18: R-129「着手 M/d」トークンが本番で表示されないバグを修正（R-135）
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [バグ修正] | 全体一覧・ガント一覧・フローノードの3画面で `currentUserId` が常にnullとなり `getLatestStart` が呼ばれず「着手 M/d」トークンが1件も表示されなかった不具合を修正。`viewmodels/useYoukanViewModel.ts` の `(getRepository() as any).getCurrentUser?.()`（Repositoryに未実装で常にundefined）と `screens/FlowScreen.tsx` の `localStorage['youkan_user']` 読み取り（Cookieセッション認証では書き込まれず常にnull）を廃止し、両者とも `useAuth().user.id`（`/auth/me`）に解決元を統一 | デプロイ検証Agentが本番実機検証で発見（`docs/requests_log.md` R-129行参照）。既存機能への悪影響なくロールバックは不要と判断されていた |

### Impact
- `03_画面設計.md` §17（変更なし、表示条件は元仕様どおり）
- 実装ファイル一覧:
  - `JWCADTategu.Web/src/features/core/youkan/viewmodels/useYoukanViewModel.ts`: `currentUserId` の `useState`＋Repository経由の非同期解決を廃止し、`useAuth().user?.id` からの直接導出に変更
  - `JWCADTategu.Web/src/features/core/youkan/screens/FlowScreen.tsx`: `localStorage['youkan_user']` を読む `currentUserId` の `useMemo` を廃止し、`useAuth().user?.id` に変更
  - `JWCADTategu.Web/src/features/core/youkan/viewmodels/__tests__/useYoukanViewModel.currentUserId.test.tsx`（新規）: `currentUserId` を直接モック注入せず、`useAuth` をモックして実際の解決チェーンを通す統合テスト
- 全体一覧（`useOverviewItems`）・ガント一覧（`RyokanGanttView`）は `useYoukanViewModel.currentUserId` を共有Propとして受け取るため、VM側修正のみで両画面とも解消
- 完了時テスト: フロントエンド全体 179ファイル/1134テストGreen（既知許容: `DecisionDetailModal`/`KeyboardAndButtons`のunhandled error 2件）、`npx tsc --noEmit` エラーなし

---

## 2026-08-18: 日次キャパ決定規則の一本化（R-130）
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [修正] | `logic/capacity.ts` `getDailyCapacity` が曜日パターン（`standardWeeklyPattern`）を見ておらず、`QuantityEngine.calculateTotalCapacityForDate` と規則が食い違っていた。`getDailyCapacity` を唯一の実装にし（曜日パターン反映）、QuantityEngine・週の残量・最遅着手日がこれを呼ぶよう統一。PHP `QuantityService` も同規則 | R-128実装Agentの発見。会議のプレモーテムで最大の失敗要因とした「分母のズレ」 |
| [削除] | 個人設定「定休日・祝日設定（Advanced JSON）」欄を廃止（`non_working_hours` は未参照の死に設定） | カラムは残す |
| [追加] | 個人設定キャパ欄に決定規則の説明文 | |

### Impact
- `02_機能仕様.md` F-11「日次キャパの決定規則」
- 実装ファイル一覧（ブランチ `fix/R-130-daily-capacity-unify`）:
  - `JWCADTategu.Web/src/features/core/youkan/logic/capacity.ts`: `getDailyCapacity` をF-11の決定規則（日別例外→曜日パターン0=定休日→holidays weekly→その曜日が土日なら0→defaultDailyMinutes）の唯一の実装に。`isHoliday` は `getDailyCapacity(...) === 0` に委譲するだけに簡素化
  - `JWCADTategu.Web/src/features/core/youkan/logic/__tests__/capacity.test.ts`（新規、TDD）: 優先順5規則それぞれのテスト
  - `JWCADTategu.Web/src/features/core/youkan/logic/QuantityEngine.ts`: `calculateTotalCapacityForDate` の独自規則（日別例外→checkIsHoliday→standardWeeklyPattern→defaultDailyMinutesの再実装）を削除し `getDailyCapacity` を呼ぶだけに（会社キャパ側 `calculateDefinedCompanyCapacityForDate` の `profile.standardWeeklyPattern` 参照は現状維持、対象外）
  - `backend/QuantityService.php`: `getDailyCapacityFromConfig` をF-11と同じ優先順に拡張（`standard_weekly_pattern` を追加）、`private` から `public` に変更（テスト容易性のため）。`isHolidayFromConfig` は重複実装になったため削除。`calcWeekLoadForUser` が `users.preferences.capacity_profile.standardWeeklyPattern` を読んで `capacityConfig['standard_weekly_pattern']` へ渡すように変更
  - `backend/tests/QuantityServiceCapacityTest.php`（新規、TDD）: フロント `capacity.test.ts` と同一の優先順・同一の数値を確認（曜日パターンに平日しかなくても土日は0になること、土曜に明示値があればそれが優先されることを含む）
  - `backend/tests/QuantityServiceWeekLoadTest.php`: 曜日パターンで平日を240分にすると `capacity_minutes` が連動するテストを追記（フロント `weekLoad.test.ts` と同一フィクスチャ・同一数値）
  - `JWCADTategu.Web/src/features/core/youkan/logic/__tests__/weekLoad.test.ts`: 曜日パターン反映（平日240分／曜日パターンで0=定休日）のテストを追記
  - `JWCADTategu.Web/src/features/core/youkan/screens/PersonalSettingsScreen.tsx`: 「定休日・祝日設定 (Advanced JSON)」欄（`non_working_hours`の保存ロジック含む）を削除し、決定規則の説明文「曜日パターンで0＝定休日。未設定なら土日休み。日別例外が最優先です。」に置換。曜日パターンの既定値に土日を明示的に0として含める（冗長だが害はないため維持）
  - `JWCADTategu.Web/src/features/core/youkan/screens/__tests__/PersonalSettingsScreen.test.tsx`（新規）: Advanced JSON欄が存在しないこと・説明文が表示されることを確認
- 完了時テスト: フロントエンド全体 180ファイル/1148テストGreen（既知許容: `DecisionDetailModal`/`KeyboardAndButtons`のunhandled error 2件）、`npx tsc --noEmit` エラーなし。PHP: `QuantityServiceCapacityTest.php`（10テスト）・`QuantityServiceWeekLoadTest.php` 全件パス（`QuantityServiceTest.php`の`testContextLogic`失敗はR-130と無関係の既存不具合、変更前から発生していたことを確認済み）
- 追加コミット（規則4の見直し）: 規則4を「holidaysも曜日パターンも未設定なら土日は0」から「1〜3で値が決まらず、その曜日が土日なら0」に変更（発注者の実データで既存ユーザーが曜日パターンに平日しか保存していない場合に土日が稼働日扱いになるリスクを解消。指揮AIの指示によりF-11本文を更新）

---

## 2026-08-18: 改善要望フォーム4件（R-131〜R-134）
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [追加] | R-131: 詳細モーダル「保留にする」に Ctrl+Shift+H ショートカット | 03 §6.1.4 |
| [変更] | R-132: フローノードの `todo` 配色を通常色に（専用の緑/tealをやめる） | 03 §18 |
| [修正] | R-133: フローノードのタイトル編集中、2回目クリックでクリック位置にキャレット | 03 §18 |
| [追加] | R-134: 誘導カードに「後で（1時間後）」（60分後再表示＋ブラウザ通知1回）、バッジからの再呼び出しを明示 | 02 F-52、03 §15.4 |

### Impact
- `JWCADTategu.Web/src/features/core/youkan/components/Modal/DecisionDetailModal.tsx`: R-131 Ctrl+Shift+Hショートカット（入力欄内は無視）、保留ボタンのtitle表記
- `JWCADTategu.Web/src/test/KeyboardAndButtons.test.tsx`: R-131のショートカット・title・入力欄ガードのテストを追記
- `JWCADTategu.Web/src/features/core/youkan/components/Flow/FlowItemNode.tsx`: R-132 `statusColors.todo`をinboxと同じ配色に変更、R-133 タイトル編集inputに`nodrag`クラスを追加
- `JWCADTategu.Web/src/features/core/youkan/components/Flow/__tests__/FlowItemNode.statusColor.test.tsx`: R-132のtodo/inbox配色一致テスト（新規）
- `JWCADTategu.Web/src/features/core/youkan/components/Flow/__tests__/FlowItemNode.editUX.test.tsx`: R-133の`nodrag`クラス付与テストを追記
- `JWCADTategu.Web/src/features/core/youkan/components/Review/ReviewPrompt.tsx`: R-134「後で（1時間後）」ボタン・snooze_until・タイマー再表示・Notification許可要求と通知
- `JWCADTategu.Web/src/features/core/youkan/components/Review/__tests__/ReviewPrompt.test.tsx`: R-134のスヌーズ・通知テストを追記
- `JWCADTategu.Web/src/features/core/session/youkanKeys.ts`: `REVIEW_PROMPT_SNOOZE_UNTIL`・`REVIEW_NOTIFY_ASKED`キーを追加
- `JWCADTategu.Web/src/components/Layout/YoukanHeader.tsx`: 要判断バッジのtitleを「クリックで捌く」に変更（1属性のみ）
- `JWCADTategu.Web/src/components/Layout/YoukanHeader.test.tsx`: バッジtitleのテストを追記

---

## 2026-08-18: 最遅着手日トークン「着手 M/d」（R-129）
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [追加] | `logic/latestStart.ts` `getLatestStart`（既存 `calculateAllocationDetails` の最古step＝最遅着手日、目安×安全係数）。全体一覧・ガント一覧・フローノードの既存日付の隣に `着手 M/d` を1トークン表示、期限が先なのに今日>着手日のときだけ赤字。目安なしは `目安？`。飽和ガード（赤字は先頭10件）。全体一覧フィルタ「着手遅れ」 | R-126会議の採用案D。期限超過分には出さない（R-127の領分） |
| [追加] | `CapacityConfig.safetyFactor`（既定1.5）を個人設定のキャパ設定欄に追加 | 係数は最遅着手日にだけ掛ける |

### Impact
- `02_機能仕様.md` F-54（新設）、`03_画面設計.md` §17（新設）、`05_技術設計.md` R-129節（新設）
- 実装ファイル一覧（2026-08-18 実装完了）:
  - `JWCADTategu.Web/src/features/core/youkan/logic/latestStart.ts`（新規）: `getLatestStart`／`resolveSafetyFactor`／`selectLateStartHighlightIds`（飽和ガード）／`formatLatestStartToken`／`formatLatestStartTooltip`
  - `JWCADTategu.Web/src/features/core/youkan/logic/__tests__/latestStart.test.ts`（新規、TDD）
  - `JWCADTategu.Web/src/features/core/youkan/types.ts`: `CapacityConfig.safetyFactor?: number` 追加
  - `JWCADTategu.Web/src/features/core/youkan/repositories/CloudYoukanRepository.ts`: `getCapacityConfig` で `capacity_profile.safetyFactor` を転送
  - `JWCADTategu.Web/src/features/core/youkan/screens/PersonalSettingsScreen.tsx`: 「安全係数」数値入力（既定1.5、1.0〜3.0、刻み0.1）を日次キャパ入力の下に追加
  - `JWCADTategu.Web/src/features/core/youkan/components/OverviewBoard/useOverviewItems.ts`: 各wrapperに `latestStart`／`latestStartHighlighted`／`latestStartTooltip` を付与、`lateStartOnly` 引数で「着手遅れ」フィルタ
  - `JWCADTategu.Web/src/features/core/youkan/components/OverviewBoard/__tests__/useOverviewItems.test.ts`: R-129分の統合テストを追記
  - `JWCADTategu.Web/src/features/core/youkan/components/OverviewBoard/OverviewItem.tsx`: 日付表示の隣にトークン表示
  - `JWCADTategu.Web/src/features/core/youkan/components/OverviewBoard/OverviewBoard.tsx`: `lateStartOnly` state追加・`useOverviewItems`/`ViewControls`へ配線
  - `JWCADTategu.Web/src/features/core/youkan/components/OverviewBoard/ViewControls.tsx`: 「着手遅れ」フィルタチップ追加
  - `JWCADTategu.Web/src/features/core/youkan/components/Calendar/RyokanGanttView.tsx`: 既存の逆算配分用`QuantityContext`構築を`quantityContext`として共通化し、タイトル列の目安時間バッジの隣にトークン表示（飽和ガード適用）
  - `JWCADTategu.Web/src/features/core/youkan/components/Flow/FlowItemNode.tsx`: `FlowItemNodeData`に`latestStart`/`safetyFactor`を追加、R-107の1行表示末尾に省スペース表示「着手M/d」
  - `JWCADTategu.Web/src/features/core/youkan/screens/FlowScreen.tsx`: `useCapacityConfig`＋ログイン中ユーザーIDから最小限の`QuantityContext`を構築し、`latestStartMap`をノードdataへ配線
- 完了時テスト: フロントエンド全体 177ファイル/1117テストGreen（既知許容: `useAssigneeView.test.ts`タイムゾーンフレーク・`DecisionDetailModal`/`KeyboardAndButtons`のunhandled error 2件）、`npx tsc --noEmit` エラーなし

---

## 2026-08-18: 今週の残量1行と登録時の一言（R-128）
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [変更] | ヘッダーの「Reality (Total Load) 123/480 min」（7px英字）を `今週 必要 Yh ／ 枠 Xh`（不足時のみ赤字 `Zh足りない`）の日本語1行に置き換え | R-126会議の採用案C。常時バー新設はしない |
| [追加] | 週負荷の式を `logic/weekLoad.ts`（TS）と `QuantityService::calcWeekLoad`（PHP）に同一定義で実装。`POST /items`・`POST /integrations/inbox`・`PATCH /items/{id}`（期限/目安変更時）のレスポンスに `week_load` を同梱、`GET /quantity/week` 新設 | 番頭が登録直後に「今週Zh足りない。外す候補: A／B」と返すため |
| [追加] | 画面側は登録・期限/目安変更の直後に不足時のみToast1回 | 判断UIなし |

### Impact
- `02_機能仕様.md` F-53（新設）、`03_画面設計.md` §16（新設）、`05_技術設計.md` R-128節（新設）
- 実装ファイル一覧（ブランチ `feat/R-128-week-load`）:
  - 新規: `JWCADTategu.Web/src/features/core/youkan/logic/weekLoad.ts`（`calcWeekLoad`／`formatWeekLoadHours`／`formatWeekLoadToastMessage`）と同 `__tests__/weekLoad.test.ts`
  - 新規: `backend/tests/QuantityServiceWeekLoadTest.php`（フロントと同一フィクスチャでの数値一致を確認）
  - 変更: `backend/QuantityService.php`（`calcWeekLoad`／`calcWeekLoadForUser`・内部ヘルパー `isHolidayFromConfig`/`getDailyCapacityFromConfig`/`getEffectiveDeadlineFromItem`）
  - 変更: `backend/QuantityController.php`（`GET /quantity/week`）、`backend/index.php`（ルーティング追加）
  - 変更: `backend/ItemController.php`（POST `/items`・PATCH `/items/{id}` 応答に `week_load` 同梱。PATCHは due_date/prep_date/estimated_minutes を含む時のみ）
  - 変更: `backend/IntegrationController.php`（POST `/integrations/inbox` 応答に `week_load` 同梱）
  - 変更: `JWCADTategu.Web/src/features/core/youkan/screens/DashboardScreen.tsx`（`weekLoad` 算出、`CAPACITY_UPDATE` イベントに同梱）
  - 変更: `JWCADTategu.Web/src/components/Layout/YoukanHeader.tsx`（旧「Reality (Total Load)」を日本語1行表示に置換）
  - 変更: `JWCADTategu.Web/src/features/core/youkan/viewmodels/useYoukanViewModel.ts`（`throwIn`・`updateItem` で登録/期限・目安変更直後に不足時のみ `WEEK_LOAD_SHORTFALL` イベント発火）
  - 変更: `JWCADTategu.Web/src/App.tsx`（`WEEK_LOAD_SHORTFALL` を受けてToast表示）
  - 変更: `JWCADTategu.Web/src/features/core/session/youkanKeys.ts`（`WEEK_LOAD_SHORTFALL` イベント追加）

---

## 2026-08-18: 要判断キュー「捌く」（R-127）
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [追加] | 要判断キュー（期限超過＋再確認到来）を `logic/reviewQueue.ts` で一元定義。ヘッダー件数バッジ・1日1回の誘導カード・右下オーバーレイの軽量カード ReviewSweep（今日やる／後日+7日／断った、3件で停止、数字キー操作）・全体一覧「要判断」フィルタ | R-126会議の採用案B。発注者指示で常設バナー・一括処分・アニメーションは不採用 |
| [追加] | 個人設定「判断の言葉」（`users.preferences.judgment_phrases`）。ReviewSweep に1フレーズずつ表示 | 発注者が集めている判断補助の短文（例「正しさより速さが正義」）。番頭も更新可 |
| [追加] | 「断った」に `meta.declined=true`。今週の断った件数（`cancelled`＋`status_updated_at`）を完了ビューに数字表示 | |

### Impact
- `02_機能仕様.md` F-52（新設）、`03_画面設計.md` §15（新設）、`04_データ設計.md` §3.2 preferences・§3.5 meta.declined、`05_技術設計.md` R-127節（新設）
- 実装ファイル一覧（ブランチ `feat/R-127-review-sweep`）:
  - 新規: `JWCADTategu.Web/src/features/core/youkan/logic/reviewQueue.ts`（`buildReviewQueue`/`countDeclinedThisWeek`）と同 `__tests__/reviewQueue.test.ts`
  - 新規: `JWCADTategu.Web/src/features/core/youkan/components/Review/ReviewSweep.tsx`・`ReviewPrompt.tsx` と同 `__tests__/`
  - 変更: `JWCADTategu.Web/src/features/core/youkan/screens/DashboardScreen.tsx`（キュー算出・ReviewPrompt/ReviewSweep描画・ヘッダーへの件数通知）
  - 変更: `JWCADTategu.Web/src/components/Layout/YoukanHeader.tsx`・同 `.test.tsx`（「全体一覧」タブの件数バッジ、`SubNavTab`にbadge対応追加）
  - 変更: `JWCADTategu.Web/src/features/core/youkan/screens/PersonalSettingsScreen.tsx`（「判断の言葉」テキストエリア、`users.preferences.judgment_phrases`）
  - 変更: `JWCADTategu.Web/src/features/core/youkan/components/OverviewBoard/useOverviewItems.ts`・`OverviewBoard.tsx`・`ViewControls.tsx`（全体一覧「要判断」フィルタチップ）
  - 変更: `JWCADTategu.Web/src/features/core/session/youkanKeys.ts`（`REVIEW_PROMPT_DISMISSED`/`REVIEW_SWEEP_PENDING`キー、`REVIEW_QUEUE_UPDATE`/`OPEN_REVIEW_SWEEP`イベント追加）
  - バックエンド・DBの変更なし（`preferences`は既存の汎用JSON列、`meta.declined`は既存`meta`列を利用）

---

## 2026-08-18: 状態`todo`（後日着手）追加・pending付帯情報（pending_condition/review_date）・decision_hold統合（R-125）
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [追加] | `JudgmentStatus`に`todo`（後日着手＝やると決めた順番待ち）を追加（7値→8値）。本道を`inbox → todo → focus → done`と定義 | ChatGPT議論`docs/reference/vision/2026-08-18_youkan-status-design-discussion_chatgpt.md`を採用 |
| [追加] | 判断モーダルに「後日着手」ボタン（`decisionToStatus('later')`→`todo`）。状態切替・右クリック・全画面表示に`todo`を追加。状況把握・登録と集中に「後日着手」バケット/セクション | 「今日やる」→focusは維持 |
| [追加] | `items.pending_condition`（何待ちか）・`items.review_date`（再確認日）を追加。`pending`時に詳細モーダルで任意入力。`review_date`到来分は「再確認」バッジで先頭表示 | 通知・自動復帰はR-126で検討 |
| [変更] | `pending`の定義を「条件・時期待ち／判断保留」、`waiting`を「人待ち」に明確化。`decision_hold`（判断保留）を`pending`に吸収し、`DecisionController.php`のhold書き込みを`pending`に統一、既存データを`UPDATE`で移行 | 仕様§4.4.1（hold→pending）とバックエンド実装の不一致を解消 |
| [変更] | R-028の「状態を7以上に増やさない」則を撤回し、上限を8値（本道4＋例外4）に再定義 | |
| [バグ修正] | `DecisionController.php::resolve()`の`decision='yes'`が`JudgmentStatus`型外のレガシー値`confirmed`を書き込んでいた（仕様§4.4.1違反）のを`focus`に修正。フローチャート等でこの経路を通ると全体一覧等のシェルフ分類から消える不具合があった。既存の`confirmed`データはシェルフ分類の読み取り側（`CloudYoukanRepository`/`YoukanRepository`）で`focus`相当として拾う読み取り互換のみ対応（データ移行SQLは未実施。件数を本番で確認してから別途判断） | レビューで発見（R-125実装中に同ファイルの既存バグとして検出） |

### Impact
- `04_データ設計.md` §3.1, §4.1, §4.4, §4.4.1, §4.6（新設）
- `02_機能仕様.md` F-02状態表
- `03_画面設計.md` §3.2.1, §6.1.3（新設）
- `05_技術設計.md` getGdbShelf構造、R-125技術仕様（新設）

#### 実装ファイル一覧（フロントエンド）
- `JWCADTategu.Web/src/features/core/youkan/types.ts`（`JudgmentStatus`8値化、`Item.pendingCondition`/`reviewDate`、`GdbShelf.todo`）
- `JWCADTategu.Web/src/features/core/youkan/logic/decisionResolution.ts`（`Decision`に`later`追加、`decisionToStatus('later')`→`todo`）
- `JWCADTategu.Web/src/features/core/youkan/logic/statusUtils.ts`（`STATUS_META.todo`、`isReviewDue`新設）
- `JWCADTategu.Web/src/features/core/youkan/repositories/CloudYoukanRepository.ts`（`getGdbShelf`にtodoバケット、decision_hold→pending同一視、`resolveDecision`型統一）
- `JWCADTategu.Web/src/features/core/youkan/repositories/YoukanRepository.ts`（同上・ローカルフォールバック側）
- `JWCADTategu.Web/src/features/core/youkan/viewmodels/useYoukanViewModel.ts`（`gdbTodo`状態を新設し全経路に配線、`resolveDecision`型統一、`getQuantityContext`からtodoを意図的に除外）
- `JWCADTategu.Web/src/api/client.ts`（`resolveDecision`の型統一）
- `JWCADTategu.Web/src/features/core/youkan/components/Modal/DecisionDetailModal.tsx`（「後日着手」ボタン、pending付帯情報入力欄）
- `JWCADTategu.Web/src/features/core/youkan/screens/DashboardScreen.tsx`（「後日着手」セクション、Pending再確認ソート、`unifiedAllItems`にtodo反映）
- `JWCADTategu.Web/src/features/core/youkan/components/PanoramaBoard/PanoramaBoard.tsx`（todoバケット、D&D対応、Pending再確認ソート）
- `JWCADTategu.Web/src/features/core/youkan/components/PanoramaBoard/ItemCard.tsx`（todo/再確認バッジ）
- `JWCADTategu.Web/src/features/core/youkan/components/Dashboard/SmartItemRow.tsx`（todo/再確認バッジ）
- `JWCADTategu.Web/src/features/core/youkan/components/OverviewBoard/OverviewItem.tsx`（todoステータスドット）
- `JWCADTategu.Web/src/features/core/youkan/components/OverviewBoard/useOverviewItems.ts`（`gdbTodo`をマージ対象へ追加）
- `JWCADTategu.Web/src/features/core/youkan/hooks/buildItemContextMenuActions.tsx`（`onResolveLater`メニュー項目）
- `JWCADTategu.Web/src/features/core/youkan/screens/FlowScreen.tsx`（右クリックメニューに後日着手を配線）
- `JWCADTategu.Web/src/features/core/youkan/components/Calendar/RyokanGanttView.tsx`（同上）
- `JWCADTategu.Web/src/features/core/youkan/components/Flow/FlowItemNode.tsx`（todo色）
- `JWCADTategu.Web/src/features/core/calendar/screens/VolumeCalendarScreen.tsx`（`handleDecision`型統一）

#### 実装ファイル一覧（バックエンド）
- `backend/db.php`（`pending_condition`/`review_date`カラム追加、`decision_hold`→`pending`の1回限りデータ移行）
- `backend/BaseController.php`（`mapItemRow`にキャメルケース変換追加）
- `backend/ItemController.php`（`update()`許可カラムに2フィールド追加）
- `backend/DecisionController.php`（`later`追加、`hold`の書き込み先を`decision_hold`から`pending`へ変更）
- `backend/GdbController.php`（未使用の旧エンドポイントだが仕様網羅のため最小限追随。詳細は本文の確認事項参照）

#### テスト（TDD Red→Green）
- `JWCADTategu.Web/src/features/core/youkan/logic/__tests__/decisionResolution.test.ts`
- `JWCADTategu.Web/src/features/core/youkan/logic/statusUtils.test.ts`
- `JWCADTategu.Web/src/features/core/youkan/repositories/CloudYoukanRepository.test.ts`
- `JWCADTategu.Web/src/features/core/youkan/viewmodels/__tests__/useYoukanViewModel.capacity.test.tsx`（新規）
- `JWCADTategu.Web/src/features/core/youkan/logic/QuantityEngine.test.ts`
- `backend/tests/test_r125_todo_pending.php`（新規）

---

## 2026-08-17: 「断る」判断の結果statusを`cancelled`へ統一・重複実装解消（R-124）
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [バグ修正] | フロー画面の詳細モーダルで「保留にする」を押すと三項演算子の握りつぶしで「断る」扱いになり`decision_rejected`が書き込まれるバグを修正 | 石鎚山プロジェクトで17件が意図せず却下扱いになった直接原因 |
| [バグ修正] | 状況把握・登録と集中（`useYoukanViewModel.resolveDecision`）の「断る」がstatus:`done`（完了）になっていたバグを修正 | |
| [バグ修正] | ガント右クリックメニューの「断る」がstatus:`done`を直接書き込んでいたバグを修正 | |
| [バグ修正] | `CloudYoukanRepository.getGdbShelf`のバケット分類が6値のみ判定しており、`cancelled`/`decision_rejected`のアイテムがどのバケットにも一致せずシェルフ（ひいては全体一覧）から消えていたバグを修正 | R-123で報告された症状の直接原因 |
| [追加] | `JudgmentStatus`型に`cancelled`を追加（6値→7値） | 発注者指示によりB案（却下を正式な状態として確立）を採用 |
| [追加] | `logic/decisionResolution.ts`の`decisionToStatus(decision, note)`にyes/hold/断るの状態決定ロジックを一本化 | 状況把握・全体一覧・フロー・ガント・カレンダーの重複実装を解消 |
| [追加] | `STATUS_META.cancelled`に`label`（狭い箇所用「断った」）・`labelWide`（広い箇所用「キャンセル・断った」）を追加 | 発注者指示: 「キャンセル」だけの婉曲表現を避け「断った」を必ず残す |
| [変更] | `hierarchy.ts`の`hideCompleted`が`done`と同様に`cancelled`も隠すよう変更 | 「履歴」として`done`と同じグループ扱い |
| [変更] | バックエンド`DecisionController.php`の`no`判定結果を`decision_rejected`から`cancelled`に変更、`CalendarController.php`/`GdbController.php`のステータス除外・包含リストに`cancelled`を追加 | 既存の`decision_rejected`データは表示互換のため読み取り側で`cancelled`同様に扱う（データ移行は別タスク） |

### Impact
- `JWCADTategu.Web/src/features/core/youkan/types.ts`
- `JWCADTategu.Web/src/features/core/youkan/logic/decisionResolution.ts`（新規）
- `JWCADTategu.Web/src/features/core/youkan/logic/statusUtils.ts`
- `JWCADTategu.Web/src/features/core/youkan/logic/hierarchy.ts`
- `JWCADTategu.Web/src/features/core/youkan/viewmodels/useYoukanViewModel.ts`
- `JWCADTategu.Web/src/features/core/youkan/screens/FlowScreen.tsx`
- `JWCADTategu.Web/src/features/core/youkan/components/Calendar/RyokanGanttView.tsx`
- `JWCADTategu.Web/src/features/core/youkan/components/Flow/FlowItemNode.tsx`
- `JWCADTategu.Web/src/features/core/youkan/hooks/buildItemContextMenuActions.tsx`
- `JWCADTategu.Web/src/features/core/youkan/repositories/CloudYoukanRepository.ts`
- `JWCADTategu.Web/src/features/core/youkan/repositories/YoukanRepository.ts`
- `backend/DecisionController.php`
- `backend/CalendarController.php`
- `backend/GdbController.php`
- `04_データ設計.md` §4.1, §4.4.1

---

## 2026-08-14: フローチャート画面に全体印刷ボタンを追加（R-101）
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [追加] | `FlowScreen.tsx`に「印刷」ボタンを追加。押下時、既存の「全体表示」相当の`fitView`をduration 0で実行してから`window.print()`を呼ぶ | R-098と同じ`.no-print`/`@media print`の仕組みを再利用 |

### Impact
- `FlowScreen.tsx`
- `03_画面設計.md` §7.9

---

## 2026-08-14: ガント完了アイテムのカレンダー要素・依存関係エッジをグレー化（R-100）
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [変更] | 完了表示トグルon時、完了済アイテムの日別割当チップ・目安納期ハンドルをグレー系に変更 | 従来は完了状態に関わらず`bg-indigo-500`/`bg-indigo-400`固定だった |
| [変更] | 依存関係矢印（`GanttDependencyArrows`）を、source/targetいずれかが完了済みならグレー系にする | 従来は`stroke="#6366f1"`固定だった |

### Impact
- `RyokanGanttView.tsx`
- `03_画面設計.md` §5.4

---

## 2026-08-14: GET /itemsに依存関係グラフを埋め込み（R-099）
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [追加] | `GET /items`系レスポンスの各アイテムに`dependsOn`（前提アイテムID配列）・`blocks`（後続アイテムID配列）を追加 | `GET /dependencies`を別途叩かなくても依存グラフを1回のリクエストで読めるようにする。AI等の外部クライアント向け |
| [設計判断] | N+1クエリを禁止し、`item_dependencies`をテナントスコープで一括取得してからメモリ上で隣接リストを構築する方式とした | R-091で「起動時APIコスト増」を理由に避けた埋め込みを、コストを抑えた形で今回は許容 |

### Impact
- `ItemController.php`
- `04_データ設計.md` §3.5

---

## 2026-08-14: ガントのマンスリー/ウィークリー表示モード＋コンテンツ印刷ボタン（R-097, R-098）
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [追加] | ガントビューに「マンスリー/ウィークリー」表示モード切替トグルを追加（R-097） | 日付範囲自体は変化しない。呼称は列幅・行高さの記憶枠の切替ラベル |
| [追加] | 日付列幅（列方向）スライダーを新設（R-097） | 既存の行高さ（行方向）スライダーと並べて常時表示。`RyokanGanttView.tsx`の`colWidth`固定値24pxを可変化 |
| [追加] | モードごとに列幅・行高さを独立して`localStorage`に記憶（R-097） | マンスリー/ウィークリーそれぞれの調整値をモード切替後も保持・復元 |
| [追加] | ガント・全体一覧にメニュー部分を除いたコンテンツのみを印刷するボタン（R-098） | `window.print()` + `@media print` CSS。専用PDFライブラリは不使用 |

### Impact
- `CalendarHeader.tsx`
- `RyokanGanttView.tsx`
- `youkanKeys.ts`（新規`localStorage`キー）
- `OverviewBoard.tsx`（印刷ボタン）
- `03_画面設計.md` §5.3

---

## 2026-08-12: Flow 上のモーダル入力応答改善（R-075）

- Flow の animated edge と全画面 `backdrop-filter` が同時に動作する合成負荷を避けるため、モーダル表示中だけ edge animation を一時停止する。
- モーダルを閉じると animation は自動的に再開し、Flow 単体の表示仕様は変更しない。
- ガントでも改善要望入力の遅延が報告されたため、共通要因である `SimpleModal` の全画面 `backdrop-filter` を廃止し、半透明背景のみとする。
- 判断詳細モーダルも同じ再合成負荷を避けるため `backdrop-filter` を廃止し、半透明背景のみとする。

## 2026-07-31: due_status意図しない変化の修正（R-073）
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [バグ修正] | `SmartDateInput`の`onBlur`で、パース結果が既存値と同じでも`onChange`が発火し、`DecisionDetailModal`側が無条件に`dueStatus: 'confirmed'`を送信していた。差分チェックを追加し、値が実際に変化した場合のみ通知するよう修正 | 本番検証（R-070デプロイ時）で「納期」フィールドをクリックしただけで`due_status`が`null`→`confirmed`に変化する副作用として発見 |
| [文書化] | `due_status`の値域（`null`/`confirmed`/`waiting_external`）と遷移条件（`due_date`の入力・変更・クリア時のみ更新）を現行仕様書に明記。旧アーカイブ設計書にのみ存在し現行仕様に未記載だった | `04_データ設計.md` §4.5 新設 |

### Impact
- `SmartDateInput.tsx`
- `DecisionDetailModal.tsx`（`dueStatus`送信箇所）
- `04_データ設計.md` §4.5

---

## 2026-07-31: Google OAuth失効検知・再連携UX実装（R-072）
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [バグ修正] | `GoogleCalendarService::refreshAccessToken()`が`invalid_grant`を判別せず、設定画面には「連携解除されました」表示が仕様として存在したのに実装が漏れており到達しなかった。`invalid_grant`検知時に`user_google_oauth.invalidated_at`/`last_error`へ記録し、設定画面の失効表示・再連携導線を実際に機能させる | 本番で`/google/calendars`が`invalid_grant`起因の500を継続的に返していたことを検証中に発見 |
| [設計判断] | 根本原因（OAuth同意画面が「テスト中」ステータスのままだとリフレッシュトークンが7日で強制失効）はGoogle Cloud Console側の設定変更が必要で、発注者本人のみが対応可能。今回のコード修正はあくまで検知・UX側の対症療法と位置付ける | `04_データ設計.md` §3.6 に注記 |

### Impact
- `backend/services/GoogleCalendarService.php`
- `backend/GoogleCalendarController.php`
- `user_google_oauth`テーブル（`invalidated_at`/`last_error`カラム追加、マイグレーション必要）
- `GoogleCalendarSection.tsx`
- `03_画面設計.md` §12.1, `04_データ設計.md` §3.6

---

## 2026-07-29: 改善要望送信フォーム（F-23, R-071）仕様確定
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [新規] | ホーム画面ヘッダーから画像添付付きで改善要望を送信できるフォームを追加 | ロゴ50%縮小＋ボタン配置（PC）、MenuDrawer「ツール」追加（スマホ） |
| [設計判断] | 送信内容はDBではなくサーバーローカルの `backend/data/requests_sub.md` に直接追記。回収は手動SSH/scpのみでAPI同期は持たない | 発注者確認済み。簡便さを優先し、複雑な同期APIは作らない方針 |
| [設計判断] | `backend/data/` を `upload.ps1` のデプロイ除外対象に追加する必要あり（既存の `.env`/`*.sqlite`/`*.log` 除外と同じ理由） | 怠ると次回デプロイで本番の蓄積データが消失するため実装時必須確認事項として明記 |

### Impact
- F-23 改善要望送信フォーム
- `03_画面設計.md` §14, `04_データ設計.md` §3.9
- `upload.ps1`（デプロイ除外リスト変更が必要）

---

## 2026-07-10: R-050 Phase1実装完了（担当者別ビュー・管理者スコープ403バグ修正）

### 出典
バックエンド実装引き継ぎ: `docs/handover/R-050-phase1-backend.md`

### 決定事項
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [完了] | バックエンド実装（管理者スコープAPI新設・`assigned_to` ID空間解決の横展開）をマージ | `feature/R-050-phase1-assignee-view` ブランチ、マージコミット `322caf1` |
| [完了] | フロントエンド実装（担当者別ビュー画面 `AssigneeViewScreen`）をマージ | `feature/R-050-phase1-assignee-view-ui` ブランチ、マージコミット `67e2586` |
| [修正] | 実機検証（claude-in-chromeでブラウザ操作）で発見したバグ「会社アカウント（テナント自身ログイン、`account_type=tenant`）で管理者スコープ切替が403になる」を修正 | `fix/R-050-phase1-tenant-account-admin-scope` ブランチ、マージコミット `ce65aa2` |

### 原因（会社アカウント403バグ）
`BaseController::assertAdminScopeAllowed()` が `memberships` テーブルを `user_id = currentUserId` で検索していたが、会社アカウントログイン時は `currentUserId` がテナントID自身であり `memberships` に該当行が無いため権限判定に失敗していた。修正後、会社アカウント・ユーザーアカウント両方で200を確認、テナント分離（他テナントのID指定で404）も維持されていることを確認済み。

### 影響範囲
- 担当者別ビュー（画面2）はエンドツーエンドで動作する状態になった
- **本番デプロイ（`upload.ps1`）は未実施**。次回セッションでデプロイ判断を行う（`docs/handover/R-050-phase1-backend.md` 参照）

---

## 2026-07-09: R-050 Phase1 フロントエンド仕様確定（担当者別ビュー画面設計）

### 出典
バックエンド実装引き継ぎ: `docs/handover/R-050-phase1-backend.md`／会議録8節ステップ3: `docs/kaigi/2026-07-09-R050テナント型AI中枢設計.md`

### 決定事項
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [追加] | `ViewState = 'assigneeView'` として独立画面を新設 | 画面4（社員向けToday）とは統合しない。03_画面設計.md §8, §13 |
| [追加] | デフォルト表示は `scope=team&assigned_to=<自分のuserId>` 固定 | 既存ルール1〜3（created_by=自分 OR assigned_to=自分）ではなく assigned_to のみに絞る。03_画面設計.md §13.2 |
| [追加] | 担当者切り替えUIは `tenant.role IN ('owner','admin')` のみ表示 | 候補は `GET /tenant/members`（users由来）と既存 `useAssignees()`（assignees由来）をマージ。03_画面設計.md §13.3 |
| [確定] | `ApiClient.getMembers()`（`/members`宛）は実体不一致のバグのため本機能では使用せず、`CompanySettingsScreen.tsx` と同じ `GET /tenant/members` 直叩きに統一する | 既存バグの修正自体は本タスクのスコープ外 |
| [追加] | 今日／明日／今週の日付バケット分類・所要時間合計・未完了/詰まり/待ち集計のロジックをPhase1で新規定義 | 03_画面設計.md §13.5〜§13.7。評価語（「超過」等）は非審判原則を踏まえ不採用 |
| [追加] | `Item.assigneeKind` 型・`ApiClient.getAllItems()` の `scope:'team'`/`assigned_to` オプションをフロントに追加 | 03_画面設計.md §13.8 |

### 影響範囲
- フロントエンド新規実装は別Agentへ委譲（`feature/R-050-phase1-assignee-view-ui` ブランチ）
- バックエンド（`backend/`配下）は変更しない

---

## 凡例

- **[新規]**: 新しい仕様の追加
- **[変更]**: 既存仕様の変更
- **[修正]**: バグ修正に伴う仕様の明確化
- **[統合]**: 既存ドキュメントからの統合

---

## 2026-07-09: R-069 `/stocks` 未認証アクセス脆弱性の緊急修正

### 背景
R-050案の設計会議調査で、本番稼働中の `backend/index.php` に登録された `/stocks` 系ルート（`GET/POST /stocks`、`PUT/PATCH /stocks/{id}`、`POST /stocks/{id}/assign`）が `StockController.php` にディスパッチされる際、`authenticate()` が一切呼ばれず `tenant_id` によるスコープ分離も無いまま到達可能であることが判明した。特に `assign()` はリクエストの任意の `userId` を検証せず `items` テーブルへ直接 `INSERT` できるため、未認証の第三者が他テナントの `items` にレコードを書き込める状態だった。

### 調査結果
- フロントエンドで `/stocks` API を呼ぶのは `useStocks.ts`（呼び出し元は `DispatchScreen.tsx` のみ）のみ。`DispatchScreen.tsx` と `CompanyDashboard.tsx` はアプリのルーティング・画面遷移から一切参照されていない死蔵コードであることを確認済み（相互参照のみ）。
- したがって `/stocks` API は実際には稼働中のどの画面からも呼ばれておらず、実害のある機能を壊さずにルート自体を無効化できると判断した。

### 決定事項
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [修正] | `backend/index.php` から `/stocks` 系ルート登録（3ブロック）をコメントアウトし無効化 | `StockController.php` 自体は削除せず残置。将来再導入する場合は `authenticate()` と `currentTenantId` スコープを実装してから有効化する旨をコード内コメントに明記 |
| [追加] | `backend/tests/test_stocks_unauthenticated_access.php` を新規追加 | PHP内蔵サーバーを一時ディレクトリで起動し、Authorizationヘッダーなしで `/stocks` 系3エンドポイントを叩き 404 になることを検証する統合テスト |

### 影響範囲
- `backend/index.php`（`/stocks` 系ルート3ブロックの無効化のみ。他ルートは無変更）
- フロントエンド変更なし（`useStocks.ts`/`DispatchScreen.tsx`/`CompanyDashboard.tsx` は死蔵のまま残置、再利用時は要再設計）
- ブランチ: `fix/stock-controller-auth`（R-050 Phase1作業とは分離）

---

## 2026-07-09: R-050 Phase1着手（テナント型AI中枢・担当者別ビュー）意思決定

### 出典
kaigi2会議（専門家6名 × 2ラウンド + 敵対的検証23エージェント）: `docs/kaigi/2026-07-09-R050テナント型AI中枢設計.md`

### 決定事項
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [変更] | 可視性ルールに管理者スコープ（4番目）を新設 | `memberships.role IN ('owner','admin')` のユーザーは所属テナント内で `assigned_to` を問わず閲覧可能。既存ルール1〜3は変更なし。04_データ設計.md §5.3 |
| [修正] | `assigned_to` のID空間表示バグ（会社アイテムの97%が `u_` 形式でJOIN失敗）をアプリケーション層で是正する方針を確定 | discriminator列追加マイグレーションは不採用。`ItemController.php::getAssigneeEmail()` の `u_` プレフィックス判定を一覧系クエリへ横展開する。04_データ設計.md §3.8 |
| [追加] | 担当候補の型 `assigneeKind: 'user' \| 'assignee'` を確定 | Phase3（AI提案設計）以降で再定義しない。04_データ設計.md §3.8 |
| [明確化] | 孤児データ（どちらのIDにも一致しない `assigned_to` 値）は自動修復せず「未割当」表示＋ログ出力とする | 実数の棚卸しはPhase1実装時またはPhase2着手前に別途実施 |
| [確定] | Phase1のスコープは「画面2: 担当者別ビュー」単体に限定 | 画面4（社員向けToday）との統合（会議での候補C）は不採用。Phase6検討時に再評価 |
| [先送り] | meta.dispatchスキーマ・dispatch_proposalsテーブル・承認ACL・サーキットブレーカー設計はPhase3着手判断時に改めて統合設計 | 今回は設計対象外 |
| [設計注記] | `DispatchScreen.tsx` / `CompanyDashboard.tsx` / `StockController.php` は土台にしない | 詳細は下記「調査結果」参照 |

### 調査結果（既存3ファイルの扱い）
- `JWCADTategu.Web/src/features/core/youkan/screens/DispatchScreen.tsx`、`CompanyDashboard.tsx`: どちらもモックデータのみのコンポーネントで、アプリ内のルーティング・画面遷移から一切参照されていない（相互参照のみ、`DispatchScreen.tsx` が `useStocks` フックを使用）。最終更新は2026-02-24で以降凍結。**死蔵コード。再利用禁止。**
- `backend/StockController.php`: kaigi会議の想定（「router.php未登録のため実害ゼロ」）とは異なり、実際には `backend/index.php` に `require_once 'StockController.php'` の上で `/stocks`・`/stocks/:id`・`/stocks/:id/assign` の3ルートが登録済みで、`router.php` 経由で到達可能。`authenticate()` 呼び出しも `tenant_id` チェックも一切経由せず、`assign()` は任意の `userId` を受け取って直接 `items` テーブルへ `INSERT` する。**単なる「再利用禁止」注記では不十分。認証・テナント分離が皆無のまま本番到達可能な状態にあるため、削除または認証追加を伴う修正が必要。** コード変更は本ドキュメント作業のスコープ外のため未対応。別タスクとして緊急度高めに起票することを推奨。

### 影響範囲
- コード変更なし（本エントリはdocs配下の仕様整理のみ）
- Phase1のバックエンド/フロントエンド実装は別Agentへ委譲（`feature/R-050-phase1-assignee-view` ブランチ予定）

---

## 2026-07-08: 量感カレンダー/ガントの表示条件整理（R-066）

### 決定事項
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [明確化] | 期限が過去でも未完了アイテムはガント・量感カレンダーに表示し続ける仕様は意図的である | 督促目的。バグではなく対応不要と確定 |
| [明確化] | 完了済みアイテムの表示/非表示は R-065 右上「完了表示」トグルによる意図的仕様 | バグではない |
| [確認] | 削除済みアイテム（ゴミ箱）は `GdbController.php`/`TodayController.php` の SQL で `deleted_at IS NULL` により元々除外済み | — |
| [確認] | アーカイブアイテムはバックエンド SQL に `is_archived` 除外の穴があるが、フロント `hierarchy.ts` の `isArchived` 判定と `VolumeCalendarScreen.tsx` の `isDisplayableCalendarItem` で現状実害なくカバーされている | バックエンド防御強化は技術的負債として R-067 に分離 |

### 影響範囲
- ガントチャート・量感カレンダーの表示条件そのものはコード変更なし（判断確定のみ）
- 未着手事項は `docs/requests.md` に R-067（アーカイブ除外のバックエンド防御強化・低優先度）、R-068（量感カレンダー本体のスクロール性能改善・高優先度）として分離起票

---

## 2026-07-08: 詳細カレンダーのスクロール即応性改善

### 決定事項
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [修正] | 詳細画面内の量感カレンダーでは、通常カレンダー用のスクロール処理を接続しない | 表示月同期・範囲自動拡張が不要なため |
| [修正] | 詳細カレンダーの量感セルでは装飾トランジションと不要なペイント範囲を抑制する | スクロール開始時の体感遅延を減らす |

### 影響範囲
- 詳細画面左カラムの量感カレンダー
- 通常の量感カレンダー、ガント、タイムラインの範囲拡張・表示月同期は維持

---

## 2026-07-06: 量感カレンダー分母ルールの確定

### 決定事項
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [変更] | 個人アカウントの「全て」は本人の業務キャパシティ合計を分母にする | 所属会社ごとのキャパシティは足し上げない |
| [変更] | 個人/会社/個別会社フィルタごとに分母を切り替える | 個人=総量-会社配分、会社=会社配分合計、個別会社=該当会社配分 |
| [追加] | 会社/チームスコープの分母を主力メンバーのキャパシティ合計と定義 | 現行実装は `memberships.is_core=1` の `daily_capacity_minutes` 合計 |
| [明確化] | 会社配分未設定の会社は 0 分として扱う | 総キャパシティへフォールバックしない |
| [追加] | メンバー別 `capacity_profile` を `memberships` に保存 | 会社/チーム分母に曜日別・日別例外・会社別配分を反映 |

### 影響範囲
- F-06 量感カレンダーのヒートマップ背景色と CapacityBar
- 詳細画面の量感カレンダーは同じ `QuantityEngine` を使うため同じ分母ルールに従う
- `/members` と `/tenant/members` は `capacityProfile` を返す。`/tenant/members/{id}` は `capacity_profile` を保存する
- 会社/チーム分母は、core member の `capacityProfile` を優先し、未設定時は `dailyCapacityMinutes` を使う

---

## 2026-06-15: スマホ詳細画面レイアウト再設計（R-064）

### 決定事項
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [変更] | スマホ横画面を2カラム化（カレンダー左・フィールド右） | `landscape-compact:` バリアント限定（高さ≤500px の横向きのみ） |
| [変更] | ヘッダーをコンパクト化 | タイトル縮小・視点ラベルバッジ非表示・日付ピル化 |
| [変更] | クイック選択ボタン群を横画面で非表示 | タップ入力（SmartDateInput）は維持 |
| [変更] | メモ/Boost をフィールドカラムへ移動 | カレンダー高さを最大化 |
| [変更] | 縦画面: カレンダー下に独立スクロール領域 | ネストスクロール回避 |
| [変更] | 目安期間ブレイクダウンを折りたたみ（デフォルト閉じ） | カレンダー直下に配置 |
| [追加] | Tailwind カスタムスクリーン `landscape-compact` | `(orientation: landscape) and (max-height: 500px)` |

### 出典
secretary/notes/2026-06-15-会議-スマホ詳細画面レイアウト再設計.md

### 影響範囲
- デスクトップ（md以上・高さ大）・縦画面の見た目は不変
- 横画面でも SmartDateInput によるタップ入力機能は維持

### 棄却した提案
- `landscape:` バリアント（デスクトップ横向きにも適用されたため `landscape-compact:` に変更）

---

## 2026-06-15: 詳細カレンダー Google 連携・量感フィルタ連動・時刻非表示（R-061〜R-063）

### 決定事項
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [追加] | 詳細モーダルカレンダーに Google 外部イベントを供給・表示（R-061） | `useExternalEvents` + `useGoogleCalendars` を `DecisionDetailModal` 内で呼び出し、3層 props で伝達 |
| [追加] | 密度トグル FULL / COMPACT（R-061） | `localStorage` キー `youkan_detail_calendar_density`、デフォルト FULL |
| [変更] | 詳細カレンダーの量感母集団を左上フィルタに連動（R-062） | `focusedTenantId` / `focusedProjectId` を渡さない方針。アイテム所属による絞り込みを廃止 |
| [変更] | 詳細カレンダーの外部イベントチップ時刻を非表示（R-063） | `ExternalEventChip.hideTime=true` prop 追加。グリッドビューは時刻表示を維持 |

### 影響範囲
- グリッドビュー（`VolumeCalendarScreen`）の表示は変更なし
- `ExternalEventChip` に `hideTime` prop が追加されるが、既存の全呼び出し側はデフォルト `false` のため影響なし

---

## 2026-05-11: スマホUI改善＋ボトムシート基盤（R-033）

### 決定事項
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [追加] | F-21 スマホフィルター | ヘッダー右に Filter アイコン＋ボトムシート |
| [追加] | F-22 スマホ用その他メニュー | DecisionDetailModal でスマホ時ボトムシート化 |
| [追加] | MobileBottomSheet 共通コンポーネント | safe-area対応、touch-action抑制 |
| [追加] | スマホメニュー設計原則6ルール | 恒久メンテナンス則 |
| [変更] | スマホヘッダー右側整理 | ForAi/Speech をヘッダー外へ集約、Filter を追加 |

### 出典
secretary/notes/2026-05-11-会議-スマホUI改善.md

### 影響範囲
- 新機能のため既存PCの挙動への影響なし
- スマホ表示時のヘッダー構成のみ変更（ForAi/Speech は MenuDrawer/フローティングで継続アクセス可）

### 棄却した提案
- アナリティクス導入（別タスク）
- SimpleModal 破壊的変更（リスク回避）
- WCAG 完全準拠（別タスク）

---

## 2026-05-07: 目玉機能復活 — ForAI機能＋タスク読み上げ機能（R-031）

### 決定事項
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [追加] | F-19 ForAI機能（AI連携エクスポート） | 過去仕様書から復活実装 |
| [追加] | F-20 タスク読み上げ機能 | Web Speech API、優先順位順、歌詞型スクロール |
| [追加] | 共通ユーティリティ6種（clipboard, useMediaQuery, speechSynthesis, useSpeechSynthesis, forAiExporter, perspectiveLabel） | 横断的に利用可能 |
| [変更] | YoukanHeader 右側に ForAI/読み上げボタン追加 | PC 用 |
| [変更] | MenuDrawer Tools セクションに ForAI 追加 | スマホ用 |

### 出典
過去仕様書: `docs/SPEC/archives/ForAI機能.md`, `読み上げ機能.md`, `20260202_Youkanの概要と用語.md`

### 影響範囲
- 新機能のため既存挙動への影響なし
- マイグレーション不要

### 棄却した提案
- JSON 出力対応（v2 で検討）
- ForAI 拡張の依存関係/場所/タスク種類（20260305 提案、別 task）

---

## 2026-05-05: 階層ラッパー型分離・virtual-header- 撤廃（R-030）

### 決定事項
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [変更] | HierarchicalWrapper を discriminated union 化 | item/header の型分離 |
| [削除] | virtual-header- プレフィックス生成（hierarchy.ts 2箇所） | 偽装ID廃止 |
| [削除] | .replace('virtual-header-', '') 散在コード（12箇所） | 不要化 |
| [削除] | OverviewBoard.tsx のローカル stripId 関数 | 不要化 |
| [修正] | OverviewItem の onNavigateToFlow バグが自動解消 | 型分離の副次効果 |
| [追加] | hierarchy.types.test.ts | 新型のTDDテスト |

### 影響範囲
- マイグレーション不要
- 既存ユーザー操作: 改善のみ（フロー画面のコア機能回復）
- 過去 URL ブックマーク（virtual-header-付き）は今後生成されないため、既存のものは破損リスクなし

### 設計判断
- TDD・MVVM・リアクティブ・メンテナンス性を最優先
- TypeScript の型システムで「ヘッダーから item.id を取り出す」誤りをコンパイル時に検出
- 関連する別R番号タスクで検討した「共通ヘルパー idUtils.ts」「多層防御」は型分離により不要となり全廃

### メンテナンス則
- 階層ラッパー型は discriminated union（type プロパティで分岐）
- ヘッダー型は item: Item を持たない
- React key には wrapper.id を使う
- item.id 加工パターン禁止

---

## 2026-05-04: R-029 3バグ複合修正

### 決定事項
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [変更] | Stream モードで is_project=1 アイテムを非表示 | プロジェクトはパノラマ・全体一覧で扱う |
| [追加] | getGdbShelf レスポンスに someday キー追加 | R-028 の暫定派生フィルターを是正 |
| [変更] | getProjectDescendantIds の WITH RECURSIVE 初期行に parent_id 追加 | 3階層以上のプロジェクトフォーカスで子孫取得 |

### 影響範囲
- マイグレーション不要
- 既存ユーザー操作感: 改善のみ（バグ修正）

---

## 2026-05-04: 「いつかやる」状態追加（R-028）

### 決定事項
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [追加] | JudgmentStatus に 'someday' を追加 | 5値→6値 |
| [追加] | パノラマに Someday バケット | Pending と Log の間 |
| [追加] | 全体一覧に Someday含む/除く フィルター | デフォルト除く |
| [追加] | 詳細モーダルに 💭 いつかやる 状態ボタン | purple 色 |
| [変更] | キャパシティ計算から someday を除外 | Youkan独自の差別化 |
| [変更] | ガント・カレンダーで someday 非表示 | 期限なきもの |
| [整理] | 既存遺物 judgmentStatus の someday を廃止 | メインstatusに統合 |

### 設計判断（会議録参照）
secretary/notes/2026-05-04-会議-いつかやる状態の追加.md

### 影響範囲
- データマイグレーション不要
- 既存ユーザー: someday 状態は明示的に使うまで影響なし
- 付随機能（自動アーカイブ・リマインダー）は別task

### 棄却した提案
- マネタイズ起点議論
- 自動アーカイブ・シャッフル表示・週次リマインダー（別task）
- フラグ別軸での実装

---

## 2026-04-27: 親→子孫カスケード機能（R-027）

### 決定事項
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [追加] | テナント/プロジェクト移動カスケード | 子孫の tenant_id 連動 |
| [変更] | archive/trash/restore/delete カスケード | is_project 条件撤廃、親子関係で常に伝播 |
| [追加] | 移動先テナント権限チェック | joinedTenants 外で 403 |
| [追加] | API応答に affectedDescendantIds | 部分リフレッシュ用 |
| [追加] | ViewModel: refreshItems / optimisticCascadeTenant | 楽観的UI更新＋ロールバック |
| [追加] | バックエンドヘルパー: cascadeTenantToDescendants 等 | update/archive 等から共用 |
| [追加] | バッチ取得API: GET /items/batch or POST /items/batch | refreshItems 用 |

### 影響範囲
- 既存ユーザーの操作感: 親操作で子孫が連動するように変わる（仕様書の意図通り）
- API互換: 戻り値に affectedDescendantIds を追加（既存呼出側は無視可、後方互換）
- DB マイグレーション不要

---

## 2026-04-22: 命名統一リファクタリング（R-026）

### 決定事項

| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [上書き] | R-008「内部モード値・コンポーネント名は変更なし」方針を破棄 | UIラベルと内部名称の一致を優先 |
| [変更] | `GlobalBoard`/`YoukanBoard` → `PanoramaBoard` | フォルダ・ファイル・コンポーネント・Props名統一 |
| [変更] | `NewspaperBoard` → `OverviewBoard` | 同上 |
| [変更] | viewMode値 `'board'` 廃止 → `'panorama'` | ヘッダー送信値と内部値を一致 |
| [変更] | viewMode値 `'newspaper'` → `'overview'` | UIラベル「全体一覧」と整合 |
| [追加] | `ViewModeContext` 新設 | CustomEvent駆動→Context駆動に移行。FilterContextとの責務分離（ViewMode=画面切替、Filter=テナント絞り込み） |
| [追加] | `components/Common/` 新設 | ContextMenu等の複数ビュー横断共通部品置き場 |
| [追加] | `logic/filterUtils.ts` 新設 | `isTenantFilter`, `isCompanyContext`, `getSelectedTenantId` ヘルパー関数群 |
| [追加] | `migrateLocalStorage` 実装 | 旧値→新値の自動変換（`youkan_schema_version=2` ガードで冪等保証） |

### 影響範囲

- 既存ユーザーの localStorage は自動マイグレーション（`'board'`→`'panorama'`、`'newspaper'`→`'overview'`、`youkan_newspaper_*`→`youkan_overview_*`）
- 外部APIへの影響なし
- `docs/SPEC/03_画面設計.md` §5.5 にコンポーネント命名規約を新設
- `docs/SPEC/05_技術設計.md` のディレクトリ構造更新

---

## 2026-04-02: フローView仕様追加（R-014）

| 種別 | 内容 | 反映先 |
|:--|:--|:--|
| [新規] | フローView（5つ目のView）の仕様策定 | 03_画面設計.md §7 |
| [新規] | `item_dependencies` テーブル定義 | 04_データ設計.md §3.5 |
| [新規] | 依存関係CRUD API定義 | 04_データ設計.md §5.1 |

---

## 2026-04-02: ガント一覧モード仕様追加+バグ修正（R-011〜R-013）

| 種別 | 内容 | 反映先 |
|:--|:--|:--|
| [新規] | ガントビューの「プロジェクト別/一覧」切替仕様を明文化 | 03_画面設計.md §5.3 |
| [新規] | 一覧モード: プロジェクト非表示・フラットリスト・ソート順・プロジェクト名末尾表示 | 03_画面設計.md §5.3 |
| [修正] | CalendarController.php: is_projectカラムSELECT追加+プロジェクト除外フィルタ | — |
| [修正] | CalendarController.php: SQLパラメータ数不一致修正 | — |
| [修正] | 本番DB不正データ（ID=NULLの「総会」レコード）を削除 | — |
| [修正] | hierarchy.ts: IDがnullのプロジェクトをprojectMapから除外する防御コード | — |
| [修正] | 循環参照防止付きツリー構築関数をhierarchy.tsに共通化 | — |

---

## 2026-03-31: View名変更（R-008）

| 種別 | 内容 | 反映先 |
|:--|:--|:--|
| [変更] | 「全体一覧」(Panorama)→「状況把握」に名称変更 | 03_画面設計.md §1, §3 |
| [変更] | 「全体一覧2」(Newspaper View)→「全体一覧」(Overview View) に名称変更 | 03_画面設計.md §1, §4 |

~~内部モード値(`panorama`, `newspaper`)、URL、localStorageキー、コンポーネント名は変更なし。~~

> **注意**: 上記の「コンポーネント名は変更なし」方針は **R-026（2026-04-22）で上書き**された。UIラベルと内部名称の一致を優先する方針に変更。

---

## 2026-03-31: 仕様追加（R-004〜R-007）

| 種別 | 内容 | 反映先 |
|:--|:--|:--|
| [変更] | ブラウザタブタイトルを「JBWOS」→「Youkan」に変更 | 03_画面設計.md §0 |
| [新規] | Overview View（旧: Newspaper View / 全体一覧）の初期フィルタを「全て」に固定 | 03_画面設計.md §4.0 |
| [新規] | `completed_at`カラム追加。完了日の記録+カレンダーでの振り返り表示 | 04_データ設計.md §3.1, 02_機能仕様.md §F-02, §F-06 |
| [新規] | Panorama View「今月を表示」ボタンで上下左右の中央スクロール | 03_画面設計.md §3.3 |

---

## 2026-03-24: SdDD形式への仕様書統合

### 実施内容

SdDD（仕様書駆動開発）の構造を導入。既存の散在したドキュメント群からYoukanの最新仕様を抽出し、以下のファイルに統合した。

| ファイル | 統合元 |
|:--|:--|
| `SPEC.md` | 新規作成（目次・概要） |
| `spec/01_概要.md` | 00_Vision/User_Voices/20260202_Youkanの概要と用語.md, 00_Vision/System_Grand_Design.md, 00_Vision/SYSTEM_PHILOSOPHY_AND_VISION.md |
| `spec/02_機能仕様.md` | spec/00_MASTER_SPEC.md, spec/03_MORNING_PLANNING.md, 00_Vision/Capacity_Management_Spec.md, 00_Vision/User_Voices/* |
| `spec/03_画面設計.md` | spec/02_VIEW_DEFINITIONS.md, spec/00_MASTER_SPEC.md |
| `spec/04_データ設計.md` | spec/01_STATE_MATRIX.md, backend/db.php, backend/ItemController.php, backend/TodayController.php |
| `spec/05_技術設計.md` | package.json, バックエンドコード構成, 00_Vision/System_Grand_Design.md |

### 注意事項

- 旧ファイル（spec/00_MASTER_SPEC.md, 01_STATE_MATRIX.md, 02_VIEW_DEFINITIONS.md, 03_MORNING_PLANNING.md）は参照用として残置
- `docs/SPEC/`（大文字）は`docs/spec/`（小文字）と同内容。SdDDでは`docs/spec/`を正とする
- 旧名称JBWOS時代のドキュメントが`docs/`配下に多数存在するが、新spec/*.mdに統合済みの内容が正
- 既存の`docs/requests.md`の内容は`request_log.md`にも転記した

---

## 2026-07-07: Volume Calendar Gantt item visibility fix
### Decisions
| Type | Content | Notes |
|:--|:--|:--|
| [Fix] | Gantt view calls `/calendar/items` with `mode=gantt` and fetches active visible tasks without limiting them to the visible date range. | Grid/range views keep the existing date-range behavior. |
| [Fix] | Project focus membership is resolved through descendant `items.id`, `items.project_id`, and `items.parent_id`. | Prevents tasks attached only by `parent_id` from disappearing in the Gantt chart. |
| [Fix] | Company filter treats `domain === 'business'` as company work even when `tenantId` is empty. | Aligns VolumeCalendarScreen with existing Youkan filtering semantics. |

### Impact
- F-06 Volume Calendar Gantt view
- `/calendar/items?mode=gantt`
- `VolumeCalendarScreen` personal/company filter

---

## 2026-07-07: `focus` / Today / focusedProject の概念分離を明文化
| 種別 | 内容 | 備考 |
|:--|:--|:--|
| [明確化] | `status = focus` はアイテム状態、Today は当日の実行キュー、`focusedProjectId` / `activeProject` はプロジェクト文脈であり、それぞれ別軸として扱うことを明記 | 全体一覧・ガント・量感カレンダーの表示条件判断で混同しないため |
