# 05. 技術設計

## 1. 技術スタック

### フロントエンド

| カテゴリ | 技術 | バージョン |
|:--|:--|:--|
| 言語 | TypeScript | ^5.2 |
| フレームワーク | React | ^18.2 |
| ビルドツール | Vite | ^5.2 |
| スタイリング | Tailwind CSS | ^3.4 |
| ルーティング | react-router-dom | ^7.12 |
| ドラッグ&ドロップ | @dnd-kit/core, @dnd-kit/sortable | ^6.3, ^10.0 |
| 日付操作 | date-fns | ^4.1 |
| アニメーション | framer-motion | ^12.26 |
| ローカルDB | Dexie (IndexedDB) | ^4.0 |
| アイコン | lucide-react | ^0.378 |
| 国際化 | i18next, react-i18next | ^25.7, ^16.5 |
| ユーティリティ | clsx, tailwind-merge, uuid | — |
| テスト | Vitest, @testing-library/react, Playwright | ^4.0, ^16.3, ^1.58 |

### バックエンド

| カテゴリ | 技術 |
|:--|:--|
| 言語 | PHP（ビルトインサーバー） |
| データベース | SQLite |
| 認証 | JWT（自前実装） |
| ID生成 | UUIDv7 |

---

## 2. アーキテクチャ

### 2.1 3層積み上げ構造

```
┌─────────────────────────────────────┐
│  Domain Plugin (例: Tategu Plugin)  │  業種特化機能
├─────────────────────────────────────┤
│  Manufacturing Layer (Extension)    │  モノづくり共通機能
├─────────────────────────────────────┤
│  Youkan Core (Platform)             │  汎用タスク管理・意思決定OS
└─────────────────────────────────────┘
```

- **Youkan Core**: タスク管理、カレンダー、ユーザー管理。全ユーザー向け
- **Manufacturing Layer**: 製作時間、原価、現場工数。製造業共通
- **Domain Plugin**: 業種特化。建具のパラメータ、姿図描画、見積ロジック等

### 2.2 Manufacturing Bus

- 複数プラグイン（建具、家具、外注等）を束ねるバス
- アプリ起動時にプラグインが自動登録
- `ManufacturingBus.getSources()`で取得可能

### 2.3 フロントエンドディレクトリ構造

```
JWCADTategu.Web/src/
├── api/              # APIクライアント
├── components/       # 共通UIコンポーネント
│   ├── Dashboard/
│   ├── Layout/       # YoukanHeader等
│   ├── QuantityCalendar/
│   ├── Settings/
│   └── Toast/
├── config/           # 設定
├── contexts/         # React Context
├── db/               # Dexie（ローカルDB）
├── features/         # 機能別モジュール
│   ├── core/
│   │   ├── auth/     # 認証
│   │   ├── calendar/ # カレンダー画面
│   │   ├── manual/   # マニュアル画面
│   │   ├── planning/ # FutureBoard
│   │   ├── plugin-system/ # プラグイン基盤
│   │   ├── projects/ # プロジェクト管理
│   │   ├── session/  # セッション管理
│   │   ├── settings/ # 設定画面
│   │   └── youkan/   # Youkanコア機能
│   │       ├── components/
│   │       │   ├── Calendar/      # 量感カレンダー
│   │       │   ├── Common/        # 複数ビュー横断の共用部品（ContextMenu等）
│   │       │   ├── Dashboard/     # ダッシュボード系
│   │       │   ├── History/       # 履歴画面
│   │       │   ├── Inputs/        # Quick Input等
│   │       │   ├── Layout/        # モチベーター等
│   │       │   ├── Modal/         # DecisionDetailModal等
│   │       │   ├── OverviewBoard/ # Overview View（旧: NewspaperBoard）
│   │       │   ├── PanoramaBoard/ # Panorama View（旧: GlobalBoard）
│   │       │   └── UI/            # UndoToast等のプリミティブ
│   │       ├── contexts/          # UndoContext, FilterContext, ViewModeContext
│   │       │   # ViewModeContext: 画面切替の責務（FilterContextはテナント絞り込みの責務）
│   │       ├── logic/             # ビジネスロジック（filterUtils等）
│   │       ├── screens/           # DashboardScreen等
│   │       ├── types/             # 型定義
│   │       └── viewmodels/        # ViewModel
│   └── plugins/
│       ├── customer/     # 顧客管理プラグイン
│       ├── manufacturing/# 製造管理プラグイン
│       ├── mock/         # モックプラグイン
│       └── tategu/       # 建具プラグイン
├── hooks/            # カスタムフック
├── i18n/             # 国際化
├── lib/              # ライブラリ
├── logic/            # ビジネスロジック（QuantityEngine等）
├── pages/            # ページコンポーネント
├── repositories/     # データアクセス層
├── test/             # テストユーティリティ
└── utils/            # ユーティリティ関数
```

### 2.4 バックエンドディレクトリ構造

```
backend/
├── router.php         # PHPビルトインサーバー用ルーター
├── index.php          # APIエントリーポイント
├── db.php             # DB接続・マイグレーション
├── BaseController.php # 基底コントローラ（認証・権限チェック）
├── ItemController.php # アイテムCRUD
├── TodayController.php # Today View用API
├── CalendarController.php # カレンダーAPI
├── ProjectController.php  # プロジェクトAPI
├── AuthController.php     # 認証API
├── UserController.php     # ユーザーAPI
├── TenantController.php   # テナントAPI
├── AssigneeController.php # 担当者API
├── SideMemoController.php # サイドメモAPI
├── Constants.php      # 定数定義
├── JWTService.php     # JWT処理
├── Uuidv7.php         # UUIDv7生成
├── QuantityService.php # 量感計算サービス
├── api/               # サブAPI（customers, deliverables）
├── scripts/           # マイグレーションスクリプト
├── tests/             # テスト
├── migrate_v*.php     # マイグレーションファイル群
└── jbwos.sqlite       # SQLiteデータベース（gitignore）
```

---

## 3. 開発環境

| 項目 | 値 |
|:--|:--|
| フロントエンドポート | 5173 |
| バックエンドポート | 8000 |
| 開発URL | http://localhost:5173/contents/Youkan/ |
| Vite base | /contents/Youkan/ |
| AppID | Youkan |

### 起動方法

```powershell
# バックエンド
cd backend
php -S 0.0.0.0:8000 router.php

# フロントエンド
cd JWCADTategu.Web
npm run dev -- --port 5173 --host 127.0.0.1
```

---

## 4. デプロイ

- **本番サーバー**: ConoHa WING（SSH経由）
- **本番URL**: https://door-fujita.com/contents/Youkan/
- **デプロイスクリプト**: `deploy.ps1`

---

## 5. 設計原則

### QuantityEngine（量感計算エンジン）の責務分離

- QuantityEngineの責務: 「与えられたアイテムリストに対して日別の負荷を計算する」
- フィルタロジックはViewModel層に統一し、QuantityEngineからは除去
- 「何を表示するか（フィルタ）」と「どう計算するか（エンジン）」は別の関心事

### ActiveTask（Now）の永続化

- 「今、この瞬間にやっていること」は常に1つ
- `active_task_id`をusersテーブルに保存
- PCで作業中に離席 → スマホで同じタスクが「Now」として表示（コンテキスト同期）

### リソース隔離ルール

複数アプリが`door-fujita.com`ドメインを共有するため、以下のリソースには`Youkan_`プレフィックスを付与:
- Cookie: `Youkan_session_id`（path=/contents/Youkan/）
- LocalStorage/SessionStorage/IndexedDB
- Cache Storage/Broadcast Channel

### リアクティブ伝播パターン

親→子孫カスケード操作は MVVM の各層で責務分離し、楽観的UI＋確定同期＋ロールバックの3段サイクルで実装する。

#### Model層（Backend / PHP）
- `ItemController::cascadeTenantToDescendants($rootId, $newTenantId): string[]`
- `ItemController::cascadeStatusToDescendants($rootId, $updatesSql, $updateParams): string[]`
- `ItemController::cascadeDeleteDescendants($rootId): string[]`
- いずれも `getAllDescendantIds()` を内部利用、トランザクションで保護
- レスポンスに `affectedDescendantIds` / `deletedDescendantIds` を含める

#### Repository層
- `ApiClient.updateItem/archiveItem/trashItem/restoreItem/destroyItem` の戻り値型を `{ success: boolean; affectedDescendantIds?: string[] }` に拡張
- `CloudYoukanRepository` / `YoukanRepository` は API応答をそのまま返す薄いレイヤ

#### ViewModel層
- `useYoukanViewModel`:
  - `refreshItems(ids: string[])`: 部分リフレッシュ（`refreshAll()` を撤廃）
  - `optimisticCascadeTenant(rootId, newTenantId)`: local state を即時更新＋ snapshot 返却
  - `restoreSnapshot(snapshot)`: ロールバック
- `updateItem` 等のサイクル:
  1. snapshot 取得
  2. 楽観的UI更新（親 + 子孫の local state 即時反映）
  3. API呼出
  4. 成功 → `refreshItems(affectedDescendantIds)` で確定同期
  5. 失敗 → `restoreSnapshot` でロールバック

#### View層
- ユーザー操作（パンくずクリック・右クリックメニュー等）から ViewModel メソッドを呼ぶのみ
- 子孫件数の視覚フィードバック（任意）は別タスク

#### メンテナンス則
- `refreshAll()` の追加使用は禁止（部分refreshの徹底）
- `is_project` 条件分岐の追加は禁止（親子関係で常に判断）
- バックエンドの巨大 `update()` メソッド肥大化を避け、ヘルパー抽出を継続

---

### Someday状態の技術仕様（R-028）

#### 型定義
- `JudgmentStatus = 'inbox' | 'waiting' | 'focus' | 'pending' | 'someday' | 'done'`
- 既存遺物 `judgmentStatus?: '...someday...'` を整理し、メイン `status` に統合
- `dueStatus?: 'someday'` は独立軸として別議論（本plan外）

#### キャパシティ計算除外
`QuantityEngine.ts` の集計関数で `someday` 状態のアイテムを集計対象外とする。
ガント・カレンダーの容量計算にも影響を与えない。

#### ビュー別表示制御
- パノラマ: 専用バケット（Pending と Log の間）
- 全体一覧: フィルタースイッチ（デフォルト除外）
- ガント・カレンダー: 強制フィルターアウト（表示オプションなし）
- 詳細モーダル: 状態切替ボタン群に追加

#### UI差別化
- アイコン: 💭（または `Cloud` lucide icon）
- カラー: `purple-400/50`（pendingのamber系、focusのblue/indigo と区別できる色）
- ラベル文言: 「いつかやる（自分で寝かせる）」
- pending ラベル: 「保留（外的要因待ち）」

#### マイグレーション不要
既存アイテムは現 status 値を維持。新値 `'someday'` はオプトインで使用される。

#### メンテナンス則
- 状態を6から7以上に増やさない（短期記憶限界）
- pending と someday の差別化UIを維持
- キャパシティ除外ルールを維持
- ガント・カレンダーでの someday 非表示を維持

---

### getGdbShelf レスポンス構造（R-029で修正）

`CloudYoukanRepository.getGdbShelf()` のレスポンス型は status 数と完全対応するキー構造:
- `active`: status='inbox' or 'focus'
- `todo`: status='todo'  ← R-125で追加
- `preparation`: status='waiting'
- `intent`: status='pending'（レガシー`decision_hold`も含む。R-125）
- `someday`: status='someday'  ← R-029で追加
- `log`: status='done' or 'cancelled'（レガシー`decision_rejected`も含む。R-124）

ViewModel の `refreshGdb` は各 raw state に対して `shelf.<key>` から直接セットする。`activeRaw.filter(status==='someday')` のような派生フィルターは禁止（R-028 の暫定実装を是正）。

---

### todo状態・pending付帯情報の技術仕様（R-125）

#### 型定義・列挙の一元化
- `JudgmentStatus = 'inbox' | 'todo' | 'focus' | 'waiting' | 'pending' | 'someday' | 'done' | 'cancelled'`（8値）
- `logic/decisionResolution.ts` の `Decision` に `'later'` を追加し、`decisionToStatus('later') === 'todo'`。`hold` はフロント・バックエンド（`DecisionController.php`）とも `pending`（`decision_hold` の新規書き込み廃止）
- `STATUS_META.todo` を追加（label「後日着手」、色は既存と被らないもの）。ラベル・色・アイコンは全画面で `STATUS_META` から参照する
- 「状態の一覧」をハードコードしている箇所（バックエンドSQLの `IN (...)`／フロントのフィルタ・バケット分類・状態切替ボタン・右クリックメニュー・フローノード色・ガント/カレンダー表示条件）を全て洗い出して `todo` を追加する。R-124で `cancelled` を追加した際の変更ファイル一覧（`06_変更履歴.md` 2026-08-17 Impact）が洗い出しの起点
- `Item` 型に `pendingCondition?: string | null`、`reviewDate?: string | null` を追加。Repository層で `pending_condition`/`review_date` と相互変換

#### DBマイグレーション
- `items` に `pending_condition TEXT NULL`、`review_date TEXT NULL` を追加（既存の `db.php` のカラム追加方式に従う。存在チェック付きの `ALTER TABLE ADD COLUMN`）
- データ移行: `UPDATE items SET status='pending' WHERE status='decision_hold'`（同マイグレーション内で1回実行）

#### キャパシティ・表示
- `QuantityEngine` の「今日のキャパ」集計は `todo` を除外（`focus`のみが今日の負荷）。日付別集計（ガント日付ヘッダー合計・カレンダー）は `due_date`/`prep_date` があれば `todo` も含む
- ガント・フロー・カレンダー・全体一覧: `todo` は既定で表示（`inbox` と同じ扱い）
- `pending` の「再確認」判定は `reviewDate <= 今日(ローカル日付)` の純粋関数を1箇所（`statusUtils.ts` 等）に置き、登録と集中・状況把握の並び替えと「再確認」バッジ表示の両方がそれを使う

#### メンテナンス則
- R-028の「状態を7以上に増やさない」は撤回し、上限は「本道4状態（inbox/todo/focus/done）＋例外4状態（waiting/pending/someday/cancelled）」の8値とする。これ以上の状態追加は原則禁止し、判断軸（重要度・確定状態など）は属性として持つ
- 「判断状態」と「作業状態」を同じstatus軸に混ぜない（`decision_hold`/`confirmed` を状態にしない）

---

### 階層子孫取得の3軸評価ルール（R-029で修正）

プロジェクトフォーカス時の子孫取得 SQL は **id / project_id / parent_id の3軸**で初期行を評価する。

`backend/BaseController.php::getProjectDescendantIds()` の WITH RECURSIVE 初期行:
- 旧: `WHERE id = ? OR project_id = ?` （2軸、parent_id のみで紐づく子孫が漏れる）
- 新: `WHERE id = ? OR project_id = ? OR parent_id = ?` （3軸、3階層以上対応）

これにより、佐礼谷 > 2F組子製作（孫プロジェクト）> 製作（タスク）のような階層で、孫プロジェクトをフォーカスした時に子タスクが正しく取得される。

DFS 版 `ItemController::getAllDescendantIds()` は既に3軸（OR）で評価済み。

---

### 階層ラッパーの型分離方針（R-030）

`logic/hierarchy.ts` の `HierarchicalWrapper` は discriminated union として定義する。

#### 型定義
```ts
export type HierarchicalWrapper =
  | { id: string; type: 'item'; item: Item; project: Item | null; depth: number }
  | { id: string; type: 'header'; projectId: string; projectTitle: string; project: Item; depth: number };
```

#### 設計原則
- ヘッダー型は `item: Item` を**持たない**（型レベルで取り出しを禁止）
- ヘッダーが必要とする情報は `projectId` / `projectTitle` / `project` で持つ
- React key には `wrapper.id` を使う（`wrapper.item.id` 経由のアクセスは禁止、ヘッダーでは型エラー）
- `'header-${proj.id}'` 形式の `wrapper.id` で React key 衝突回避
- 階層 UI 拡張時はラッパー型に新 variant を追加（例: `'tenant_header'`）

#### 禁止事項
- `virtual-header-` プレフィックスの**生成も使用も禁止**
- 「item.id を加工して別の意味を持たせる」パターン禁止（意味の違う ID は別フィールドで持つ）
- レイヤー境界（API・URL・ナビゲーション）の ID は**常に Item の実ID**

#### 廃止された慣行
過去の R-Earlier では、ヘッダー Wrapper に `item: { ...proj, id: 'virtual-header-${proj.id}' }` という偽装IDを持たせていたが、
- レイヤー境界での剥がし忘れがフロー画面のコア機能崩壊（プロジェクトフィルタ・新規作成・依存関係）を引き起こした
- 12箇所で `.replace('virtual-header-', '')` を直書きする散在コードが生まれた

これらの問題を根本撤廃するため、R-030 で型分離方針を採用。

---

### ForAI機能・読み上げ機能（R-031）

#### 共通ユーティリティ

| ファイル | 内容 |
|:--|:--|
| `src/lib/clipboard.ts` | `copyToClipboard(text)`, `downloadText(text, filename)`, `downloadMarkdown(md, filename)` |
| `src/hooks/useMediaQuery.ts` | `useMediaQuery(query)`、プリセット `useIsMobile()` |
| `src/features/core/youkan/logic/speechSynthesis.ts` | `YoukanSpeechSynthesis` ラッパークラス（Web Speech API 直接ラッパー） |
| `src/features/core/youkan/hooks/useSpeechSynthesis.ts` | React hook（再生中状態・seek・pause/resume） |
| `src/features/core/youkan/logic/forAiExporter.ts` | アイテム配列 → markdown 文字列変換 pure function |
| `src/features/core/youkan/logic/perspectiveLabel.ts` | filterMode + joinedTenants → 立場ラベル文字列 |

#### Web Speech API

- ブラウザ標準 `SpeechSynthesisUtterance` 使用、無料
- 言語: `ja-JP`
- iOS Safari ではユーザー操作起点でしか `speak()` できないため、ボタンクリック起点で発火
- `isSupported()` でフォールバック表示（Chrome / Safari は確実に対応）

#### 設計原則

- ForAI 出力は markdown 1本に集約。新形式は別関数（`forAiExporter.toJson()` 等）で
- Speech 状態は単一 hook（`useSpeechSynthesis`）で管理。Context 化は需要次第
- ボタン配置はヘッダー（PC）+ メニュー or フローティング（スマホ）の2系統
- Someday は ForAI/読み上げ対象外（夢リスト哲学、R-028 と整合）
- フィルタ変更時は Speech を自動 stop（リアクティブ）

#### 再利用する既存資産

| 資産 | 用途 |
|:--|:--|
| `useYoukanViewModel` | アイテム取得（`viewmodels/useYoukanViewModel.ts`） |
| `useFilter` | filterMode 取得（`contexts/FilterContext.tsx`） |
| `useAuth` | joinedTenants 取得（`features/core/auth/providers/AuthProvider.tsx`） |
| `compareFocusItems` | 各グループ内ソート（`logic/sorting.ts`）|
| `buildHierarchicalList` | ForAI のプロジェクト構造生成（`logic/hierarchy.ts`） |

#### メンテナンス則

- `refreshAll()` の追加使用は禁止（R-027 の部分 refresh 方針に従う）
- ForAI 出力は markdown 1本に集約。新形式追加は別関数で
- Speech は単一 hook で状態管理（Context は使わず単純化）
- Someday アイテムは ForAI/読み上げ対象外（夢リスト哲学）
- ボタン配置はヘッダー集約 → スマホはメニュー or フローティングのどちらかに分配（ヘッダーには置かない）

---

### スマホメニュー設計原則（R-033）

スマホ画面（<768px）でのメニュー・選択UI設計の恒久ルール。

#### 6つの原則

1. **スマホで4選択肢以上はボトムシート**: ドロップダウン・ポップオーバーは使わない
2. **ヘッダー右はスマホで2アイコン以下**: 超過分は MenuDrawer / フローティング / ボトムシートへ集約
3. **ボトムシート z=50**: MenuDrawer (z=100) より下、モーダル/フローティングと同階級
4. **`pb-[env(safe-area-inset-bottom)]` 必須**: iPhone ホームインジケーター対応
5. **`touch-action: none` で背景スクロール抑制**: iOS Safari の Pull-to-Refresh 暴発防止
6. **閉じ方3つ**: ✕ボタン / 背景タップ / Esc（任意で下スワイプ）

#### 共通コンポーネント

- `JWCADTategu.Web/src/features/core/youkan/components/Common/MobileBottomSheet.tsx`
- Props: `{ isOpen, onClose, title?, children }`
- framer-motion で slide-up アニメーション

#### useIsMobile() 分岐パターン

PC/スマホ両対応コンポーネントは `useIsMobile()` で分岐:
```tsx
{isMobile ? (
  <MobileBottomSheet isOpen={open} onClose={close}>{items}</MobileBottomSheet>
) : (
  <DropdownMenu>{items}</DropdownMenu>
)}
```

#### 棄却された案

- SimpleModal の破壊的変更（既存呼出を壊すため新規追加で済ます）
- スマホ全般 safe-area 見直し（スコープ膨張のためボトムシート内のみ対応）
- WCAG 完全準拠（別タスク）

#### 設計判断の根拠

secretary/notes/2026-05-11-会議-スマホUI改善.md

---

### 要判断キュー「捌く」の技術仕様（R-127）

#### 純粋関数（1箇所）
- `logic/reviewQueue.ts`: `buildReviewQueue(items: Item[], today: string): Item[]`。対象判定と並び順は `02_機能仕様.md` F-52 の定義に厳密に従う。有効締切は `flowAutoPlace.ts` の `getEffectiveDeadline` を再利用、再確認判定は R-125 の `isReviewDue` を再利用する。ヘッダーバッジ件数・誘導カード・ReviewSweep・全体一覧フィルタ・（番頭側の実装参照）はすべてこの関数の結果を使う
- `logic/declinedStats.ts`（または `statusUtils.ts` 内）: `countDeclinedThisWeek(items, today)` = `status==='cancelled' && status_updated_at >= 今週月曜0:00`。ローカル日付基準

#### データ供給
- 全アイテムは既に ViewModel（`useYoukanViewModel`）にロード済み。新規APIは作らない。件数バッジは `unifiedAllItems`（R-125）相当から算出
- 「後日」: `resolveDecision(id,'later')` に加えて `updateItem(id,{prep_date})`（既存の `updates` 引数で同時送信できるならそれを使う）
- 「断った」: `resolveDecision(id,'no')` ＋ `updateItem(id,{meta:{...meta, declined:true}})`

#### コンポーネント
- `components/Review/ReviewSweep.tsx`（新規、≈150行以内を目安）: 右下オーバーレイ、キーボード1/2/3/Esc、3件で完了ビュー
- `components/Review/ReviewPrompt.tsx`（新規）: 1日1回の誘導カード。`localStorage['youkan_review_prompt_dismissed']`
- ヘッダーバッジ: `YoukanHeader.tsx` の既存ナビ項目に件数を渡すだけ
- 判断の言葉: `PersonalSettingsScreen` にテキストエリア。保存は `users.preferences` を読み書きしている既存の設定保存経路（`getCapacityConfig`/`saveCapacityConfig` と同じRepositoryメソッド群）に `judgmentPhrases` を追加する。バックエンドは `preferences` JSON のキー追加のみで新カラム不要

#### メンテナンス則
- 判断先の状態は必ず `decisionToStatus` を通す（R-124）
- キューの対象・並びを画面ごとに再実装しない（`buildReviewQueue` 一本）
- 「古い期限超過の自動処分・一括処分」は実装しない（発注者指示）
- 文言に評価語（褒め・励まし・叱責）を入れない

---

### 今週の残量1行と登録時の一言の技術仕様（R-128）

#### 式は1箇所ずつ
- フロント: `logic/weekLoad.ts` `calcWeekLoad(items, capacityConfig, today)` → `{capacityMinutes, needMinutes, shortfallMinutes, weekEnd, overCandidates}`。日次キャパは既存 `getDailyCapacity(date, capacityConfig)`、有効締切は `getEffectiveDeadline`。ヘッダー1行・Toast はこの関数の結果だけを使う
- バックエンド: `QuantityService.php` に `calcWeekLoad($userId, $today)` を追加（同じ式・同じキー名を snake_case で）。`ItemController`（POST/PATCH）・`IntegrationController`（inbox）・新規 `GET /quantity/week` がこれを呼ぶ。日次キャパは既存 `getDailyCapacity` と `users.preferences`/`capacity_profile` の曜日パターン・例外を反映（フロントの `getDailyCapacity` と同じ解釈になるようテストで突き合わせる）
- 両者の式が食い違わないよう、同一フィクスチャ（アイテム数件＋キャパ設定）で TS と PHP のテストが同じ数値を返すことを確認する

#### レスポンス同梱
- `POST /items`・`POST /integrations/inbox`・`PATCH /items/{id}`（`due_date`/`prep_date`/`estimated_minutes` を含む時のみ）のレスポンスに `week_load` を追加。既存キーは変えない（後方互換）

#### メンテナンス則
- 逆算配分（`allocateBackwardsCore`）を週負荷に混ぜない（単純合計で定義。理由: PHP側と一致させるため）
- 安全係数は掛けない（R-129で `safetyFactor` を導入しても週負荷は素の目安のまま）
- 評価語禁止。不足時以外は色を付けない

---

### 最遅着手日トークンの技術仕様（R-129）

- `logic/latestStart.ts`: `getLatestStart(item, ctx: QuantityContext, safetyFactor: number, today: string): { date: string | null; isLate: boolean; reason: 'ok'|'no-estimate'|'not-applicable' }`。内部は既存 `QuantityEngine.calculateAllocationDetails(endDate, Math.round(estimatedMinutes*safetyFactor), ctx, tenantId)` の `steps[0].date`。有効締切は `getEffectiveDeadline`。**QuantityEngine 本体は変更しない**
- `CapacityConfig.safetyFactor?: number`（`types.ts`）。未設定は1.5。保存は既存 `saveCapacityConfig` 経路（`users.preferences`）。バックエンド変更なし
- 表示3箇所は `getLatestStart` の戻り値だけを使い、各画面で計算式を再実装しない。全体一覧・ガント一覧はアイテム数が多いので `useMemo` でアイテム配列・ctx・係数が変わった時だけ再計算する
- 飽和ガード: 全体一覧・ガント一覧で `isLate` の件数を数え、赤字は先頭10件（表示順）まで。11件目以降は灰字のまま
- メンテナンス則: 係数は最遅着手日以外に掛けない／期限超過分に出さない／着手日はDBに保存しない（計算値）

### 番頭連携API の技術仕様（R-140）

- `backend/ReviewQueueService.php`（新規）: `ReviewQueueService::build(array $items, string $today): array`。対象判定・並び順は `logic/reviewQueue.ts` の `buildReviewQueue` と**同一定義**（`02_機能仕様.md` F-52）。有効締切＝`due_date`/`prep_date` の早い方
- **同一性の担保**: 共有フィクスチャ `backend/tests/fixtures/review_queue_cases.json`（入力アイテム配列＋today＋期待ID順）を1つ置き、TS 側 `logic/__tests__/reviewQueue.fixture.test.ts` と PHP 側 `backend/tests/test_review_queue_service.php` の両方が同じファイルを読んで検証する。定義を変えるときはフィクスチャを先に変える。フィクスチャの `estimated_minutes` は DB 行と同じく `null`＝未入力（並びの最後）・`0` は 0 として扱う（items.estimated_minutes は DEFAULT 0 のため実運用では 0 が並びの先頭に来る）。有効締切の計算は `QuantityService::getEffectiveDeadlineFromItem`（R-140 で public static 化）を共用する
- `IntegrationController::digest()`: `BaseController::authenticate()`（JWT／api_token 両対応）→ 呼び出しユーザーの `scope=aggregated` 相当のアイテム集合を取得 → `ReviewQueueService::build` ／ 既存 `QuantityService::calcWeekLoad` ／ 未連絡遅延（F-55 の対象条件＋`meta.contacted_at` 無し、案件別集計）／ 断った件数 ／ focus 一覧を組み立てて返す。アイテム集合の取得は `ItemController` の aggregated 取得ロジックを `BaseController::fetchAggregatedItems()` に切り出して再利用し、SQL を二重に書かない。focus の並びは `TodayController::compareFocusOrder()`（static 化）を共用。`IntegrationController` は `BaseController` を継承し、`/integrations/inbox` の独自 api_token 認証も `authenticate()` に統一
- `UserController` に `/user/api-tokens` の GET/POST/DELETE。トークン生成は `bin2hex(random_bytes(32))`。DELETE は `user_id = 本人` を条件に含める
- フロント: `PersonalSettingsScreen` に `ApiTokenSection`（`repositories` 層に `listApiTokens/createApiToken/revokeApiToken`）。ViewModel は `useApiTokens` フック
- テスト: PHP（ReviewQueueService フィクスチャ一致・digest の他ユーザー非混入・トークン発行→利用→失効→401）、Vitest（フィクスチャ一致・ApiTokenSection の発行/一覧/失効表示）

