# 引き継ぎ資料（2026-08-18〜19 R-139〜R-150 セッション）

## このセッションで何をしたか

前セッション引継書（`2026-08-18-R125-R135-new-youkan-session.md`）と `docs/requests.md` から再開。指揮AIはコードを触らず、実装・調査・マージ・デプロイはすべてサブエージェント（worktree 隔離、デプロイは共有チェックアウトで単独）。Codex にはレビュー1本を委譲。

## 本番反映済み（すべて 2026-08-19 デプロイ）

| R | 内容 | マージ |
|:--|:--|:--|
| R-139 | `POST /items/{id}/destroy` 500（`destroy_permanent` が 2026-02 の archive/trash 導入時から未定義） | `154265e` |
| R-140 | 番頭連携 `GET /integrations/digest`（要判断キューを PHP `ReviewQueueService` で実装、TS と共有フィクスチャで一致テスト）＋個人設定「外部連携トークン」発行/一覧/失効 | `cb60d79` |
| R-141 | `POST /integrations/inbox` の `created_by`/`tenant_id` 未設定（番頭登録が本人に見えない） | `3ac647b` |
| R-142 | フロー・状況把握から開いた詳細モーダルが「PRIVATE／Inbox（未分類）」（`joinedTenants`/`allProjects` の props 渡し漏れ。データは正常） | `07ea6ab` |
| R-143 | 右クリック「後日着手 (h)」を全メニューで「今日やる」直下に | `3360b22` |
| R-145 | ガント時間軸の 24h はみ出しブロックが幅1分（F-40「右端揃え」の実装取り違え） | `8908ca7` |
| R-146 | ガント「プロジェクト別」表示でも日付ヘッダーの CapacityBar＋合計時間（絞り込み中は案件合計） | `7fad73f` |
| R-147 | 週負荷の分子/分母の母集団を F-53 に明文化、超過分パネルの編集欄を有効締切側（納期／マイ期限）に | `85e0a5e` |
| R-148 | 「集計値の隣に母集団1語」ルール（`03_画面設計.md` §0）を Codex レビュー #1〜#7 に一括適用（面積が無い所は `title`） | `85e0a5e` |
| R-150 | ガント ホバー行の強調（見出し列＋時間軸、inset 下線・行高不変） | `b3de7f9` |

## 仕様確定・実装予定（未着手）
- **R-144 予実管理 v1**（`02_機能仕様.md` F-57、`03` §21、`05` 末尾）: 押し出し見込トークン `予定通りなら M/d (+N)`（前工程の有効締切起点・直後1工程のみ・計算値）＋締切変更時の3択カード。発注者指示「実装はのちほど」。会議録 `docs/kaigi/2026-08-19-予実管理.md`（v2: `meta.planned_deadline` 初回保存＋「予定→今」＋案件行「余裕 N日」、v3: R-128 の「今日」版）

## 未対応（`docs/requests.md`）
- R-149候補: 時間丸めの統一（`45m` vs `0.8h`）
- データ不整合の棚卸し（tenant_id 空×project_id あり 33件 等。板２とは無関係）
- R-126 残論点、Beaver 連携、マネージャーAI 連携（活動履歴API は R-140 で見送り）ほか従来分

## 次の一手（次セッションの指揮AIへ）
1. 発注者から R-144（予実管理 v1）の着手指示が出たら、F-57 の仕様どおり実装Agentを1本起動（`logic/pushForecast.ts` 純関数→3箇所表示→3択カード。目安・キャパ規則は R-129 の `getLatestStart` を再利用）
2. R-148 の使い勝手（tooltip で足りているか、フロー帯の案件名省略「大規…」が気にならないか）を発注者に確認。文字表示へ変えるなら面積の再検討
3. R-149候補（時間丸め）は R-148 の運用を見てから
4. 番頭側（frontPC）が digest に切り替えたら `docs/handover/2026-08-18-番頭への依頼事項.md` §1・§2・§6 を「digest 経由」に書き換える
5. 掃除: `.claude/worktrees/` に `R-101`・`agent-a31a20361bb197614`・`feature-R109-flow-date-grouping` の孤立ディレクトリ（`.git` 無し、`git worktree list` 未登録）が残存（削除時に権限で失敗。手動削除可）。ローカルブランチが100本（`worktree-agent-*` の残骸多数、いずれもマージ済み）— `git branch --merged master | grep worktree-agent | xargs git branch -d` で一掃可

## 本番の状態（2026-08-19 深夜時点）
- 稼働バンドル `index-BMC7Fzwb.js`（R-150 まで）
- 番頭用の既存トークン `AiSecretary relay`（3/25 発行）は個人設定の一覧に表示される。検証用トークンはすべて失効済み
- 本番 DB の既知の不整合（`docs/requests.md`「データ不整合の棚卸し」）は未処置。板２上下加工は `focus`・藤田建具店／石鎚山で正常

## 参照
- Codex レビュー: `docs/reference/review/2026-08-19-同形異義UIレビュー_codex.md`
- 番頭への依頼事項（digest 追記済み）: `docs/handover/2026-08-18-番頭への依頼事項.md`
- R-148 前後スクリーンショット: `docs/screenshots/R-148/`

## 運用上の教訓
- 共有チェックアウトでデプロイAgentが作業中に指揮AIが docs を編集すると、Agent 側で `git stash` 退避が必要になった（今回2回）。**デプロイAgent稼働中は docs も編集しない**（メモを scratchpad に書いておき、完了後に台帳へ）
- 調査Agent（読み取り専用・本番DBコピー）→ R番号採番→修正Agent、の流れは速い。板２（データ正常・表示バグ）とガント極小バー（仕様と実装の取り違え）はどちらも「見た目の症状」と「原因」が別物だった
- 「テスト名は仕様、アサーションは実装」の取り違えテストが存在した（R-145）。テストを直すときは「実装に合わせる」のではなく「仕様に合わせる」旨をコメントに残す運用を Agent に指示した
- 本番検証で「はみ出し」など日付依存の状態は翌日に消える。検証できなかった項目はユニットテストで担保した旨を台帳に明記する
