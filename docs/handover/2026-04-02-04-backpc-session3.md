# 引き継ぎ書: backPC 第3回セッション (2026-04-02〜04-04)

**作成日**: 2026-04-04
**作成者**: Claude Code（指揮AI）

---

## 今日やったこと

### SdDD環境セットアップ (2026-04-02)
- `CLAUDE.md` 新規作成（方式A: sddd/rules.md 参照方式）
- `docs/sddd/rules.md` にSdDDルール本体を配置
- `docs/` 大規模整理: 旧ファイル20+を `99_Archive/`、参照資料を `reference/` に移動
- `docs/spec/` にSdDD仕様書6ファイルのみに整理

### 機能実装 (R-004〜R-008)
- R-004: ブラウザタブタイトル「JBWOS」→「Youkan」
- R-005: 全体一覧を「全て」フィルタで開く
- R-006: `completed_at` カラム追加 + カレンダー振り返り表示
- R-007: ガントチャート「今月を表示」中央スクロール
- R-008: View名変更（全体一覧→状況把握、全体一覧2→全体一覧）

### バグ修正 (R-009〜R-013)
- R-009: 会社プロジェクト表示の無限再帰（循環参照防止 — hierarchy.ts共通化）
- R-010: 総会プロジェクト表示バグ（showGroups時のプロジェクト名付加抑止）
- R-011: ガント一覧モード切替が動作しない（VolumeCalendarScreenのshowGroups接続）
- R-012: ガント一覧モード改善（フラットリスト化・ソート・プロジェクト名末尾表示）
- R-013: [総会]バグ根本修正（本番DB不正データ削除 + CalendarControllerパラメータ修正 + 防御コード）

### UI調整
- 全体一覧の列幅ボトルネック修正（columnWidth倍率削減 + QuickInput追従）
- 見出し/プロジェクト名にタイトル制限適用
- 見出し罫線を上に移動 + 隙間2px統一
- ガントチャート行高さ縮小（40px→28px）
- 「今月を表示」の垂直スクロール修正（データベース判定に変更）

### フローView (R-014) — 全Phase完了
- **Phase 1**: item_dependenciesテーブル + DependencyController CRUD API + Dependency型定義
- **Phase 2**: @xyflow/react でFlowScreen実装、カスタムノード、未配置リスト、ドラッグ&ドロップ、エッジ接続
- **Phase 3**: プロジェクトグループ化（8色パレット）、キーボードショートカット10種、重ねてリンク
- **Phase 4**: ガント連動（SVG矢印線、日程制約チェック、カスケード調整）
- **Phase 5**: 自動配置（納期順チェーン＋座標自動計算）、新規アイテム自動チェーン追加、エッジ上ドロップ挿入
- **レビュー修正**: hasCycleテナント分離、cascadeDependencyDates動作修正、パフォーマンス改善
- **UX改善**: 当たり判定縮小（80→40px）、ドラッグプレビュー、接続トースト+取り消し
- **追加機能**: プロジェクト集約配置、エッジ右クリック削除、完了アイテムグレー表示
- **バグ修正**: 自動配置409/400エラー防止、フロー画面データ取得（Promise.allSettled化）、プロジェクト別グルーピング修正

### 全体一覧改善
- 依存関係トポロジカルソート（Kahn's algorithm）

---

## 現在の状態

### ブランチ
- `master`: 全変更がデプロイ済み
- `feature/flow-view`: masterにマージ済み（削除可能）

### requests.md
```
- 全体一覧で依存関係順に表示（完了済み）
```

### 未解決・注意事項
- フローチャートの「自動配置」は基本動作するが、大量アイテムでの動作確認が不十分
- チャンクサイズ警告（1,369KB）が出ている → 将来的にコード分割を検討
- vite.svg の404エラーが本番で出ている（軽微）

---

## 本番環境の状態

| パス | 状態 |
|------|------|
| `contents/Youkan/` | 本番稼働中（全変更反映済み） |
| `contents/Youkan/backend/jbwos.sqlite` | item_dependencies テーブル作成済み |

---

## 参照すべきファイル

| 目的 | ファイル |
|------|---------|
| 仕様書目次 | `docs/SPEC.md` |
| フローView仕様 | `docs/spec/03_画面設計.md §7` |
| 依存関係テーブル | `docs/spec/04_データ設計.md §3.5` |
| ガント一覧仕様 | `docs/spec/03_画面設計.md §5.3` |
| requests | `docs/requests.md` |
| 対応履歴 | `docs/request_log.md` |
| SdDDルール | `docs/sddd/rules.md` |
| 引き継ぎ（前回） | `docs/handover/2026-03-24-backpc-session1.md` |
