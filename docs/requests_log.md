# 要望対応履歴 (requests_log.md)

運用開始日: 2026-03-24

| 日付 | ID | 内容 | 対応状況 | 反映先spec |
|:--|:--|:--|:--|:--|
| 2026-03-24 | R-001 | ドラッグ&ドロップの範囲をカード全体に拡大 | 完了（2026-03-24デプロイ済み） | 02_機能仕様.md |
| 2026-03-24 | R-002 | 総会プロジェクトのバグ（projectTitle混入・project_id NULL・real_project_title JOIN不足） | 完了（2026-04-01デプロイ済み。showGroups時のプロジェクト名付加抑止+proj.title修正） | 04_データ設計.md |
| 2026-03-24 | R-003 | 登録と集中画面の右クリックメニューをNewspaper Boardと統一 | 完了（2026-03-24デプロイ済み） | 03_画面設計.md |
| 2026-03-31 | R-004 | ブラウザタブのタイトルを「JBWOS」→「Youkan」に変更 | 完了（2026-03-31デプロイ済み） | 03_画面設計.md §0 |
| 2026-03-31 | R-005 | 全体一覧2を「全て」フィルタで開く | 完了（2026-03-31デプロイ済み） | 03_画面設計.md §4.0 |
| 2026-03-31 | R-006 | 各アイテムに「完了にした日」を記録+カレンダー振り返り表示 | 完了（2026-03-31デプロイ済み） | 04_データ設計.md §3.1, 02_機能仕様.md §F-02/F-06 |
| 2026-03-31 | R-007 | ガントチャート「今月を表示」で今月が中央に表示（上下左右スクロール） | 完了（2026-03-31デプロイ済み） | 03_画面設計.md §3.3 |
| 2026-03-31 | R-008 | View名変更: 全体一覧→状況把握、全体一覧2→全体一覧 | 完了（2026-03-31デプロイ済み） | 03_画面設計.md §1/§3/§4 |
| 2026-04-01 | R-009 | 会社プロジェクト表示で無限再帰（循環参照防止なし） | 完了（2026-04-01デプロイ済み） | — |
| 2026-04-01 | R-010 | 総会プロジェクト表示バグ（ガントチャートでプロジェクト名が重複付加） | 完了（2026-04-01デプロイ済み） | — |
| 2026-04-01 | R-011 | ガント一覧モード切替が動作しない | 完了（2026-04-02デプロイ済み） | 03_画面設計.md §5.2 |
| 2026-04-01 | R-012 | ガント一覧モード改善（フラットリスト化・ソート・プロジェクト名表示） | 完了（2026-04-02デプロイ済み） | 03_画面設計.md §5.2 |
| 2026-04-02 | R-013 | [総会]バグ根本修正（本番DB不正データ削除+CalendarControllerパラメータ修正+防御コード） | 完了（2026-04-02デプロイ済み） | — |
| 2026-04-02 | R-014 | フローView（5つ目のView）: タスク依存関係のフローチャート可視化・編集 | 完了（2026-04-04デプロイ済み。Phase5プロジェクト単位表示はR-016で対応） | 03_画面設計.md §7, 04_データ設計.md §3.5/§5.1 |
| 2026-04-08 | R-015 | 全体一覧で依存関係順にソート表示 | 完了（2026-04-04デプロイ済み） | — |
| 2026-04-08 | R-016 | フローチャートのプロジェクト単位表示（選択画面・ヘッダー・導線・フロー内タスク管理） | 完了（2026-04-08デプロイ済み） | 03_画面設計.md §7.6/§7.7, 02_機能仕様.md §F-16 |
| 2026-04-08 | R-017 | Newspaper Viewに目安時間表示+インライン編集 | 完了（2026-04-08デプロイ済み） | 03_画面設計.md §4.2, 02_機能仕様.md §F-17 |
| 2026-04-08 | R-018 | マネージャーAI連携用API（晴樹さん行動分析） | 未着手（要検討） | — |
| 2026-04-10 | R-019 | フローチャート画面UI改善（ノード内の隙間2px化・未配置リスト余白削減・ノード/未配置リスト右クリックメニュー・DELキーで完全削除） | 完了（2026-04-10デプロイ済み） | 03_画面設計.md §7 |
| 2026-04-10 | R-020 | ガントチャート目安時間インライン編集（左リストの時間バッジ＋クリック編集） | 完了（2026-04-10デプロイ済み） | 03_画面設計.md §5 |
| 2026-04-10 | R-021 | ガントチャートでマイ期限が未定のセルクリックでprep_date設定 | 完了（2026-04-10デプロイ済み） | 03_画面設計.md §5 |
| 2026-04-10 | R-022 | ガントチャート依存関係矢印の可視性修正（z-index引き上げ＋色濃化）| 完了（2026-04-10デプロイ済み） | — |
| 2026-04-10 | R-023 | ガントチャート右クリックメニュー統合（タイトル列・青バー対応）+ 全画面でDel削除統一（メニュー表示中） | 完了（2026-04-10デプロイ済み） | 03_画面設計.md §5/§7 |
| 2026-04-10 | R-024 | 新規登録欄ショートカット変更（Shift+Enter→今日やる、Alt+Enter→登録+詳細表示、Alt+Enter空欄→前回アイテム詳細表示） | 完了（2026-04-10デプロイ済み） | 03_画面設計.md §2 |
| 2026-04-10 | R-025 | フローチャートプロジェクト選択画面のフィルター修正（会社/個人/テナント別フィルターが機能するよう FilterContext を接続） | 完了（2026-04-10デプロイ済み） | 03_画面設計.md §7.6 |
| 2026-04-22 | R-026 | 命名統一とコード基盤整理（GlobalBoard/YoukanBoard→PanoramaBoard、NewspaperBoard→OverviewBoard、viewMode値統一、filterUtils新設、ViewModeContext新設、localStorage マイグレーション） | 対応中（refactor/naming-unification ブランチ） | 03_画面設計.md, 05_技術設計.md, 06_変更履歴.md |
| 2026-04-27 | R-027 | 親→子孫カスケード機能（MVVM準拠）: テナント/プロジェクト変更時に子孫 tenant_id を連動、archive/trash/restore/delete を is_project 不問でカスケード、移動先テナント権限チェック（joinedTenants 外で 403）、API応答に affectedDescendantIds を追加、ViewModel に楽観的UI更新＋部分リフレッシュ＋ロールバックの3段サイクルを実装 | 対応中（feature/R-027-cascade ブランチ） | 02_機能仕様.md, 05_技術設計.md, 06_変更履歴.md |
| 2026-05-04 | R-028 | 「いつかやる」（someday）状態追加: JudgmentStatus を5値→6値に拡張、キャパシティ計算から除外（夢リスト）、パノラマに専用バケット追加、全体一覧にフィルタースイッチ、ガント・カレンダーで非表示、pending との差別化UI（⏸ amber系 vs 💭 purple系）、既存遺物 judgmentStatus.someday をメインstatusに統合 | 対応中（feature/R-028-someday ブランチ） | 02_機能仕様.md, 03_画面設計.md, 05_技術設計.md, 06_変更履歴.md |
| 2026-05-04 | R-029 | 3バグ複合修正: (1)Stream モードでプロジェクトアイテム（is_project=1）を非表示、(2)getGdbShelf レスポンスに someday キー追加（パノラマ Someday バケット表示修復）、(3)getProjectDescendantIds の WITH RECURSIVE 初期行に parent_id チェック追加（3階層以上のプロジェクトフォーカスで子孫取得） | 対応中（feature/R-029-bugfix ブランチ） | 03_画面設計.md, 05_技術設計.md, 06_変更履歴.md |
| 2026-05-05 | R-030 | 階層ラッパー型分離・virtual-header- 概念の根本撤廃: HierarchicalWrapper を discriminated union 化（item/header の型分離）、virtual-header- プレフィックス生成と12箇所の .replace 散在コードを全廃、OverviewItem の onNavigateToFlow バグが型分離の副次効果で自動解消 | 対応中（feature/R-030-hierarchy-types ブランチ） | 05_技術設計.md, 06_変更履歴.md |
| 2026-05-07 | R-031 | 目玉機能復活 — ForAI機能＋タスク読み上げ機能: F-19 ForAI機能（現フィルタ表示中アイテムを markdown 形式に変換、クリップボードコピー/md ダウンロード）、F-20 タスク読み上げ機能（Web Speech API、優先順位順、歌詞型スクロール、一時停止/再生/10秒戻る）。共通ユーティリティ6種（clipboard, useMediaQuery, speechSynthesis, useSpeechSynthesis, forAiExporter, perspectiveLabel）を新規追加 | 対応中（feature/R-031-foray-and-tts ブランチ） | 02_機能仕様.md, 03_画面設計.md, 05_技術設計.md, 06_変更履歴.md |
| 2026-05-11 | R-033 | スマホUI改善＋ボトムシート基盤: 背景①スマホでフィルター（全て/個人/会社/テナント）が非表示、背景②詳細モーダルの「その他」メニューがスマホ画面外にはみ出す。対応: MobileBottomSheet 共通コンポーネント新設（framer-motion slide-up, safe-area対応, touch-action抑制）＋ F-21 スマホフィルター（ヘッダー右 Filter アイコン+ボトムシート）＋ F-22 スマホ用その他メニュー（DecisionDetailModal でスマホ時ボトムシート化）＋スマホヘッダー右側整理（ForAi/Speech を MenuDrawer/フローティングへ集約）。スマホメニュー設計原則6ルール恒久化。 | 対応中（feature/R-033-mobile-bottom-sheet ブランチ） | 02_機能仕様.md, 03_画面設計.md, 05_技術設計.md, 06_変更履歴.md |
| 2026-06-02 | R-034 | Google カレンダー連携（4 Phase 段階）: 議事録 `secretary/notes/2026-06-02-会議-Googleカレンダー連携.md`。Phase 1=量感カレンダー各セル下端に進捗棒グラフ追加（単色、100%超で赤、完了/未完了で濃淡）。Phase 2=primary Google Calendar 連携（読み取り `calendar.readonly`、AES-256-GCM トークン保管）。Phase 3=共有カレンダー＋テナント割当。Phase 4=番頭AIへ external_events 渡し。タイトル非保存・15分キャッシュ・60秒クールダウン手動更新ボタン | Phase 1 実装済み（実機検証待ち）、Phase 2 仕様詰め中（feature/R-034-phase2-spec ブランチ） | 02_機能仕様.md, 03_画面設計.md |
| 2026-06-02 | R-034-P2 | R-034 Phase 2 仕様変更: グリッドビューに Google カレンダー予定をテキスト表示（`📅 HH:MM タイトル`）。元会議結論の「タイトル非保存」を撤回し**タイトル・場所を DB 保存 OK** に変更（Youkan は晴樹さん専用環境のため本人判断で許容）。OAuth は unverified app のまま、認証起点は**設定画面の Google カレンダー連携セクション**に確定。新規テーブル 2 つ（`user_google_oauth`, `external_events_cache`）、新規 API 4 件＋既存 `/calendar/grid` に `external_events` フィールド追加。設定画面（§12）新設 | 仕様詰め中（feature/R-034-phase2-spec ブランチ） | 02_機能仕様.md, 03_画面設計.md §5.6.5/§12, 04_データ設計.md §3.6/§3.7/§5.1 |
| 2026-06-02 | R-035 | 完了アイテム表示統一: 全画面（パノラマ/全体一覧/ガント/カレンダー/登録と集中/詳細モーダル/読み上げ）で `done` アイテムを `slate-400` + 取り消し線で統一表示。量感計算には含めて棒グラフ進捗化（完了部分=淡色、未完了部分=濃色）。R-034 Phase 1 と一体実装 | 着手中（feature/R-035-completed-style ブランチ予定） | 02_機能仕様.md, 03_画面設計.md |
| 2026-06-02 | R-036 | ガント完了スイッチバグ修正: `CalendarHeader` の「完了を表示」スイッチを押しても完了済(done)アイテムの表示・非表示が切り替わらない問題。期待: on=表示, off=非表示、デフォルト=on、セッションスコープ（FilterContext と同様） | 着手中（feature/R-036-gantt-completed-toggle ブランチ予定） | 03_画面設計.md §5.4 |
| 2026-06-04 | R-037 | 詳細モーダル タイトル空欄編集: タイトル欄が空白文字だけ／空でも常時入力欄を表示し設定可能にする | 完了（2026-06-03デプロイ済み） | 03_画面設計.md §5.6.8 |
| 2026-06-04 | R-038 | グリッドビュー ヘッダー追従＆「今月を表示」ボタン修正: グリッドの月マスを縦スクロールしてもヘッダー左上の「2026 / 6月」表示が変化しない問題と、「今月を表示」ボタンが反応しない問題。期待: (1) 縦スクロールでビューポート中央に入った月をヘッダーに反映、(2) 「今月を表示」で今月行が画面中央に滑らかにスクロール。ガントビューの `onVisibleMonthChange` パターンを縦スクロール版で移植 | 着手中（feature/R-038-grid-header-sync ブランチ予定） | 03_画面設計.md §5.6 |
| 2026-06-04 | R-039 | Phase 3 UX: ガント／タイムラインビューにも Google 予定表示オプション追加。`useExternalEvents` の `viewMode === 'grid'` ガード撤廃・全ビューで取得し、各ビューの UI で表示。ガント=日付列に `📅` チップを左端追加、タイムライン=時刻軸の対応位置に挿入。設定画面（§12.1）に「ガント／タイムラインにも表示する」トグル追加（デフォルト on）。R-034 Phase 2 の delta（タイトル DB 保存ルール）はそのまま継承 | 着手中（feature/R-039-phase3-ux ブランチ予定） | 02_機能仕様.md F-06, 03_画面設計.md §5.x, §12.1 |
| 2026-06-04 | R-040 | R-039 フォローアップバグ修正: ガントビューで Google 予定が描画されない不具合。`useExternalEvents.buildEventsByDate` が key を `YYYY-MM-DD` 形式で生成しているのに対し、ガントは `normalizeDateKey`（`toDateString` 形式）で取得していたため常に undefined。ガント内にローカル `toYmdKey` を追加し `externalEventsByDate.get(toYmdKey(day))` に変更（`normalizeDateKey` は `data-gantt-date`／`data-testid` の従来用途で残置）。グリッド／タイムラインは元から `YYYY-MM-DD` で取得しており影響なし | 完了（2026-06-04マージ・デプロイ済み） | — (実装バグ修正) |
| 2026-06-04 | R-041 | **複数 Google カレンダー対応**: 現状 primary のみ取得（`GoogleCalendarService.php:188` で hardcoded）→ Google `calendarList.list` API で利用可能カレンダー一覧取得、カレンダー画面に「表示するカレンダー」ボタン押下でポップオーバー／MobileBottomSheet 表示、チェックリストで ON/OFF。初期は全 ON。events 取得は curl_multi 並列 5 接続、カレンダー別キャッシュ分離（UI フィルタは即時）。`calendarList.list` は 24h バックグラウンド refresh＋ボタン手動。色は Google API の `backgroundColor` を tint 背景＋左 3px 色帯。Youkan タスクは実線、Google 予定は点線ボーダー。並べ替えは R-043 へ後追い | 仕様確定（議事録 `secretary/notes/2026-06-04-会議-R041-R042仕様確定.md`）、Y1/Y2/Y3 で Agent 並列着手 | 02_機能仕様.md F-06 Phase 3, 03_画面設計.md §5.6, 04_データ設計.md §3.8 |
| 2026-06-04 | R-042 | **カレンダー表示範囲拡張＋スクロール追加読み込み**: 現状 `currentDate ± 2 ヶ月` 固定（`VolumeCalendarScreen.tsx:64-66`）→ 初期 ±6 ヶ月（13 ヶ月）取得、月単位キャッシュ key 化。IntersectionObserver の sentinel を端 200px 手前に配置し +3 ヶ月単位で lazy load。ロード中はスケルトン表示。DOM 上限は v1 では設けず観測ベース（YAGNI） | 仕様確定（議事録同上）、Y1/Y2/Y3 で Agent 並列着手 | 02_機能仕様.md F-06, 03_画面設計.md §5.6 |
| 2026-06-11 | R-056 | **詳細モーダル編集時の全量 refetch 廃止**: `DecisionDetailModal` の `onUpdate` コールバック内で毎回 `refreshAll`（6 API 並列）が呼ばれていた問題を修正。`useYoukanViewModel.updateItem` が楽観的更新で 6 リスト全てを即時更新済みのため `onUpdate` 後の `refreshAll` は冗長。`DashboardScreen.tsx` の `onUpdate` ハンドラから `handleRefresh()` 呼び出しを削除し、`await updateItem(id, updates)` のみ残す。`onClose`/`onDecision`/`onDelete` の `handleRefresh()` は整合保証のため維持。他マウント箇所（`FlowScreen`/`PanoramaBoard`/`SpeechView`）はもともと `refreshAll` を呼んでいなかったため変更不要。`ProjectRegistryScreen`/`VolumeCalendarScreen` は独自 state（`fetchProjects`/`refresh`）を持ち YoukanViewModel の 6 リストとは独立しているため変更対象外。TDD: `DashboardScreen.onUpdate.test.tsx` 2 件追加（`onUpdate` で `refreshAll` が呼ばれないこと・`updateItem` が正しい引数で呼ばれること） | 完了（feature/R-056-detail-modal-no-refetch ブランチ） | — |
| 2026-06-11 | R-057 | **QuantityEngine 計算量削減・ログ除去**: `calculateCapacityForDate` 内の console.log 2 箇所（Capacity Match / Capacity Skip）を削除。`calculateMetrics` / `calculateVolume` / `allocateBackwardsCore` / `calculateAllocationDetails` / `calculateAllocationDays` の 1 回の呼び出しチェーン内で `Map<string, number>`（キー: `${dateKey}\|${tenantId ?? ''}`）を共有し、`calculateCapacityForDate` の結果をメモ化。static/グローバルキャッシュは使わず呼び出しスコープで生成して引数で受け渡す。public メソッドのシグネチャは後方互換を維持（キャッシュ引数はオプショナル）。TDD: 新規テスト 13 件追加、既存 608 件すべて green | 完了（feature/R-057-quantity-engine-cache ブランチ） | — |
| 2026-06-11 | R-058 | **詳細モーダルの再レンダリング隔離・初期描画軽量化**: 本番実測でモーダル内 DOM 1,531 ノード・カレンダーセル 161 個。タイトル/メモの毎キーストロークで全サブツリーが再レンダリングされ INP 331ms（presentation delay 312ms）。対応: (1) `DecisionDetailModal` — console.log 2 箇所削除・`allocationDetails` deps から `activeDateInput` 除去・`localStorage` 読み取りを useMemo 1 回化・`new Date(dueDate)`/`new Date(prepDate)` を useMemo 化・`onSelectDate` を useCallback 化。(2) `SideCalendarPanel` / `DetailQuantityCalendar` / `CalendarCell` を `React.memo` でラップ。(3) `RyokanCalendar` — `renderItemTitle` / `onExternalEventClick` / `onExternalEventsMoreClick` / `onJumpToDate` を useCallback 化・mini モード（`isMini`）の初期 range を ±2ヶ月 → ±1ヶ月に縮小（約 161 → 90 セル）。(4) `RyokanGridView` — `CalendarCell` への `onAction` ラッパーを useCallback 化。TDD: 3 テスト追加（再レンダリング隔離検証 + mini 範囲縮小検証 2 件）。スクロールによる自動拡張は既存挙動のまま | 完了（feature/R-058-detail-modal-rerender ブランチ） | — |
| 2026-06-11 | R-059 | **プロジェクト作成ダイアログの元請（顧客名）欄に最近使った得意先名ドロップダウン追加（最大30件）**: `ManufacturingProjectFields.tsx` の顧客名入力欄右端に ChevronDown ボタンを追加。押下で `useRecentClientNames` フック（`GET /projects?scope=aggregated` 取得・updatedAt 降順・重複除去・空文字除外・最大30件）が返す一覧をドロップダウン表示。項目クリックで入力欄に反映して閉じる。入力済み文字で部分一致絞り込み対応。外側クリック・Escape で閉じる。ダークモード対応。新バックエンドAPIなし。TDD: フックテスト5件（重複除去・最大30件・降順・エラー時空配列）＋コンポーネントテスト6件（ボタン存在・リスト表示・fetch呼び出し・クリック反映・絞り込み・Escape閉じ）計11件 green | 完了（2026-06-11マージ・デプロイ済み）。※本要望はユーザーの指示違い（別AI宛の文言）と判明。元請欄は「会社スコープ＋manufacturingプラグイン有効」の既存条件下でのみ表示されるため現アカウント設定では非表示＝実質休眠機能。コード・テストは無害のため残置 | — |
| 2026-06-11 | R-060 | **全体一覧インライン入力の挿入位置を「期限なし群の末尾」に変更**: (1) 全体一覧（OverviewBoard）の「＋」ボタン押下で表示されるインライン入力欄を「プロジェクト見出し直下」から「期限なし群の一番下」に移動。(2) 確定した新規アイテムも同位置（期限なし群の末尾）に表示されるよう、全体一覧限定で期限なし群内部ソートを createdAt 昇順（登録順）に反転（オプトイン: `noDeadlineCreatedAsc` フラグ）。実装: `compareOverviewItems`（新関数）、`HierarchyOptions.noDeadlineCreatedAsc`（新フィールド）、`inlineAddPosition.ts`（新ファイル: `getInlineAddInsertIndex`）、`InlineAddRow.tsx`（新コンポーネント）、`OverviewBoard.tsx`（`inlineAddProjectId` state + splice）、`OverviewItem.tsx`（インライン入力 state/JSX 削除・`onStartInlineAdd` prop 化）。ガント・PanoramaBoard の `compareGeneralList2Items` は無変更で `hierarchy.guard.test.ts` が回帰を担保。TDD: 新規テスト 8+8+3=19 件、既存 614 件 all green | 完了（feature/R-060-inline-add-position ブランチ） | 03_画面設計.md §4 |
| 2026-06-11 | R-060-Y2 | **R-060 フォローアップバグ修正: APIレスポンスに createdAt(ms) が欠落しソートが無効化されていた問題**: `BaseController.php::mapItemRow` に `createdAt`/`updatedAt` の camelCase→ms 変換（`created_at` 秒×1000）を追加。楽観的更新（`Date.now()` ms）と単位を統一し、リロード後も期限なし群の昇順ソートが正しく機能するようになった。副次効果として `compareGeneralList2Items`（ガント・PanoramaBoard の期限なし群 createdAt 降順）と `compareGanttListItems`（同降順）も初めて実質有効になった（元々意図された挙動への修正）。フロントには防御的フォールバック `getCreatedAtMs(item)` ヘルパーを追加し `created_at`（秒）のみのアイテムでも正しく ms 変換する。`compareByStartLimitGroups` / `compareGanttListItems` / `compareInboxItems` を同ヘルパー経由に変更。TDD: PHP 8 件 green + vitest 6 件追加・649 件 all green | 完了（feature/R-060-Y2-createdat-mapping ブランチ） | — |
| 2026-06-13 | R-061 | **アイテム詳細画面カレンダーにGoogle予定を表示＋密度トグル追加**: `DecisionDetailModal` の左カラムカレンダーに Google 外部イベントと進捗バーが表示されなかった問題（グリッドビューとの唯一の差: 外部イベントデータが3層で渡っていなかった）を修正。対応: (1) `useExternalEvents(from, to, 'grid')` と `useGoogleCalendars()` を `DecisionDetailModal` 内に集約し、`externalRange` を `useMemo([dueDate, prepDate])` で安定化（±1ヶ月）。(2) `SideCalendarPanel` に `externalEventsByDate?: Map<string, ExternalEvent[]>` / `googleCalendars?: GoogleCalendar[]` を追加し `DetailQuantityCalendar` へスルー。(3) `DetailQuantityCalendar` に同2プロップ追加→ `RyokanCalendar` へ渡す。(4) 密度 state `density: 'full'|'compact'`（localStorage `youkan_detail_calendar_density`、デフォルト'full'）を追加し、ヘッダーに FULL⇄COMPACT トグルボタン（`data-testid="density-toggle-btn"`）を追加。`layoutMode={density === 'full' ? 'panorama' : 'mini'}` で置換。`CalendarCell`/`RyokanCalendar`/`RyokanGridView` は無改修。`YOUKAN_KEYS.DETAIL_CALENDAR_DENSITY` を `youkanKeys.ts` に追加。既存テスト（`DecisionDetailModal.test.tsx`/`KeyboardAndButtons.test.tsx`/`InteractionTests.test.tsx`/`R-058-rerender-isolation.test.tsx`）に軽量 vi.mock を追加しネットワーク呼び出し抑止。TDD: 新規テスト18件（4ファイル）追加、全体667件 all green | 完了（feature/R-061-detail-calendar-external-events ブランチ） | 03_画面設計.md §5.6 |
| 2026-06-15 | R-062 | **詳細カレンダーの量感母集団を左上フィルタ連動にし、アイテム所属プロジェクト/テナントによる量感絞り込みを解除**: アイテム詳細画面のカレンダーで他アイテムの量感（背景ヒートマップ）が極端に少ない原因②に対処。`DetailQuantityCalendar` が `RyokanCalendar` に渡していた `focusedTenantId={_item?.tenantId}` / `focusedProjectId={_item?.projectId}` の2 prop を削除（= undefined を渡す = 量感絞り込みなし）。量感の母集団は左上フィルタ（全て/個人/会社）= `filteredItems` のみに連動。`externalEventsByDate`（Google カレンダー予定）はフィルタ切替の影響を受けない独立経路を維持。`focusedTenantId`/`focusedProjectId` が undefined になることで `QuantityEngine.calculateVolume` の `relevantItems` が `filteredItems` 全体になり、グリッドビュー全体と同等の量感になる。TDD: 新規テスト9件追加（`DetailQuantityCalendar.volumeFilter.test.tsx`）、全体676件 all green | 完了（feature/R-062-detail-calendar-volume-filter ブランチ） | — |
| 2026-06-15 | R-063 | **アイテム詳細カレンダーのGoogle予定の時刻を非表示にし情報密度を向上**: `ExternalEventChip` に `hideTime?: boolean` prop を追加（既定 false）。true のとき時刻ラベル（HH:MM / [終日]）を非表示にし、📅とタイトルのみ表示。prop を `DetailQuantityCalendar`（`hideExternalEventTime=true` 固定）→ `SideCalendarPanel` → `DetailQuantityCalendar` → `RyokanCalendar` → `RyokanGridView` → `CalendarCell` の鎖で伝達。`MobileBottomSheet` 内の「他N件」展開シートの `ExternalEventChip` にも `hideTime` を伝達。グリッドビュー（`VolumeCalendarScreen`）の `RyokanCalendar` 呼び出しは変更なし（時刻表示維持）。タイムライン/ガント経路は対象外。TDD: `ExternalEventChip.hideTime.test.tsx`（7件）追加、全テスト683件 all green | 完了（feature/R-063-064-detail-mobile-redesign ブランチ） | — |
| 2026-06-15 | R-064 | **スマホ詳細画面（DecisionDetailModal）レイアウト再設計**: スマホ横画面でカレンダー左カラムが `h-[45vh]` 固定で約162pxに潰れて読めない問題を解消。会議録（2026-06-15）の結論を実装。(1) 横画面（landscape）: 中央エリアを `landscape:flex-row` 2カラム化、カレンダーカラムを `landscape:h-full` で全高確保。Tailwind `landscape:` バリアントで CSS 完結。(2) カレンダーカラム専有化: メモ/Boost をフィールド（右）カラムへ移動。(3) 目安期間ブレイクダウン: カレンダーカラム直下に折りたたみ（`data-testid="allocation-toggle-btn"`、デフォルト閉じ）で配置。量感×日付の文脈を維持しつつカレンダー高さを最大化。(4) 縦画面（portrait）: カレンダーに `flex-1 min-h-0`、独立スクロール分離。(5) 右カラムを独立スクロール（`overflow-y-auto`）でネストスクロール回避。デスクトップ（`md:flex-row`）の既存レイアウトを壊さない。フッター（h-[64px]）は維持。TDD: `DecisionDetailModal.R064.test.tsx`（6件）追加、全テスト689件 all green、ビルド成功 | 完了（feature/R-063-064-detail-mobile-redesign ブランチ） | 03_画面設計.md §5.6 |
| 2026-06-15 | R-064-Y2 | **スマホ横画面でヘッダーが191px占有しカレンダー高さ0pxになるバグを修正**: R-064実装後も実機検証（667×375 landscape）でカレンダーグリッドの高さが0pxになる問題が継続。根本原因: モーダルヘッダーが縦191px（画面高375pxの51%）を占有。対応: `tailwind.config.js` に `screens: { landscape: { raw: '(orientation: landscape)' } }` を追加し `landscape:` バリアントを確実に有効化。`DecisionDetailModal.tsx` で landscape 時のヘッダー各要素をコンパクト化: (1)Top Row padding 圧縮（`p-4 pb-2` → `landscape:p-2 landscape:pb-1`）。(2)タイトルinput フォント縮小（`text-2xl md:text-3xl` + `landscape:text-base landscape:leading-none landscape:py-0`）。(3)視点ラベルバッジ（「自分の時間管理」等）を `landscape:hidden` で非表示。(4)日付エリア padding 圧縮（`pb-3 px-4` → `landscape:px-2 landscape:pb-1`、各ボックスに `landscape:p-1`）。(5)日付ラベルフォント縮小（`landscape:text-[7px]`）・アイコン縮小（`landscape:w-[11px] landscape:h-[11px]`）。(6)カレンダーカラムに `landscape:min-h-0` 追加でフレックス高さ伝搬を補強。目標: ヘッダー≤90px・カレンダー≥150px。縦画面/デスクトップ（portrait・md:以上）は一切変更なし。全テスト689件 all green、ビルド成功 | 完了（feature/R-064-Y2-landscape-header-compact ブランチ） | — |
| 2026-06-15 | R-064-Y3 | **横画面で日付入力を1行化しクイック選択を非表示にしてカレンダー高さをさらに確保**: R-064-Y2後も実機検証（667×375 landscape）でカレンダーグリッドが47px=1行分しか見えない問題が継続。主因: (1)納期/マイ期限の日付ボックスが縦に大きい（各~110px・縦積みアイコン＋ラベル＋大フォント日付の構造）。(2)クイック選択ボタン群（今日/明日/来週月/今月を表示）がカレンダー列の高さを奪う。対応: `DecisionDetailModal.tsx` で landscape 時の日付ボックスを横一列ピルに変更（`landscape:flex-row landscape:items-center landscape:gap-1 landscape:py-0.5 landscape:px-1.5`・日付テキスト `landscape:text-xs`）。Top Row padding さらに圧縮（`landscape:p-1 landscape:pb-0`）。日付エリア padding さらに圧縮（`landscape:px-1 landscape:pb-0.5`）。パンくずの mb 削減（`landscape:mb-0 landscape:h-5`）。タイトル更に縮小（`landscape:text-sm`）。マイ期限の警告ラベルを landscape で「⚠ 納期超過」→「⚠」のみに短縮。`SideCalendarPanel.tsx` でクイック選択ボタン div に `landscape:hidden` を追加し landscape では非表示化（縦画面・デスクトップでは従来通り表示）。縦画面/デスクトップ（portrait・md:以上）は一切変更なし。landscape でも SmartDateInput によるタップ入力機能は維持。全テスト689件 all green、ビルド成功 | 完了（feature/R-064-Y3-landscape-compact-more ブランチ） | — |
| 2026-06-15 | R-064-Y4 | **デスクトップ退行修正: コンパクト化をスマホ横（高さ≤500px の横向き）限定に変更**: R-064-Y2/Y3 で使用した Tailwind の `landscape:` バリアント（`(orientation: landscape)`）がデスクトップ（例 1280×800）にも適用され、PC版詳細画面でタイトル小字化・日付ピル化・バッジ非表示・クイック選択非表示が発生していた退行を修正。対応: `tailwind.config.js` の `screens` に `'landscape-compact': { raw: '(orientation: landscape) and (max-height: 500px)' }` を追加（スマホ横専用バリアント）。`DecisionDetailModal.tsx` と `SideCalendarPanel.tsx` の全 `landscape:` クラス（計28箇所）を `landscape-compact:!` に置換（`!` = important で md: ブレークポイントとの衝突を回避）。これにより高さ>500px の環境（PC・タブレット縦）ではコンパクト化が一切適用されず、スマホ横（高さ≤500px）のみに限定。生成CSS に `(orientation: landscape) and (max-height: 500px)` メディアクエリが含まれることを確認。全テスト689件 all green、ビルド成功 | 完了（feature/R-064-Y4-compact-only-mobile-landscape ブランチ） | — |
| 2026-06-16 | R-065 | **完了表示UIを右上ボタンに一本化し全カレンダービューで機能・完了アイテムをグレー表示**: カレンダー画面に「完了アイテムの表示/非表示」UIが2つある二重化を解消（R-036 ガント専用「完了を表示」トグルを統合・置換）。(1) `CalendarHeader.tsx` からガント専用「完了を表示」スイッチ（role=switch・147-179行）を削除し `showCompleted`/`onShowCompletedChange` props も削除。`CheckCircle2` import 削除。(2) `VolumeCalendarScreen.tsx`: `showCompletedInGantt` state 削除 → `useFilter()` から `hideCompleted` を取得。`visibleItems` を `!hideCompleted` ベースに変更（hideCompleted=false→完了表示, true→非表示）。`RyokanCalendar` の `completedItems` を `hideCompleted ? [] : filteredCompletedItems` に変更（グリッド完了バッジも右上ボタン連動）。(3) `DashboardScreen.tsx`: `showCompletedInGantt` state 削除 → `ganttItems = applyGanttCompletedFilter(unifiedAllItems, !hideCompleted)` に変更。(4) 完了グレー（COMPLETED_ITEM_CLASS）は `RyokanGanttView.tsx:760` で既実装済みを確認。右上 ViewContextBar の完了ボタンが calendar ビューで disabled になっていないことも確認（元々無条件表示）。TDD: CalendarHeader テスト（R-036→R-065: gantt でトグルが描画されない 2件）・VolumeCalendarScreen showCompleted テスト（hideCompleted 連動 6件）・DashboardScreen showCompleted テスト（hideCompleted 連動 4件）更新。全テスト 686 件 all green（旧 689 件比 -3 は R-036 CalendarHeader トグルテスト削減分）、ビルド成功 | 完了（feature/R-065-unify-completed-toggle ブランチ） | 03_画面設計.md §5.4 |
| 2026-06-16 | R-065-Y2 | **バックエンドルーティングバグ修正: `/calendar/completed?query` が getCompletedItems に届かず getLoad にフォールスルーしていた問題を修正**: 本番実測で `/calendar/completed?start_date=...&end_date=...`（クエリあり）が pending 68件を返し、クエリなし URL では正しく done 38件を返すことを確認。根本原因: `CalendarController::handleRequest()` が `preg_match('#/completed$#', $_SERVER['REQUEST_URI'])` で末尾一致を判定していたが、`$_SERVER['REQUEST_URI']` にはクエリ文字列が含まれるため `$` アンカーが `?start_date=...` の前にマッチせず、両エンドポイントが `else`（getLoad）にフォールスルーしていた。修正: `$path = parse_url($_SERVER['REQUEST_URI'], PHP_URL_PATH)` でパス部分のみ抽出し、`$path` に対して正規表現を適用。`/calendar/items?query` も getItems に正しく振り分けられるようになる（これまでフォールスルーで getLoad が返っていたが今月相当のデータが偶然一致していた）。TDD: `backend/tests/test_calendar_routing.php`（7件）を新規追加、全件 PASS。フロントエンドテスト 686件 all green、退行なし。**本番検証済み（2026-06-16）**: `/calendar/completed?query`=done 38件・`/calendar/items?query`=アクティブ43件（done混入なし）。ガント/グリッド両ビューで右上「完了表示/完了非表示」トグルが連動し、完了アイテムが `text-slate-400 line-through`（グレー）で表示され、非表示切替で0件・再表示で復帰することを確認 | 完了（fix/R-065-Y2-calendar-completed-routing ブランチ） | — |
| 2026-07-07 | R-066 | **量感カレンダー/ガント改善（表示条件整理・挿入メニュー・スクロール性能）**: (1) 表示条件整理 — ガントに完了済み・削除済み・過去のアイテムが表示される懸念を個別判定。完了済みは R-065 右上「完了表示」トグルによる意図的仕様でありバグではない。削除済みはゴミ箱（`deleted_at`）が `GdbController.php`/`TodayController.php` の SQL で元々除外済み。アーカイブ（`is_archived`）はバックエンド SQL に穴があるが、フロント `hierarchy.ts:231`（`if (item.isArchived) return false`）と `VolumeCalendarScreen.tsx:27` の `isDisplayableCalendarItem` で現状実害なくカバーされている（バックエンド防御強化は R-067 として別途起票）。過去のアイテム（期限が何年前でも未完了なら表示され続ける）は督促目的の意図的仕様と決定し対応不要と結論（`06_変更履歴.md` 2026-07-07 参照）。(2) ガントプロジェクト別表示の左列アイテムタイトル右クリックメニューに「前に挿入」「後に挿入」を追加。選択アイテムと同テナント・同プロジェクトでインライン入力による新規項目追加を実装（`buildItemContextMenuActions.tsx` に `onInsertBefore`/`onInsertAfter`、`RyokanGanttView.tsx` にインライン挿入 UI/ロジック）。(3) 量感カレンダー/詳細画面カレンダーのスクロール性能改善は、詳細画面カレンダーのみ完了（`CalendarCell.tsx`/`RyokanCalendar.tsx`/`RyokanGridView.tsx` の一部でスクロール中の装飾トランジション・不要ペイント範囲を抑制）。量感カレンダー本体（グリッド表示）のスクロール性能改善は未着手のため R-068 として別途起票 | 一部完了（表示条件整理=完了、挿入メニュー=完了 `ac6f72b`、スクロール性能=詳細画面カレンダーのみ完了 `ed3695a`・量感カレンダー本体は未着手→R-068） | 03_画面設計.md §5, 06_変更履歴.md |
| 2026-07-09 | R-069 | **【セキュリティ緊急修正】`/stocks` 系エンドポイント未認証アクセス脆弱性**: R-050案の設計会議調査で発覚。`backend/index.php` に登録済みの `/stocks`・`/stocks/{id}`・`/stocks/{id}/assign` ルートが `StockController.php` にディスパッチされる際、`authenticate()` を一切呼ばず `tenant_id` スコープ分離も無いまま本番で到達可能だった。特に `assign()` は任意の `userId` を検証せず `items` テーブルへ直接 `INSERT` でき、未認証の第三者が他テナントの `items` にレコードを書き込める状態だった。調査の結果、フロントエンドで本APIを呼ぶ `useStocks.ts`（呼び出し元 `DispatchScreen.tsx`）はアプリのルーティングから一切参照されない死蔵コードと確認。実害のある機能を壊さず安全側に倒すため、認証追加ではなく `backend/index.php` から `/stocks` ルート登録3ブロックをコメントアウトして無効化（`StockController.php` 本体は将来の再設計用に残置）。TDD: PHP内蔵サーバーを一時ディレクトリで起動し未認証で3エンドポイントを叩く統合テスト `backend/tests/test_stocks_unauthenticated_access.php` を新規追加。修正前は全3件 FAIL（200/500で到達・攻撃成立を確認）、修正後は全3件 PASS（404）。既存PHPテスト・vitest(705件)に新規の退行なし | 完了（fix/stock-controller-auth ブランチ） | 06_変更履歴.md |
| 2026-07-29 | R-070 | **アイテム詳細画面（DecisionDetailModal）でのカレンダー操作が10秒以上重い問題**: 「納期」フィールドクリック・左上フィルタ（全て/個人/会社）切替のたびにカレンダーグリッド全体が固まる不具合を調査。根本原因は `RyokanCalendar.tsx` の `handleDayAction` が `useCallback` でラップされていないこと。stateが変わるたびに新しい関数参照が生成され、`RyokanGridView.tsx` の `handleCellAction` も連鎖的に新規参照になり、`React.memo` 済みの全 `CalendarCell`（数十〜数百）のメモ化が毎回崩壊してフル再レンダリングされる。詳細画面を開いた瞬間の遅延は `DecisionDetailModal.tsx` が毎回 `useExternalEvents`/`useGoogleCalendars`（Google連携API）を叩くことと重い `allocationDetails` useMemoが追加要因。フィルタ切替時は `QuantityEngine.calculateMetrics` のフル再計算も加わり最も重い。カレンダースクロールの遅さは詳細画面内は `ed3695a`（2026-07-08）の最適化が有効で問題なし、量感カレンダー本体（`VolumeCalendarScreen.tsx`）は `disableRangeExtension` 未適用のままで **R-068** そのものが原因。対応方針: (1) `handleDayAction` の `useCallback` 化、(2) `RyokanGridView.tsx` の `externalEvents={... \|\| []}` を安定参照化、(3) R-068（`VolumeCalendarScreen.tsx` へのスクロール最適化適用）を本タスクで一体実装。**実装完了・検証中**: `fix/R-070-calendar-rerender-optimization` ブランチで対応。(1) `RyokanCalendar.tsx` の `handleDayAction` を `useCallback` 化（依存先の `resetHighlights` も `useCallback` 化して参照を安定させた上で依存配列に追加）。(2) `RyokanGridView.tsx` にモジュールスコープの `EMPTY_EXTERNAL_EVENTS` 定数を追加し `externalEvents={... \|\| []}` を置換。(3) R-068: `RyokanCalendarProps`/`RyokanGridView`/`CalendarCell` に新規 `scrollOptimized` prop を追加（`volumeOnly` に依存せず装飾トランジション抑制・`content-visibility:auto`+`contain`による不要ペイント範囲抑制を有効化。可変高さセル向けに `contain-intrinsic-size` フォールバックも付与）。`VolumeCalendarScreen.tsx` から `scrollOptimized={true}` を渡して量感カレンダー本体に適用。TDD: 失敗するテストを先に作成（`CalendarCell` re-render 回数を `React.memo` ラッパーでカウントし、無関係な再レンダリングで cell 側が再レンダリングされないことを検証する新規テスト3ファイル・5件）→ Red確認→ 実装 → Green確認。全体テスト回帰: 122ファイル732件 all green（14 skip、既存の無関係な1件のunhandled error警告は本修正と無関係と確認済み）。chrome-devtools MCPでdev環境（localhost:5173/8000）実機検証: デバッグユーザーアカウントでログイン、量感カレンダーグリッド表示・詳細画面カレンダー（DecisionDetailModal埋め込み）でのフィルタ切替（全て/会社）が視覚的破綻なく動作、コンソールエラーなしを確認。合成スクロールベンチマーク（プログラム的scrollTop駆動、rAFフレーム計測）は既存デバッグデータのアイテム数が少なく本番相当の負荷差は再現しにくかったが、根本原因の再レンダリング崩壊自体は自動テストで定量的に実証済み（無関係な状態変化に対しCalendarCell再レンダリング回数が旧実装で全セル分発生していたのに対し修正後は0件）。masterへマージ済み（コミット `7a35940`）。**本番デプロイ・実機検証完了（2026-07-29）**: `upload.ps1` のSSH互換性バグ（後述）修正後、R-050 Phase1と合わせて本番デプロイ。chrome-devtools MCPで本番実機検証: 量感カレンダーグリッドのプログラム的スクロール（rAF計測、平均16.6ms/最大30.9ms、フリーズなし）、アイテム詳細画面「納期」フィールドクリックの即時反応（フリーズなし）を確認。consoleエラーなし（既存の別件Google連携トークン失効による500エラーのみ検出、本修正とは無関係） | 完了（2026-07-29デプロイ済み） | 03_画面設計.md §5.6 |
| 2026-07-31 | R-073 | **due_status意図しない変化の修正**: 詳細画面で「納期」フィールドをクリックしただけ（実際には日付を変更していない）で、アイテムの`due_status`が`null`→`confirmed`に変化する不具合を調査。根本原因は`SmartDateInput.tsx`の`handleBlur()`が、ブラー時に入力文字列を再パースして`onChange`を呼ぶ際、パース結果が既存値と同じかどうかの差分チェックをしていないこと。`DecisionDetailModal.tsx`側の`onChange`ハンドラ（`SmartDateInput`の`onChange`、サイドカレンダーの`handleSideCalendarSelectDate`の計2箇所）が値の変化有無を問わず`dueStatus: 'confirmed'`を無条件送信しているため誘発される。旧アーカイブ設計書（`docs/99_Archive/Reference_Log/202601122223再定義/納期_入力表示活用_設計完全版.md`）では「納期の入力・変更・クリア時のみ」更新する設計だったが、現行仕様書には`due_status`の値域・遷移条件が未記載だった。対応方針: (1) `SmartDateInput.tsx`に差分チェックを追加（根本修正）、(2) `04_データ設計.md`に`due_status`の値域・遷移条件を明文化。**実装完了・検証中**: `fix/R-073-due-status-blur-bug` ブランチで対応。`SmartDateInput.tsx` の `handleBlur()` に `isSameDate()` 比較を追加し、パース結果（または空文字→クリア）が既存値と同じ場合は `onChange` を呼ばないよう修正（`DecisionDetailModal.tsx` 側の2箇所は無修正、`SmartDateInput` からの `onChange` が発火しなくなることで自動的に解決）。TDD: `SmartDateInput.test.tsx` を新規作成（既存値のままブラー→`onChange`未呼び出し／実際に変更してブラー→`onChange`呼び出し／未設定から空文字ブラー→未呼び出し／既存値ありから空文字ブラー→`onChange(null)`呼び出しの4件）→ Red確認 → 実装 → Green確認。`DecisionDetailModal.test.tsx` にも統合テスト2件（納期欄を変更せずブラー→`dueStatus:'confirmed'`未送信／実際に変更してブラー→`due_date`と`dueStatus:'confirmed'`送信）を追加。vitest全件（126ファイル752件、うち1件`useAssigneeView.test.ts`失敗は本修正と無関係の日付依存の既存失敗と確認済み、1件`KeyboardAndButtons.test.tsx`のunhandled errorも既存かつ無関係と確認済み）。chrome-devtools MCPでdev環境（localhost:5173/8000）実機検証: デバッグユーザーアカウントでログイン、既存アイテム（納期2026/02/18設定済み）の詳細画面を開き納期欄にフォーカス→変更せず他要素へブラー→ネットワークタブでPUT/PATCHリクエストが発生しないことを確認。続けて実際に日付を2026/09/20へ変更してブラー→`{"due_date":"2026-09-20","dueStatus":"confirmed"}` が送信されることを確認。2026-07-31、masterへマージ済み・本番デプロイ完了。本番検証: 既存アイテム詳細画面で納期欄をクリックし変更せず他要素へブラーしても、ネットワークタブでPUT/PATCHリクエストが発生しないことを確認 | 完了（2026-07-31デプロイ済み） | 04_データ設計.md §4.5 |
| 2026-07-31 | R-072 | **Google OAuth失効検知・再連携UX実装**: 本番の`/google/calendars`で`invalid_grant`エラーによる500が継続発生していた件を調査。根本原因は、OAuth同意画面が「テスト中」ステータスのままでリフレッシュトークンが7日で強制失効している可能性が最有力（Google Cloud Console側の設定変更が必要で、発注者本人のみ対応可能）。加えて実装側にも欠陥があり、`GoogleCalendarService::refreshAccessToken()`が`invalid_grant`を判別せずエラーを投げるだけ、`user_google_oauth`テーブルに失効管理カラムが無く検知の仕組み自体が無い、自動同期経路（`GET /google/calendar/events`）はエラーを握りつぶしてキャッシュにフォールバックし続けユーザーが失効に気づけない、手動更新・一覧取得経路は生の500エラーをそのまま返すのみで再連携導線が無い、という状態だった。設定画面の仕様（`03_画面設計.md` §12.1）には元々「Google 側で連携が解除されました。再連携してください」という失効表示が定義されていたが、検知処理自体が実装されておらずこの表示に到達していなかった（仕様と実装の乖離）。対応方針: `user_google_oauth`に`invalidated_at`/`last_error`カラムを追加し`invalid_grant`検知時に記録、設定画面で既存仕様通りの失効表示・再連携導線を実際に機能させる。根本原因のOAuth公開設定変更は本タスクのスコープ外（発注者が別途対応）。**実装完了**: (1) `backend/migrate_v30_google_oauth_invalidation.php`新規作成、`user_google_oauth`に`invalidated_at`/`last_error`カラムを追加。(2) `GoogleCalendarService::refreshAccessToken()`がGoogleのエラーレスポンスの`error`フィールドから`invalid_grant`を判別し、専用例外`GoogleOAuthInvalidGrantException`を投げると同時に`invalidated_at`/`last_error`をDBに記録。リフレッシュ成功時・`exchangeCodeForTokens()`（再連携）時は自動的に失効状態をクリア。(3) `GoogleCalendarController`: `shouldAutoSync()`が`invalidated_at`非NULLの間は自動同期をスキップ（無駄なGoogle APIコール防止）。`GET /google/oauth/status`のレスポンスに`invalidated`真偽値フィールドを追加。`refresh()`/`listCalendars()`は`invalid_grant`検知時に生の500ではなく409+「Google 側で連携が解除されました。再連携してください」の専用メッセージを返すよう改善。(4) フロント`GoogleCalendarSection.tsx`: `invalidated`がtrueの間は警告バナー「Google 側で連携が解除されました。再連携してください」を表示し、「今すぐ更新」ボタンを「Google で再ログイン」（既存の未連携時`handleStartOAuth`を再利用、`POST /google/oauth/start`→Google認可URLへ遷移）に置換。TDD: PHPテスト新規追加（GoogleCalendarServiceTest 4件・GoogleCalendarControllerTest 4件）、vitest新規3件を追加、全件green（既存テストに回帰なし。既知の無関係な事前既存失敗（QuantityServiceTest等）はR-072実装前後で同一と確認済み）。実機検証: 本番は本タスクの変更が未デプロイのため`invalidated`フィールド自体が存在せず確認不可（想定通り）。dev環境（localhost:5173/8000、デバッグユーザーfjt.suntree@gmail.com）でDBに疑似的な`invalidated_at`を投入し、個人設定画面のGoogleカレンダー連携セクションで失効表示・「Google で再ログイン」ボタン（クリックで実際のGoogle OAuth同意画面へ正しく遷移することまで確認、ログイン自体は未実施）を確認。**発注者への案内**: 根本原因はGoogle Cloud ConsoleのOAuth同意画面が「テスト中」ステータスのままである可能性が最有力（この状態だとリフレッシュトークンが7日で強制失効する）。発注者ご本人でGoogle Cloud Consoleにログインし、OAuth同意画面を「本番公開」へ切り替えていただく必要がある（このタスクでは対応不可）。2026-07-31、masterへマージ済み・本番デプロイ完了。デプロイ後に`backend/migrate_v30_google_oauth_invalidation.php`をSSH経由で実行し`invalidated_at`/`last_error`カラムを追加（Migration v30 completed successfully.）。本番検証: 個人設定画面のGoogleカレンダー連携セクションで「Google 側で連携が解除されました。再連携してください」表示と「Google で再ログイン」ボタンが正しく表示されることを確認 | 完了（2026-07-31デプロイ済み） | 03_画面設計.md §12.1, 04_データ設計.md §3.6 |
| 2026-07-29 | R-071 | **改善要望送信フォーム（画像添付付き、F-23）**: ホーム画面ヘッダーの「Youkan」ロゴを50%サイズ（`text-sm`→`text-[7px]`）に縮小し、隣に「改善要望を送る」ボタンを配置（PC）。スマホは`MenuDrawer`「ツール」セクションに同項目を追加。押下で`SimpleModal`ベースの新規モーダル（`ImprovementRequestModal.tsx`、`ForAiModal.tsx`をテンプレート）を開き、本文テキスト（必須）＋画像1枚（任意、5MBまで、クリップボード貼り付け対応）を送信。認証必須のAPI（`POST /improvement-requests`）経由でサーバーローカルの`backend/data/requests_sub.md`に日時・本文・画像リンクを追記、画像は`backend/data/requests_sub_uploads/`に保存。DBテーブルは追加しない。**設計判断（発注者確認済み）**: 本番⇔gitリポジトリの同期は自動化せず、発注者/指揮AIが不定期にSSH/scpで`backend/data/`配下を手動回収し、良い要望は人力で`docs/requests.md`に書き写す運用とする。**実装時必須確認事項**: `backend/data/`は`.env`/`*.sqlite`/`*.log`と同様に`upload.ps1`のデプロイ除外リストに追加しないと、次回デプロイで蓄積データが消失する。**実装完了・検証中**: `feature/R-071-improvement-request-form`ブランチで対応。フロント（`YoukanHeader.tsx`ロゴ縮小＋ボタン追加、`MenuDrawer.tsx`メニュー項目追加、`ImprovementRequestModal.tsx`新規、`ApiClient.submitImprovementRequest()`追加）・バックエンド（`ImprovementRequestController.php`新規、`authenticate()`必須、`POST /improvement-requests`ルーティング追加、`backend/data/requests_sub.md`追記＋`requests_sub_uploads/`保存）・`upload.ps1`のデプロイ除外リストへの`data`追加、すべて実装完了。TDD: バックエンド統合テスト`backend/tests/test_improvement_request_controller.php`（未認証401・空本文400・画像なし送信・画像あり送信＋保存確認・requests_sub.mdフォーマット確認・5MB超拒否の16件）とフロントエンドテスト（`ImprovementRequestModal.test.tsx`11件、`YoukanHeader.test.tsx`/`MenuDrawer.test.tsx`追加分）をRed確認後に実装しGreen確認済み。既存テスト回帰: vitest全体123ファイル748件pass（14 skip、既存の無関係な1件のunhandled error警告はR-070で確認済みのものと同一で本修正と無関係）。バックエンドPHPテストは個別実行で全ファイル確認し、`QuantityServiceTest.php`の1件失敗と`test_real_project_title_join.php`の1件失敗（意図的な「修正前」記録用アサーション）はいずれもmasterとの差分ゼロで本修正と無関係な既存の事象と確認済み。chrome-devtools MCPで実機検証（dev環境）: PCヘッダーからのボタン押下→モーダル表示→本文入力＋画像添付（ファイル選択・クリップボード貼り付け両方）→送信→トースト表示→`requests_sub.md`/`requests_sub_uploads/`への正しい保存を確認。スマホ幅でMenuDrawer経由の導線・モーダル表示も確認。2026-07-31、masterへマージ済み・本番デプロイ完了。本番検証: ホーム画面ヘッダーのロゴ縮小表示・「改善要望を送る」ボタン・モーダルからの送信（テストデータ送信後、`backend/data/requests_sub.md`への追記を確認しテストデータは削除して原状回復済み）を確認 | 完了（2026-07-31デプロイ済み） | 02_機能仕様.md F-23, 03_画面設計.md §14, 04_データ設計.md §3.9 |
| 2026-08-12 | R-076 | **ガントチャートで依存関係のあるアイテムを順序どおりに表示（AのあとBならAが上）**: 原文「ガントチャートにおいて、依存関係があるなら、あとになるアイテムは下に表示されるべき。例えばAのあとBができるという依存関係なら、Aよりも下にBが表示されてほしい」。調査の結果、この要件自体は R-015（2026-04-08完了）で `hierarchy.ts` の `sortWithDependencies`（トポロジカルソート・Kahn's algorithm）としてすでに実装済みで、`RyokanGanttView.tsx` の `buildHierarchicalList` 呼び出しに `dependencies` が渡され、プロジェクト別モード（`showGroups=true`）・全体一覧モード（`showGroups=false`）双方に反映されていることを確認。ただし、依存グラフに循環（例: A→B→A）が含まれる場合、トポロジカルソートで解決できないノードが最終結果配列から**静かに欠落する**バグを発見（無限ループはしないが、該当アイテムがガント一覧から消えるデータ消失）。原因は `sortWithDependencies` 内で、チェーンの先頭（predecessorが0件のノード）が存在しない完全な循環コンポーネントが `chainHeads` に一切含まれず、`independentItems`（チェーン外アイテム）からも除外されたまま結果に統合されないこと。**実装完了**: `hierarchy.ts` の `sortWithDependencies` に、トポロジカルソート結果 `topoOrder` に含まれないノード（＝循環で解決不能なノード）を検出し、依存制約を無視した通常ソート（`independentItems`扱い）へフォールバックする処理を追加（データ消失防止・安全側）。TDD: `hierarchy.test.ts` に失敗するテストを先に作成（単純循環A→B→A・長い循環A→B→C→A・循環グループと正常チェーンの混在の3件）→Red確認→実装→Green確認。フロントエンド全体テスト回帰: 127ファイル774件中773件pass（14 skip）。唯一の失敗 `useAssigneeView.test.ts` はmasterでも同一内容で失敗する既存の無関係な事象と確認済み（`git stash`でR-076変更を退避しても同じ失敗を再現）。`KeyboardAndButtons.test.tsx`実行時の`DecisionDetailModal.tsx`由来の unhandled exception も同様に既存かつ無関係と確認済み。chrome-devtools MCPがセッション内の別プロセスに占有されていたため claude-in-chrome MCP で代替し、dev環境（ポート競合回避のため一時的にlocalhost:5199/8099を使用、検証用のvite.config.tsプロキシ変更は最終的に元へ復元）で実機検証: デバッグユーザーアカウントでログイン、依存関係なしタスクA(納期9/20)・B(納期8/15)を作成した状態でガント一覧がB→Aの順（納期昇順）に表示されることを確認。続けて依存関係API（`POST /api/dependencies`、A→B）でA=前提・B=後続の依存を追加した後、ガントチャートを再読み込みし、プロジェクト別モード・一覧モード双方でA→Bの順に反転して表示されることをスクリーンショットで確認。検証用アイテム・依存関係は検証後にAPI経由で削除し原状回復済み。**本番デプロイ・実機検証完了（2026-08-12）**: `upload.ps1`でデプロイ後、本番の佐礼谷プロジェクト（既存の依存関係あり実データ）でガント一覧を確認し、依存関係の上流アイテム「B1 階段下板戸」が下流アイテム「B1 ガラス戸６枚 アクリル切断」より上に表示されることを確認 | 完了（2026-08-12デプロイ済み） | — |
| 2026-08-12 | R-075 | **Flow／ガントのモーダル入力遅延修正**: フローチャート画面・ガント画面でモーダル（判断詳細・改善要望送信）を開いてテキスト入力すると1文字あたり約1秒かかる不具合を調査。共通根因は `SimpleModal.tsx` の全画面 `backdrop-blur-sm`（判断詳細モーダルは独自オーバーレイで同様に `backdrop-blur-sm` を使用）で、文字入力によるモーダル再描画のたびに背後の大規模な Flow／ガント画面へ `backdrop-filter` の再合成負荷が掛かっていた。Flow 画面はエッジが常時 `animated: true` で CSS animation しているため、ぼかし越しの背景自体も継続的に変化する増幅要因があった（ガントには該当なし）。**実装完了**: `SimpleModal.tsx`・`DecisionDetailModal.tsx` のオーバーレイから `backdrop-blur-sm` を削除し半透明背景のみに変更、両オーバーレイに識別属性 `data-youkan-modal-overlay` を付与、`index.css` に `body:has([data-youkan-modal-overlay])` 配下で Flow の animated edge を一時停止するセレクターを追加（モーダルを閉じると自動再開）。TDD: `FlowModalAnimationPause.test.ts`（識別属性・Flow animation一時停止・SimpleModalのblur禁止の3件）・`ImprovementRequestModal.test.tsx`（本文5回変更で背景が再レンダーされないことを検証するレンダーカウント試験を含む12件）を追加、新規18/18 pass。既存回帰確認は `git stash` で修正前状態と比較し、`useAssigneeView.test.ts`・`KeyboardAndButtons.test.tsx` 起因の unhandled Promise error はいずれも修正前から存在する既存の無関係事象と確認済み（詳細は `docs/handover/R-075-analysis.md`）。masterマージ後の全体テスト（128ファイル778件）も新規追加分に回帰なし、既存の無関係な `useAssigneeView.test.ts` 失敗1件のみ残存。**本番デプロイ・実機検証完了（2026-08-12）**: `upload.ps1`でデプロイ後、本番でフローチャート画面を開いた状態で改善要望送信モーダルを開き本文欄に入力、背景がぼかされておらず（`backdrop-blur-sm`除去確認）目に見える遅延がないことを確認。JS計測でも入力イベント間隔は0.2〜1.4msで遅延なし | 完了（2026-08-12デプロイ済み） | 03_画面設計.md, 06_変更履歴.md |
| 2026-07-09 | R-050 | **テナント型AI中枢・担当者割り振り・業務ディスパッチ機能 Phase1着手（kaigi2会議・専門家6名+検証23エージェントで意思決定）**: 会議結論全文は `docs/kaigi/2026-07-09-R050テナント型AI中枢設計.md`。Phase1のスコープを「画面2: 担当者別ビュー」単体に限定（画面4社員向けTodayとの統合は反証により不採用）。着手の必須前提2点を決定: (a) 可視性ルールに管理者スコープを新設 — 所属テナントで `memberships.role IN ('owner','admin')` のユーザーは `assigned_to` を問わず閲覧可能（既存の「創建者=自分 OR 担当者=自分」ルールは変更しない）。(b) `assigned_to` のID空間表示バグ是正 — 実データ検証で会社アイテムの97%が `u_`（users.id）形式で既存の `LEFT JOIN assignees` が失敗していることを確認。discriminator列追加マイグレーションは行わず、`ItemController.php::getAssigneeEmail()` に実装済みの `u_` プレフィックス判定を一覧系クエリへ横展開するアプリケーション層修正に限定。孤児データは自動修復せず「未割当」表示＋ログ出力。担当候補の型を `assigneeKind: 'user' \| 'assignee'` として確定しPhase3で再定義しないことを決議。meta.dispatchスキーマ・dispatch_proposals（AI提案ステージングテーブル）・承認ACL・サーキットブレーカー設計はPhase3着手判断時に改めて統合設計（今回は対象外）。DispatchScreen.tsx / CompanyDashboard.tsx / StockController.php は「土台にしない」と決議（StockController.phpは router.php 経由の `index.php` に実登録済みで認証・テナント分離なしで到達可能なことを確認したため、単なる再利用禁止注記では不十分と判断。詳細は `06_変更履歴.md` 参照）。原案の背景・汎用ユースケース・Phase2以降の画面1/3/4/5/6構想は `docs/reference/vision/R050_Tenant_AI_Business_OS.md` に保存し、`docs/requests.md` 本文は要約版に置換。**その後Phase1実装完了**: (1)バックエンド（管理者スコープAPI新設・assigned_to ID空間解決の横展開）`feature/R-050-phase1-assignee-view` ブランチ、マージコミット `322caf1`。(2)フロントエンド（担当者別ビュー画面 AssigneeViewScreen）`feature/R-050-phase1-assignee-view-ui` ブランチ、マージコミット `67e2586`。(3)実機検証（claude-in-chromeでブラウザ操作）で発見したバグ「会社アカウント（テナント自身ログイン、account_type=tenant）で管理者スコープ切替が403になる」を修正: `fix/R-050-phase1-tenant-account-admin-scope` ブランチ、マージコミット `ce65aa2`。原因は `BaseController::assertAdminScopeAllowed()` が `memberships` テーブルを `user_id=currentUserId` で検索するが、会社アカウントログイン時は currentUserId がテナントID自身で memberships に行がないため。修正後、会社アカウント・ユーザーアカウント両方で200を確認、テナント分離（他テナントのID指定で404）も維持されていることを確認済み。担当者別ビュー（画面2）はエンドツーエンドで動作する状態。**本番デプロイ・実機検証完了（2026-07-29）**: `upload.ps1` のSSH互換性バグ修正後デプロイ実施。chrome-devtools MCPで本番実機検証: 会社アカウント（藤田建具店テナントコンテキスト）にモード切替後、メニューから「担当者別ビュー」画面に遷移でき、担当者タブ切替・未完了/詰まり/待ち件数・今日/明日/今週の担当タスク一覧が表示されることを確認。consoleエラーなし | 完了（2026-07-29デプロイ済み） | 04_データ設計.md, 06_変更履歴.md |
| 2026-08-12 | R-074 | **フローチャート操作性・軽量化バンドル（F-24）**: 発注者要望「フローチャートの動きを速くすることと操作性アップを優先」。3点対応。(1) 依存線描画バグ修正: 選択ノードでEnterキーを押して新規ノードを作成すると、依存関係は作成されるのに接続線が画面に表示されないことがある不具合を調査。根本原因は `FlowScreen.tsx` の `createNodeBelow`/`handleEdgeInsert`/`onNodeDragStop` の重なり自動接続の3経路が、依存関係作成後に `setDependencies` のみを呼び、`edges` state への反映を `[placedItems, dependencies, ...]` に依存する巨大な派生 `useEffect` に一任していたこと。この派生effectは先頭に `if (isDragging.current) return;` というrefガードを持ち、`isDragging.current` が `true` のタイミングで `dependencies` が変化すると早期リターンして edges に反映されないまま取り残される（ref変化は再レンダリングのトリガーにならないため、次の無関係な state 変化まで復旧しない）。手動でハンドルドラッグ接続する `onConnect` のみ `setEdges` を直接呼んでおり、この問題を踏んでいなかった。対応: `dependencyToEdge()`（Dependency→Edge変換の単一関数）と `appendDependencyToState()`（`setDependencies`と`setEdges`を同時に更新するヘルパー）を新設し、4つの依存関係作成経路すべてを `onConnect` と同じ「即時反映」パターンに統一。派生effectは最終的な整合性を保証する再同期として残置。(2) 接続線アニメーション廃止→矢印表示: `dependencyToEdge()` で `animated: false` 固定・`markerEnd: { type: MarkerType.ArrowClosed }` を設定（2箇所: 派生effect内のmap、`onConnect`内のedge構築処理）。(3) Enter連続追加UXフロー新規実装: `FlowItemNode.tsx` の既存 `isNewNode`（タイトル自動フォーカス+全選択）はそのまま流用。タイトル編集中の `Tab` キーでタイトル確定→目安時間欄をローカルstateで自動オープン（新規 `chainOnConfirm` state）。目安時間欄で `Enter` を押すと、`chainOnConfirm` が true の場合のみ目安時間を保存し `onChainCreate(itemId)` を呼んで選択中ノードを親として次のノードを連鎖作成（Tab経由でない通常の目安時間クリック編集では誤発火しないよう scope 限定、テストで担保）。`onChainCreate` は `FlowScreen.tsx` で `createNodeBelow(itemId, 0)` を呼ぶが、`createNodeBelow` の定義位置が edge構築useEffectより後方のため、TDZ回避に `createNodeBelowRef`（useRef）経由で受け渡し。TDD: 失敗するテストを先に作成（`FlowScreen.enterEdge.test.tsx` 3件——通常操作でedge描画・ドラッグ中〈isDragging=true〉でも即時反映される回帰再現・アニメーションなし矢印表示の確認、`FlowItemNode.chainCreate.test.tsx` 4件——タイトル自動フォーカス確認・Tab→目安時間欄オープン・Tab経由Enterでのchain作成・通常クリック編集ではchain不発火）→Red確認→実装→Green確認。既存テスト回帰なし確認（vitest全件126ファイル778件中763 pass・14 skip・1件は`useAssigneeView`の既存の日付境界依存の無関係な失敗でmaster baselineでも同一失敗を確認済み。build（tsc+vite）成功）。実機検証: chrome-devtools MCPは他Agent（R-076担当）使用中のプロファイルロックで起動不可のため、claude-in-chrome MCPで代替。Enterキーでの新規ノード作成を複数回連続実施し、毎回接続線（実線+矢印）が表示されることを確認、アニメーションなしを確認。Tabキーでの目安時間欄フォーカス移動は、claude-in-chrome（CDPベース自動化拡張）経由のTabキー送信がブラウザネイティブなフォーカス走査を先に発火させ`document.activeElement`が`<body>`になってしまい`FlowItemNode`のonKeyDownハンドラにTabキーイベントが到達しないことを一時デバッグログで確認（同じ入力欄でEnterキーは正常にハンドラへ到達することは確認済みのため、Tabキー特有の自動化ツール側の制約と判断。詳細は `docs/handover/R-074-analysis.md` 参照）。Tab→目安時間→Enter連鎖の動作自体はVitestで実DOM操作によりRed→Green確認済みだが、実ブラウザでの目視確認は次のAgentまたは発注者による追加確認を推奨。**本番デプロイ・実機検証完了（2026-08-12）**: `upload.ps1`でデプロイ後、本番の佐礼谷プロジェクトで既存ノードを選択しEnterキーでノード連鎖作成を計2回連続実施、いずれも新規ノードが即座に作成され接続線（矢印マーカー、アニメーションなし）が欠落なく描画されることを確認。DOM調査で全43本のedgeが`animated: false`・marker type `arrowclosed`であることも確認。検証用ノードは全てDelete操作で削除し原状回復済み（佐礼谷プロジェクトは検証前後とも46件で一致） | 完了（2026-08-12デプロイ済み） | 02_機能仕様.md F-24 |
| 2026-08-12 | R-078 | **ガント/フロー右クリックメニューの「前に挿入」「後に挿入」にキーボードショートカット追加**: 改善要望フォーム経由（2026-08-12 05:13、スクリーンショット添付）。「この右クリックメニューの前に挿入はa、後に挿入はbでキーボード操作できるようにして」。R-066で追加された「前に挿入」「後に挿入」メニュー項目（ガント左列アイテムの右クリックメニュー）が対象。フロー画面（`FlowScreen.tsx`）は`buildItemContextMenuActions`を共用しているが`onInsertBefore`/`onInsertAfter`コールバックを渡していないため、そもそも「前に挿入」「後に挿入」メニュー自体が現状表示されない（R-066はガント限定実装）。対応するメニューが無いためフロー側は対象外（本要望の範囲外）。**実装完了**: `ContextMenu.tsx`は既に`ContextMenuAction.shortcut`フィールドによる汎用キーボードショートカット機構（`e.key`一致でクリック実行）を持っていたため、`buildItemContextMenuActions.tsx`の「前に挿入」「後に挿入」アクションに`shortcut: 'a'`/`'b'`を付与するのみで対応。ラベルも「前に挿入 (a)」「後に挿入 (b)」に変更し発注者の意図（ラベル横の表示）に沿わせた。TDD: `buildItemContextMenuActions.test.ts`に2件（ショートカット付与確認・onClickコールバック確認）、`ContextMenuInsertShortcuts.test.tsx`に3件（aキーでonInsertBefore発火・bキーでonInsertAfter発火・ラベル表示）追加、Red確認後実装しGreen確認。既存テスト回帰なし（781 pass/14 skip、既存の無関係なunhandled errorのみ）。claude-in-chrome MCPでdev環境実機検証: ガントプロジェクト別モードでアイテム右クリック→メニューに「前に挿入 (a)」「後に挿入 (b)」表示を確認→aキー押下で「前に追加...」インライン入力欄が開くことを確認→Escapeでキャンセル→再度右クリック→bキー押下で「後に追加...」インライン入力欄が開くことを確認。コンソールエラーはデバッグユーザーのGoogle OAuth未連携による既知の無関係エラーのみ。**本番デプロイ・実機検証完了（2026-08-12）**: `upload.ps1`でデプロイ後、本番ガント画面でアイテムを右クリックしメニューに「前に挿入 (a)」「後に挿入 (b)」が表示されること、aキー押下で「前に追加...」入力欄が開くこと、bキー押下で「後に追加...」入力欄が開くことを確認。テスト入力はEscapeでキャンセルし原状回復済み | 完了（2026-08-12デプロイ済み） | 02_機能仕様.md F-26 |
| 2026-08-12 | R-079 | **フローチャート要素タイトル編集中、テキストボックス内ドラッグでノードが動いてしまう**: 改善要望フォーム経由（2026-08-12 08:29）。「タイトルを編集できる状態のとき、テキストボックス内をドラッグすると要素がドラッグしてしまうが、そのときはテキストをドラッグで選択したいので、テキストボックスが動かないでほしい」。**修正**: `FlowItemNode.tsx`のタイトル編集用`<input>`に`onMouseDown`/`onClick`の`stopPropagation`が漏れていたことが原因（同ファイルの目安時間input・タイムバッジには既に付与済みで対称性が崩れていた）。目安時間input側と同じ2ハンドラを追加。TDD: `FlowItemNode.editUX.test.tsx`にmousedownで`stopPropagation`が呼ばれることを検証するテストを追加、Red確認後実装しGreen確認。実機検証（chrome-devtools MCP、dev環境）: タイトル編集input内でmousedown→mousemove→mouseupを発行してもノードの`transform`（位置）が変化しないことをDOM直接確認。仕様: `02_機能仕様.md` F-27。**本番デプロイ・実機検証完了（2026-08-12）**: `upload.ps1`でデプロイ後、本番フローチャート画面でノードタイトルをF2編集状態にし、テキストボックス内でmousedown→mousemove→mouseupを発行してもノードの`transform`（位置）が変化せずテキストのみ選択（selectionStart:0/selectionEnd:8）されることをDOM直接確認 | 完了（2026-08-12デプロイ済み） | 02_機能仕様.md F-27 |
| 2026-08-12 | R-080 | **フローチャートでEnter押下時、新規ノードのタイトル「新規アイテム」が選択状態になっていない（要実機確認）**: 改善要望フォーム経由（2026-08-12 08:32）。「Enterで新ノードを作って『あ』と打ってEnterを押せば、あを打つ前は『新規アイテム』という文字が選択状態になっていて…」という期待挙動の報告。R-074（2026-08-12 07:13デプロイ）で`FlowItemNode.tsx`の`isNewNode`による自動フォーカス＋全選択は実装済みのはずだが、本報告はデプロイ後（08:32）のため本番で実際に機能していない可能性がある。**手がかり**: `docs/handover/R-077-analysis.md`「今回スコープ外として残した関連不具合」に、ノード作成・編集のたびにReactFlowの選択状態が失われる不具合（`FlowScreen.tsx`派生useEffectが`itemNodes`再構築時に`selected`プロパティを保持していない）が記録されていた。**実機調査で特定した根本原因**: chrome-devtools MCPでEnter押下直後のDOM状態を直接観測した結果、新規ノードの`<input>`は正しくマウントされ`select()`もされていたが`document.activeElement`にならない（フォーカスが乗らない）ことを確認。さらに調査すると、xyflowはノードの初回サイズ計測が完了するまでノード要素に`visibility:hidden`を付与し（`@xyflow/react`内部の`hasDimensions`判定）、計測完了後に`onNodesChange`の`dimensions`変更として`measured`をnodes stateへ書き戻す仕組みだが、`FlowScreen.tsx`の派生useEffectが`itemNodes`を毎回ゼロから作り直すため、この`measured`（および同じ理由で`selected`も）を全ノード分について即座に握りつぶしていた。その結果、新規ノード作成直後に発生する2回目以降の再構築（依存関係作成の`setDependencies`等）で計測情報が失われ、ノードが`visibility:hidden`に固定され続けてフォーカスが黙って失敗していた（ブラウザは非表示要素へフォーカスできない仕様のため）。手がかりに記載されていた「selected消失」バグと全く同じ「派生useEffectが内部状態を保持しない」パターンが、`selected`だけでなく`measured`（可視化状態）も破壊しており、これがR-080の直接原因と判明。**本番デプロイ・実機検証完了（2026-08-12）**: `upload.ps1`でデプロイ後、本番フローチャート画面で既存ノードを選択しEnterキーで新規ノード作成、新規ノードのタイトル入力欄「新規アイテム」が全選択状態（selectionStart:0/selectionEnd:6）かつフォーカス済みであることをDOM直接確認。検証用ノードはDelete操作で削除し原状回復済み | 完了（2026-08-12デプロイ済み） | 02_機能仕様.md F-24 |
| 2026-08-12 | R-081 | **ガントチャートで日付未配置タスクの視覚的強調**: 発注者要望「ガントチャートでタスクはあるけど日付が決まらず配置されていないタスクは、タスク名の背景色が少し強調されるようにしてほしい。このタスクはまだカレンダー上に置いてないんだなってわかる」。R-078〜R-080と合わせて優先デプロイ指定。仕様: `02_機能仕様.md` F-28。対象は`RyokanGanttView.tsx`の一覧表示（showGroups=false）のタスク名列。**未配置判定**: `prep_date`・`due_date`がともに未設定（両方null/未設定）を「カレンダー未配置」とした。既存のソート順ロジック（`sorting.ts`の`getEffectiveDeadline`がnullを返す条件、および`compareGanttListItems`の「期限なしアイテムが先頭」ルール）と同じ意味になるよう単純な否定チェック（`!item.prep_date && !item.due_date`）を採用（`getEffectiveDeadline`自体はsorting.ts内のprivateヘルパーのため再利用せず、同義の条件を独立実装）。**実装**: タスク名セル（`data-testid="gantt-title-cell-{id}"`）に、未配置時のみ`bg-amber-50 dark:bg-amber-900/20`を付与（既存の`bg-white dark:bg-slate-900`と排他）。title属性で「カレンダーに未配置（日付未設定）」ツールチップも付与。TDD: `RyokanGanttView.unscheduledHighlight.test.tsx`に3件（未配置→強調あり／due_dateのみ設定→強調なし／prep_dateのみ設定→強調なし）追加、Red確認後実装しGreen確認。既存テスト回帰なし（vitest全件779 pass/14 skip、既存の無関係なunhandled errorのみ）。`git diff --stat`は本体2ファイル（RyokanGanttView.tsx +9/-1、新規テスト+81行）のみ、無関係な変更混入なし。実機検証: dev環境（frontend代替ポート5273→backend代替ポート8099、通常ポート5173/8000は他Agent使用中のため一時的にvite.config.tsを検証後に元へ戻す運用で対応）で、debugユーザー既存データのうち「保険を確認する」「保険を確認する3」「個人のフィルタで追加したアイテム１」（いずれもprep_date/due_dateともnull）が琥珀色ハイライト、「保険を確認する2」「買い物に行く」等（due_date設定済み）が通常の白背景であることをDOM（`getComputedStyle`）とDB（sqlite直接照会）の両方で突合し確認。**本番デプロイ・実機検証完了（2026-08-12）**: `upload.ps1`でデプロイ後、本番ガント一覧で日付未配置タスク行の背景に`bg-amber-50`クラス（`rgb(255, 251, 235)`）が付与されていることをDOM直接確認 | 完了（2026-08-12デプロイ済み） | 02_機能仕様.md F-28 |
| 2026-08-12 | R-082 | **詳細画面「その他」メニューのボタン展開（PC・画面幅が広い時）**: 改善要望フォーム経由（2026-08-10 16:08）。「詳細画面の『その他』のボタンから完了とかのボタンが分かれていますが、それを画面幅が広くてボタンの数を増やせるなら、完了やプロジェクト化もその他から出して、ボタンとしてその他ボタンの右に並べてほしい、その他を開いてから完了を押すという手間を減らしたい」。要望原文で明示された「完了」「プロジェクト化」の2項目のみを対象とし、残り3項目（いつかやる/アーカイブ/ゴミ箱）はその他メニューに残す方針で仕様化。**実装完了**: `DecisionDetailModal.tsx` の Footer Action Bar で `!isMobile` の場合、その他ボタンの右に「プロジェクトに変換/解除」「完了」の独立ボタンを追加（既存onClickロジックをそのまま流用）。PC版その他ポップオーバー内からは該当2項目を削除し「いつかやる/アーカイブ/ゴミ箱」のみ残置。モバイル版（`MobileBottomSheet`）は無改修。TDD: `DecisionDetailModal.test.tsx` に失敗するテストを先に追加（独立ボタンのクリックで即時実行される2件・ポップオーバー内に完了/プロジェクトに変換が含まれないことを検証する1件の計3件）→Red確認→実装→Green確認。既存テスト回帰なし確認（vitest全体129ファイル779件pass・14 skip、KeyboardAndButtons.test.tsx実行時の既存unhandled exception 1件はmaster baselineでも同一発生を確認済みで本修正と無関係）。chrome-devtools MCPで実機検証（dev環境、ポート競合回避のため一時的にlocalhost:5273を使用）: デバッグユーザーアカウントでログインし、PC幅（1440×900）で「その他...」ボタンの右に「プロジェクトに変換」「完了」が独立ボタンとして表示され、その他ポップオーバーには「いつかやる/アーカイブ/ゴミ箱」の3項目のみが残ることをスクリーンショットで確認。独立「完了」ボタンをクリックし即座にアイテムが完了処理されモーダルが閉じることを確認。モバイル幅（390×844）ではその他ボタンのみ表示され、ボトムシートに従来通り5項目（プロジェクトに変換/完了/いつかやる/アーカイブ/ゴミ箱）全てが残ることを確認。**本番デプロイ・実機検証完了（2026-08-12）**: `upload.ps1`でデプロイ後、本番詳細画面をPC幅で開き、「その他...」ボタンの右に「プロジェクトに変換」「完了」が独立ボタンとして表示されることを確認 | 完了（2026-08-12デプロイ済み） | 02_機能仕様.md F-29 |
| 2026-08-12 | R-083 | **Youkanのロゴマーク・アイコン作成、ファビコン設定**: 改善要望フォーム経由（2026-08-12 11:05）。「youkanのロゴマークとアイコンをつくって。そしてファビコンを設定して。」プロジェクト名「羊羹」をモチーフにしたシンプルな幾何学的SVGアイコンを3案作成（三分割ブロック／小豆粒／抹茶カット）し指揮AIへ提示、「三分割ブロック」を採用。追加修正2点（縦スリット2本を角丸を避けた内側収まりからブロック端から端まで完全貫通させる／どこかに「Y」＝Youkanの頭文字を入れる）の指示を受け、Y字の入れ方2パターン（スリット自体をY字化／スリット維持＋Y字を内挿）を試作し再提示、指揮AIが「スリット維持＋Y字を内挿」案を確定。**実装完了**: `JWCADTategu.Web/public/favicon.svg`新規作成（角丸ブロック#7A3A2E＋縦スリット2本#F3E4D0が上下端まで貫通＋中央に小さいY字パス）。`index.html`のfavicon linkを`%BASE_URL%favicon.svg`に差し替えようとしたところ、Vite devサーバー限定で`%BASE_URL%`（絶対パス`/contents/Youkan/`）がさらにbase接頭辞で二重化される不具合（本番buildでは単一で正しいがdevでは`/contents/Youkan/contents/Youkan/favicon.svg`になりSPAフォールバックがヒットする）を発見したため、単純な相対パス`href="favicon.svg"`に変更してdev/prod双方の食い違いごと解消。`YoukanHeader.tsx`にfaviconと同一デザインの`YoukanMark`コンポーネントを追加し、モバイル用アイコンロゴ（⚡emoji代替）・デスクトップ用ロゴ（⚡emoji代替、「Youkan」テキストの左）の両方に適用。副次的に`JWCADTategu.Web/.gitignore`のGatsby由来の誤った`public`全体除外設定（`public/favicon.svg`がコミットされない原因になっていた）を発見・削除。既存テスト回帰なし確認（vitest 784 passed/0 failed/14 skipped）、`npm run build`成功（distにfavicon.svg同梱、`href="favicon.svg"`で正しく解決）。claude-in-chrome/chrome-devtools MCPでdev環境実機検証: PC幅でヘッダーロゴ（アイコン+"Youkan"）表示、モバイル幅(390px)でアイコンのみロゴ表示、tab側faviconリンクをfetchしstatus 200・content-type image/svg+xmlで実体のSVGが返ることを確認、コンソールエラーなし。**追加修正・本番デプロイ完了（2026-08-13）**: 本番デプロイ後の実機検証で、フローチャート等のネストルート（`/flows/{id}`）へ直接アクセス・リロードした場合、相対パス`href="favicon.svg"`がブラウザによって現在のURLパス基準で解決され404になる問題を発見。`index.html`のfavicon linkをAppID絶対パス`href="/contents/Youkan/favicon.svg"`直書きに修正し再デプロイ。本番でルート直下・ネストルート直リロード双方でfaviconが200・image/svg+xmlで解決されることを確認 | 完了（2026-08-13デプロイ済み） | 02_機能仕様.md F-30 |
| 2026-08-12 | R-084 | **ガント「前に挿入」「後に挿入」で依存関係も自動的に繋ぎ直す**: 会話内発言（2026-08-12）。「ガントチャートの後に挿入、前に挿入の機能で入力したら、依存関係も上手く挿入されてほしい。タスクA→B→Cという依存関係があり、Bの後に挿入でDを作ったとき、A→B→D→Cという依存関係が生まれるようなイメージ。」指揮AI事前調査済み: 現状`RyokanGanttView.tsx`の`submitInlineInsert()`（R-066実装）は新規アイテム作成＋`ApiClient.reorderItems()`で表示順を並べ替えるのみで、依存関係（Dependency）の作成・繋ぎ変えは一切行っていない。指揮AI方針: 「前に挿入」はsourceItemへの依存（X→source型）を全て新規アイテムへ繋ぎ変え（X→new）、new→sourceを新規作成。「後に挿入」はsourceItemからの依存（source→Y型）を全て新規アイテムから繋ぎ変え（new→Y）、source→newを新規作成。分岐がある場合も既存依存を漏れなく新規アイテム経由に繋ぎ変えるシンプルな規則とする。仕様: `02_機能仕様.md` F-31 | 実装完了・テストGreen（`submitInlineInsert()`に依存関係の削除＋作成による繋ぎ変えロジックを実装。新規テスト5件・既存回帰なし）。**本番デプロイ・実機検証完了（2026-08-13）**: `upload.ps1`でデプロイ後、本番で分離した制御実験用の3アイテム（BRANCHIN→SOURCE→AFTER、SOURCEへの入力エッジと出力エッジを両方持つ構成）を作成し、SOURCEに対して「後に挿入」を実行。結果はBRANCHIN→SOURCE（入力エッジ）が変更されず維持され、SOURCE→NEW→AFTER（出力エッジのみ新規アイテム経由に繋ぎ変え）となることをAPI直接照会で確認、仕様通りの動作。なお実データ（サンプル作るプロジェクト）での最初の検証時に無関係アイテム間へのスプリアスな依存関係生成が一度観測されたが、分離した制御実験では再現せず、原因はブラウザセッション内の複数ビュー往復によるフロントエンド状態の副作用の可能性が高いと判断（詳細は完了報告を参照）。検証用アイテム・依存関係はすべて削除し原状回復済み | 完了（2026-08-13デプロイ済み） | 02_機能仕様.md F-31 |
| 2026-08-12 | R-085 | **フローチャート新規ノード目安時間入力後Enter押下時「Database Error during update」エラー**: 会話内発言（2026-08-12 23:47/23:51）。「フローチャートで新規作成したアイテムにタイトルを決めたアト、目安時間を入力してEnterを押したら以下のエラーが出た。API通信エラー PUT /items/{id}: Database Error during update」。発注者自身の仮説（23:51補足）: 「ノードを選択状態からTabでも新しいノードが作れるし、Enterでも新しいノードが作れる。それとTabによるタイトル入力→目安時間入力への移動と衝突しているかも。ノード選択状態とタイトル入力状態で、Tabの機能を切り替えたほうが良い。ノード選択下状態でTabなら枝分かれで新規ノード追加、タイトル入力状態でTabキーが押されたらそのタイトルがあるノードの目安時間の編集へ移動」。指揮AI事前調査済み: `BaseController.php`の`updateGeneric()`相当処理（277行目付近）がPDOExceptionを捕捉し500 "Database Error during update"を返す実装（323行目）。実際のDBエラー詳細はサーバーの`error_log`にのみ出力される（`[BaseController] Update Error on $table ($id): ...`）ため、実装Agentは本番/devのPHPエラーログを確認して具体的なSQLエラー内容（SQLite `database is locked`等）を特定すること。`handleEstimatedMinutesChange`（`FlowScreen.tsx` 214行目）が`PUT /items/{id}`を発行する経路。発注者の仮説通りTab/Enterの二重発火・競合状態（同一itemIdへの並行PUT、または新規ノード作成の中間状態でのPUT）を疑い、実機で再現条件を特定してから対応方針を決めること。エラーメッセージ改善（原因不明でも「なぜ失敗したか」がユーザーにわかる文言に）も要望に含まれる。仕様: `02_機能仕様.md` F-32 **原因究明・実装完了（2026-08-12）**: 本番`backend/php_errors.log`を実際に確認したところ、報告時刻と一致する`[BaseController] Update Error on items (...): SQLSTATE[HY000]: General error: 5 database is locked`を特定（同種のエラーは09-Apr/06-Jun/31-Jul/12-Augと繰り返し発生していた既知の潜在バグと判明）。ローカルでも`backend/tests/test_completed_at.php`が100%決定的にこの事象を再現することを発見し、`PDO->commit()`自体が失敗元であることを特定。**真因**: SQLiteの既定ジャーナルモード（rollback journal）では、あるコネクションの未消費SELECT（`fetch()`一回だけで`closeCursor()`を呼ばない、このコードベース全体で頻出するパターン）が共有ロックを保持したままになり、別コネクションの`COMMIT`（EXCLUSIVEロックへの昇格）を阻害して即座に失敗させる。発注者の仮説（ノード選択状態のEnter/Tabが同時に新規ノードを作る）は実装調査の結果誤りと判明（`shouldIgnoreKeyEvent`によりINPUT要素からのキー入力はグローバルなノード作成ショートカットの対象外）。ただし「Enterキー1回が2つの書き込みリクエストを同時発火させる」という洞察は的確で、`FlowItemNode.tsx`の目安時間欄Enterハンドラが`onEstimatedMinutesChange`（PUT /items/{id}）と`onChainCreate`（POST /items等の新規ノード作成）をawaitなしで同時発火しており、これが実際の引き金だった。**修正**: (1) `backend/db.php`の`getDB()`に`PRAGMA journal_mode = WAL`を追加（読み取りが書き込みを、書き込みが読み取りをブロックしないWALモードに変更。既存の`enable_wal.php`という一度きりの手動スクリプトが存在したが実際には本番・devとも適用されていなかったため、接続処理自体に恒久的に組み込んだ）。(2) `backend/BaseController.php`の`updateEntity()`がPDOException時にトランザクションを開いたまま`sendError()`（内部で`exit`）を呼んでいた欠陥を修正し、`exit`前に確実に`rollBack()`するよう変更。(3) `FlowItemNode.tsx`の目安時間確定処理を`async`化し、`onEstimatedMinutesChange`の完了を待ってから`onChainCreate`を呼ぶよう逐次化（同時書き込みの発生源そのものを削減）。(4) `ApiClient`（`client.ts`）で500エラー時のメッセージを、原因不明でも分かりやすい「通信エラーが発生しました。しばらくしてから再度お試しください。」に統一（4xx以下は従来通りバックエンドの具体的メッセージを維持）。発注者提案のTab/Enter機能切り替えは、真因が別種の問題だったため不採用（スコープ外）。**指揮AIレビュー指摘対応**: WALモード導入で生成される`*.sqlite-wal`/`*.sqlite-shm`補助ファイルが`backend/.gitignore`・`upload.ps1`の除外パターンに含まれておらず、ローカルのdev用WALファイルがデプロイパッケージへ混入し本番の未チェックポイントコミットを上書きするリスクを指摘され、両方に除外パターンを追加（実機で書き込み中に実際に`-wal`/`-shm`が生成されること、`git check-ignore`と`Copy-Item -Exclude`双方で正しく除外されることを確認済み）。TDD: 新規4件（`test_sqlite_wal_mode.php`、`test_update_entity_transaction_rollback.php`、`FlowItemNode.chainCreateSequencing.test.tsx`、`client.friendlyServerError.test.ts`）をRed確認後実装しGreen確認。既存テスト回帰なし確認（vitest 794 passed/1件`useAssigneeView`既存日付境界依存失敗は無関係の既存事象・14 skip、PHPテスト全件green、`test_completed_at.php`は本修正前は100%決定的に失敗していたが修正後は4/4 green）。実機確認（dev環境、chrome-devtools MCP）: フローチャートで新規ノード作成→タイトル→Tab→目安時間→Enterのチェーン作成フローを3回連続実施し、いずれもエラーなく目安時間保存・新規ノード作成・依存関係接続まで完了することを確認（ネットワークタブは全リクエスト200、コンソールエラーなし）。**本番デプロイ・実機検証完了（2026-08-13）**: `upload.ps1`でデプロイ前にSSHで本番`backend/jbwos.sqlite`の`PRAGMA journal_mode`が`wal`であること、`-wal`/`-shm`補助ファイルが異常な形で残っていないこと、`GET /auth/me`が401（正常応答、DB接続healthy）を返すことを確認してからデプロイ実行。デプロイ後、本番フローチャートでチェーン作成フロー（タイトル→Tab→目安時間→Enter）を5回連続実施したところ、**1回のみ`PUT /items/{id}`が500 "Database Error during update"を返す事象を観測**（コンソールに`[FlowScreen] タイトル更新失敗`ログ）。ただし直後に同一内容のリクエストが200で成功しており（自動リトライか偶発的な再送かは未特定）、最終的にAPI照会でタイトル・目安時間とも正しく保存されていることを確認（データ損失なし）。残り4回のチェーン作成ではエラー再発なし。修正前は同種のエラーが決定的に再現していた（`test_completed_at.php`が修正前100%失敗）ことと比較し、発生頻度は大幅に低減しているが、極めて低頻度の残存事象がある可能性を確認。ユーザー影響は自動リトライにより最小化されているが、念のため次回要望棚卸し時に指揮AIへ申し送り推奨 | 完了（2026-08-13デプロイ済み。極低頻度の残存事象を確認、詳細は完了報告参照） | 02_機能仕様.md F-32 |
| 2026-08-12 | R-086 | **フローチャートでエッジ選択時の視覚的フィードバック強化**: 会話内発言（2026-08-12 23:45）。「フローチャートでエッジをクリックして選択したとき、選択状態だということがわかりづらい。発光するようにしてほしい。」対象: `FlowScreen.tsx`のedge選択状態管理・`dependencyToEdge()`（`@xyflow/react`のedge style）。CSS `filter: drop-shadow(...)`等でのグロー表現を想定。仕様: `02_機能仕様.md` F-33。**実装完了**: `dependencyToEdge()`に`isSelected`引数を追加し、選択中のedgeへ`filter: drop-shadow(0 0 4px #3b82f6) drop-shadow(0 0 8px #3b82f6)`のグロー表現を付与。edge再構築の派生useEffectの依存配列に既存の`selectedEdgeIds`（`onSelectionChange`で取得）を追加し、選択状態が変わるたびにedgeへ反映されるようにした（選択解除で自動的に消える）。TDD: `FlowScreen.edgeSelectionGlow.test.tsx`新規2件（選択時にstyleへfilter付与・解除時に消える）→Red確認→実装→Green確認。既存テスト回帰なし確認（vitest全件137ファイル806件中791 pass・14 skip、唯一の失敗`useAssigneeView.test.ts`は既存の日付境界依存の無関係な事象）。`git diff --stat master...HEAD`はFlowScreen.tsx（+10/-4）とテスト新規146行の2ファイルのみ。claude-in-chrome MCPでdev環境実機検証: フローチャート画面で依存関係のあるノード間のedgeをクリックすると青い発光（drop-shadow）が付与されること、DOM上で`class`に`selected`が付与され`style`に`filter: drop-shadow(...)`が2重に設定されることを確認。別要素（キャンバス空白部）をクリックして選択解除すると発光が消え`filter`が除去されることも確認。検証用に作成したノード・依存関係は削除し原状回復済み。**本番デプロイ・実機検証完了（2026-08-13）**: `upload.ps1`でデプロイ後、本番フローチャートでedgeをクリックし選択、`classList`に`selected`が付与され`filter: drop-shadow(rgb(59, 130, 246) 0px 0px 4px) drop-shadow(rgb(59, 130, 246) 0px 0px 8px)`が適用されることをDOM直接確認 | 完了（2026-08-13デプロイ済み） | 02_機能仕様.md F-33 |
| 2026-08-13 | R-087 | **ガントチャート日付ヘッダーに合計時間数を表示**: 会話内発言（2026-08-13）。「ガントチャートで縦（列）が日付だけど、その日付の下に合計何時間になっているのかを表示する機能をつけて。その日のキャパ割合を色で表すバーが存在するから、その下に時間数を書こうか。単位は時間だ。」指揮AI事前調査済み: `RyokanGanttView.tsx`の日付ヘッダー列（590〜632行目付近）は幅`w-6`(24px)の狭い列で、曜日・日付・`CapacityBar`（R-034実装、4px高さのキャパ割合バー）が縦に並んでいる。`stats.total`（分単位の合計割当時間）は既に`dailyCapacityStats`から取得済みでCapacityBarに渡されている。CapacityBarの直下に時間換算（分→時間表記、小数点の扱いは実装Agentが判断）のテキストを追加する想定。仕様: `02_機能仕様.md` F-34 | 実装完了・テストGreen（既存の`formatMinutes`（`logic/timeParser.ts`、90分→"1.5h"、120分→"2h"、0分→非表示）を再利用しCapacityBar直下に表示。新規テスト3件・既存回帰なし）。**本番デプロイ・実機検証完了（2026-08-13）**: `upload.ps1`でデプロイ後、本番ガント一覧の日付ヘッダーでCapacityBar直下に「8h」「19h」等の合計時間数表示を確認 | 完了（2026-08-13デプロイ済み） | 02_機能仕様.md F-34 |
| 2026-08-13 | R-083 | **バグ修正: フローチャートのネストルート直接アクセス時にfaviconが404になる**: R-083本番デプロイ後の実機検証中にデプロイAgentが発見。`index.html`の`<link rel="icon">`が相対パス`favicon.svg`のため、`https://door-fujita.com/contents/Youkan/flows/{id}`のようなネストしたルートへ直接アクセス・リロードすると、ブラウザが現在URLパス基準（`/contents/Youkan/flows/favicon.svg`）でfaviconを解決しようとしSPAフォールバックがヒットし404相当（content-type text/htmlが返る）になっていた。ルート直下では問題なし。**実装完了**: このプロジェクトはAppID（`Youkan`）が固定でパスが変わらないため、動的解決（`%BASE_URL%`等）は使わず`index.html`のfavicon linkをAppID絶対パス`href="/contents/Youkan/favicon.svg"`に直書き。既存テスト回帰なし確認（vitest 790 passed/14 skipped、既存の無関係なunhandled error 2件はmaster baselineと同一事象）。dev環境（`npm run dev`）とproductionビルド（`npm run build` → `npm run preview`）の両方で、ルート直下・ネストルート（`/contents/Youkan/flows/abc123`相当）双方のHTMLが`href="/contents/Youkan/favicon.svg"`を返し、`curl`で`/contents/Youkan/favicon.svg`が200・image/svg+xmlで解決されることを確認 | 完了 | 02_機能仕様.md F-30 |
| 2026-08-13 | R-088 | **フローチャートのダブルクリックによるノード作成時の依存関係自動設定＋エッジ描画バグ**: 会話内発言（2026-08-13 00:29）。「何もない場所をダブルクリックしたら、新しいノードが作られる。これは便利。ただ、そのとき、なにかノードが選択された状態で、そのノードより上をダブルクリックしたらそのノードの前に行うべきだという依存関係、そのノードより下をダブルクリックしたら、そのノードの後に行うべきだという依存関係ニ設定、ノードが選択されていない状態でのダブルクリックなら、依存関係なしでのノード作成としてほしい。また、こうやって作られたノードの依存関係のエッジが描画されないバグがある。更新ボタンを押したらエッジが表示されます。このバグもなおして。」原文では「ガントチャート」とあるが、指揮AI事前調査済み: 空白部ダブルクリックでノード作成する機能は`RyokanGanttView.tsx`（ガント）には存在せず、`FlowScreen.tsx`（フローチャート）の`handlePaneDoubleClick`（858行目付近）のみに存在する。発注者の呼称違いと判断し、フローチャート画面を対象として仕様化する。現状`handlePaneDoubleClick`は`createNewItem(x, y)`で座標のみ渡してアイテムを作成しており、依存関係・選択状態は一切見ていない。要件: 現在の選択ノード（`selectedNodeIds[0]`）がある場合、ダブルクリック位置のY座標と選択ノードのY座標を比較し、クリック位置が選択ノードより上なら「新規ノード→選択ノード」、下なら「選択ノード→新規ノード」の依存関係を作成する。選択ノードがなければ依存関係なしで作成（現状維持）。**エッジ描画バグに関する注意**: 依存関係作成を行う既存の4経路（onConnect/createNodeBelow/handleEdgeInsert/ドラッグ重なり自動接続）は全て`appendDependencyToState()`（R-074で新設、setDependencies+setEdges同時更新）を経由しているため即座にedgeが反映される。今回新設する5つ目の経路（ダブルクリック作成時の依存関係）も同じ`appendDependencyToState()`を必ず経由させること（別実装すると同種のedge非表示バグを再現する）。仕様: `02_機能仕様.md` F-35。**実装完了（2026-08-13）**: `feature/R-088-pane-doubleclick-dependency`ブランチ（worktree: `.claude/worktrees/agent-ac6f515dd8135e25e`、master `7ac1b95`ベース、コミット`13d2ea2`）。`FlowScreen.tsx`の`handlePaneDoubleClick`で`selectedNodeIds[0]`から`allItems.find()`で選択アイテムを取得し、そのmeta.flow_yとクリック位置のY座標を比較して依存関係の向きを決定、既存4経路と同じ`dependencyRepo.createDependency()`→`appendDependencyToState()`を経由させてedgeを即時反映する実装。TDD: 新規テスト3件（`FlowScreen.paneDoubleClickDependency.test.tsx`）をRed確認後実装しGreen確認。既存回帰確認（vitest全件822件中1件失敗は`useAssigneeView.test.ts`で本タスク非関連ファイル・分岐元でも同じ失敗を再現し無関係と確認済み）。実機検証（chrome-devtools MCP、dev環境）: 選択ノードより上でダブルクリック→新規ノードが前提（source=新規/target=選択）、選択ノードより下でダブルクリック→選択ノードが前提（source=選択/target=新規）、選択なしでダブルクリック→依存関係なし、の3パターン全てをネットワークリクエストで確認。edgeはリロード不要で即時描画され、実際にページリロード後も依存関係が正しく永続化されていることを確認。検証用に作成したテストノードはAPI経由で削除し原状回復済み。指揮AIレビュー承認済み、masterへマージ。**本番デプロイ・実機検証完了（2026-08-13）**: `upload.ps1`でデプロイ後、本番フローチャートで選択ノードより上/下でのダブルクリック（source/target方向が期待通り）、選択なしでのダブルクリック（依存関係なし）の3パターンをAPI照会で確認。なお、これとは別に本プロジェクトには既存の「Phase5自動チェーン追加」機能（`ItemController.php::autoChainAndPlace()`、プロジェクト内でアイテム作成時にチェーン末尾ノードへ自動的に依存関係を張る仕様、R-088より前から存在）が働くため、検証時に新規アイテムへ「葉っぱを加工する→新規アイテム」という一見無関係な依存関係が付随して見えたが、これはR-088のロジックではなく既存Phase5機能による意図された挙動と判明（コード調査で確認）。テストデータはすべて削除し原状回復済み | 完了（2026-08-13デプロイ済み） | 02_機能仕様.md F-35 |
| 2026-08-13 | R-089 | **R-085（フロー目安時間更新エラー）低頻度残存事象の追加調査**: 2026-08-13デプロイAgent報告。R-085のWALモード化・書き込み逐次化デプロイ後の本番実機検証で、チェーン作成（タイトル→Tab→目安時間→Enter）5回連続実施のうち1回、低頻度で同種の「Database Error during update」（500）が再発した。直後の同一内容リクエストは200で成功し、最終的にデータは正しく保存されていた（データ損失なし）。修正前はほぼ決定的に再現していたため大幅に改善しているが、完全解消はできていない可能性がある。実装Agentは`docs/handover/R-085-analysis.md`（もしAgentが作成していれば）または`docs/requests_log.md` R-085の記載を先に読み、WALモード化・busy_timeout・トランザクションrollback修正・Enter時の逐次化という既存の対策を把握した上で、それでもなお発生しうる別の競合経路がないか調査すること（例: 目安時間更新以外の並行書き込み、busy_timeout設定値が短すぎる可能性等）。低頻度のため自動化での確実な再現は困難な可能性が高く、まず本番/devのエラーログで直近の発生パターンを確認してから対応方針を決めること **原因究明・実装完了（2026-08-13）**: 本番`backend/php_errors.log`をSSHで確認したところ、R-085デプロイ後も09-Apr/06-Jun/31-Jul/12-Augと同種の「database is locked」が散発しており、直近では2026-08-13 01:29:04に`PUT /items/{id}`が同一item idへ同一秒に2件発火し、後着側が失敗していることを特定（R-085実機検証時に観測された事象そのもの）。**真因**: `FlowItemNode.tsx`の目安時間入力欄は`onKeyDown`(Enter)と`onBlur`の両方に`handleTimeEditConfirm`を配線していたが、Enter確定後に保存が完了して`isTimeEditing`が`false`になりinput要素がDOMから削除される際、実ブラウザの標準挙動としてフォーカス中要素の除去に伴い`blur`イベントが発火する。この時、削除される直前のinput要素に紐づいた（＝更新前の`item.estimatedMinutes`をクロージャで保持した）古い`handleTimeEditConfirm`が再度呼ばれ、同一itemIdへ`PUT /items/{id}`が二重発火していた。R-085で対処した「PUT×POSTの並行書き込み」とは別に、「PUT×PUT（同一エンティティへの二重書き込み）」という新しい競合経路であり、WALモード・busy_timeout・逐次化のいずれの対策も対象外だった。**修正**: `FlowItemNode.tsx`に編集セッション単位（開始→確定/キャンセル）で確定を1回のみに制限する`hasConfirmedRef`を追加し、`handleTimeEditConfirm`の冒頭でガード。二重発火そのものを消去する根本修正のため、フロントエンドの自動リトライ機構は不要と判断し追加しなかった。TDD: `FlowItemNode.chainCreateSequencing.test.tsx`に新規1件（Enter確定直後にonBlurが発火しても保存は1回のみ）を追加しRed確認（`toHaveBeenCalledTimes(1)`に対し実際は2回）→実装→Green確認。既存テスト回帰なし確認（vitest全件805 passed/14 skipped、失敗3件は`useAssigneeView`日付境界依存・`PanoramaBoard.showGroups`タイムアウト・`KeyboardAndButtons`内既存unhandled exceptionでいずれも本修正と無関係な既存事象）。`git diff --stat`は`FlowItemNode.tsx`(+14)と対応テスト(+35)の2ファイルのみ。**本番デプロイ・実機検証完了（2026-08-13）**: `upload.ps1`でデプロイ後、本番フローチャートでチェーン作成フロー（タイトル→Tab→目安時間→Enter）を5回連続実施し、いずれもエラーなく正しいタイトル・見積時間で保存されることを確認（console.errorフックでも新規エラー検出なし）。R-085デプロイ時に観測された低頻度の500エラーは今回の5回では再現せず、R-089の修正で解消されたと判断。検証用アイテムはすべて削除し原状回復済み | 完了（2026-08-13デプロイ済み） | 02_機能仕様.md F-32 |
| 2026-08-13 | R-090 | **フローチャートのズームアウト制限緩和＋「全体」ズーム・パンボタン新設**: 会話内発言（2026-08-13 00:22）。「フローチャート画面のズームアウトの制限をもっとゆるめて、遠くまでズームアウトできるようにしてほしい。また、ヘルプのボタンの下に『全体』がちょうど入る位置にズーム・パンするボタンをつくってほしい。」対象: `FlowScreen.tsx`の`ReactFlow`コンポーネント（`minZoom`等のprops）・ヘルプボタン周辺のUI配置。仕様: `02_機能仕様.md` F-36 **実装完了**: `ReactFlow`の`minZoom`を既定値0.5から0.05へ緩和（未設定=既定0.5だったものを明示的に指定）。ヘルプボタン（`top-3 right-3`）の直下（`top-12 right-3`）に「全体」ボタンを新設。クリックで`fitView({ duration: 300, padding: 0.1 })`を実行し既存のfitView呼び出し箇所と同じオプションに揃えた。TDD: `FlowScreen.zoomControls.test.tsx`新規3件（minZoom緩和の検証・ボタン表示・クリックでfitView呼び出し）→Red確認→実装→Green確認。既存テスト回帰なし確認（vitest全件822件中807 pass・14 skip、唯一の失敗`useAssigneeView.test.ts`は既存の日付境界依存の無関係な事象）。`git diff --stat master..HEAD`はFlowScreen.tsx（+10/-1）とテスト新規105行の2ファイルのみ。chrome-devtools MCPでdev環境実機検証: デバッグユーザーアカウントでログインしフローチャート画面（個人のプロジェクト1、全て自動配置後）を開き、ズームアウトボタンを連打してscale=0.05（旧上限0.5の10倍緩和）まで到達しZoom Outボタンが自然にdisabled化されること、ノードが極小の点まで縮小表示されることを確認。その状態から新設「全体」ボタンをクリックすると全ノードが画面内に収まるようfitViewされることを確認。コンソールエラーなし。**本番デプロイ・実機検証完了（2026-08-13）**: `upload.ps1`でデプロイ後、本番フローチャートでズームアウトボタンを連打しscale=0.05まで到達することを確認（旧上限0.5からの緩和を確認）。「全体」ボタンは`fitView({duration:300,padding:0.1})`を呼ぶ実装であることをコードで確認済み（xyflow標準API呼び出し）。ボタンクリック後の視覚的な収まり具合は、本番デプロイAgentのブラウザセッションで画面表示ジオメトリが取得できない環境上の制約（`window.innerWidth`等が0を返す状態、RDPセッション未接続の可能性）により今回のセッションでは視覚確認できなかった。zoom/fitView自体はxyflowライブラリの標準機能を正しく呼び出しているため機能的には問題ないと判断するが、次回何らかの形で画面が見える状態の際に目視確認を推奨 | 完了（2026-08-13デプロイ済み。「全体」ボタンの視覚確認は環境都合で未実施、次回目視推奨） | 02_機能仕様.md F-36 |
| 2026-08-13 | R-091 | **依存関係順ソートを全ビューへ展開**: 会話内発言（2026-08-13 03:53）の一部。「全体一覽画面、状況把握画面、グリッド、タイムラインにおいても、ガントと同じように依存関係のあるタスクは、その依存関係による前後の序列を崩さずに並ぶ。」指揮AI事前調査済み: R-076で実装した依存関係考慮ソート（`hierarchy.ts`の`sortWithDependencies`）は既に汎用ロジックとして存在し、共通関数`buildHierarchicalList()`の内部（`showGroups=false`時のみ）で`compareGanttListItems`を使って呼ばれている。`buildHierarchicalList()`自体はガント（`RyokanGanttView.tsx`）・全体一覧（`useOverviewItems.ts`）・状況把握（`BucketColumn.tsx`、showGroups=true時のみ）で共用されているが、`dependencies`引数の扱いが画面ごとに異なる: (1) 全体一覧は`useOverviewItems.ts`45行目で`dependencies: []`が固定（R-048で「OverviewBoardでは依存関係を考慮しない、起動時APIコスト削減のため」と明記されたコメントあり）、(2) 状況把握はshowGroups=trueのときのみ`buildHierarchicalList`を使うが`dependencies`未指定（デフォルト非考慮）、showGroups=falseのときは別関数`sortItemsHierarchically`（依存関係非対応）を使用。グリッドビュー・タイムラインビューの実装状況は未調査。**重要な設計判断**: 全体一覧で依存関係データを反映するには`/dependencies`の追加取得が必要になり、R-048で意図的に避けた起動時APIコストが増える可能性がある。実装Agentは着手前にこのトレードオフを認識し、影響が大きい場合は指揮AIに確認すること。仕様: `02_機能仕様.md` F-37 | 実装完了（マージ・デプロイは指揮AIレビュー待ち）。調査結果: グリッド・タイムラインは独自の並び替えロジックを持たず、`RyokanCalendar.tsx`が`QuantityEngine.calculateMetrics()`で作る共通の`metrics.contributingItems`（`CalendarCell`の日別チップ・内訳パネルの元データ）をそのまま描画しているだけだった。対応: (1) `hierarchy.ts`の`sortWithDependencies`を`export`し`sortItemsHierarchically`にも`dependencies`引数を追加、(2) `QuantityEngine.ts`の`QuantityContext`に`dependencies`を追加し`calculateVolume()`内で日別contributorsを依存関係順に並べ替え（グリッド・タイムライン両方に自動反映）、(3) `useOverviewItems.ts`（全体一覧）・`PanoramaBoard.tsx`（状況把握）・`RyokanCalendar.tsx`（グリッド・タイムライン共通）にそれぞれ`DependencyRepository.getDependencies()`をマウント時1回だけ呼ぶ`useEffect`を追加（R-048の「起動時APIコスト増を避ける」制約は「画面表示時1回取得は許容」の範囲内で上書き）。実機検証: 依存関係のあるテストアイテム2件（pred→succ）を作成し、全体一覧・状況把握（プロジェクト別/一覧の両モード）・グリッドで画面上に前後の序列が保たれることをスクリーンショットで確認、タイムラインはDOM直接検証（`cal-chip-*`のDOM順序）で確認（グリッドと同一の`CalendarCell`/`QuantityEngine`を共有するため実装上は同一コードパス）。テスト: 新規テスト24件追加、既存回帰なし（vitest 836件中835件パス、1件は無関係な既存flaky）。**本番デプロイ・実機検証完了（2026-08-13）**: `upload.ps1`でデプロイ後、本番の「サンプル作る」プロジェクト既存チェーン（デザインを決める→太さ決める→細い部材をつくる）で、全体一覧・状況把握の両ビューでDOM順序が期待通りであることを確認。グリッド・タイムラインは新規コンソールエラーなし（既存の無関係なGoogle連携エラーのみ）で表示され、共通コードパス（`QuantityEngine`/`CalendarCell`）経由のため実装上健全と判断 | 完了（2026-08-13デプロイ済み） | 02_機能仕様.md F-37 |
| 2026-08-13 | R-092 | **全体一覧の右クリックメニュー統一＋挿入時の依存関係自動構築**: 会話内発言（2026-08-13 03:53）の一部。「全体一覽画面のアイテムを右クリックした時に、ガントチャートとおなじ右クリックメニューが出るようにしてほしい、もちろん全部機能して。項目も全部同じ、後に挿入と前に挿入も同じようにつくって。（中略）また、これら挿入では依存関係が構築されるのも同じ。」指揮AI事前調査済み: 全体一覧（`OverviewBoard.tsx`）は現状、ガントの`buildItemContextMenuActions`（完了/プロジェクト化/いつかやる/アーカイブ/削除/前に挿入/後に挿入等の多機能メニュー、R-078でショートカット対応済み）とは全くの別実装で、`useItemContextMenu.ts`という「削除（Delete）のみ」の簡易フックを使っている。要望を満たすには`OverviewBoard.tsx`の右クリックメニューを`buildItemContextMenuActions`ベース（`ContextMenu.tsx`共通コンポーネント）に置き換える必要がある。挿入時の依存関係自動構築はR-084（ガント「前に挿入」「後に挿入」の依存関係繋ぎ変え）と同等のロジックを、全体一覧側の挿入実装（存在すれば）に適用する。連続インライン入力UX（タイトル確定→次のインラインボックス出現→目安時間フォーカス連鎖）は別要望として本件のスコープ外。仕様: `02_機能仕様.md` F-38 **実装完了・レビュー待ち**: `feature/R-092-overview-context-menu` ブランチで対応。`OverviewBoard.tsx` の右クリックメニューを `useItemContextMenu` の独自アクション配列から `buildItemContextMenuActions`（ガントと共通）に置き換え、詳細/名前変更・プロジェクト化・前に挿入(a)・後に挿入(b)・今日やる・完了にする(d)・断る・ゴミ箱(Del) をガントと同一構成にした。前/後挿入は `InlineAddRow` を再利用したインライン入力で新規アイテムを作成し（`viewModel.throwIn`）、R-084相当の依存関係自動繋ぎ変え（挿入位置に応じて既存の依存関係を削除し新規アイテム経由で再作成）を実装。既存の `useItemContextMenu` によるグローバルDeleteキー機能（メニューを閉じた後も直前右クリックしたアイテムを削除できる）は変更せず維持。TDD: `OverviewBoard.contextMenu.test.tsx` を新規作成（メニュー項目一覧・完了/削除アクション・Deleteキー回帰・前後挿入と依存関係自動構築のR-084シナリオの計7件）→ Red確認 → 実装 → Green確認。vitest全件実行（144ファイル833件、うち1件 `useAssigneeView.test.ts` は本修正と無関係の既存失敗と確認済み）。chrome-devtools/claude-in-chrome MCPでdev環境（localhost:5173/8000）実機検証: 右クリックメニューの項目一致、後に挿入→依存関係1本作成（`POST /items`→`GET /dependencies`→`POST /dependencies`）、前に挿入→既存依存関係の繋ぎ変え（`DELETE /dependencies/...`→`POST /dependencies`×2）、メニューを閉じた後のDeleteキーでの削除、を確認。検証用アイテムは作成後にゴミ箱へ移動し後片付け済み。masterへのマージは指揮AIレビュー後 **【方針転換・再対応 2026-08-13】**: 指揮AIレビュー後、発注者確認の結果「既存項目（いつかやる/アーカイブ/とりかかる/保留/待機）も残しつつ統合」を選択。`buildItemContextMenuActions`への全面置き換えを撤回し、元のハードコード配列（プロジェクト化/今日やる(Focus)/とりかかる(Execute)/保留(Pending)/💭いつかやる(Someday)/待機(Waiting)/完了にする(d)/アーカイブ/ゴミ箱(Del)）を維持したまま「前に挿入 (a)」「後に挿入 (b)」の2項目のみ追加（`ContextMenu.tsx`のshortcutフィールドでa/bキーも割り当て）。挿入時の依存関係自動構築ロジック（`submitInlineInsert`/`DependencyRepository`経由）は変更なし。テストは既存項目（いつかやる/アーカイブ）のクリック動作確認を追加し9件全件Green、vitest回帰なし、tscビルド成功。実機検証は既存項目維持＋前後挿入追加のメニュー表示をスクリーンショットで確認済み。**指揮AIレビュー承認・master マージ済み（2026-08-13）**。**本番デプロイ・実機検証完了（2026-08-13）**: `upload.ps1`でデプロイ後、本番全体一覧でアイテムを右クリックし、既存9項目（プロジェクト化/今日やる/とりかかる/保留/いつかやる/待機/完了にする/アーカイブ/ゴミ箱）に加え「前に挿入 (a)」「後に挿入 (b)」が表示されることを確認。bキーで「後に追加...」インライン入力欄が開き、作成したテストアイテムに正しい向きの依存関係（既存アイテム→新規アイテム）が自動構築されることをAPI照会で確認。既存項目「💭いつかやる」もテストアイテムでクリックし、ステータスが正しく`someday`に変わることを確認。検証用アイテムはすべて削除し原状回復済み | 完了（2026-08-13デプロイ済み） | 02_機能仕様.md F-38 |
| 2026-08-13 | R-093 | **技術的負債: vite.config.js/vite.config.d.tsのコミット済み残骸を削除**: 複数Agent（R-081, R-086担当）が実機検証中に発見・報告。`JWCADTategu.Web/vite.config.js`・`vite.config.d.ts`という過去に`tsc`等で生成されたコンパイル済みファイルがgit管理下に残っており、Viteの設定ファイル探索順で`vite.config.ts`より優先して読み込まれる。指揮AI確認済み: 現状2ファイルの内容はほぼ同一（構文の新旧差のみ）で実害はないが、将来`.ts`側を編集しても`.js`側が優先され反映されない不具合の原因になりうる。対応: 両ファイルをgit管理から削除し`.gitignore`へ追加 **実装完了（2026-08-13）**: `chore/R-093-remove-stale-vite-config-artifacts`ブランチで対応。`git rm`で両ファイルを削除し、`JWCADTategu.Web/.gitignore`に`vite.config.js`・`vite.config.d.ts`を追加（再発防止）。削除後`npm.cmd run build`（`tsc -b && vite build`）を実行し、`tsc -b`は両ファイルを再生成しないこと・`vite build`が`vite.config.ts`のmanualChunks設定（vendor-react等の分割）を正しく反映してビルドが成功することを確認。`npm.cmd run dev`も`vite.config.ts`のbase設定（`/contents/Youkan/`）を正しく読み込み起動することを確認。vitest全件回帰確認（142ファイル830 pass・14 skip、失敗1件は`DecisionDetailModal.test.tsx`関連の既存テスト分離起因のuncaught exceptionで本修正と無関係。単体実行では該当ファイル全件Green）。masterへのマージは指揮AIレビュー後。**本番デプロイ確認完了（2026-08-13）**: `upload.ps1`でデプロイ後、本番トップ画面が通常通り表示・動作すること、新規コンソールエラーがないこと（既存の無関係なGoogle連携エラーのみ）を確認。設定ファイルのみの変更のため詳細な機能検証は省略 | 完了（2026-08-13デプロイ済み） | — |
| 2026-08-13 | R-094 | **ガント・全体一覧の前後挿入で連続インライン入力UX（目安時間フォーカス連鎖）**: 会話内発言（2026-08-13 03:53）の一部。「前・後に挿入でインライン入力のテキストボックスが現れ、そこに入力してEnterで確定した場合はその後に挿入するために続けてインラインテキストボックスが出現する。と同時に、いま確定したタイトルの行の目安時間入力にフォーカスして入力状態になる。そこもEnterで確定したら、次は先程出現して空欄のままのインラインボックスにフォーカスし、入力状態になる。もし出現した状態のインラインテキストボックスが入力がされなければその行を保存せずに消える。」対象範囲確定（発注者確認済み）: ガント（`RyokanGanttView.tsx`のsubmitInlineInsert/前に挿入・後に挿入）・全体一覧（`OverviewBoard.tsx`のsubmitInlineInsert、R-092で実装）の両方。指揮AI事前調査済み: R-074（`FlowItemNode.tsx`/`FlowScreen.tsx`）に概念的に同一のチェーン作成UX（タイトル確定→Tab→目安時間欄フォーカス→Enter→次ノード作成）が既に実装済みで参考にできる（`chainOnConfirm` state、`onChainCreate`コールバックのパターン）。目安時間のインライン編集自体は既存機能（F-17、`OverviewItem.tsx`151/216/224行目付近に`formatMinutes`ベースの実装あり、ガント側にも`RyokanGanttView.tsx`に`timeInputRef`ベースの実装あり）として両画面に既に存在するため、これを流用できる。仕様: `02_機能仕様.md` F-39 | R-094-A（ガント側`RyokanGanttView.tsx`）・R-094-B（全体一覧側`OverviewBoard.tsx`）ともに実装完了・master反映済み。**本番デプロイ・実機検証完了（2026-08-13）**: `upload.ps1`でデプロイ後、本番のガント一覧・全体一覧の両方で、アイテム右クリック→「後に挿入」→タイトル入力しEnter→作成された行の目安時間欄（placeholder="1h"）へ自動フォーカスされ同時に次の空インライン行（"後に追加..."）が出現→目安時間入力しEnter→データがAPI照会で正しく保存（ガント側30分・全体一覧側20分）されフォーカスが次の空インライン行に移ることを確認。次の空行を未入力のままfocusoutすると行が消えることも確認（`blur()`のみでは反応せず`focusout`イベントで消えることをDOM操作で確認）。コンソールエラーは新規発生なし（既存の無関係なGoogle連携エラーのみ）。検証用アイテムは依存関係含めすべて削除し原状回復済み | 完了（2026-08-13デプロイ済み） | 02_機能仕様.md F-39 |
| 2026-08-13 | R-096 | **社内共通認証基盤（auth-hub）の新規構築＋Youkanの移行**: 原文（意訳）「Youkan・DotLog・Beaver・catalog-systemなど、ログイン機能の有無がバラバラなサービス全部を、共通の認証で連携できないか。Googleログインがしたい。どれか1つのサービスでGoogleログインすれば、他の藤田建具店のサービスもそのログイン状態を利用してログイン状態を維持できるようにしたい。1箇所ログインすれば他のサービスもログインしている状態にしたい。」起点は2026-08-06のBeaver設計提案（`Beaver/docs/wiki/knowledge/auth_architecture_proposal_20260806.md`）。追加決定: ①Google＋パスワード併用、既存パスワードユーザーのセルフサービスGoogleリンクも追加 ②今回はYoukanまで、DotLog/Beaver/catalog-systemは各`requests.md`に将来対応を記録。認証専用の新規リポジトリ`C:\Fujiruki\Projects\auth-hub`（ポート5186/8009）を新設し、TDDで実装（全8テストファイルGreen）。Youkan側は「置き換え」ではなく「追加」方式（発注者確認済み: 会社アカウントログイン・自己サインアップは廃止せず維持、個人ログイン画面に「社内共通ログイン（Google対応）」を追加）。`BaseController::authenticate()`が`df_session`（auth-hub）と既存JWTの両方を受け付ける設計。既存ユーザーのbcryptハッシュはそのまま移設可能と実証（パスワードリセット不要）。ブランチ`feature/R-096-auth-hub-migration`→`master`へfast-forwardマージ（`eb304c8`）。バックエンドテスト7件・フロントエンドVitest841件全Green。仕様書: `auth-hub/docs/SPEC/01_認証仕様.md`。**本番デプロイ・実機検証完了（2026-08-13〜14）**: 本番Youkan DBバックアップ取得後、auth-hubを本番へ新規デプロイ（`https://door-fujita.com/contents/auth/`）、実ユーザー9名を`migrate_users_to_auth_hub.php --apply`で本番移行（bcryptハッシュそのまま、パスワードリセット不要）、Youkanをmasterマージ後本番デプロイ。本番でのクロスアプリ認証を実機検証（auth-hubでログイン→`df_session`のみでYoukanの`/api/auth/me`が同一ユーザーに解決されることを確認）。既存のJWTパスワードログイン・会社アカウントログイン・自己サインアップの回帰なしも確認。**デプロイ作業中に副次的に発見した問題も対応**: (1) 本番`backend/jbwos.sqlite`（全ユーザーのbcryptハッシュ含む）が`.htaccess`の穴で認証不要で直接ダウンロード可能だった件を発見・`.htaccess`に`*.sqlite`等の拒否ルールを追加し即日修正（403化を実機確認）。(2) 本番サーバー手前のWAF（SiteGuard）がフォームPOSTの`redirect`パラメータを値のパス形式ゆえに除去し、共通ログイン後にYoukanへ戻れないバグを発見→戻り先をPOSTボディではなくログインフォームの`action`クエリ（`?rd=...`）に載せる方式へ変更し解決（本番で302リダイレクト確認）。SMTP（ConoHa WING、`noreply@door-fujita.com`）・Google OAuthクライアント（発注者がGoogle Cloud Consoleで発行）も本番`.env`に設定、パスワードリセットメール送信・Google認可画面への遷移を実機確認。管理者アカウント（`fjt.suntree@gmail.com`）を`is_admin=1`に昇格し`/admin/users`の権限ガード（未ログイン302・一般ユーザー403）を確認 | 完了（2026-08-14本番デプロイ・実機検証完了） | `auth-hub/docs/SPEC/01_認証仕様.md`, `04_データ設計.md` §3.2・§5.2 |
| 2026-08-13 | R-095 | **バグ: 全体一覧「その他」ドロップダウンで`action.onClick is not a function`例外**: R-094-B実装Agentが実機検証中に偶発的に踏んで報告。指揮AI原因特定済み: `OverviewBoard.tsx`の右クリックメニュー配列（R-092で復元したハードコード配列）に`{ separator: true }`という要素が2箇所（プロジェクト化/挿入系とステータス変更系の間、ステータス変更系と削除系の間）含まれているが、この要素は`onClick`プロパティを持たない。一方`ContextMenu.tsx`（共通コンポーネント）の`ContextMenuAction`インターフェースは`separator`フィールドをそもそもサポートしておらず、`actions.map()`内で全項目間に`index > 0`ベースで自動的に薄い区切り線を挿入する設計になっている。そのため`{ separator: true }`はセパレータとして機能せず、`onClick`を持たない「空のボタン」としてそのまま描画され、クリックされると`action.onClick()`（`ContextMenu.tsx` 103行目）が`undefined()`呼び出しとなり例外が発生する。対応方針: `OverviewBoard.tsx`の`{ separator: true }`要素2箇所を削除する（`ContextMenu.tsx`が既に全項目間へ自動でセパレータを描画するため、削除しても視覚的な区切り自体は失われない。グループ単位の強めの区切りが均等な薄い区切りに変わる程度の軽微な見た目の違いのみ）。**実装完了（2026-08-13）**: `fix/R-095-overview-contextmenu-separator-crash`ブランチで対応（master `bf66eb2`ベース）。`OverviewBoard.tsx`の`{ separator: true }`要素2箇所を削除。あわせて、削除後は型が正しく合うため`actions`プロパティに付いていた`.filter(Boolean) as any`の型キャストも除去（このキャストが`ContextMenuAction`インターフェースと合わない要素を型検査ですり抜けさせていたため、根本原因の一つとして併せて解消）。TDD: `OverviewBoard.contextMenu.test.tsx`に新規2件（メニュー内の全ボタンにonClickとラベルがあることの検証・旧separator位置に隣接する項目「今日やる」「アーカイブ」のクリック回帰確認）を追加しRed確認（新設した「全ボタンにラベルがある」テストが空文字ボタンの存在で失敗することを確認）→実装→Green確認。`tsc --noEmit`エラーなし。既存テスト回帰なし確認（vitest全件148ファイル853件中839 pass・14 skip、0 failed。既存の無関係なunhandled error 2件（`DecisionDetailModal`/`KeyboardAndButtons`関連、本修正の対象外ファイル）はmaster baselineと同一の既存事象）。`git diff --stat master..HEAD`は`OverviewBoard.tsx`(+1/-3)とテスト新規(+25)の2ファイルのみ。chrome-devtools MCPでdev環境実機検証: 全体一覧の右クリックメニューに空ボタン（旧separator）が表示されないことを確認、旧separator位置に隣接する「今日やる」「アーカイブ」を含むメニュー全11項目（プロジェクト化/前に挿入/後に挿入/今日やる/とりかかる/保留/いつかやる/待機/完了にする/アーカイブ/ゴミ箱）を複数アイテムに対して順にクリックし、いずれもJS例外なし・API成功（200番台）を確認。マージ・本番デプロイは指揮AIレビュー後。**本番デプロイ・実機検証完了（2026-08-13）**: `upload.ps1`でデプロイ後、本番全体一覧で複数のテストアイテムに対し右クリックメニュー全11項目（プロジェクト化/前に挿入(a)/後に挿入(b)/今日やる/とりかかる/保留/いつかやる/待機/完了にする/アーカイブ/ゴミ箱）を実際にクリックし、旧separator位置に隣接する「今日やる」「アーカイブ」を含めいずれも例外なし・console.errorフックでも新規エラー検出なしを確認。アーカイブは一覧から正しく除外、プロジェクト化は`is_project=1`への変更をAPI照会で確認。検証用アイテムはすべて削除し原状回復済み | 完了（2026-08-13デプロイ済み） | — |
| 2026-08-14 | R-097 | **ガントビューにマンスリー/ウィークリー表示モード切替＋列幅・行高さの独立記憶スライダー**: 原文（意訳、2回のメッセージ）。1回目:「現在あるガントチャートに、1日の幅を変更するスライダーと行間を変更するスライダーを追加してほしい。現在の見え方自体に不満はないが、調整機能をつけることで使い方が広がると思う。マンスリー表示とウィークリー表示の2種類に分け、ウィークリー表示に切り替えたらスライダーが表示され、その調整幅は記憶されてほしい。マンスリーに戻ってまたウィークリーに戻っても、前回ウィークリーで調整した幅が再現されてほしい」。2回目（方針修正、行高さスライダーが既存と判明した後）:「マンスリー表示とウィークリー表示の切り替えは作りたいが、どちらの表示でも行方向のスライダーと列方向のスライダー両方を常に表示しておいてほしい。マンスリーのときの列幅・行高さ、ウィークリーのときの列幅・行高さ、というようにメモリ機能のように使いたい」。指揮AI事前調査済み: `CalendarHeader.tsx`に既存の行高さ（密度）スライダー（12〜32px、`localStorage`キー`youkan_gantt_row_height`）はあるが、`RyokanGanttView.tsx`の日付列幅（`colWidth`）は`24`固定でスライダー化されていない。仕様: `03_画面設計.md` §5.3。**実装完了（2026-08-14）**: `feature/R-097-098-gantt-scale-and-print`ブランチ（worktree `.claude/worktrees/R-097-098`、master `de13355`ベース）で対応。実装Agentが実機検証中に重要な発見: ヘッダー「カレンダー→ガント」の実体は`VolumeCalendarScreen.tsx`（`App.tsx`の`currentView==='calendar'`ルーティング先）であり、当初の事前調査が前提としていた`DashboardScreen.tsx`内部の`viewMode==='calendar'`分岐はUI導線が存在せず到達不能な死んだコードパスと判明。指揮AI確認の上、配線先を`VolumeCalendarScreen.tsx`へ変更する方針に修正して実装（`DashboardScreen.tsx`は無改変に戻した）。`youkanKeys.ts`に`GANTT_SCALE_MODE`/`GANTT_COL_WIDTH_MONTHLY`/`GANTT_ROW_HEIGHT_MONTHLY`/`GANTT_COL_WIDTH_WEEKLY`/`GANTT_ROW_HEIGHT_WEEKLY`の5キー追加。`CalendarHeader.tsx`にマンスリー/ウィークリー切替トグルと列幅スライダー（16〜80px）を追加、既存密度スライダー（12〜32px）と横並びで常時表示。`RyokanGanttView.tsx`の`colWidth`ハードコード24を実propに変更（4箇所の固定`w-6`→動的`style`）、従来無視されていた`rowHeight`propも実際に使用するよう修正（2箇所の固定`h-7`→動的`style`、`GanttDependencyArrows`への`rowHeight`ハードコード28も実propに変更）。`RyokanCalendar.tsx`/`RyokanCalendarTypes.ts`に`ganttColWidth`/`ganttRowHeight`propを追加。`VolumeCalendarScreen.tsx`にスケールモード別のstate管理（ローカルstate、`useVolumeCalendarViewModel`は不変更）・`localStorage`永続化・モード切替時の記憶値復元ロジックを実装（従来`rowHeight={24}` `onRowHeightChange={()=>{}}`で「VolumeCalendar doesn't support rowHeight yet」だった箇所を解消）。マンスリー初期値は既存動作を完全維持（列幅24px・行高さ28px相当）。TDD: `CalendarHeader.test.tsx`（新規20件）・`RyokanGanttView.colWidth.test.tsx`（新規3件）・`VolumeCalendarScreen.ganttScale.test.tsx`（新規6件、既存ファイルと同じ「ロジック抽出」方式）を先にRed確認後実装しGreen確認。既存テスト回帰なし（vitest全件858 passed / 14 skipped、既存の無関係な1件失敗`useAssigneeView.test.ts`はmasterでも同一失敗を確認済み）。claude-in-chrome MCPで実機検証（dev環境、chrome-devtools MCPはセッション内で切断されたため代替使用）: ヘッダー→カレンダー→ガントで実際の画面に遷移し、ウィークリーに切替→列幅を24→68・密度を28→14に調整→マンスリーに戻す（24/28に復帰、ウィークリー側の値に影響なし）→再度ウィークリーに戻す（68/14が正しく復元）→ページ全体リロード後も68/14が維持されることを確認。副次的判明事項（技術的負債）: 密度スライダーはこれまで`DashboardScreen.tsx`の`ganttRowHeight`という単一stateを介して実質Panoramaボードの行高さにのみ効いており、Gantt側は死んだ配線だった。今回Gantt専用の別stateに繋ぎ替えたことでPanoramaボードの行高さはUIから調整する手段がなくなったが、指揮AI確認の上「各ビューが専用の行高さを持つ方が正しい設計」として復旧UI追加は不要と判断（YAGNI）。また`DashboardScreen.tsx`内の到達不能な`viewMode==='calendar'`パス自体の削除は今回スコープ外（技術的負債として`docs/requests.md`に記録済み）。**指揮AIレビュー承認・masterマージ済み（コミット`3d88463`）。本番デプロイ・実機検証完了（2026-08-14）**: `upload.ps1`でデプロイ後、本番（`https://door-fujita.com/contents/Youkan/`）でヘッダー→カレンダー→ガントへ実際に遷移し、ウィークリーに切替→列幅24→69・密度28→14に調整→マンスリーに戻す（24/28に復帰、ウィークリー側は無影響）→再度ウィークリーに戻す（69/14が復元）→ページ全体リロード後も69/14が維持されることをDOM値照会で確認。コンソールエラーは新規発生なし（既存の無関係なGoogle連携エラーのみ） | 完了（2026-08-14本番デプロイ・実機検証完了） | 03_画面設計.md §5.3 |
| 2026-08-14 | R-098 | **ガント・全体一覧にメニュー部分を除いたコンテンツのみを印刷するボタン**: 原文（意訳）「上部のメニュー等を消したカレンダー表示部分だけを印刷するボタンが欲しい。ガントチャートだけでなく、全体一覧画面でもメニュー部分以外を印刷するボタンになってほしい。要はメニュー部分を除いたコンテンツ部分だけを表示・印刷するボタン」。ガント画面での相談の流れ（依存関係付きガントチャート・スイムレーン・A3印刷でのチーム共有）の延長で追加された要望。指揮AI方針: 専用PDF生成等は使わず、ブラウザネイティブ`window.print()`＋`@media print`CSSでヘッダー・メニュー等のUIクロームを非表示にする方式とする。仕様: `03_画面設計.md` §5.3。**実装完了（2026-08-14）**: R-097と同一ブランチ（`feature/R-097-098-gantt-scale-and-print`）で対応。`index.css`に`.no-print`クラスの`@media print`ルールを追加。`CalendarHeader.tsx`（ガント画面、`isGantt`ゲート）と`OverviewBoard.tsx`（全体一覧画面）それぞれに「印刷」ボタン（`onClick={() => window.print()}`）を追加。UIクロームとして`YoukanHeader.tsx`（グローバルヘッダー・MenuDrawer含む）・`CalendarHeader.tsx`本体・`OverviewBoard.tsx`のツールバー・`SideMemoWidget.tsx`（右下フローティング一時メモ）・`SpeechFloatingButton.tsx`（スマホ用フローティング読み上げボタン）に`no-print`クラスを付与。TDD: `CalendarHeader.test.tsx`・`OverviewBoard.print.test.tsx`（新規1件）に`window.print`スパイでの発火検証を追加、Red確認後実装しGreen確認。claude-in-chrome MCPで実機検証: ガント画面（`VolumeCalendarScreen.tsx`実体）・全体一覧画面の両方で印刷ボタン押下による`window.print()`発火を確認、`.no-print`を疑似適用した状態のスクリーンショットでヘッダー・ツールバー・フローティングウィジェットが非表示になりコンテンツ本体のみ残ることを確認。**指揮AIレビュー承認・masterマージ済み（コミット`3d88463`）。本番デプロイ・実機検証完了（2026-08-14）**: `upload.ps1`でデプロイ後、本番のガント画面・全体一覧画面それぞれで印刷ボタン押下による`window.print()`発火、`.no-print`疑似適用でヘッダー・ツールバー等が非表示になりコンテンツ本体のみ残ることを確認。コンソールエラーは新規発生なし（既存の無関係なGoogle連携エラーのみ） | 完了（2026-08-14本番デプロイ・実機検証完了） | 03_画面設計.md §5.3 |
| 2026-08-14 | R-099 | **`GET /items`APIに依存関係グラフを埋め込む**: 原文（意訳）「AIが『Youkan APIには明示的な依存グラフのフィールドはありませんでした』と言っていた。AIにも依存関係を把握させたいので、読めるようにしてほしい」。発注者確認: `GET /dependencies`を別途叩いて突き合わせる現行方式ではなく、`GET /items`本体に依存関係フィールドを埋め込む方式を選択。各アイテムに`dependsOn`（前提アイテムID配列）・`blocks`（後続アイテムID配列）を追加。R-091で意図的に避けた「起動時APIコスト増」への対策として、N+1クエリ禁止・`item_dependencies`一括取得＋メモリ上マップ構築を実装条件とした。仕様: `04_データ設計.md` §3.5 **実装完了（2026-08-14）**: `feature/R-099-items-api-dependency-fields`ブランチ。`BaseController.php`に`buildDependencyMap()`新設、`mapItemRow($item, $dependencyMap)`拡張。`ItemController.php`の9箇所（getMyItems各scope・getProjectItems・getSubTasks・show）に配線。新規テスト18件全Green、N+1回避も確認済み。**指揮AIによる独立レビュー（Codex）で発見・修正**: 新規テストが実DB（`jbwos.sqlite`）を直接汚染し`tenant_id IS NULL`の個人依存関係を無条件削除する破壊的バグがあり、専用一時DB接続＋削除範囲を`r099_`プレフィックス限定へ修正（`fix/R-099-test-safety`、コミット`8de3240`）。あわせて`buildDependencyMap()`のテナント境界（`joinedTenants`に`currentTenantId`が欠けるケース）の依存関係欠落も修正。修正後テスト23件全Green。**本番デプロイ・実機検証完了（2026-08-14）**: `upload.ps1`でデプロイ後、本番`GET /items`（aggregated/company/personal各scope）で`dependsOn`/`blocks`フィールドが実データ134件に正しく含まれることをAPI照会で確認 | 完了（2026-08-14本番デプロイ・実機検証完了） | 04_データ設計.md §3.5 |
| 2026-08-14 | R-100 | **ガント完了表示モードで、完了アイテムのカレンダー要素・依存関係エッジもグレーにする**: 原文（意訳）「ガントチャートで完了を表示するモードでは、完了したアイテムのカレンダー上の要素と依存関係のエッジの色もグレーにしてほしい。完了したのかどうかがわかりづらい」。指揮AI事前調査済み: R-035でアイテムタイトル文字列は完了時に取り消し線＋グレー（`COMPLETED_ITEM_CLASS`）になっているが、`RyokanGanttView.tsx`の日別「割当チップ」（1051行目付近、`bg-indigo-500`固定）・目安納期ハンドル（`bg-indigo-400`固定）・依存関係矢印（`GanttDependencyArrows`内`stroke="#6366f1"`固定）は完了状態を反映しておらず常に同じ色。仕様: `03_画面設計.md` §5.4 に追記予定。エッジのグレー化条件は「source/targetいずれかのアイテムが完了なら灰色」とする（指揮AI判断、片方のみ完了のケースを含めて安全側に倒す）。顧客納期の赤マーカーは対象外（別概念のため） **実装完了（2026-08-14）**: `feature/R-100-gantt-completed-gray`ブランチ。`RyokanGanttView.tsx`の日別割当チップ・目安納期ハンドルを`isItemDone(item)`に応じてグレー系に切替、`GanttDependencyArrows`にも`isDimmed`判定（source/targetいずれかが完了ならグレー）を追加し矢印・マーカーをグレー化。顧客納期の赤マーカーは対象外のまま維持。新規テスト7件、vitest全件回帰なし。指揮AIによる独立レビュー（Codex）でも実装ロジック自体は仕様通りと確認済み（テスト網羅性の指摘のみ、実害なしと判断し追加修正は見送り）。**本番デプロイ・実機検証完了（2026-08-14）**: `upload.ps1`でデプロイ後、本番ガント画面の「完了表示」モードで完了アイテムの割当チップ52個が`.bg-slate-400`、依存関係エッジ17本が`stroke="#94a3b8"`＋グレー矢印マーカーになっていることをDOM照会で確認 | 完了（2026-08-14本番デプロイ・実機検証完了） | 03_画面設計.md §5.4 |
| 2026-08-14 | R-101 | **フローチャート画面にも全体を印刷するボタンを追加**: 原文（意訳）「フローチャートも全体を印刷するボタンを作って」。R-098（ガント・全体一覧の印刷ボタン）の延長。指揮AI事前調査済み: `FlowScreen.tsx`には既にR-090で追加された「全体表示」ボタン（`fitView({ duration: 300, padding: 0.1 })`、React Flowの`useReactFlow().fitView`）があり、これをduration 0で流用すれば現在のズーム/パン位置に関わらず全ノードを収めてから印刷できる。R-098と同じ`.no-print`/`@media print`の仕組みを再利用。仕様: `03_画面設計.md` §7.9 **実装完了（2026-08-14）**: `feature/R-101-flow-print-button`ブランチ。`FlowScreen.tsx`に印刷ボタン追加（`handlePrint`: `fitView({duration:0,padding:0.1})`即時実行→`window.print()`）。`.no-print`をヘルプ・全体表示・印刷ボタン・Controls・MiniMap・FlowHeaderに付与。新規テスト2件、vitest全件回帰なし。**指揮AIによる独立レビュー（Codex）で発見・修正**: 「未配置」パネル（`Flow/UnplacedItemList.tsx`）に`.no-print`が付与されておらず全体印刷時に混入する漏れがあり、修正（`fix/R-101-print-unplaced`、コミット`0c381f2`）。**本番デプロイ・実機検証完了（2026-08-14）**: `upload.ps1`でデプロイ後、本番フローチャート画面で「未配置」パネルに`.no-print`が付与され`@media print`ルールで非表示になることをCSSOM経由で確認 | 完了（2026-08-14本番デプロイ・実機検証完了） | 03_画面設計.md §7.9 |
| 2026-08-14 | R-102 | **フローチャート印刷ボタンで全体が中央に印刷されない**: 改善要望フォーム経由（2026-08-14 13:42）。「フローチャートで印刷ボタンを押してみたら、全体が中央に印刷されていない。中央にして。」本日デプロイしたR-101（フローチャート印刷ボタン）の後続バグ。スクリーンショットで、印刷プレビューの用紙右端の細い帯にフローチャート全体が押し込まれ、用紙の大部分が空白になっている症状を確認。指揮AI事前調査済み: `index.css`の印刷用CSSは`.no-print`の非表示化のみで、React Flowのビューポート要素を印刷用紙サイズにフィットさせるスタイルが存在しない。仕様: `03_画面設計.md` §7.9 **実装完了（2026-08-14）**: `fix/R-102-print-centering`ブランチ。**根本原因**: `FlowScreen.tsx`の`handlePrint`が`fitView({duration:0,padding:0.1})`実行直後に`window.print()`を呼んでいたが、この時点の`fitView`は画面表示サイズ（ブラウザウィンドウ基準）を元にReact Flowのビューポート（パン/ズーム）transformを計算する。その後`window.print()`により印刷用紙サイズへレイアウトが切り替わっても、既に計算済みの画面基準transformはそのまま持ち越されるため、用紙という別サイズの領域に対して画面サイズ基準のズーム/パン位置が適用され、全体のごく一部（用紙右端の細い帯）しか可視領域に収まらなくなっていた。**修正**: (1) `FlowScreen.tsx`に`beforeprint`イベントリスナーを追加（`useEffect`でマウント時に登録・アンマウント時に解除）し、印刷用レイアウトが確定するタイミングで再度`fitView`を実行するようにした。印刷ボタン以外（ブラウザのCtrl+P等）からの印刷にも同様に効く。(2) `index.css`の`@media print`ブロックに`[data-testid="flow-canvas-root"]`（フローチャートのReact Flowラッパー要素）へ`width:100%!important;height:100vh!important;`を追加し、印刷レイアウト自体も用紙サイズに明示的にフィットするようにした。TDD: `FlowScreen.print.test.tsx`に`beforeprint`イベント発火で`fitView`が再実行されることを検証する新規テスト、`index.print.test.ts`に`index.css`の`@media print`ブロックが`flow-canvas-root`を用紙サイズへフィットさせていることを検証する新規テストを追加、いずれもRed確認後実装しGreen確認。既存テスト回帰なし（vitest全件871 passed・14 skipped、既存の無関係な2件のunhandled error警告は`DecisionDetailModal`/`KeyboardAndButtons`関連で本修正と無関係）。`npm.cmd run build`成功確認。`git diff --stat master..HEAD`は`FlowScreen.tsx`・`FlowScreen.print.test.tsx`・`index.css`・新規`index.print.test.ts`の4ファイルのみ。masterへのマージ・本番デプロイは指揮AIレビュー後 | 実装完了。指揮AIレビュー待ち（マージ・デプロイ未実施） | 03_画面設計.md §7.9 |
| 2026-08-14 | R-103 | **目安時間の全角入力を半角として認識する**: 改善要望フォーム経由（2026-08-13 15:35）。「目安時間に全角で入力したら半角に変換して認識して」。指揮AI事前調査済み: `timeParser.ts`の`parseTimeInput()`が半角数字専用の正規表現（`\d`）のみでマッチしており、全角数字（`１２３`等）・全角記号（`ｈ`/`ｍ`等）を入力すると全パターンにマッチせず`null`を返す（未入力扱いになり保存されない）。単一の共通関数`parseTimeInput()`をフローチャート・ガント・全体一覧の全ての目安時間入力欄が経由するため、この関数のみの修正で全画面に自動的に効く。仕様: `03_画面設計.md` §7.7.4 **実装完了（2026-08-14）**: `fix/R-103-time-input-fullwidth`ブランチ。`parseTimeInput()`冒頭で`input.normalize('NFKC')`により全角英数字・記号（全角数字・`ｈ`/`ｍ`/`Ｈ`/`Ｍ`・全角ピリオド等）を半角へ正規化してから既存の判定ロジックに通すよう修正（ライブラリ追加なし）。TDD: `timeParser.test.ts`に全角入力パターン5件（`９０`・`３０ｍ`・`１ｈ`・`１．５ｈ`・`１Ｈ`）を追加しRed確認（`null`が返り失敗）後実装しGreen確認。既存テスト回帰なし確認（vitest全件151ファイル870 pass・14 skip、既存の無関係なunhandled error 2件は本修正の対象外ファイルでmaster baselineと同一の既知事象）。`git diff --stat master..HEAD`は`timeParser.ts`（+1/-1）とテスト新規（+8）の2ファイルのみ。マージ・本番デプロイは指揮AIレビュー後 | 実装完了（指揮AIレビュー待ち） | 03_画面設計.md §7.7.4 |
| 2026-08-14 | R-104 | **フローチャートの各ノードに納期・マイ期限をさりげなく表示する**: 改善要望フォーム経由（2026-08-14 13:44）。「ふろーちゃーとに、それぞれののーどのマイ期限、もしくは納期がいつになっているかもさりげなく添えるとしたらどういうやりかたにするのがいいかな？やってみてほしい」。提案型の要望のため表示方法自体の設計を実装Agentへ委ねた。指揮AI事前調査済み: `FlowItemNode.tsx`は既に目安時間バッジ（`formatMinutes(item.estimatedMinutes)`）を持っており、同様のパターンで`item.due_date`（顧客納期）・`item.prep_date`（マイ期限）を追加するのが自然。ガント画面（`RyokanGanttView.tsx`）の配色（顧客納期=赤系、マイ期限=インジゴ系）との一貫性を持たせること。未設定なら非表示、設定済みでも小さく控えめな表示とすること。**実装完了（2026-08-14）**: `feature/R-104-flow-node-due-date`ブランチ（worktree、master `dbc2317`ベース）。ノード内の目安時間バッジ行の下に新たな行を追加し、`due_date`設定時は`text-red-400`で「納期 M/d」、`prep_date`設定時は`text-indigo-400`で「期限 M/d」を`text-[8px]`の控えめなプレーンテキストで表示（アイコン・背景色なし、ガント画面の赤/インジゴの配色を踏襲）。両方とも未設定ならこの行自体を描画しない。両方設定時は両方を横並び表示（実装Agent判断、視覚的コストが低く情報量が増えるため）。日付フォーマットは`date-fns`の`format(d, 'M/d')`でガント画面と統一。TDD: `FlowItemNode.dueDate.test.tsx`新規5件（未設定時非表示・due_dateのみ・prep_dateのみ・両方表示・配色検証）をRed確認（4/5失敗）後実装しGreen確認。既存テスト回帰なし確認（vitest全件874 passed / 14 skipped、`DecisionDetailModal.test.tsx`/`KeyboardAndButtons.test.tsx`関連の既存unhandled rejection 2件はmaster baselineと同一の既存事象で本修正と無関係）。`git diff --stat master..HEAD`は`FlowItemNode.tsx`(+15)とテスト新規1ファイルの2ファイルのみ。claude-in-chrome MCPでdev環境実機検証: worktree専用のローカルDB・ポート（backend 8000、frontend 5199）を一時起動し、デバッグユーザーでログインしフローチャート「全プロジェクト」画面で複数の実データアイテムを確認。`due_date`のみ設定のアイテムは赤字「納期 M/d」のみ、`due_date`・`prep_date`両方設定のアイテム（例:「机を整理する」）は赤字「納期 2/20」＋インジゴ字「期限 2/18」が目安時間バッジの下に控えめなサイズで並んで表示されることをスクリーンショットとDOM照会の両方で確認。検証用に変更したローカルDBの内容（デバッグユーザーのパスワードリセットのみ、実データの作成・削除なし）はworktree専用の使い捨てDBのため原状回復不要。マージ・本番デプロイは指揮AIレビュー後 | 実装完了・実機検証済み（マージ・デプロイは指揮AIレビュー待ち） | 03_画面設計.md §7.10 |
| 2026-08-12 | R-077 | **フローチャートでEnterキーによるノード追加が失敗する（API Error: 409）**: R-074本番デプロイ直後に発注者から報告。「フローチャートでノードを選んでEnterを押したら『ノード追加失敗 Error: API Error: 409』と表示される。ただし409後もノード・依存関係自体は作られているらしく、接続線が表示されないだけ」との追加情報も判明。**根本原因**: バックエンド（`DependencyController::create()`）は依存関係の`UNIQUE(source_item_id, target_item_id)`重複を409 "Dependency already exists"として返す仕様だが、フロントエンド`DependencyRepository.createDependency()`はこれを他の失敗と区別せず例外として投げていた。呼び出し元`createNodeBelow`（`FlowScreen.tsx`）のcatchブロックが「ノード追加失敗」トーストを表示し、かつ例外により`appendDependencyToState`が呼ばれないため、サーバー側には既に存在する依存関係がローカル`edges` stateに反映されず接続線も描画されなかった。副次的に、`ApiClient`のエラーメッセージ抽出が`errorData.message`（存在しないkey）のみを見ており、バックエンドの`{error: message}`形式の具体的な理由が伝わらず汎用的な「API Error: 409」表示になっていたことも判明。**実装完了**: `DependencyRepository.createDependency()`が409を捕捉した場合、例外を投げず既存の依存関係を取得して返す（=呼び出し元が求める終状態「source→targetの依存関係が存在すること」は既に達成済みとして冪等に扱う）。この修正は`DependencyRepository`という単一の共通経由点に集約されているため、`createNodeBelow`だけでなく`handleEdgeInsert`・ドラッグ重なり自動接続等の全呼び出し元に自動的に効く。`ApiClient`のエラーメッセージ抽出も`errorData.error || errorData.message`に修正。TDD: 新規5件（`client.errorStatus.test.ts`、`DependencyRepository.test.ts`）をRed確認後実装しGreen確認。既存テスト回帰なし確認（vitest全件775 pass・1件`useAssigneeView.test.ts`失敗は無関係な既存事象、14 skip）。実機確認: 単発Enterフロー（POST /items→POST /items/{id}→POST /dependencies、3リクエストとも200、edge描画）が修正後も正常動作することを確認。409を伴う重複シナリオ自体の自動化での決定論的再現はできなかったため、単体テストで新しい409ハンドリングを直接検証。調査中に副次的なバグ（Tab→目安時間欄でDOMフォーカスが逃げる、ノード作成のたびに選択状態が揮発する）も発見、`docs/handover/R-077-analysis.md`に次期要望候補として記録。**本番デプロイ・実機検証完了（2026-08-12）**: `upload.ps1`でデプロイ後、本番の「サンプル作る」プロジェクトで既存の依存関係を持つノード「太さ決める」を選択しEnterキーでノード作成、接続線（矢印マーカー）が欠落なく描画されることを確認。さらに409を実際に再現させるため、既存依存関係（太さ決める→細い部材をつくる）に対して手動でエッジを再接続し、ネットワークタブで`POST /dependencies`が409を返した直後に`GET /dependencies?item_id=...`が200で応答、UIには「ノード追加失敗 API Error: 409」ではなく成功トースト（接続作成）が表示され、consoleにもエラーが一切出力されないことを確認（修正の核心シナリオを本番で直接検証）。検証用に作成したテストノードはDelete操作で削除し原状回復済み | 完了（2026-08-12デプロイ済み） | — |
| 2026-08-16 | R-105 | **ガントチャート「時間軸タイムライン表示」**: ウィークリー/デイリー表示で、タスクを「目安時間÷24h」の幅・その日の中の開始オフセット位置を持つブロックとして描画し、横ドラッグで開始位置を手動調整できるようにする。位置は `items.meta.gantt_time_blocks = {"YYYY-MM-DD": 開始オフセット分}` に保存（既存の汎用JSON列 `meta` を再利用、新規テーブルなし）。実装中に実機検証で重大バグを発見: `POST /items/{id}` による保存自体は成功しDBには正しく書き込まれるのに、ページリロード後にブロックが開始オフセット0へ戻る。根本原因は `backend/CalendarController.php` の3つのSELECT文（`getLoad`・`getItems`・`getCompletedItems`）がいずれも明示カラムリストを使っており `items.meta` を含んでいなかったこと。`BaseController::mapItemRow()` は `meta` キーが存在しない場合に `null` を代入する実装のため、単体取得 `GET /items/{id}` では正しく返る一方、ガントビューが実際に使う `GET /calendar/items?mode=gantt` では常に `meta: null` になり、保存済みの位置が表示に反映されなかった。対応: 3箇所すべてのSELECTに `items.meta` を追加（`items.` 明示カラムリストを持つ他のSELECTが存在しないことをgrepで確認済み）。TDD: 一時DBを使う再現テスト `backend/tests/test_r105_calendar_meta.php` を先に作成しRed確認（gantt/rangeモード・getLoad・getCompletedItemsの4件失敗）→修正→Green（5 passed。meta未設定アイテムが `meta === null` で返る後方互換ケースを含む）。テスト結果: vitest 156ファイル905 passed/14 skip/0 failed、PHP関連テスト42 passed/0 failed。実機検証: ドラッグ→保存→フルリロード後も位置が保持されること（825分→0分〈左端クランプ〉→360分の3パターン）、24時間はみ出し時に❗️マークとtooltip「割当: 600 分 / 開始: 19:45（24:00をはみ出しています）」が出ること、ウィークリーでも同じ時間軸形式が適用されること、マンスリーは従来の日次チップ表示のままであること（後方互換）を確認。**別件の未修正バグを検出**: デイリー/ウィークリー表示でページを新規ロードした直後、行が1つも描画されない（マンスリーでは発生せず、モードを一度切り替えると復旧）。根本原因はR-046-Y1の `.gantt-row-cv { contain-intrinsic-size: auto 28px }`（`JWCADTategu.Web/src/index.css`）が横幅にも28pxを適用してしまい、行が未描画の初回は親 `min-w-max`（`RyokanGanttView.tsx`）のmax-content計算が潰れてビューポート幅になること。結果 `sticky left-0` のラベル列が画面外へ押し出される。**この副次バグも同ブランチで修正済み**: 当初検討した「contain-intrinsic-size を高さのみに限定する」案（`auto none auto 28px` / `contain-intrinsic-width: none`）は、computed値としては正しく反映されるものの Chrome では幅方向のサイズ拘束が解除されずレイアウトが直らないことを実機で確認（対照実験として `content-visibility: visible` を注入した場合のみ解消することも確認）。そのため `index.css` は一切変更せず、`RyokanGanttView.tsx` のボディ側スクロールコンテンツ（`min-w-max`）に明示幅「ラベル列256px（w-64）＋ 日数 × 列幅」を与えて max-content 依存自体を廃する方針に変更。R-046のペイントスキップ最適化（`content-visibility: auto`・`contain-intrinsic-size: auto 28px`）はcomputed値がそのまま維持されることを実機で確認済み。TDD: 明示幅を検証するテストを `RyokanGanttView.timelineBlocks.test.tsx` に追加しRed確認（colWidth=24/96の2件失敗）→修正→Green。実機検証: ページ新規ロード直後のデイリー・ウィークリー表示で行が正しく描画されること、ヘッダーとボディのscrollWidthが完全一致すること（デイリー15040px＝256+154×96、マンスリー3952px＝256+154×24）、sticky ラベル列が x=4 に固定されること、マンスリーの日次チップ表示が従来どおりであることを確認 **本番デプロイ・実機検証完了（2026-08-16）**: masterへマージ（`20d73cd`）後、vitest全件907 passed/14 skipped/0 failed・本番ビルド成功を確認し`upload.ps1`でデプロイ（配信JS `index-kPpTR2Rz.js`）。本番でガント画面を検証: (1)「マンスリー/ウィークリー/デイリー」3モードのトグルが表示される、(2) デイリー・ウィークリーとも時間軸タイムラインブロックが142件描画され、幅が所要時間比例であること（480分→33.3%、720分→50%、60分→4.17%）、(3) ブロックを横ドラッグすると開始位置が更新されtooltipが「割当: 480 分 / 開始: 12:00」に変わること、(4) **フルリロード後もデイリーモードと開始位置12:00が再現されること**（`CalendarController`の`items.meta`欠落バグ修正が本番で効いていることを直接確認）、(5) マンスリーはブロック0件・従来の日次チップ171行のままで後方互換が保たれること。consoleエラーはGoogleカレンダー連携の再連携要求1件のみでR-105と無関係。検証で変更したブロック位置は開始0:00へ戻し、表示モードもマンスリーへ戻して原状回復済み | 完了（2026-08-16デプロイ済み） | 03_画面設計.md, 04_データ設計.md |
| 2026-08-16 | R-106 | **ガント列幅スライダーの最大値を表示領域幅にする＋「密度」を「行高」に改称**: 改善要望フォーム経由。2026-08-14 14:35原文「列幅の最大が80ですが、120にして。それと、行高さ調整のスライダーの名前が『密度』ですが『行高』に変更して」、2026-08-16 12:28原文（上記を発展させた最新の要望）「ガントチャートの列幅のスライダーの最大が80になってますが、最大を表示領域幅にしてください」。指揮AI判断: 2件は同一テーマの連続要望であり、8/16の「表示領域幅」が最新かつ8/14の固定値120を包含する上位互換のため、8/16の内容を仕様として採用（固定値120要望は別対応不要）。現状調査済み: `CalendarHeader.tsx`の`COL_WIDTH_MAX = { monthly: 80, timeline: 240 }`が固定値でスライダーmaxを決めている（R-105実装時にtimelineモード=weekly/dailyのみ240へ拡張済み、monthlyは80のまま）。仕様: 列幅スライダーの最大値を、ガント表示エリア（日付列が並ぶスクロール可能領域、タスク一覧列を除く）の可視幅に動的に追従させる（`ResizeObserver`等で実装Agントが判断）。「密度」ラベル（`CalendarHeader.tsx` L201, L203の`aria-label`含む）を「行高」に改称。仕様: `03_画面設計.md` §5.3に追記予定。**実装完了（2026-08-16, `feature/R-105-Y2-R-106-gantt-timeline-polish`）**: `COL_WIDTH_MAX`固定値を廃止し、`CalendarHeader`自身の`clientWidth`を`useLayoutEffect`＋`ResizeObserver`で計測して`GANTT_STICKY_COL_WIDTH=256`を引いた値をスライダー`max`にした（ヘッダーはガント本体と同じ幅の兄弟要素のため、propsのバケツリレー不要）。モード別の上限差はなくしマンスリーも同じ上限に統一。ラベル・`aria-label`を「行高」へ改称。列幅値表示の幅を`w-6`→`w-8`（4桁対応）。テスト: `CalendarHeader.test.tsx` 22件green（上限追従3モード＋リサイズ追従＋行高ラベル＋旧「密度」非表示を新規追加）。実機検証済み（1920px幅で`max=1664`、親要素を1000pxに縮めると`max=744`へ追従、スライダー最大で1日列が表示領域いっぱいになることを確認）。**本番デプロイ・実機検証完了（2026-08-16）**: masterへマージ（`8d89b37`、fast-forward）後、vitest全件917 passed/14 skipped/0 failed・本番ビルド成功を確認し`upload.ps1`でデプロイ（配信JS `index-DWUa3Gw4.js`）。本番ガント画面で列幅スライダーの`max`がヘッダー幅2560px − タスク一覧列256px ＝ `2304` になっていること（固定値80/240ではない）、マンスリー・ウィークリー・デイリーの3モードとも同じ上限が適用されること、行高スライダーのラベル・`aria-label`が「行高」（`min=12`/`max=32`）であることをDOM照会で確認 | 完了（2026-08-16デプロイ済み） | 03_画面設計.md §5.3 |
| 2026-08-16 | R-105-Y2 | **ウィークリー/デイリーガントの時間軸ブロックに目安時間表示＋依存矢印をブロック端に合わせる**: 会話内発言原文（意訳）「ガントチャートのウィークリー・デイリーが良くなってる、ありがとう。2点お願いがある。1つ目、ウィークリー/デイリーのガントチャートでもノードの中心に目安時間を表記してほしい、マンスリーには書いてあるよね。2つ目、ウィークリー/デイリーのガントチャートの依存関係の矢印の始点と終点をそれぞれのノードの先頭と末尾に合わせてほしい、現状はマス目の先頭と末尾だよね」。指揮AI事前調査済み: (1) `RyokanGanttView.tsx`の時間軸ブロック（`timelineMode`時、L1147-1190付近）にはテキストラベルが無く❗️はみ出し警告のみ表示。マンスリー表示の日次チップ（L1198-1201）は`step.allocatedMinutes >= 60 ? Math.round(...)+'h' : ''`で時間ラベルを表示済みのためこれと同様の表示を追加する。(2) `GanttDependencyArrows`コンポーネント（L1278-）の矢印座標計算（L1337, L1339）は`timelineMode`の有無にかかわらず常に「日付セルの右端/左端」（`(sourceDayIdx + 1) * colWidth` / `targetDayIdx * colWidth`）を使っており、時間軸ブロックの実際の開始・終了位置を考慮していない。`timelineMode`時は`timeBlockLayoutsByDate`を`GanttDependencyArrows`にpropsとして渡し、対応する日付のブロックがあればそのオフセット・幅から始点・終点を計算するよう改修が必要（ブロックが無い日＝時間軸換算できないケースは既存の日付セル端計算にフォールバック）。仕様: `03_画面設計.md` §5.3に追記予定。**実装完了（2026-08-16, `feature/R-105-Y2-R-106-gantt-timeline-polish`）**: (1) 時間軸ブロックを`justify-center`にし、`step.allocatedMinutes >= 60`のとき`Xh`ラベルをブロック中央に表示（60分未満は非表示。マンスリー日次チップと同一書式）。(2) `GanttDependencyArrows`に`timeBlockLayoutsByDate`をpropsで渡し、`getDateIndex`を`getDatePosition`（index＋日付キーを返す）に変更。ソース側は`startOffsetMinutes + displayWidthMinutes`、ターゲット側は`startOffsetMinutes`から座標を算出し、ブロックが無い日は従来の日付セル端へフォールバック。マンスリー（`timelineMode`でない）は`undefined`を渡すため従来動作のまま。テスト: `RyokanGanttView.timelineBlocks.test.tsx` 13件green（時間ラベル表示・60分未満非表示・ブロック端座標・フォールバック・非timelineMode後方互換を新規追加）。実機検証済み（ウィークリーで`4h`/`10h`ラベル表示、依存矢印がソースブロック右端(x=7400)からターゲットブロック左端(x=7496)へ描画されることをDOM座標で確認。マンスリーでは矢印がセル端(x1=x2=2056)・日次チップ表示のままで後方互換を確認）。**追加バグ修正（指揮AI実機検証で発見）**: ブロックは`QuantityEngine`のバックワード割当により、prep_date当日のキャパシティが0（休日等）の場合は前の稼働日へ前倒し配置される。当初実装は矢印座標を`prep_date`の日だけで引いていたため実ブロックを見つけられず、片側のみ日付セル端へフォールバックして左右が逆転した矢印（`x1=7744 > x2=7496`）になっていた。修正: `timeBlockLayoutsByDate`から各アイテムの実ブロック日レンジを導出し、ソースは最終日・ターゲットは初日を基準に座標算出（`allocationMap`を新規propsで渡す必要はなく、既存propsだけで解決）。表示範囲外・ブロック無しは従来どおりセル端フォールバック。再現テスト追加（prep_date当日をキャパ0にして前倒しさせるケース）でRed確認後に修正、全体917 passed / 14 skipped / 0 failed。実機で当該データ（太田様邸リフォーム12h→机を整理する2h）の矢印が`x1=x2=7496`（ソース8/14ブロック右端→ターゲット8/14ブロック左端）になることを確認 **本番デプロイ・実機検証完了（2026-08-16）**: masterへマージ（`8d89b37`、fast-forward）後、vitest全件917 passed/14 skipped/0 failed・本番ビルド成功を確認し`upload.ps1`でデプロイ（配信JS `index-DWUa3Gw4.js`）。本番ガント画面で検証: (1) ウィークリー・デイリーとも時間軸ブロック内に白字の目安時間ラベル（`12h`/`8h`/`2h`/`1h`等）が90件描画されること、(2) 依存矢印29本の始点・終点が対応する時間軸ブロックの左右端と誤差0.5px以内で一致すること（矢印x座標が`5574.75`・`5508.625`のように日付セル境界の整数倍でない＝ブロック端基準であることも併せて確認）、(3) マンスリーでは時間ラベル0件・矢印x座標がセル境界の整数倍（`cellUnits` 67/77/78）で従来動作のままであること。consoleエラーはGoogleカレンダー連携の再連携要求1件のみで本件と無関係。検証はDOM照会のみで実データの作成・変更を行っていないため原状回復不要 | 完了（2026-08-16デプロイ済み） | 03_画面設計.md §5.3 |
| 2026-08-17 | R-107 | **フローチャートノードの状態・目安時間・期限表示を1行にまとめる**: 改善要望フォーム経由（2026-08-17 01:54）。「フローチャート画面のノードの中のレイアウトを変えたい。現状は、状態・目安時間のあと改行して『期限 8/16』のようになっているが、これを改行をやめてたい。添付画像の例なら『Inbox 1h 8/16』と書いてほしい。8/16　と表示されている根拠がマイ期限なら青、納期なら赤　というルールであれば、『期限』というラベルも不要になる」（スクリーンショット添付、ノード「枠面取り」：現状は「枠面取り」/「INBOX 1h」/「期限 8/16」の3行）。R-104のフォローアップ。指揮AI事前調査済み: `FlowItemNode.tsx` L187-223（ステータス・目安時間の行）とL224-237（納期・マイ期限の行、「納期 M/d」赤字・「期限 M/d」インジゴ字とラベル付き）が別々の`<div>`のため改行されている。要望は両者を1行に統合し、「納期」「期限」のラベルテキストを削除して色（赤=納期、インジゴ=マイ期限）のみで区別する。仕様: `03_画面設計.md` §7.10「レイアウト圧縮（R-107）」に追記済み。**実装済み（2026-08-17、`feature/R-107-flow-node-layout-compact`）**: `FlowItemNode.tsx`の納期・マイ期限用`<div>`を廃止し、ステータス・目安時間のflexコンテナ内へ統合。ラベル「納期」「期限」を削除し`text-red-400`（納期）/`text-indigo-400`（マイ期限）の色のみで区別、`title`属性に「顧客納期: M/d」「マイ期限: M/d」を残してホバーで判別可能にした。テスト: `FlowItemNode.dueDate.test.tsx`をR-107仕様へ更新（7件green、うち1行統合・ラベル非表示の検証を追加）。全体918/933 green（既存失敗`useAssigneeView.test.ts`1件はmasterでも失敗する本件と無関係の回帰）。実機検証: フローチャート画面で「PENDING 24h 3/4 4/1」「INBOX — 3/4」「DECISION_HOLD 24h 3/26 3/17」と1行表示、色は`rgb(248,113,113)`=納期・`rgb(129,140,248)`=マイ期限を確認 **本番デプロイ・実機検証完了（2026-08-17）**: masterへマージ（`e770526`）後、vitest全件918 passed/14 skipped（既存フレーキー`useAssigneeView.test.ts`1件のみ失敗、本件と無関係）・本番ビルド成功を確認し`upload.ps1`でデプロイ（配信JS `index-CsPftcz1.js`）。本番フローチャート画面（石鎚山プロジェクト、実データ）でDOM照会により検証: 日付付きノード多数（枠仕上げ 8/17 赤、枠面取り 8/17 インジゴ等）でステータス・目安時間・日付が1行に表示され、「納期」「期限」のラベルテキストが表示されないこと（`hasLabel: false`）、色分けが`rgb(248,113,113)`=納期・`rgb(129,140,248)`=マイ期限で正しいことを確認。consoleエラーなし。DOM照会のみで実データの作成・変更を行っていないため原状回復不要 | 完了（2026-08-17デプロイ済み） | 03_画面設計.md §7.10 |
| 2026-08-17 | R-108 | **フローチャート複数選択まとめ移動後、選択解除すると元の位置に戻るバグ**: 会話内発言原文「フローチャート画面でシフトをしながらドラッグすると範囲選択ができますよね　それで選択した状態でどれか要素をつかんでドラッグするとその選択をされたアイテムが全部いっぺんに相対距離を保ちながら移動できるんですけど　移動した後に何もないところをクリックして　選択解除をしたら、　全部元の位置に戻っていってしまいます　これはバグだと思うので直してほしいです」。指揮AI事前調査済み: `FlowScreen.tsx`の`onNodeDragStop`（L436-469付近）で`selectedNodes.length > 1`のとき、各ノードについて`await updateItemMeta(node.id, { flow_x, flow_y })`をforループでシーケンシャル呼び出しし、その後`setAllItems`でローカルstateを更新、続けて`buildGroupNodes`で`nodes`ステートを再構築している（L458-469）。`updateItemMeta`は`allItems`をuseCallback依存配列に持つクロージャ。選択解除（`onSelectionChange`、L328-331）自体は`selectedNodeIds`/`selectedEdgeIds`のstate更新のみに見えるが、選択状態変化をトリガーに`nodes`または`allItems`の再構築処理が走り、サーバー保存がまだ反映されていない古い位置データで上書きしている可能性がある。根本原因の特定・修正方針は実装Agentに委ねる。発注者指示: 実装をAgentへ委譲したうえで並行してR-107をデプロイし、R-108修正完了後に再度デプロイする。実装Agentが特定した根本原因: `onNodeDragStop`がサーバー保存（`updateItemMeta`）を待ってからローカルの`allItems`を更新していたため、保存完了前に選択解除で再レンダリングが起きると、`allItems`から`nodes`を再構築する派生useEffectが移動前の座標でノードを上書きしていた。保存より先にローカル座標を確定させる順序に変更（複数選択パス・単一ノードドラッグパスの両方）。本番検証: claude-in-chromeのモディファイア制約でノードドラッグ・範囲選択のイベントが発火せずUI操作での再現ができなかったため、本番配信バンドル`index-B7VWtes4.js`を直接取得し、複数選択パス・単一ノードパスとも`setAllItems`が`await updateItemMeta`より先に実行される順序になっていることをコードレベルで確認。consoleエラーなし。実データの座標は検証前後で変化なし（原状回復不要） | 完了（2026-08-17デプロイ済み） | 03_画面設計.md §7.11 |
| 2026-08-17 | R-112 | **フローチャート「自動整理」ボタン新設**: 会話内発言原文「ついでだから、　マインドマップに自動整理ボタンを追加しよう　フローチャートがそれぞれが重ならず、エッジ同士も重なりが少ない位置関係、そしてノード同士の隙間もできるだけエッジが見やすい範囲で隙間は狭く」「自動整理も日付表示と同じように、戻るができる仕様に」。相談で確定: エンジンは自前実装（外部レイアウトライブラリ不使用）、プロジェクトごとに整理して横並び。仕様: 右上ボタン列に「自動整理」ボタン、簡易Sugiyama（Longest Path層分け＝上→下、重心法で層内並び替え、実測ノードサイズで詰めて配置）、実行前位置をバックアップし「元に戻す」で1段階Undo（R-109と同機構）、日付表示ON中は無効。**実装完了（2026-08-17、ブランチ`feature/R-111-112-flow-arrange`、R-111と同ブランチで直列実装）**: 新規`logic/flowAutoArrange.ts`に`calculateAutoArrange(items, deps, sizes?)`を実装。`project_id`でグループ分け（未所属は最後）→`flowDateGrouping.ts`の`computeDepthWithin`をexportして流用しLongest Path層分け→層内の初期順は元`flow_x`昇順、重心法（隣接層の接続先平均順位）を下向き→上向きに4回掃引して交差削減→層内は実測幅（`sizes`未指定時180×60）+GAP_X(40)で詰めてプロジェクト内最大層幅に中央揃え、縦は層最大高さ+GAP_Y(60)で累積→プロジェクトはPROJECT_GAP(200)で横並び。`screens/FlowScreen.tsx`に`top-[180px] right-3`へ「自動整理」ボタン（`isDateGrouping`中はdisabled）、`handleAutoArrange`（位置バックアップ→`nodes`の`measured`からサイズMap→`calculateAutoArrange`→`applyPlacements`）を追加。「元に戻す」の表示条件を`isDateGrouping`から`hasPositionBackup` state（自動整理・日付表示どちらの実行後もtrue）に変更。テスト: `logic/__tests__/flowAutoArrange.test.ts`新規7件（重なりなし・依存先が下・交差ケースで並べ替わる・プロジェクト横並び・sizesで間隔変化・flow_x同点解消・空配列）、`screens/__tests__/FlowScreen.autoArrange.test.tsx`新規4件（ボタン表示・押下で保存・元に戻すで復元・日付表示中disabled）。実機検証（`php -S 127.0.0.1:8000`・Vite dev、フローチャート「全プロジェクト」画面・既存デバッグデータ約20件）: 自動整理ボタン押下で2プロジェクトが横並びに重ならず配置され、未所属アイテムも重ならず1行に整列すること、「元に戻す」で押下前の配置に完全復元されること、日付表示チェックON中は自動整理ボタンがdisabled表示になることを確認。consoleエラーなし。使用したのはworktree専用のローカルDBで新規レコード作成なし（既存データの位置変更のみ、原状回復不要）。全体テスト: vitest 160ファイル959 passed/14 skipped/1 failed（既存フレーキー`useAssigneeView.test.ts`のみ、masterでも同様に失敗する本件と無関係の事象）。GAP_X=40/GAP_Y=60/PROJECT_GAP=200は実機の見た目で妥当と判断しデフォルト値のまま採用 | 完了（2026-08-17デプロイ済み。本番検証: フローチャート「全プロジェクト」画面で右上ボタン列に「自動整理」ボタンの存在・有効状態を確認、既存ノード・エッジの通常表示・consoleエラーなしを確認。実データ保護のため日付表示ONや自動整理クリックは実施せず） | 02_機能仕様.md F-43, 03_画面設計.md §7.13 |
| 2026-08-17 | R-111 | **日付表示の帯内配置を「横位置維持・縦移動のみ」に修正**: 会話内発言原文「確かに日が縦に積まれているのは正しい。ありがとう。しかしそのなかのノードがすべて横に並んでしまってるじゃないか。（中略）日付で整理するときは元のマップの横方向移動はできるだけせずに縦方向移動だけで日付ごとの領域に移動してほしかったんだ。なぜなら元のマインドマップの形をできるだけ変えたくなかったから」。相談で確定: 帯内で行をずらす順序は依存関係優先（依存元が上・依存先が下、同順位なら元の上下関係）。仕様: `flow_x`は変更せず、帯内でXが重なるノードだけ別の行にずらす。帯の高さは行数で可変、帯のyは累積。R-110の「帯内は依存深さ順に横一列」を置き換える。**実装完了（2026-08-17、ブランチ`feature/R-111-112-flow-arrange`）**: `logic/flowDateGrouping.ts`の`calculateDateGroupLayout`を書き換え。`flow_x`は元の値のまま`PlacementResult`に渡し、帯内は`computeDepthWithin`の深さ昇順→同深さは元`flow_y`昇順で処理し、`row = 帯内の依存元ノードのrowの最大値+1`を下限にX区間`[flow_x, flow_x+NODE_WIDTH]`が既配置ノードと重ならない最初の行へ配置（重ならなければ同じ行に詰める）。`flow_y = bandY + BAND_PADDING_TOP + row*ROW_HEIGHT`、帯高さ`max(rows*ROW_HEIGHT+BAND_PADDING_TOP+BAND_PADDING_BOTTOM, BAND_MIN_HEIGHT)`で可変、`bandY`は前の帯の`y+height`の累積。帯のx/widthは全配置ノードの`minX-LABEL_MARGIN_WIDTH`〜`maxX+NODE_WIDTH`で全帯共通。`COL_WIDTH`/`BAND_HEIGHT`定数を廃止し`ROW_HEIGHT`(110)/`BAND_PADDING_TOP`(60)/`BAND_PADDING_BOTTOM`(20)/`BAND_MIN_HEIGHT`(150)を新設、`NODE_WIDTH`(180)は`components/Flow/flowGrouping.ts`からexportして共有。`computeDepthWithin`もexportしR-112の`flowAutoArrange.ts`から流用。テスト: `logic/__tests__/flowDateGrouping.test.ts`を新ルールの期待値へ全面書き換え・追加（flow_x不変・X非重複は同行・X重複は別行・依存先は依存元より下・帯高さ可変・帯幅共通等、計24件）、`screens/__tests__/FlowScreen.dateGrouping.test.tsx`の期待座標も新ルールへ追随（依存先が縦移動のみで下の行に置かれることを検証）し4件green。実機検証（`php -S 127.0.0.1:8000`・Vite dev、フローチャート「全プロジェクト」画面）: 日付表示チェックONで各ノードの横位置が変わらず縦移動だけで日付帯へ収まること、帯が8区間（日付未定含む）で上から下へ積み上がり高さが可変であること、「元に戻す」で直前の配置へ完全復元されることを確認。consoleエラーなし | 完了（2026-08-17デプロイ済み。本番検証: フローチャート「全プロジェクト」画面で通常表示・consoleエラーなしを確認。実データ保護のため日付表示ONは実施せず） | 02_機能仕様.md F-42, 03_画面設計.md §7.12 |
| 2026-08-17 | R-110 | **R-109日付グルーピング表示の軸入れ替え**: 会話内発言原文「いまフローチャートをみて日付表示をONにしてみたけど、なんで日付が列ごとで縦になってるんだ？横だってば！フローチャートの流れは上から下だから、日付は横線区切り日付は上から下にむけて増えて進んでいく」。R-109実装は「日付＝X軸（横の帯）、依存の深さ＝帯内でY軸（縦積み）」になっており、フロー本来の「上から下へ流れる」設計と軸が逆だったため修正。発注者との確認により以下を確定: (1) 帯は画面幅ではなくフロー全体のコンテンツ幅いっぱいの横長帯とし、上から下へ日付順に積み上げる。(2) 帯内のノードは依存の深さ順に横一列に並べる。(3) ラベルは帯の左上に、日付（曜日つき）→改行→合計→改行→最短、の順で縦に並べる。(4) 帯同士の区切りは横線（`borderBottom`）。実装対象: `logic/flowDateGrouping.ts`（軸入れ替え、`BAND_WIDTH`/`ROW_HEIGHT`/`BAND_HEADER`/`BAND_FOOTER`→`BAND_HEIGHT`/`COL_WIDTH`/`LABEL_MARGIN_WIDTH`に再編、`bandLabel`に曜日追加）、`components/Flow/DateBandNode.tsx`（ラベルを左上3行スタックに変更）、`screens/FlowScreen.tsx`（帯の罫線を`borderLeft`/`borderRight`→`borderBottom`に変更）。クリティカルパス計算・依存の深さ計算・グルーピング・Undo・チェックボックスUI等の既存ロジックは軸に依存しないため変更不要。**実装完了（2026-08-17、`fix/R-110-flow-date-band-axis`）**: `flowDateGrouping.ts`の定数を`BAND_WIDTH`/`ROW_HEIGHT`/`BAND_HEADER`/`BAND_FOOTER`→`COL_WIDTH`（250、帯内でノードを横に並べる間隔）/`BAND_HEIGHT`（220、帯の縦の厚み）/`LABEL_MARGIN_WIDTH`（140、帯左のラベル領域幅）に再編。帯は`bandX=0`固定・`bandY=index*BAND_HEIGHT`で縦積み、全帯共通の幅は「全グループ中の最大アイテム数×COL_WIDTH＋LABEL_MARGIN_WIDTH」。帯内ノードは`flow_x = LABEL_MARGIN_WIDTH + col*COL_WIDTH`、`flow_y = bandY + (BAND_HEIGHT-NODE_HEIGHT)/2`で帯内中央寄せ。同depthのタイブレークを`flow_y`比較→`flow_x`比較に変更。`bandLabel`は`format(parseISO(dateKey), 'M/d(E)', {locale: ja})`（`date-fns/locale`の`ja`、他画面での使用例を踏襲）で曜日付き（例:「8/16(日)まで」）に変更。`DateBandNode.tsx`はラベルを上下2箇所分散→左上に3行スタック（日付→合計→最短）に変更。`FlowScreen.tsx`は帯の罫線を`borderLeft`/`borderRight`→`borderBottom`1本に変更。TDD: `logic/__tests__/flowDateGrouping.test.ts`を新軸の期待値へ全面書き換えRed確認（5件失敗）→実装しGreen確認（19件）。既存の統合テスト`screens/__tests__/FlowScreen.dateGrouping.test.tsx`（R-109実装時の旧軸を前提にしたテストで本タスクの対象ファイル一覧には明記されていなかったが、軸反転により3件が新たにRedになることを実行して発見）も新軸の期待値へ更新しGreen確認（4件）。全体テスト実行: vitest 162ファイル943 passed/14 skipped/1 failed（既存フレーキー`useAssigneeView.test.ts`のみ、masterでも失敗する本件と無関係の事象）。実機検証（`php -S 127.0.0.1:8000`・Vite dev、フローチャート「全プロジェクト」画面・既存デバッグデータ16件で「全て自動配置」後に日付表示チェックON）: 帯が2/18〜3/17の8区間で上から下へ日付順に積み上がること、各帯内でノードが左から右に並ぶこと（例: 3/4(水)帯に3ノードが横一列）、各帯左上に「M/d(曜)まで」「合計Xh」「最短Xh」が縦3行で表示されること（例:「3/4(水)まで／合計 26h／最短 24h」）、帯同士が横線で区切られていること、「元に戻す」ボタンでチェックON前の配置に完全復元されること（DOM上の位置が一致）を確認。consoleエラーなし。検証で使用したのはworktree専用にコピーしたローカルDBのため原状回復不要 | 完了（2026-08-17デプロイ済み。masterへfast-forwardマージ後、vitest 943 passed/14 skipped/1 failed（既存フレーキー`useAssigneeView.test.ts`のみ）を確認しビルド・`upload.ps1`でデプロイ。本番実機検証はR-109と同様に実データ保護のため確認範囲を限定: フローチャート「全プロジェクト」画面（244ノード規模）を「日付表示」チェックOFF＝通常表示で開き、既存ノードの位置・表示が壊れていないこと、チェックボックス自体がDOM上に存在すること（クリックはせずuid照会のみ）、consoleエラーが出ていないことを確認。実際のON/OFF切り替えによる軸入れ替えの見た目確認は、実装Agentがローカルworktree環境で実施済みのものを踏襲） | 03_画面設計.md §7.12 |
| 2026-08-17 | R-109 | **フローチャートに日付グルーピング表示（合計時間・クリティカルパス最短時間つき）を追加**: 会話内発言原文＋手書き図添付。「フローチャート画面について上部のメニューが多分何段かあると思うんだけど、その一番下の段、メニューの一番下の段、メニューの一番下の段に日付表示チェックマークを作ってほしい。そのチェックマークを入れると今送る写真のように日付ごとに納期いつまでにこれをやるっていうのをごとに、そのタスクが表示される。フローチャートの並びで整理して、配置して、いつこれをやる、いつこれをやるっていうのはもうちょっと背景色を変えて分かりやすく表示してほしい。（中略）その日に与えられている合計のタスクの時間も書かれてますね。それと枝分かれ分岐して、その日の中で分岐があったりする中で、その中で1番時間がかかる経路の合計時間を最短でもこの時間かかりますよって言う意味で、最短何時間って言う表示も合計表示の下に書いて欲しいです」。手書き図: 縦の区切り線で「8/16まで」「8/17まで」「8/18まで」の3区間。区間1は3ノード直列（合計4h）。区間2は3ノード目から2方向に分岐（上1ノード・下3ノード直列）して1ノードに合流（合計7h・最短4.5h）。区間3は1ノード（合計2h）。発注者指定により実装着手前に相談を実施、以下を確定: (1) 区切り基準は有効締切（`due_date`/`prep_date`の早い方）。(2) 「合計」はその日が締切のタスクの目安時間を単純合計（R-105ガントの日別按分ロジックは使わない）。(3) チェックON時、日付ごとに自動整列し実際に`flow_x`/`flow_y`を書き換える。既存の上下位置をできるだけ保ちつつy軸方向で整列。(4) 「最短」＝クリティカルパスは、その日の区間内だけで独立計算（区間をまたぐ依存は無視）。(5) チェックON適用前の全ノード位置をバックアップし「元に戻す」ボタンで1段階Undo可能にする。指揮AI事前調査済み: `FlowScreen.tsx`のヘッダーは1段のみで既存トグルは無く、ヘルプ/全体表示/印刷ボタンと同じ`absolute top-*`フローティング配置パターンに新規チェックボックスを追加するのが自然。ノード自動配置は`flowAutoPlace.ts`の`calculateAutoPlacement`（Longest Path Layeringアルゴリズム、`computeLayers`でレイヤー計算、同一レイヤー内は有効締切順ソート）。依存関係は`item_dependencies`テーブル・`DependencyRepository`のシンプルなCRUD、DAGトポロジカルソートは`hierarchy.ts`の`sortWithDependencies`（Kahn's algorithm、循環依存は安全側フォールバック）。R-105ガントの`QuantityEngine.allocateBackwardsCore`は複数日按分の唯一のロジックだが、今回は「合計は単純合計」の方針のため転用しない。仕様: `03_画面設計.md` §7.12に追記予定。**実装完了（2026-08-17、ブランチ`feature/R-109-flow-date-grouping`）**: 新規`logic/flowDateGrouping.ts`に純粋関数として(1)有効締切での日付グルーピング（未設定は「日付未定」区間として末尾）(2)区間内クリティカルパス（最長経路、直列は合計・並列分岐は最大値・区間外の依存は無視・循環依存ガードあり）(3)日付軸レイアウト計算（x=区間ごとの帯、区間内は依存の深さ順→同深さは既存y座標順でy軸整列）を実装。区間帯は既存の`projectGroup`ノードと同じ「背景枠ノード」パターンで新規`dateBand`ノードとして描画し、上部に「M/dまで」ラベル、下部に「合計Xh」「最短Yh」を表示。右上フローティング列に「日付表示」チェックボックス＋グルーピング中のみ「元に戻す」ボタンを追加。位置バックアップはフロントのrefで1段階保持。位置保存はR-108の教訓どおりローカルstate確定→サーバー保存の順。適用直後は既存の`shouldFitViewRef`経由で全体表示に合わせ直す。テスト: 新規19件（ロジック）＋4件（UI）、フロー関連333件全green（既存フレーキー`useAssigneeView`・`DecisionDetailModal`はmasterでも同様に失敗する既知の無関係な失敗）。実機検証済み: 8/16区間（1h→2h→1hの直列）＝合計4h・最短4h、8/17区間（分岐＋合流）＝合計4.5h・最短3.5h、8/18区間＝合計2h・最短2h と手書き図どおりの計算・表示・ノード再配置・元に戻すを確認 | 完了（2026-08-17デプロイ済み。ただし本番での日付表示ON/OFF操作確認は実データ保護のため見送り。244ノード規模の全プロジェクト画面でチェックONすると全ノードの flow_x/flow_y が実際に書き換わるため、本番では「機能が存在しコンソールエラーが出ないこと」＋「既存ノードのx座標分布が日付グルーピング形式（320px刻み）に書き換わっていない＝実データ無傷」の確認に留めた。機能自体はローカルworktreeで実機検証済み） | 03_画面設計.md §7.12 |
